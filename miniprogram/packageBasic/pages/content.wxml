<!--packageBasic/pages/content/content.wxml-->
<view class="content-container">
  <!-- 搜索框 -->
  <view class="search-box">
    <input
      class="search-input"
      placeholder="搜索面试题..."
      value="{{searchText}}"
      bindinput="onSearch"
    />
  </view>

  <!-- 章节导航 -->
  <scroll-view class="chapter-nav" scroll-x>
    <view
      class="chapter-item {{currentSection === '' ? 'active' : ''}}"
      bindtap="selectSection"
      data-section=""
    >全部({{total}})</view>
    <view
      wx:for="{{sections}}"
      wx:key="id"
      class="chapter-item {{currentSection === item.name ? 'active' : ''}}"
      bindtap="selectSection"
      data-section="{{item.name}}"
    >{{item.name}}({{item.count}})</view>
  </scroll-view>

  <!-- 问题列表 -->
  <view class="question-list">
    <view
      wx:for="{{displayQuestions}}"
      wx:key="index"
      class="question-card card"
      bindtap="showQuestionDetail"
      data-index="{{index}}"
    >
      <!-- 问题标题 -->
      <view class="question-header">
        <view class="question-number">{{item.index}}</view>
        <view class="question-title">{{item.title}}</view>
        <view class="question-status">
          <text wx:if="{{item.isMastered}}" class="status-badge mastered">已摸清</text>
          <text wx:elif="{{item.isToImprove}}" class="status-badge to-improve">待加强</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 加载更多 -->
  <view class="load-more" bindtap="loadMore" wx:if="{{hasMore}}">
    点击加载更多 (已加载 {{loadedCount}}/{{total}})
  </view>

  <!-- 回到顶部 -->
  <view class="back-top" bindtap="backToTop" wx:if="{{showBackTop}}">↑</view>
</view>

<!-- 题目详情弹窗 -->
<question-detail
  show="{{showDetail}}"
  question="{{currentQuestion}}"
  bind:close="closeQuestionDetail"
/>