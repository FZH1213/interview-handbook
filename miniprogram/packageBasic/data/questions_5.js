// 数据文件 - 自动生成
module.exports = {"total": 538, "startIndex": 400, "endIndex": 480, "questions": [{"s": "CSS基础", "t": "清除浮动", "m": "清除浮动的目的，是让父元素能够正常包住浮动的子元素。可以在浮动元素后添加带有clear: both的空div，但这会引入没有实际语义的标签。也可以给父元素设置overflow: hidden或auto来触发BFC。我一般更倾向于使用clearfix伪元素，因为它同样通过clear: both生效，又能保持文档结构清晰；兼容IE6时再补zoom: 1。\n在浮动元素后面添加clear:both的空div元素\n<divclass=\"container\"><divclass=\"left\"></div><divclass=\"right\"></div><divstyle=\"clear:both\"></div></div>\n给父元素添加overflow:hidden或者auto样式，触发BFC\n<divclass=\"container\"><divclass=\"left\"></div><divclass=\"right\"></div></div>\n.container{width:300px;background-color:#aaa;overflow:hidden;zoom:1;/*IE6*/}\n使用伪元素，也是在元素末尾添加一个点并带有clear: both属性的元素实现的。\n<divclass=\"container clearfix\"><divclass=\"left\"></div><divclass=\"right\"></div></div>\n.clearfix{zoom:1;/*IE6*/}.clearfix:after{content:\".\";height:0;clear:both;display:block;visibility:hidden;}\n推荐使用第三种方法，不会在页面新增div，文档结构更加清晰", "f": [{"q": "资讯页左右两栏都设了float，父容器背景高度却变成0，有人只给父容器补一个固定高度；你会同意吗？", "a": "不同意用固定高度掩盖塌陷，因为浮动子项脱离普通文档流，内容增加后仍会溢出。应在末尾清除浮动，或让父元素通过overflow: hidden、auto形成BFC；固定高度只适合内容尺寸被严格约束的少数场景。"}, {"q": "老后台几十个列表都存在父级塌陷，模板团队不允许在每个列表末尾插入空div，你会怎样统一修复？", "a": "给这些父容器统一挂.clearfix，使用::after生成块级伪元素并设置clear: both，即可在末尾清除浮动。它不新增无语义的真实节点，文档结构更清晰；若仍需兼容非常旧的环境，源码中的zoom: 1属于针对旧版IE的兼容处理。"}, {"q": "卡片父级用overflow: hidden清浮动后，子元素阴影被截断；结构不能加真实节点时，你会怎么改？", "a": "应撤掉用于清浮动的overflow: hidden，改用父元素的::after配合content、display: block和clear: both。overflow方案通过形成BFC包含浮动，但同时会裁剪超出边界的视觉内容；伪元素法更适合需要保留阴影或外溢效果的容器。"}, {"q": "线上页脚压到浮动商品列表上方，开发工具显示.clearfix:after存在却没有撑开父级；你会逐项检查哪些声明？", "a": "先检查伪元素是否实际生成，content缺失时::after不会产生可用于清除的盒子。再确认它具有display: block和clear: both，并排除规则被覆盖；若清除节点不在浮动元素所属的同一父容器末尾，也无法修复目标父级的高度。"}, {"q": "维护者在“空div清除”“父级overflow: auto”“伪元素清除”之间争论，页面还要求结构简洁且不能出现额外滚动条，你会选哪一个？", "a": "优先选伪元素清除，因为它不增加真实div，也不会像overflow: auto那样可能引入滚动或改变溢出表现。空节点法直观但污染结构，overflow法代码短却附带裁剪或滚动语义；只有这些副作用符合容器需求时才应采用后两者。"}]}, {"s": "CSS基础", "t": "左边定宽，右边自适应方案", "m": "左边定宽、右边自适应，可以让左侧设置固定宽度并左浮动，再由右侧占据剩余空间。常见写法是左侧width: 120px、float: left，右侧通过margin-left: 120px留出位置。另一种方式是让两边都浮动，并把右侧宽度写成calc(100% - 120px)。前一种写法更直接，后一种适合确实需要明确计算右侧宽度的场景。\nfloat + margin，float + calc\n/* 方案1 */.left{width:120px;float:left;}.right{margin-left:120px;}/* 方案2 */.left{width:120px;float:left;}.right{width:calc(100% - 120px);float:left;}", "f": [{"q": "管理台左栏固定120px并浮动，右栏也写了width: 100%，结果被挤到下一行；同事说右栏本来就是“占满剩余空间”，错在哪里？", "a": "width: 100%按包含块宽度计算，并不会自动扣除左侧浮动的120px，两栏总宽度因此可能超过容器。可让右栏不设宽度而使用margin-left: 120px，或把右栏宽度明确写成calc(100% - 120px)并配合浮动。"}, {"q": "老门户页必须保留float，侧栏固定120px，右侧文章要随窗口伸缩；你会怎样写最少且稳定的布局规则？", "a": "让左栏设置width: 120px; float: left，右栏设置margin-left: 120px，即可把右侧可用区域留出来。右栏宽度由普通块级布局自动填充，通常不必再计算；若左右间还要留缝，应把间距一并计入margin-left。"}, {"q": "产品把侧栏从120px改成可配置值，但现有float + calc在多处各写了一遍宽度；你会怎样避免两边配置失配？", "a": "左栏宽度和右栏扣减值必须来自同一个尺寸约束，否则任一处漏改都会产生空隙或换行。可统一维护该值，再用于左栏width与右栏的calc(100% - 固定值)；如果无需显式控制右栏宽度，改用等值margin-left会减少一处计算。"}, {"q": "发布后右栏偶尔掉到左栏下方，只有容器加了边框和内边距的页面复现；你会怎样排查calc(100% - 120px)？", "a": "先检查两栏最终占用宽度是否还包含边框、内边距或额外外边距，因为这些尺寸可能让总和超过父容器。再核对两栏是否都成功浮动、calc是否被覆盖，以及父级可用宽度；必要时调整盒模型或把额外间距纳入计算，避免临界宽度换行。"}, {"q": "代码评审中，一方主张右栏用margin-left: 120px，另一方坚持float + calc；右栏只是普通文章流，你会选哪套？", "a": "普通文章流优先用左栏浮动、右栏等值margin-left，它不要求右栏也浮动，规则更少且能自然占据剩余宽度。float + calc适合确实需要明确右栏宽度或两栏都参与浮动的结构，但尺寸、间距和盒模型必须同步计算，维护成本更高。"}]}, {"s": "CSS基础", "t": "左右两边定宽，中间自适应", "m": "左右两边定宽、中间自适应，可以用浮动配合外边距，也可以直接用flex。浮动方案让左右分别靠两侧排列，中间通过margin: 0 120px留出空间；需要明确宽度时，也能用calc(100% - 240px)计算。更简洁的写法是父容器设置display: flex，左右固定为120px，中间设置flex: 1。具体选择取决于现有布局方式，维护旧浮动页面时不必强行改写。\nfloat，float + calc, 圣杯布局（设置BFC，margin负值法），flex\n.wrap{width:100%;height:200px;}.wrap > div{height:100%;}/* 方案1 */.left{width:120px;float:left;}.right{float:right;width:120px;}.center{margin:0 120px;}/* 方案2 */.left{width:120px;float:left;}.right{float:right;width:120px;}.center{width:calc(100% - 240px);margin-left:120px;}/* 方案3 */.wrap{display:flex;}.left{width:120px;}.right{width:120px;}.center{flex:1;}", "f": [{"q": "直播页左右栏各120px，中间栏写了width: 100%，上线后被挤到下一行；负责人认为左右浮动不会占普通流宽度，你怎么解释并修正？", "a": "浮动虽脱离普通文档流，但中间栏的100%仍按父容器完整宽度计算，无法与两侧共同容纳。可让左右分别float: left、float: right，中间使用margin: 0 120px；也可显式计算中间宽度，但必须把两侧总宽扣除。"}, {"q": "新闻详情页要求左右广告栏固定120px，中间正文自适应，现有float结构不能重写；你会给出什么落地代码？", "a": "左栏设width: 120px; float: left，右栏设width: 120px; float: right，中间正文设margin: 0 120px。中间不必固定宽度，会由块级布局占据扣除外边距后的空间；还要保证页面中的排列和清浮动规则与现有结构相容。"}, {"q": "设计把右栏改为160px、左栏仍为120px，原来的.center { margin: 0 120px }出现遮挡，你会怎样调整而不是继续套对称值？", "a": "中间栏应改为margin-left: 120px; margin-right: 160px，分别为两侧保留实际宽度。若采用calc，中间宽度也要扣除总计280px，不能沿用100% - 240px；侧栏还有额外间距时同样必须纳入约束。"}, {"q": "一个三栏页面在窄窗口下中间栏消失，开发工具显示.center { width: calc(100% - 240px); margin-left: 120px }，你会先查哪些尺寸关系？", "a": "先确认父容器实际可用宽度是否小于两侧合计240px，以及边框、内边距、外边距是否使三栏总占用继续增加。再检查左右浮动方向和中间宽度是否被其他规则覆盖；该方案没有为极窄容器定义降级行为，必要时必须另设最小宽度或重排布局。"}, {"q": "新建后台的三栏框架不受旧浏览器约束，团队在float + margin、float + calc和flex之间选型，你会推荐哪一种？", "a": "推荐父容器使用display: flex，左右栏固定width: 120px，中间栏设flex: 1，约束关系直接且无需负担浮动清除。旧结构难改时，float + margin比显式calc少一层宽度计算；若中间内容存在不可收缩项，仍需另外处理溢出风险。"}, {"q": "三栏容器改成flex后，中间长表格把右侧固定栏顶出屏幕；这和“中间flex: 1自适应”是否矛盾，你怎么排查？", "a": "不矛盾，flex: 1只声明中间项分配剩余空间，内部内容仍可能提出更大的最小尺寸需求。应检查长表格或不换行内容的溢出约束，并确保左右固定栏没有被允许意外收缩；浮动方案同样需要面对超长内容，只是故障表现可能不同。"}]}, {"s": "CSS基础", "t": "CSS动画和过渡", "m": "transition适合属性状态变化时的平滑过渡，animation配合@keyframes则能描述完整的多阶段动画。transition主要控制目标属性、持续时间、速度曲线和延迟；animation还可以设置播放次数、方向及结束后的状态，并通过animationend监听完成。关键帧既能写成from到to，也能用百分比安排多个阶段。实现位移、旋转或缩放时，我会优先使用transform，简单显隐可用opacity，通常更适合连续动画。\nanimation / keyframes\nanimation-name: 动画名称，对应@keyframesanimation-duration: 间隔animation-timing-function: 曲线animation-delay: 延迟animation-iteration-count: 次数infinite: 循环动画animation-direction: 方向alternate: 反向播放animation-fill-mode: 静止模式forwards: 停止时，保留最后一帧backwards: 停止时，回到第一帧both: 同时运用forwards / backwards常用钩子:animationend\ninfinite: 循环动画\nalternate: 反向播放\nforwards: 停止时，保留最后一帧backwards: 停止时，回到第一帧both: 同时运用forwards / backwards\n动画属性: 尽量使用动画属性进行动画，能拥有较好的性能表现\ntranslatescalerotateskewopacitycolor\ntransform\n位移属性translate( x , y )旋转属性rotate()缩放属性scale()倾斜属性skew()\ntransition\ntransition-property（过渡的属性的名称）。transition-duration（定义过渡效果花费的时间,默认是0）。transition-timing-function:linear(匀速) ease(慢速开始，然后变快，然后慢速结束)（规定过渡效果的时间曲线，最常用的是这两个）。transition-delay（规定过渡效果何时开始。默认是 0）\n般情况下，我们都是写一起的，比如：transition： width 2s ease 1s\n关键帧动画animation\n一个关键帧动画，最少包含两部分，animation 属性及属性值（动画的名称和运行方式运行时间等）。@keyframes（规定动画的具体实现过程）\nanimation 属性可以拆分为\nanimation-name规定@keyframes 动画的名称。animation-duration规定动画完成一个周期所花费的秒或毫秒。默认是0。animation-timing-function规定动画的速度曲线。默认是 “ease”，常用的还有linear，同transtion。animation-delay规定动画何时开始。默认是 0。animation-iteration-count规定动画被播放的次数。默认是 1，但我们一般用infinite，一直播放\n而@keyframes的使用方法，可以是from->to（等同于0%和100%），也可以是从0%->100%之间任意个的分层设置。我们通过下面一个稍微复杂点的demo来看一下，基本上用到了上面说到的大部分知识\neg:@keyframesmymove{from{top:0px;}to{top:200px;}}/* 等同于： */@keyframesmymove{0%{top:0px;}25%{top:200px;}50%{top:100px;}75%{top:200px;}100%{top:0px;}}\n用css3动画使一个图片旋转\nloader{display:block;position:relative;-webkit-animation:spin 2s linear infinite;animation:spin 2s linear infinite;}@-webkit-keyframesspin{0%{-webkit-transform:rotate(0deg);-ms-transform:rotate(0deg);transform:rotate(0deg);}100%{-webkit-transform:rotate(360deg);-ms-transform:rotate(360deg);transform:rotate(360deg);}}@keyframesspin{0%{-webkit-transform:rotate(0deg);-ms-transform:rotate(0deg);transform:rotate(0deg);}100%{-webkit-transform:rotate(360deg);-ms-transform:rotate(360deg);transform:rotate(360deg);}}", "f": [{"q": "商品卡片从列表左侧滑入时，开发写了transition: left 300ms ease，滚动过程中位移不连贯；只依据这些样式，你会先改哪一处？", "a": "先把位移从left改为transform: translateX()，并让transition-property指向transform。资料明确把translate、scale、rotate、skew、opacity列为更适合动画的属性，但仅凭样式不能断言卡顿一定来自布局计算，仍需结合页面负载排查。"}, {"q": "设计稿要求加载图标持续匀速旋转，另一位开发却用悬停触发的transition实现；这套方案为什么无法直接满足要求，你会怎么写？", "a": "持续旋转应使用animation配合@keyframes，例如从transform: rotate(0deg)变化到rotate(360deg)，并设置linear infinite。transition依赖属性状态发生变化，本身不负责循环播放；代价是无限动画会持续运行，离开可见区域后是否暂停需要另行设计。"}, {"q": "弹窗打开后要分三段完成上移、轻微回落和停稳，产品又要求关闭时反向播放；你会怎样组织关键帧和播放方向？", "a": "用百分比关键帧描述三个阶段，并通过animation-duration与animation-timing-function控制整体节奏；需要往返播放时可设置animation-direction: alternate。alternate会按迭代轮次反向执行，不等同于任意时刻点击关闭就从当前进度倒放，交互中断仍需额外状态控制。"}, {"q": "线上反馈横幅动画结束后突然跳回初始位置，代码里@keyframes的100%已经是目标位置；你会优先检查哪个属性？", "a": "优先检查animation-fill-mode，若要结束后保留最后一帧，应使用forwards，需要同时处理开始前与结束后的样式可用both。它只影响动画执行区间外采用哪一帧，不会把关键帧结果永久写回元素原始样式，后续状态切换仍可能覆盖视觉结果。"}, {"q": "表单校验提示只需在错误出现时淡入，开发在transition和animation之间争执；你会依据什么选型？", "a": "若提示只是从既有状态平滑过渡到错误状态，使用transition配置属性、时长、曲线和延迟更直接。若要安排多个阶段或独立循环，则animation与@keyframes更合适；两者都应优先选择资料列出的动画属性，避免为了统一写法引入不必要的关键帧。"}]}, {"s": "CSS基础", "t": "CSS3的新特性", "m": "CSS3常见的新特性可以归为视觉效果、盒模型与响应式能力、变换动画三类。视觉上有圆角、阴影和渐变，布局相关可用box-sizing，响应式页面则依赖@media。交互效果通常由transform、transition和animation完成，其中transition需要状态变化触发，animation可以按关键帧自行运行。文本场景还可以用word-break和text-overflow处理换行与溢出。\ntransition：过渡transform: 旋转、缩放、移动或倾斜animation: 动画gradient: 渐变box-shadow: 阴影border-radius: 圆角word-break:normal|break-all|keep-all; 文字换行(默认规则|单词也可以换行|只在半角空格或连字符换行)text-overflow: 文字超出部分处理text-shadow: 水平阴影，垂直阴影，模糊的距离，以及阴影的颜色。box-sizing:content-box|border-box盒模型媒体查询@media screen and (max-width: 960px) {}还有打印print", "f": [{"q": "资讯卡片标题限定一行，内容超出后产品要求显示省略号；开发只加了text-overflow: ellipsis，页面仍把整段文字画出来，你会怎么判断？", "a": "text-overflow只规定溢出内容的呈现方式，单独设置通常不足以形成可见截断，还要确保元素确实产生受限宽度和溢出状态。资料只列出了该特性的用途，未给出完整组合，因此不能把失败归因于浏览器版本，应继续核对换行与溢出相关样式。"}, {"q": "英文订单号把移动端表格撑出屏幕，运营又要求普通英文句子尽量按单词换行；word-break应怎样选，风险是什么？", "a": "普通文本优先保留normal的默认换行规则；若订单号这类长串必须在任意字符处断开，可针对该字段使用break-all。keep-all只允许在半角空格或连字符等位置换行，不适合无分隔符长串；全局使用break-all则可能破坏普通单词的可读性。"}, {"q": "设计系统规定按钮视觉宽度为120px，组件同时有边框和内边距，接入方发现最终占位超过120px；你会改哪个盒模型配置？", "a": "可将组件设为box-sizing: border-box，让声明宽度包含内容区、内边距和边框，更容易维持设计稿给出的外部尺寸。content-box下声明宽度只约束内容区，额外边框和内边距会扩大盒子；切换盒模型后，依赖原内容区宽度的子元素可能需要重新校验。"}, {"q": "打印订单页时，网页导航和操作按钮也出现在纸面上；团队想再复制一套打印页面，你会先提出什么方案？", "a": "先使用@media print为打印介质覆盖样式，隐藏导航与操作区，并调整正文布局，而不是立即维护第二套页面。媒体查询也能按屏幕条件应用规则，例如max-width；但资料没有覆盖所有打印兼容细节，分页、背景和纸张尺寸仍应在目标浏览器实际验证。"}, {"q": "营销页需要圆角卡片、阴影、渐变背景和悬停放大，开发准备全部导出成图片；你会保留哪些为CSS，实现时注意什么？", "a": "圆角、盒阴影、渐变以及悬停缩放都可分别用border-radius、box-shadow、gradient和transform: scale()表达，通常无需把整块视觉固化成图片。这样便于尺寸和状态变化，但复杂视觉是否能完全还原仍取决于设计要求，不能仅凭特性清单承诺一致效果。"}]}, {"s": "CSS基础", "t": "列举几个css中可继承和不可继承的元素", "m": "通常，控制文字表现的属性更容易继承，而控制盒子尺寸、位置和布局的属性通常不会继承。例如color、font-family、line-height、text-align可以从父元素传给后代，visibility和cursor也可继承。相对地，display、margin、padding、width、position等不会自动继承，否则父级布局很容易意外影响子元素。遇到边界情况我会查看属性定义，而不是只靠这条经验判断。\n不可继承的：display、margin、border、padding、background、height、min-height、max-height、width、min-width、max-width、overflow、position、left、right、top、bottom、z-index、float、clear、table-layout、vertical-align所有元素可继承：visibility和cursor。内联元素可继承：letter-spacing、word-spacing、white-space、line-height、color、font、font-family、font-size、font-style、font-variant、font-weight、text-decoration、text-transform、direction。终端块状元素可继承：text-indent和text-align。列表元素可继承：list-style、list-style-type、list-style-position、list-style-image`。\ntransition和animation的区别\nAnimation和transition大部分属性是相同的，他们都是随时间改变元素的属性值，他们的主要区别是transition需要触发一个事件才能改变属性，而animation不需要触发任何事件的情况下才会随时间改变属性值，并且transition为2帧，从from .... to，而animation可以一帧一帧的\n---", "f": []}, {"s": "浏览器", "t": "浏览器架构", "m": "现代浏览器采用多进程架构，把浏览器界面、页面渲染、网络、GPU和插件等职责隔离开。浏览器主进程负责界面、交互和子进程管理，渲染进程运行Blink与V8，把HTML、CSS和JavaScript转成可交互页面。进程隔离后，某个页面或插件崩溃通常不会拖垮整个浏览器，沙箱也能限制恶意程序获取系统权限。代价是进程会占用独立资源，但能换来更好的稳定性、安全性和多核利用。\n单进程浏览器时代\n单进程浏览器是指浏览器的所有功能模块都是运行在同一个进程里，这些模块包含了网络、插件、JavaScript运行环境、渲染引擎和页面等。其实早在2007年之前，市面上浏览器都是单进程的\n缺点不稳定：一个插件的意外崩溃会引起整个浏览器的崩溃不流畅：所有页面的渲染模块、JavaScript执行环境以及插件都是运行在同一个线程中的，这就意味着同一时刻只能有一个模块可以执行不安全：可以通过浏览器的漏洞来获取系统权限，这些脚本获取系统权限之后也可以对你的电脑做一些恶意的事情，同样也会引发安全问题以上这些就是当时浏览器的特点，不稳定，不流畅，而且不安全\n不稳定：一个插件的意外崩溃会引起整个浏览器的崩溃不流畅：所有页面的渲染模块、JavaScript执行环境以及插件都是运行在同一个线程中的，这就意味着同一时刻只能有一个模块可以执行不安全：可以通过浏览器的漏洞来获取系统权限，这些脚本获取系统权限之后也可以对你的电脑做一些恶意的事情，同样也会引发安全问题\n多进程浏览器时代\n由于进程是相互隔离的，所以当一个页面或者插件崩溃时，影响到的仅仅是当前的页面进程或者插件进程，并不会影响到浏览器和其他页面，这就完美地解决了页面或者插件的崩溃会导致整个浏览器崩溃，也就是不稳定的问题JavaScript也是运行在渲染进程中的，所以即使JavaScript阻塞了渲染进程，影响到的也只是当前的渲染页面，而并不会影响浏览器和其他页面，因为其他页面的脚本是运行在它们自己的渲染进程中的Chrome把插件进程和渲染进程锁在沙箱里面，这样即使在渲染进程或者插件进程里面执行了恶意程序，恶意程序也无法突破沙箱去获取系统权限。\n最新的Chrome浏览器包括：1个浏览器（Browser）主进程、1个 GPU 进程、1个网络（NetWork）进程、多个渲染进程和多个插件进程\n浏览器进程。主要负责界面显示、用户交互、子进程管理，同时提供存储等功能。渲染进程。核心任务是将HTML、CSS和 JavaScript 转换为用户可以与之交互的网页，排版引擎Blink和JavaScript引擎V8都是运行在该进程中，默认情况下，Chrome会为每个Tab标签创建一个渲染进程。出于安全考虑，渲染进程都是运行在沙箱模式下。GPU进程。其实，Chrome刚开始发布的时候是没有GPU进程的。而GPU的使用初衷是为了实现3D CSS的效果，只是随后网页、Chrome的UI界面都选择采用GPU来绘制，这使得GPU成为浏览器普遍的需求。最后，Chrome在其多进程架构上也引入了GPU进程。网络进程。主要负责页面的网络资源加载，之前是作为一个模块运行在浏览器进程里面的，直至最近才独立出来，成为一个单独的进程。插件进程。主要是负责插件的运行，因插件易崩溃，所以需要通过插件进程来隔离，以保证插件进程崩溃不会对浏览器和页面造成影响", "f": [{"q": "客服说一个页面里的插件崩溃后整个浏览器都应该退出，因为“插件属于浏览器”；结合现代多进程架构，你会怎样反驳？", "a": "现代浏览器会把插件和页面等职责放入相互隔离的进程，插件崩溃通常只影响对应插件进程，不应必然带走浏览器和其他页面。单进程时代才容易因一个模块异常导致整体崩溃；进程隔离降低故障范围，但不能据此保证所有异常都绝不扩散。"}, {"q": "后台报表执行一段很长的JavaScript后当前标签页失去响应，但地址栏和其他页面仍可操作；这与哪些进程职责吻合？", "a": "JavaScript引擎V8与排版引擎Blink都运行在渲染进程中，长脚本阻塞该进程时，主要影响当前页面的执行和渲染。浏览器主进程负责界面、交互和子进程管理，其他页面通常还有各自的渲染进程；具体进程分配策略不宜由现象单独推断。"}, {"q": "安全评审认为把渲染进程拆开只是为了防崩溃，与恶意网页获取系统权限无关；你会指出哪处判断有问题？", "a": "多进程设计还配合沙箱限制渲染进程和插件进程访问系统资源，即使其中执行恶意程序，也更难直接突破边界取得系统权限。进程隔离解决的是影响范围，沙箱解决的是权限约束，两者目标相关但并不等价；沙箱也不是可以忽略浏览器漏洞的绝对安全保证。"}, {"q": "页面能显示但图片和脚本长期加载失败，另一个标签页的界面交互仍正常；排查时你会优先区分哪些进程？", "a": "应优先区分负责资源加载的网络进程、负责页面转换与交互的渲染进程，以及管理界面和子进程的浏览器主进程。资源普遍加载异常更应关注网络链路或网络进程，单页解析执行异常则偏向渲染进程；仅凭一个标签页现象仍不足以锁定故障点。"}, {"q": "团队讨论是否把所有浏览器能力塞回单进程以节省开销，稳定性、安全性和流畅性之间该怎么取舍？", "a": "单进程减少了进程边界，却会让插件、页面、网络和脚本故障更容易牵连整个浏览器，也难以借助隔离与沙箱控制影响范围。多进程的价值是稳定性、安全性及利用多核能力，代价是架构和资源管理更复杂；资料没有给出内存数据，不能虚构量化收益。"}]}, {"s": "浏览器", "t": "JavaScript单线程模型", "m": "JavaScript单线程指同一时刻只有一个调用链在执行，并不代表浏览器只有一个线程。这样设计能避免多个线程同时修改DOM时产生冲突，但长时间运行的脚本也会阻塞页面渲染。异步任务会进入任务队列，等当前任务完成、执行线程空闲后再按事件循环取出执行。计算密集型工作可以交给Web Worker，不过它不能直接操作DOM，而且通信成本高于计算收益时并不划算。\nJavaScript语言的一大特点就是单线程，也就是说，同一时间只能做一件事，前面的任务没做完，后面的任务只能等着。\n1. 为什么JavaScript是单线程的呢?\n这主要与JavaScript用途有关。它的主要用途是与用户互动，以及操作DOM。如果JavaScript是多线程的，会带来很多复杂的问题，假如 JavaScript有A和B两个线程，A线程在DOM节点上添加了内容，B线程删除了这个节点，应该是哪个为准呢? 所以，为了避免复杂性，所以设计成了单线程。虽然 HTML5 提出了Web Worker标准。Web Worker 的作用，就是为 JavaScript 创造多线程环境，允许主线程创建 Worker 线程，将一些任务分配给后者运行。但是子线程完全受主线程控制，且不得操作DOM。所以这个并没有改变JavaScript单线程的本质。一般使用 Web Worker 的场景是代码中有很多计算密集型或高延迟的任务，可以考虑分配给 Worker 线程。但是使用的时候一定要注意，worker 线程是为了让你的程序跑的更快，但是如果 worker 线程和主线程之间通信的时间大于了你不使用worker线程的时间，结果就得不偿失了。\n2. 浏览器内核中线程之间的关系\nGUI渲染线程和JS引擎线程互斥js是可以操作DOM的，如果在修改这些元素的同时渲染页面（js线程和ui线程同时运行），那么渲染线程前后获得的元素数据可能就不一致了。JS阻塞页面加载js如果执行时间过长就会阻塞页面\njs是可以操作DOM的，如果在修改这些元素的同时渲染页面（js线程和ui线程同时运行），那么渲染线程前后获得的元素数据可能就不一致了。\njs如果执行时间过长就会阻塞页面\n3. 浏览器是多进程的优点\n默认新开 一个 tab 页面 新建 一个进程,所以单个 tab 页面崩溃不会影响到整个浏览器。第三方插件崩溃也不会影响到整个浏览器。多进程可以充分利用现代 CPU 多核的优势。方便使用沙盒模型隔离插件等进程,提高浏览器的稳定性。\n4. 进程和线程又是什么呢\n进程（process）和线程（thread）是操作系统的基本概念。\n进程是 CPU 资源分配的最小单位（是能拥有资源和独立运行的最小单位）。线程是 CPU 调度的最小单位（是建立在进程基础上的一次程序运行单位）。\n由于每个进程至少要做一件事,所以一个进程至少有一个线程。系统会给每个进程分配独立的内存,因此进程有它独立的资源。同一进程内的各个线程之间共享该进程的内存空间（包括代码段,数据集,堆等）。\n进程可以理解为一个工厂不不同车间，相互独立。线程是车间里的工人，可以自己做自己的事情,也可以相互配合做同一件事情。\n5. 任务队列\n单线程就意味着，所有任务都要排队执行，前一个任务结束，才会执行后一个任务。如果一个任务需要执行，但此时JavaScript引擎正在执行其他任务，那么这个任务就需要放到一个队列中进行等待。等到线程空闲时，就可以从这个队列中取出最早加入的任务进行执行（类似于我们去银行排队办理业务，单线程相当于说这家银行只有一个服务窗口，一次只能为一个人服务，后面到的就需要排队，而任务队列就是排队区，先到的就优先服务）\n注意：如果当前线程空闲，并且队列为空，那每次加入队列的函数将立即执行。\n为什么会有任务队列？ 由于 JS 是单线程的，同步执行任务会造成浏览器的阻塞，所以我们将 JS 分成一个又一个的任务，通过不停的循环来执行事件队列中的任务。", "f": [{"q": "同事说浏览器能同时下载图片，所以页面里的JavaScript也会自动并行修改同一个DOM；你会怎样指出概念混淆？", "a": "浏览器内部可以有多个进程和线程，但页面主线程上的JavaScript仍按单线程模型执行，不会自动由两个线程同时修改同一DOM。这种设计避免添加节点与删除节点并发发生时的结果冲突；网络并行能力不能直接推出脚本执行模型也相同。"}, {"q": "数据看板需要在前端执行大量计算，输入期间明显卡顿；开发打算把计算搬到Web Worker，你会先确认哪些边界？", "a": "计算密集型或高延迟任务适合交给Web Worker，从而避免长时间占用页面主线程。Worker受主线程控制且不能操作DOM，结果必须回传后再由主线程更新界面；若任务很小或通信成本超过计算收益，迁移反而可能得不偿失。"}, {"q": "动画播放时，一段同步脚本持续处理大批数据，结果点击和页面绘制一起停住；为什么渲染线程没有绕过脚本继续工作？", "a": "资料指出GUI渲染线程与JS引擎线程互斥，脚本执行时间过长会阻塞页面加载和渲染。这样可避免脚本修改DOM的同时渲染线程读取到前后不一致的数据；拆分任务或移出重计算能缓解阻塞，但不能让主线程上的DOM操作并行执行。"}, {"q": "线上按钮点击后偶尔很晚才响应，监控显示事件已经进入任务队列；你会先排查事件是否丢失，还是主线程是否被占用？", "a": "应先检查主线程是否正在执行耗时任务，因为队列中的任务只有在线程空闲后才能按顺序取出执行。事件进入队列说明它不一定丢失，更可能是在等待前面的同步工作完成；若线程空闲且队列原本为空，新增任务才可能立即得到执行。"}, {"q": "两个标签页中，一个页面脚本死循环，另一个页面仍正常；有人据此断言JavaScript已经变成多线程，你会如何解释？", "a": "这反映的是浏览器多进程隔离，而不是单个页面里的JavaScript改成了多线程。不同标签页默认可由不同渲染进程承载，各自运行脚本，因此一个页面阻塞通常不影响其他页面；具体标签与进程映射可能受实现策略影响，不应把“默认”理解为绝对一一对应。"}, {"q": "架构师要在主线程分片执行和Web Worker之间选方案，任务还需要频繁更新DOM；你会偏向哪边？", "a": "若核心工作必须频繁读写DOM，Worker不能直接完成这些操作，只能承担可独立计算的部分，再把结果交回主线程。计算量大且通信较少时Worker更有价值，交互步骤多且每次计算很轻时，主线程分任务可能更简单；最终取舍应比较阻塞时间与通信成本。"}]}, {"s": "浏览器", "t": "Chrome 打开一个页面需要启动多少进程？分别有哪些进程？", "m": "Chrome打开一个页面至少需要四个进程：浏览器主进程、网络进程、GPU进程和渲染进程。主进程负责界面、交互和子进程管理，网络进程加载资源，GPU进程负责绘制与合成，渲染进程则运行Blink和V8，把页面变成可交互内容。实际数量通常不止四个，因为还可能存在多个渲染进程和插件进程。渲染与插件独立运行，本质上是为了通过沙箱和进程隔离控制安全风险及崩溃影响。\n打开 1 个页面至少需要 1 个网络进程、1 个浏览器进程、1 个 GPU 进程以及 1 个渲染进程，共 4 个；最新的 Chrome 浏览器包括：1 个浏览器（Browser）主进程、1 个 GPU 进程、1 个网络（NetWork）进程、多个渲染进程和多个插件进程。\n浏览器进程：主要负责界面显示、用户交互、子进程管理，同时提供存储等功能。渲染进程：核心任务是将 HTML、CSS 和 JavaScript 转换为用户可以与之交互的网页，排版引擎 Blink 和 JavaScript 引擎 V8 都是运行在该进程中，默认情况下，Chrome 会为每个 Tab 标签创建一个渲染进程。出于安全考虑，渲染进程都是运行在沙箱模式下。GPU 进程：其实，Chrome 刚开始发布的时候是没有 GPU 进程的。而 GPU 的使用初衷是为了实现 3D CSS 的效果，只是随后网页、Chrome 的 UI 界面都选择采用 GPU 来绘制，这使得 GPU 成为浏览器普遍的需求。最后，Chrome 在其多进程架构上也引入了 GPU 进程。网络进程：主要负责页面的网络资源加载，之前是作为一个模块运行在浏览器进程里面的，直至最近才独立出来，成为一个单独的进程。插件进程：主要是负责插件的运行，因插件易崩溃，所以需要通过插件进程来隔离，以保证插件进程崩溃不会对浏览器和页面造成影响。", "f": [{"q": "测试同学说打开一个空白标签页只需要一个进程，因为页面里没有业务代码；你会怎么纠正这个判断？", "a": "即使是空白页，也不能把整个Chrome页面理解成单进程运行；按基础架构至少涉及浏览器、网络、GPU和渲染进程。它们分别承担界面与子进程管理、资源加载、绘制合成和页面解析执行，是否空白只影响工作量，不改变职责隔离。"}, {"q": "一个后台管理页同时加载HTML、样式、脚本和接口数据，负责人要求你说明这些任务分别落在哪些进程，你会怎么划分？", "a": "网络资源加载主要由网络进程负责，HTML、CSS和JavaScript转换成交互页面的核心工作由渲染进程完成，其中包含Blink与V8。浏览器进程负责界面、用户交互和子进程管理，最终绘制与图层合成还会使用GPU进程。"}, {"q": "产品从单标签页改成 20 个标签页，并安装了多个插件，却要求进程数仍固定为 4 个，这个约束为什么不成立？", "a": "4 个只是打开一个页面时所涉及的基础进程类别和最低数量描述，并不是整个浏览器会长期固定为 4 个进程。Chrome默认可能为各个Tab创建渲染进程，插件也可能运行在独立插件进程中，因此标签页和插件增加后，实际进程数通常会随之变化。"}, {"q": "线上页面突然白屏，但浏览器地址栏、菜单和其他标签页仍能操作，你会优先怀疑哪个进程，并怎样缩小范围？", "a": "应优先检查该页面对应的渲染进程，因为Blink、V8以及页面内容生成都运行在那里，而浏览器界面仍正常说明主进程未必失效。可用Chrome任务管理器观察对应任务是否崩溃或资源异常，再区分是脚本执行、页面渲染还是插件影响。"}, {"q": "架构评审时有人建议把插件直接放进浏览器主进程，以减少进程数量和通信成本，你会接受吗？", "a": "通常不应接受，因为插件更容易发生崩溃，独立插件进程的价值正是隔离故障，避免连带破坏浏览器和页面。合并可能减少部分进程与通信开销，却会扩大故障影响面；除非运行环境和插件风险受到严格控制，否则不值得牺牲隔离性。"}, {"q": "页面请求已经返回，但内容迟迟没有显示；后端认定网络进程有问题，你会怎样串联网络进程与渲染进程判断责任边界？", "a": "请求返回只能说明网络进程完成了资源加载，不代表页面已经完成解析、布局、绘制和合成。应继续检查渲染进程中的Blink、V8是否被脚本或渲染工作阻塞，并观察GPU绘制合成环节；仅凭接口成功或失败不能覆盖整条显示链路。"}]}, {"s": "浏览器", "t": "渲染机制", "m": "浏览器渲染是一条从DOM、CSSOM到渲染树，再经过布局、绘制和图层合成的流水线。修改元素尺寸或位置会触发布局，也就是回流，后续通常还要重绘；只改颜色、背景等外观，一般只需重绘，所以回流成本更高。像transform、opacity这类动画可以利用独立图层，尽量减少对布局的影响，但图层也不能创建过多。我一般还会把频繁更新放进requestAnimationFrame，并避免循环读取offsetWidth、offsetHeight后又立即改样式。\n1. 浏览器如何渲染网页\n概述：浏览器渲染一共有五步\n处理HTML并构建DOM树。处理CSS构建CSSOM树。将DOM与CSSOM合并成一个渲染树。根据渲染树来布局，计算每个节点的位置。调用GPU绘制，合成图层，显示在屏幕上\n第四步和第五步是最耗时的部分，这两步合起来，就是我们通常所说的渲染\n具体如下图过程如下图所示\n渲染\n网页生成的时候，至少会渲染一次在用户访问的过程中，还会不断重新渲染\n重新渲染需要重复之前的第四步(重新生成布局)+第五步(重新绘制)或者只有第五个步(重新绘制)\n在构建CSSOM树时，会阻塞渲染，直至CSSOM树构建完成。并且构建CSSOM树是一个十分消耗性能的过程，所以应该尽量保证层级扁平，减少过度层叠，越是具体的CSS选择器，执行速度越慢当HTML解析到script标签时，会暂停构建DOM，完成后才会从暂停的地方重新开始。也就是说，如果你想首屏渲染的越快，就越不应该在首屏就加载JS文件。并且CSS也会影响JS的执行，只有当解析完样式表才会执行JS，所以也可以认为这种情况下，CSS也会暂停构建DOM\n2. 浏览器渲染五个阶段\n2.1 第一步：解析HTML标签，构建DOM树\n在这个阶段，引擎开始解析html，解析出来的结果会成为一棵dom树dom的目的至少有2个\n作为下个阶段渲染树状图的输入成为网页和脚本的交互界面。(最常用的就是getElementById等等)\n当解析器到达script标签的时候，发生下面四件事情\nhtml解析器停止解析,如果是外部脚本，就从外部网络获取脚本代码将控制权交给js引擎，执行js代码恢复html解析器的控制权\n由此可以得到第一个结论1\n由于<script>标签是阻塞解析的，将脚本放在网页尾部会加速代码渲染。defer和async属性也能有助于加载外部脚本。defer使得脚本会在dom完整构建之后执行；async标签使得脚本只有在完全available才执行，并且是以非阻塞的方式进行的\n2.2 第二步：解析CSS标签，构建CSSOM树\n我们已经看到html解析器碰到脚本后会做的事情，接下来我们看下html解析器碰到样式表会发生的情况js会阻塞解析，因为它会修改文档(document)。css不会修改文档的结构，如果这样的话，似乎看起来css样式不会阻塞浏览器html解析。但是事实上css样式表是阻塞的。阻塞是指当cssom树建立好之后才会进行下一步的解析渲染\n通过以下手段可以减轻cssom带来的影响\n将script脚本放在页面底部尽可能快的加载css样式表将样式表按照media type和media query区分，这样有助于我们将css资源标记成非阻塞渲染的资源。非阻塞的资源还是会被浏览器下载，只是优先级较低\n2.3 第三步：把DOM和CSSOM组合成渲染树（render tree）\n2.4 第四步：在渲染树的基础上进行布局，计算每个节点的几何结构\n布局(layout)：定位坐标和大小，是否换行，各种position,overflow,z-index属性\n2.5 调用 GPU 绘制，合成图层，显示在屏幕上\n将渲染树的各个节点绘制到屏幕上，这一步被称为绘制painting\n3. 渲染优化相关\n3.1 Load 和 DOMContentLoaded 区别\nLoad事件触发代表页面中的DOM，CSS，JS，图片已经全部加载完毕。DOMContentLoaded事件触发代表初始的HTML被完全加载和解析，不需要等待CSS，JS，图片加载\n3.2 图层\n一般来说，可以把普通文档流看成一个图层。特定的属性可以生成一个新的图层。不同的图层渲染互不影响，所以对于某些频繁需要渲染的建议单独生成一个新图层，提高性能。但也不能生成过多的图层，会引起反作用。\n通过以下几个常用属性可以生成新图层\n3D变换：translate3d、translateZwill-changevideo、iframe标签通过动画实现的opacity动画转换position: fixed\n3.3 重绘（Repaint）和回流（Reflow）\n重绘和回流是渲染步骤中的一小节，但是这两个步骤对于性能影响很大\n重绘是当节点需要更改外观而不会影响布局的，比如改变color就叫称为重绘回流是布局或者几何属性需要改变就称为回流。\n回流必定会发生重绘，重绘不一定会引发回流。回流所需的成本比重绘高的多，改变深层次的节点很可能导致父节点的一系列回流\n以下几个动作可能会导致性能问题\n改变window大小改变字体添加或删除样式文字改变定位或者浮动盒模型\n很多人不知道的是，重绘和回流其实和 Event loop 有关\n当Event loop执行完Microtasks后，会判断document是否需要更新。因为浏览器是60Hz的刷新率，每16ms才会更新一次。然后判断是否有resize或者scroll，有的话会去触发事件，所以resize和scroll事件也是至少16ms才会触发一次，并且自带节流功能。判断是否触发了media query更新动画并且发送事件判断是否有全屏操作事件执行requestAnimationFrame回调执行IntersectionObserver回调，该方法用于判断元素是否可见，可以用于懒加载上，但是兼容性不好更新界面以上就是一帧中可能会做的事情。如果在一帧中有空闲时间，就会去执行requestIdleCallback回调\n常见的引起重绘的属性\ncolorborder-stylevisibilitybackgroundtext-decorationbackground-imagebackground-positionbackground-repeatoutline-coloroutlineoutline-styleborder-radiusoutline-widthbox-shadowbackground-size\n3.4 常见引起回流属性和方法\n任何会改变元素几何信息(元素的位置和尺寸大小)的操作，都会触发重排，下面列一些栗子\n添加或者删除可见的DOM元素；元素尺寸改变——边距、填充、边框、宽度和高度内容变化，比如用户在input框中输入文字浏览器窗口尺寸改变——resize事件发生时计算offsetWidth和offsetHeight属性设置style属性的值\n回流影响的范围\n由于浏览器渲染界面是基于流失布局模型的，所以触发重排时会对周围DOM重新排列，影响的范围有两种\n全局范围：从根节点html开始对整个渲染树进行重新布局。局部范围：对渲染树的某部分或某一个渲染对象进行重新布局\n全局范围回流\n<body> <div class=\"hello\"> <h4>hello</h4> <p><strong>Name:</strong>BDing</p> <h5>male</h5> <ol> <li>coding</li> <li>loving</li> </ol> </div> </body>\n当p节点上发生reflow时，hello和body也会重新渲染，甚至h5和ol都会收到影响\n局部范围回流\n用局部布局来解释这种现象：把一个dom的宽高之类的几何信息定死，然后在dom内部触发重排，就只会重新渲染该dom内部的元素，而不会影响到外界\n3.5 减少重绘和回流\n使用translate替代top\n<div class=\"test\"></div> <style> .test { position: absolute; top: 10px; width: 100px; height: 100px; background: red; } </style> <script> setTimeout(() => { // 引起回流 document.querySelector('.test').style.top = '100px' }, 1000) </script>\n使用visibility替换display: none，因为前者只会引起重绘，后者会引发回流（改变了布局）把DOM离线后修改，比如：先把DOM给display:none(有一次Reflow)，然后你修改100次，然后再把它显示出来不要把DOM结点的属性值放在一个循环里当成循环里的变量\nfor(leti=0;i<1000;i++){// 获取 offsetTop 会导致回流，因为需要去获取正确的值console.log(document.querySelector('.test').style.offsetTop)}\n不要使用table布局，可能很小的一个小改动会造成整个table的重新布局动画实现的速度的选择，动画速度越快，回流次数越多，也可以选择使用requestAnimationFrameCSS选择符从右往左匹配查找，避免DOM深度过深将频繁运行的动画变为图层，图层能够阻止该节点回流影响别的元素。比如对于video标签，浏览器会自动将该节点变为图层。", "f": [{"q": "一个卡片组件只把文字颜色从黑色改成红色，开发却说一定会重新计算整页布局；你会怎样反驳？", "a": "单纯修改color通常只改变外观而不改变元素几何结构，因此会触发重绘，不必然触发布局回流。只有宽高、边距、字体或内容等影响位置和尺寸的变化才需要重新布局；不过重绘仍有成本，影响区域过大时也可能造成卡顿。"}, {"q": "一个 1000 行列表在循环中反复读取offsetHeight，同时逐项修改style，滚动时明显卡顿，你会怎样改造？", "a": "这类读写交替可能迫使浏览器反复获得最新布局结果，应先批量读取所需尺寸并缓存，再集中修改样式，避免在循环中持续查询几何属性。大量节点还可先离线或隐藏后统一修改，但display: none的切换本身会产生回流，需要权衡修改次数。"}, {"q": "动效设计要求悬浮按钮每帧修改top，视觉同学又不接受改变运动轨迹；你会选择什么实现，并说明限制？", "a": "应保持相同轨迹但改用transform: translate(...)表达位移，避免持续修改top引发几何布局和后续重绘。频繁动画还可使用requestAnimationFrame对齐浏览器更新节奏；若动画必须真实改变文档流位置，就无法完全绕开回流。"}, {"q": "首屏的HTML很小，但头部同步加载大型脚本和多份样式表后长时间空白，你会沿渲染链路排查什么？", "a": "先检查同步script是否暂停了HTML解析，以及样式表是否迟迟未完成CSSOM构建并影响后续渲染和脚本执行。可将非关键脚本后移或使用defer、async，并尽快加载必要样式、按media条件拆分；依赖执行顺序的脚本不能盲目改为async。"}, {"q": "一个长列表滚动时大量区域持续重绘，团队准备给每个列表项都加will-change，你会批准这个方案吗？", "a": "不应直接给所有列表项提层；独立图层可隔离频繁渲染区域，但图层过多也会产生反作用。应先确认真正高频变化的节点，只对动画或重绘热点使用will-change、translate3d等手段，并在性能面板中验证布局与绘制是否确实减少。"}, {"q": "监控显示DOMContentLoaded很早触发，但首屏图片仍未出现，产品却认为页面已经完全加载；你会如何解释并设置判断口径？", "a": "DOMContentLoaded只表示初始HTML已完成加载和解析，不要求图片等资源全部完成，因此不能作为完整页面就绪的唯一口径。若关注全部资源完成应观察load，若关注可交互首屏则还要结合脚本执行和实际渲染状态；两者代表的业务体验并不相同。"}]}, {"s": "浏览器", "t": "缓存机制", "m": "浏览器缓存的关键区别是是否发送请求：强缓存命中时不请求服务器，协商缓存则要向服务器确认资源有没有变化。首次响应会连同缓存头一起保存，后续浏览器根据Cache-Control或Expires判断缓存是否仍然有效；失效后再进入协商阶段。no-cache并不是完全不缓存，而是使用前必须向服务器校验，资源未变化时可以避免重新下载。真正禁止保存的是no-store，而public和private则用于控制代理缓存能否保存响应。\n1. 首先得明确 http 缓存的好处\n减少了冗余的数据传输，减少网费减少服务器端的压力Web缓存能够减少延迟与网络阻塞，进而减少显示某个资源所用的时间加快客户端加载网页的速度\n2. 常见 http 缓存的类型\n私有缓存（一般为本地浏览器缓存）代理缓存\n3. 然后谈谈本地缓存\n本地缓存是指浏览器请求资源时命中了浏览器本地的缓存资源，浏览器并不会发送真正的请求给服务器了。它的执行过程是\n第一次浏览器发送请求给服务器时，此时浏览器还没有本地缓存副本，服务器返回资源给浏览器，响应码是200 OK，浏览器收到资源后，把资源和对应的响应头一起缓存下来第二次浏览器准备发送请求给服务器时候，浏览器会先检查上一次服务端返回的响应头信息中的Cache-Control，它的值是一个相对值，单位为秒，表示资源在客户端缓存的最大有效期，过期时间为第一次请求的时间减去Cache-Control的值，过期时间跟当前的请求时间比较，如果本地缓存资源没过期，那么命中缓存，不再请求服务器如果没有命中，浏览器就会把请求发送给服务器，进入缓存协商阶段。\n与本地缓存相关的头有：Cache-Control、Expires，Cache-Control有多个可选值代表不同的意义，而Expires就是一个日期格式的绝对值。\n3.1 Cache-Control\nCache-Control是HTPP缓存策略中最重要的头，它是HTTP/1.1中出现的，它由如下几个值\nno-cache：不使用本地缓存。需要使用缓存协商，先与服务器确认返回的响应是否被更改，如果之前的响应中存在ETag，那么请求的时候会与服务端验证，如果资源未被更改，则可以避免重新下载no-store：直接禁止游览器缓存数据，每次用户请求该资源，都会向服务器发送一个请求，每次都会下载完整的资源public：可以被所有的用户缓存，包括终端用户和CDN等中间代理服务器。private：只能被终端用户的浏览器缓存，不允许CDN等中继缓存服务器对其缓存。max-age：从当前请求开始，允许获取的响应被重用的最长时间（秒）。must-revalidate，当缓存过期时，需要去服务端校验缓存的有效性。\n例如：Cache-Control: public, max-age=1000# 表示资源可以被所有用户以及代理服务器缓存，最长时间为1000秒。\n注意，虽然你可能在其他资料中看到可以使用 meta 标签来设置缓存，比如像下面的形式：\n<meta http-equiv=\"expires\" content=\"Wed, 20 Jun 2021 22:33:00 GMT\"\n但在 HTML5 规范中，并不支持这种方式，所以尽量不要使用 meta 标签来设置缓存。\n3.2 Expires\nExpires是HTTP/1.0出现的头信息，同样是用于决定本地缓存策略的头，它是一个绝对时间，时间格式是如Mon, 10 Jun 2015 21:31:12 GMT，只要发送请求时间是在Expires之前，那么本地缓存始终有效，否则就会去服务器发送请求获取新的资源。如果同时出现Cache-Control：max-age和Expires，那么max-age优先级更高。他们可以这样组合使用\nCache-Control: public Expires: Wed, Jan 10 2018 00:27:04 GMT\n3.3 所谓的缓存协商\n当第一次请求时服务器返回的响应头中存在以下情况时\n没有Cache-Control和ExpiresCache-Control和Expires过期了Cache-Control的属性设置为no-cache时\n那么浏览器第二次请求时就会与服务器进行协商，询问浏览器中的缓存资源是不是旧版本，需不需要更新，此时，服务器就会做出判断，如果缓存和服务端资源的最新版本是一致的，那么就无需再次下载该资源，服务端直接返回304 Not Modified状态码，如果服务器发现浏览器中的缓存已经是旧版本了，那么服务器就会把最新资源的完整内容返回给浏览器，状态码就是200 Ok，那么服务端是根据什么来判断浏览器的缓存是不是最新的呢？其实是根据HTTP的另外两组头信息，分别是：Last-Modified/If-Modified-Since与ETag/If-None-Match。\nLast-Modified 与 If-Modified-Since\n具体工作流程如下：\n浏览器第一次请求资源时，服务器会把资源的最新修改时间Last-Modified:Thu, 29 Dec 2011 18:23:55 GMT放在响应头中返回给浏览器第二次请求时，浏览器就会把上一次服务器返回的修改时间放在请求头If-Modified-Since:Thu, 29 Dec 2011 18:23:55发送给服务器，服务器就会拿这个时间跟服务器上的资源的最新修改时间进行对比服务端再次收到请求，根据请求头If-Modified-Since的值，判断相关资源是否有变化，如果没有，则返回304 Not Modified，并且不返回资源内容，浏览器使用资源缓存值；否则正常返回资源内容，且更新Last-Modified响应头内容。\n如果两者相等或者大于服务器上的最新修改时间，那么表示浏览器的缓存是有效的，此时缓存会命中，服务器就不再返回内容给浏览器了，同时Last-Modified头也不会返回，因为资源没被修改，返回了也没什么意义。如果没命中缓存则最新修改的资源连同Last-Modified头一起返回\n这种方式虽然能判断缓存是否失效，但也存在两个问题：\n精度问题，Last-Modified的时间精度为秒，如果在1秒内发生修改，那么缓存判断可能会失效；准度问题，考虑这样一种情况，如果一个文件被修改，然后又被还原，内容并没有发生变化，在这种情况下，浏览器的缓存还可以继续使用，但因为修改时间发生变化，也会重新返回重复的内容。\n第一次请求返回的响应头Cache-Control:max-age=3600Expires: Fri, Jan12201800:27:04 GMT Last-Modified: Wed, Jan10201800:27:04 GMT\n第二次请求的请求头信息If-Modified-Since: Wed, Jan10201800:27:04 GMT\n这组头信息是基于资源的修改时间来判断资源有没有更新，另一种方式就是根据资源的内容来判断，就是接下来要讨论的ETag与If-None-Match\nETag与If-None-Match\n为了解决精度问题和准度问题，HTTP 提供了另一种不依赖于修改时间，而依赖于文件哈希值的精确判断缓存的方式，那就是响应头部字段 ETag 和请求头部字段 If-None-Match。\nETag/If-None-Match与Last-Modified/If-Modified-Since的流程其实是类似的，唯一的区别是它基于资源的内容的摘要信息（比如MD5 hash）来判断\n浏览器发送第二次请求时，会把第一次的响应头信息ETag的值放在If-None-Match的请求头中发送到服务器，与最新的资源的摘要信息对比，如果相等，取浏览器缓存，否则内容有更新，最新的资源连同最新的摘要信息返回。用ETag的好处是如果因为某种原因到时资源的修改时间没改变，那么用ETag就能区分资源是不是有被更新。\n具体工作流程如下：\n浏览器第一次请求资源，服务端在返响应头中加入Etag字段，Etag字段值为该资源的哈希值当浏览器再次跟服务端请求这个资源时，在请求头上加上If-None-Match，值为之前响应头部字段ETag的值；服务端再次收到请求，将请求头If-None-Match字段的值和响应资源的哈希值进行比对，如果两个值相同，则说明资源没有变化，返回304 Not Modified；否则就正常返回资源内容，无论是否发生变化，都会将计算出的哈希值放入响应头部的ETag字段中\n这种缓存比较的方式也会存在一些问题，具体表现在以下两个方面。\n计算成本。生成哈希值相对于读取文件修改时间而言是一个开销比较大的操作，尤其是对于大文件而言。如果要精确计算则需读取完整的文件内容，如果从性能方面考虑，只读取文件部分内容，又容易判断出错。计算误差。HTTP 并没有规定哈希值的计算方法，所以不同服务端可能会采用不同的哈希值计算方式。这样带来的问题是，同一个资源，在两台服务端产生的 Etag 可能是不相同的，所以对于使用服务器集群来处理请求的网站来说，使用 Etag 的缓存命中率会有所降低。\n需要注意的是，强制缓存的优先级高于协商缓存，在协商缓存中，Etag 优先级比 Last-Modified高\n第一次请求返回的响应头：Cache-Control: public, max-age=31536000ETag:\"15f0fff99ed5aae4edffdd6496d7131f\"\n第二次请求的请求头信息：If-None-Match:\"15f0fff99ed5aae4edffdd6496d7131f\"\n缓存位置\n浏览器缓存的位置的话，可以分为四种,优先级从高到低排列分别👇\nService WorkerMemory CacheDisk CachePush Cache\nService Worker\n这个应用场景比如PWA，它借鉴了Web Worker思路，由于它脱离了浏览器的窗体，因此无法直接访问DOM。它能完成的功能比如：离线缓存、消息推送和网络代理，其中离线缓存就是Service Worker Cache。\nMemory Cache\n指的是内存缓存，从效率上讲它是最快的，从存活时间来讲又是最短的，当渲染进程结束后，内存缓存也就不存在了。\nDisk Cache\n存储在磁盘中的缓存，从存取效率上讲是比内存缓存慢的，优势在于存储容量和存储时长。\nDisk Cache VS Memory Cache\n两者对比，主要的策略👇\n内容使用率高的话，文件优先进入磁盘比较大的JS，CSS文件会直接放入磁盘，反之放入内存。\nPush Cache\n推送缓存，这算是浏览器中最后一道防线吧，它是HTTP/2的内容\n浏览器缓存总结\n浏览器缓存分为强缓存和协商缓存。当客户端请求某个资源时，获取缓存的流程如下\n先根据这个资源的一些 http header 判断它是否命中强缓存，先检查Cache-Control，如果命中，则直接从本地获取缓存资源，不会发请求到服务器；当强缓存没有命中时，客户端会发送请求到服务器，服务器通过另一些request header验证这个资源是否命中协商缓存，称为http再验证，如果命中，服务器将请求返回，但不返回资源，而是返回304告诉客户端直接从缓存中获取，客户端收到返回后就会从缓存中获取资源；（服务器通过请求头中的If-Modified-Since或者If-None-Match字段检查资源是否更新）强缓存和协商缓存共同之处在于，如果命中缓存，服务器都不会返回资源； 区别是，强缓存不对发送请求到服务器，但协商缓存会。当协商缓存也没命中时，服务器就会将资源发送回客户端。当 ctrl+f5 强制刷新网页时，直接从服务器加载，跳过强缓存和协商缓存；当 f5刷新网页时，跳过强缓存，但是会检查协商缓存；\n强缓存\nExpires（该字段是 http1.0 时的规范，值为一个绝对时间的 GMT 格式的时间字符串，代表缓存资源的过期时间）Cache-Control:max-age（该字段是 http1.1的规范，强缓存利用其 max-age 值来判断缓存资源的最大生命周期，它的值单位为秒）\n协商缓\nLast-Modified（值为资源最后更新时间，随服务器response返回，即使文件改回去，日期也会变化）If-Modified-Since（通过比较两个时间来判断资源在两次请求期间是否有过修改，如果没有修改，则命中协商缓存）ETag（表示资源内容的唯一标识，随服务器response返回，仅根据文件内容是否变化判断）If-None-Match（服务器通过比较请求头部的If-None-Match与当前资源的ETag是否一致来判断资源是否在两次请求之间有过修改，如果没有修改，则命中协商缓存）", "f": [{"q": "商品详情页第二次打开时，Network显示请求携带If-None-Match，但响应是200而不是304；有人据此断言浏览器没有使用缓存，你怎么判断？", "a": "不能仅凭200判断浏览器未使用缓存，因为携带If-None-Match已说明客户端正在用上次保存的ETag发起再验证。若服务端当前ETag不一致，就应返回新资源和200；只有标识一致时才返回不带资源实体的304。"}, {"q": "发版后的首页HTML已配置协商缓存，第二次请求带着If-None-Match；服务端需要怎样处理，浏览器收到响应后又怎样取资源？", "a": "服务端应比较请求中的If-None-Match与当前资源的ETag，一致时返回304，不再传输资源内容。浏览器随后从本地缓存读取实体；若两者不一致，则服务端返回更新后的资源，并随响应提供新的校验标识。"}, {"q": "一个高频访问的小图标和一个体积较大的JS文件都命中了本地缓存，前端同事认为它们必然进入同一种缓存位置，你会怎样纠正？", "a": "两者不一定进入相同位置，浏览器还会在Memory Cache与Disk Cache之间进行选择。内存缓存读取更快但随渲染进程结束而消失；较大的JS、CSS通常更倾向磁盘，而具体归属仍受浏览器策略影响。"}, {"q": "用户按普通F5后接口返回304，按Ctrl+F5却下载了完整资源；值班同学怀疑服务端随机失效缓存，你怎么解释并排查？", "a": "这种差异符合两种刷新方式的缓存流程：普通F5会跳过强缓存，但仍进行协商校验，所以可能收到304。Ctrl+F5会跳过强缓存和协商缓存，直接从服务端加载；排查时应对比请求头、状态码及实际传输内容。"}, {"q": "运营页更新频繁，架构师主张所有资源都只用Cache-Control: max-age，后端则主张全部依赖ETag；你如何说明两种方案的请求成本？", "a": "max-age命中强缓存时不会向服务器发请求，适合生命周期内无需重新确认的资源；ETag属于协商缓存，每次再验证仍会产生请求，但未变化时只返回304。选择取决于更新及时性要求，不能把免请求与免传资源视为同一件事。"}, {"q": "一个PWA页面断网后仍展示旧数据，清理普通磁盘缓存也没有恢复；同时DevTools标记资源来自Service Worker，下一步查哪里？", "a": "应优先检查Service Worker的离线缓存和网络代理逻辑，因为它的缓存位置优先于Memory Cache、Disk Cache和Push Cache。仅清理磁盘缓存可能绕不开其响应；还要核对注册状态与缓存更新策略，但不能直接归因于HTTP强缓存。"}]}, {"s": "浏览器", "t": "浏览器存储", "m": "浏览器端常用的持久化方案有cookie、localStorage、sessionStorage和IndexedDB，选择时主要看数据用途、生命周期与容量。cookie通常保存身份或登录状态，约4K，请求时会自动携带；另外三者不随请求发送，更适合客户端数据。localStorage会一直保留到被清理，sessionStorage通常随页面会话结束而清理，大量结构化数据可以考虑IndexedDB。涉及登录态时还要关注HttpOnly、Secure和SameSite，分别用于限制脚本访问、安全传输以及降低跨站请求风险。\n我们经常需要对业务中的一些数据进行存储，通常可以分为 短暂性存储 和 持久性储存。\n短暂性的时候，我们只需要将数据存在内存中，只在运行时可用持久性存储，可以分为 浏览器端 与 服务器端浏览器:cookie: 通常用于存储用户身份，登录状态等http中自动携带， 体积上限为4K， 可自行设置过期时间localStorage / sessionStorage: 长久储存/窗口关闭删除， 体积限制为4~5MindexDB服务器:分布式缓存redis数据库\n浏览器:cookie: 通常用于存储用户身份，登录状态等http中自动携带， 体积上限为4K， 可自行设置过期时间localStorage / sessionStorage: 长久储存/窗口关闭删除， 体积限制为4~5MindexDB服务器:分布式缓存redis数据库\ncookie: 通常用于存储用户身份，登录状态等http中自动携带， 体积上限为4K， 可自行设置过期时间localStorage / sessionStorage: 长久储存/窗口关闭删除， 体积限制为4~5MindexDB\nhttp中自动携带， 体积上限为4K， 可自行设置过期时间\n分布式缓存redis数据库\ncookie和localSrorage、session、indexDB 的区别\n从上表可以看到，cookie已经不建议用于存储。如果没有大量数据存储需求的话，可以使用localStorage和sessionStorage。对于不怎么改变的数据尽量使用localStorage存储，否则可以用sessionStorage存储。\n对于cookie，我们还需要注意安全性\nName，即该Cookie的名称。Cookie一旦创建，名称便不可更改。Value，即该Cookie的值。如果值为Unicode字符，需要为字符编码。如果值为二进制数据，则需要使用BASE64编码。Max Age，即该Cookie失效的时间，单位秒，也常和Expires一起使用，通过它可以计算出其有效时间。Max Age如果为正数，则该Cookie在Max Age秒之后失效。如果为负数，则关闭浏览器时Cookie即失效，浏览器也不会以任何形式保存该Cookie。Path，即该Cookie的使用路径。如果设置为/path/，则只有路径为/path/的页面可以访问该Cookie。如果设置为/，则本域名下的所有页面都可以访问该Cookie。Domain，即可以访问该Cookie的域名。例如如果设置为.zhihu.com，则所有以zhihu.com，结尾的域名都可以访问该Cookie。Size字段，即此Cookie的大小。Http字段，即Cookie的httponly属性。若此属性为true，则只有在HTTP Headers中会带有此 Cookie 的信息，而不能通过document.cookie来访问此 Cookie。Secure，即该Cookie是否仅被使用安全协议传输。安全协议。安全协议有HTTPS、SSL等，在网络上传输数据之前先将数据加密。默认为false。", "f": [{"q": "登录页把明文用户编号写进Cookie，开发认为加上HttpOnly后就绝对安全；作为安全评审你会指出什么？", "a": "HttpOnly只限制脚本通过document.cookie读取，并不等于明文值本身安全。登录态相关的value不应直接使用明文用户标识，还应结合Secure限制安全协议传输，并用SameSite降低跨站请求携带带来的风险。"}, {"q": "一个后台系统要保存登录态、当前窗口的筛选条件和较大规模的结构化离线数据，你会分别落到哪类存储？", "a": "登录态通常由Cookie承载，筛选条件若只需随当前窗口存在可放sessionStorage，较大且需持久化的数据更适合IndexedDB。Cookie约受4K限制且随请求进入请求头，localStorage、sessionStorage通常只有数兆容量。"}, {"q": "客服要求关闭页面后筛选条件消失，但产品又要求重新打开浏览器仍保留用户偏好；只允许在localStorage和sessionStorage间选，你怎么拆分？", "a": "筛选条件应放入sessionStorage，因为其生命周期与页面窗口相关，关闭后会被清理；用户偏好应放入localStorage，除非主动清理否则会持续存在。两者都不会自动参与服务端通信，但都不适合超出其容量边界的数据。"}, {"q": "线上接口请求头突然膨胀，排查发现同一域名下写入了大量业务Cookie；业务方却说这些数据只在个人中心页面使用，你怎么处理？", "a": "应把不需要服务端识别的业务数据移出Cookie，按生命周期改放localStorage、sessionStorage或更大容量的IndexedDB。Cookie会在匹配其作用范围的请求中自动进入请求头，即使接口本身不用这些值，也会持续产生传输负担。"}, {"q": "多子域系统把登录Cookie的Domain设为顶级业务域、Path设为/，便利性与隔离性发生冲突，你会怎么评审？", "a": "该配置会扩大Cookie的可用范围，使匹配该域及路径的更多页面和请求都可能携带它。若登录态只服务某个子域或路径，应尽量收窄Domain与Path；范围缩小会增加多系统共享登录态的设计成本。"}, {"q": "安全团队要求登录Cookie只能走HTTPS、脚本不可读取，并尽量减少跨站请求携带；你会配置哪些属性，还要提醒什么边界？", "a": "应配置Secure、HttpOnly与合适的SameSite：分别限制安全协议传输、阻止脚本读取并约束跨站携带。它们针对的是不同风险面，不能互相替代；同时仍需妥善处理登录态value，不能把敏感身份直接作为明文保存。"}]}, {"s": "浏览器", "t": "跨域方案", "m": "跨域的核心是绕开或满足浏览器同源策略，常见方案可分为服务端放行、代理转发和标签请求。接口能改服务端时优先配置CORS；开发环境可用webpack devServer.proxy或http-proxy-middleware做反向代理。只需发起GET请求时可以用JSONP，图片上报则适合img ping，但它拿不到响应正文。页面间通信可考虑iframe或postMessage，同时要留意交互安全和数据传递限制。\n很多种方法，但万变不离其宗，都是为了搞定同源策略。重用的有jsonp、iframe、cors、img、HTML5 postMessage等等。其中用到html标签进行跨域的原理就是html不受同源策略影响。但只是接受Get的请求方式，这个得清楚。\n延伸1：img iframe script 来发送跨域请求有什么优缺点？\n1.iframe\n优点：跨域完毕之后DOM操作和互相之间的JavaScript调用都是没有问题的缺点：1.若结果要以URL参数传递，这就意味着在结果数据量很大的时候需要分割传递，巨烦。2.还有一个是iframe本身带来的，母页面和iframe本身的交互本身就有安全性限制。\n2. script\n优点：可以直接返回json格式的数据，方便处理缺点：只接受GET请求方式\n3. 图片ping\n优点：可以访问任何url，一般用来进行点击追踪，做页面分析常用的方法缺点：不能访问响应文本，只能监听是否响应\n延伸2：配合 webpack 进行反向代理？\nwebpack在devServer选项里面提供了一个proxy的参数供开发人员进行反向代理\n'/api':{target:'http://www.example.com',// your target hostchangeOrigin:true,// needed for virtual hosted sitespathRewrite:{'^/api':''// rewrite path}},\n然后再配合http-proxy-middleware插件对api请求地址进行代理\nconstexpress=require('express');constproxy=require('http-proxy-middleware');// proxy api requestsconstexampleProxy=proxy(options);// 这里的 options 就是 webpack 里面的 proxy 选项对应的每个选项// mount `exampleProxy` in web serverconstapp=express();app.use('/api',exampleProxy);app.listen(3000);\n然后再用nginx把允许跨域的源地址添加到报头里面即可\n说到nginx，可以再谈谈CORS配置，大致如下\nlocation/{if($request_method='OPTIONS'){add_header'Access-Control-Allow-Origin''*';add_header'Access-Control-Allow-Methods''GET, POST, OPTIONS';add_header'Access-Control-Allow-Credentials''true';add_header'Access-Control-Allow-Headers''DNT, X-Mx-ReqToken, Keep-Alive, User-Agent, X-Requested-With, If-Modified-Since, Cache-Control, Content-Type';add_header'Access-Control-Max-Age'86400;add_header'Content-Type''text/plain charset=UTF-8';add_header'Content-Length'0;return200;}}", "f": [{"q": "埋点页面用new Image().src上报成功，产品却要求读取服务端返回的用户标签；沿用图片ping为什么行不通？", "a": "图片ping可以向跨域URL发起请求，但脚本不能读取响应正文，因此无法取得用户标签。它更适合点击追踪和页面分析，只能借助加载或失败事件判断是否响应；若业务依赖响应数据，就必须改用允许读取结果的受控方案。"}, {"q": "开发环境把/api配到devServer.proxy后请求正常，静态文件部署到测试环境却开始跨域；你会把代理落在哪一层？", "a": "devServer.proxy只属于本地开发服务器，打包后的静态资源不会携带这段转发逻辑。测试环境应由实际承载流量的服务处理，例如让Nginx按/api反向代理，或由接口端配置CORS；两边规则不一致仍会造成环境差异。"}, {"q": "旧页面用script加载跨域数据，接口负责人现在要求改成POST并提交较大的筛选条件；原方案还能继续吗？", "a": "依赖script的跨域方案只能发起GET，无法直接满足新的POST约束。把大量数据塞进查询参数还会受到URL长度、缓存和日志暴露等边界影响，应改由代理或CORS承载请求；迁移成本是服务端需要同步调整接口策略。"}, {"q": "线上跨域接口连真实业务日志都没有，只看到浏览器先发出OPTIONS；你会先核对Nginx的哪些响应内容？", "a": "真实请求未到业务服务时，应先检查预检响应是否允许当前源、方法和请求头，并确认OPTIONS能直接返回成功状态。还要核对Access-Control-Allow-Credentials与来源策略是否匹配，以及配置是否覆盖目标路径；任一项不匹配，浏览器都可能拦住后续请求。"}, {"q": "安全负责人反对放开整站CORS，前端又希望所有环境都使用统一的/api地址；你会如何取舍？", "a": "更稳妥的选择是让页面始终请求同域/api，再由各环境的Nginx转发到真实接口，从浏览器视角规避跨域。若必须让第三方源直接访问，再按明确来源、方法和请求头配置CORS；代理增加运维配置，开放CORS则扩大暴露边界。"}, {"q": "微前端容器需要和跨域iframe交换筛选状态，数据量持续增长；你会继续拼接URL参数还是改用什么机制？", "a": "大量状态通过URL参数传递会面临长度限制、拆分复杂和信息暴露，不适合作为持续通信通道。应使用postMessage传递结构化消息，并在接收端校验origin、消息类型和数据格式；它解决通信问题，但不会自动解除跨域DOM的安全限制。"}]}, {"s": "浏览器", "t": "XSS 和 CSRF", "m": "XSS是把恶意代码注入页面并执行，CSRF是借用用户已有登录态发送非本人意愿的请求。XSS分持久型和非持久型，防护重点是不信任用户输入：普通内容做转义，富文本采用白名单过滤，并用CSP限制资源加载和执行。CSRF利用了浏览器自动携带目标域名Cookie的机制，但攻击站点通常拿不到该Cookie。工程上应避免用GET修改数据，并结合验证token、验证码及SameSite等措施。\n1. XSS\n涉及面试题：什么是XSS攻击？如何防范XSS攻击？什么是CSP？\nXSS简单点来说，就是攻击者想尽一切办法将可以执行的代码注入到网页中。XSS可以分为多种类型，但是总体上我认为分为两类：持久型和非持久型。持久型也就是攻击的代码被服务端写入进数据库中，这种攻击危害性很大，因为如果网站访问量很大的话，就会导致大量正常访问页面的用户都受到攻击。\n举个例子，对于评论功能来说，就得防范持久型XSS攻击，因为我可以在评论中输入以下内容\n这种情况如果前后端没有做好防御的话，这段评论就会被存储到数据库中，这样每个打开该页面的用户都会被攻击到。非持久型相比于前者危害就小的多了，一般通过修改URL参数的方式加入攻击代码，诱导用户访问链接从而进行攻击。\n举个例子，如果页面需要从URL中获取某些参数作为内容的话，不经过过滤就会导致攻击代码被执行\n<!-- http://www.domain.com?name=<script>alert(1)</script> --><div>{{name}}</div>\n但是对于这种攻击方式来说，如果用户使用Chrome这类浏览器的话，浏览器就能自动帮助用户防御攻击。但是我们不能因此就不防御此类攻击了，因为我不能确保用户都使用了该类浏览器。\n对于XSS攻击来说，通常有两种方式可以用来防御。\n转义字符\n首先，对于用户的输入应该是永远不信任的。最普遍的做法就是转义输入输出的内容，对于引号、尖括号、斜杠进行转义\nfunctionescape(str){str=str.replace(/&/g,'&amp;')str=str.replace(/</g,'&lt;')str=str.replace(/>/g,'&gt;')str=str.replace(/\"/g,'&quto;')str=str.replace(/'/g,'&#39;')str=str.replace(/`/g,'&#96;')str=str.replace(/\\//g,'&#x2F;')returnstr}\n通过转义可以将攻击代码<script>alert(1)</script>变成\n// -> &lt;script&gt;alert(1)&lt;&#x2F;script&gt;escape('<script>alert(1)</script>')\n但是对于显示富文本来说，显然不能通过上面的办法来转义所有字符，因为这样会把需要的格式也过滤掉。对于这种情况，通常采用白名单过滤的办法，当然也可以通过黑名单过滤，但是考虑到需要过滤的标签和标签属性实在太多，更加推荐使用白名单的方式\nconstxss=require('xss')lethtml=xss('<h1 id=\"title\">XSS Demo</h1><script>alert(\"xss\");</script>')// -> <h1>XSS Demo</h1>&lt;script&gt;alert(\"xss\");&lt;/script&gt;console.log(html)\n以上示例使用了js-xss来实现，可以看到在输出中保留了h1标签且过滤了script标签\nCSP\nCSP本质上就是建立白名单，开发者明确告诉浏览器哪些外部资源可以加载和执行。我们只需要配置规则，如何拦截是由浏览器自己实现的。我们可以通过这种方式来尽量减少XSS攻击。\n通常可以通过两种方式来开启 CSP：\n设置HTTP Header中的Content-Security-Policy设置meta标签的方式<meta http-equiv=\"Content-Security-Policy\">\n这里以设置HTTP Header来举例\n只允许加载本站资源\nContent-Security-Policy: default-src ‘self’\n只允许加载 HTTPS 协议图片\nContent-Security-Policy: img-src https://*\n允许加载任何来源框架\nContent-Security-Policy: child-src 'none'\n当然可以设置的属性远不止这些，你可以通过查阅文档(opens new window)的方式来学习，这里就不过多赘述其他的属性了。\n对于这种方式来说，只要开发者配置了正确的规则，那么即使网站存在漏洞，攻击者也不能执行它的攻击代码，并且CSP的兼容性也不错。\n2 CSRF\n跨站请求伪造（英语：Cross-site request forgery），也被称为one-click attack或者session riding，通常缩写为CSRF或者XSRF， 是一种挟制用户在当前已登录的Web应用程序上执行非本意的操作的攻击方法\nCSRF就是利用用户的登录态发起恶意请求\n如何攻击\n假设网站中有一个通过 Get 请求提交用户评论的接口，那么攻击者就可以在钓鱼网站中加入一个图片，图片的地址就是评论接口\n<img src=\"http://www.domain.com/xxx?comment='attack'\"/>\nres.setHeader('Set-Cookie',`username=poetry2;sameSite = strict;path=/;httpOnly;expires=${getCookirExpires()}`)\n在B网站，危险网站向A网站发起请求\n<!DOCTYPEhtml><html><body><!-- 利用img自动发送请求 --><imgsrc=\"http://localhost:8000/api/user/login\"/></body></html>\n会带上A网站的cookie\n// 在A网站下发cookie的时候，加上sameSite=strict，这样B网站在发送A网站请求，不会自动带上A网站的cookie，保证了安全// NAME=VALUE 赋予Cookie的名称及对应值// expires=DATE Cookie 的有效期// path=PATH 赋予Cookie的名称及对应值// domain=域名 作为 Cookie 适用对象的域名 （若不指定则默认为创建 Cookie 的服务器的域名） (一般不指定)// Secure 仅在 HTTPS 安全通信时才会发送 Cookie// HttpOnly 加以限制，使 Cookie 不能被 JavaScript 脚本访问// SameSite Lax|Strict|None 它允许您声明该Cookie是否仅限于第一方或者同一站点上下文res.setHeader('Set-Cookie',`username=poetry;sameSite=strict;path=/;httpOnly;expires=${getCookirExpires()}`)\n如何防御\nGet请求不对数据进行修改不让第三方网站访问到用户Cookie阻止第三方网站请求接口请求时附带验证信息，比如验证码或者tokenSameSite Cookies: 只能当前域名的网站发出的http请求，携带这个Cookie。当然，由于这是新的cookie属性，在兼容性上肯定会有问题\nCSRF攻击，仅仅是利用了http携带cookie的特性进行攻击的，但是攻击站点还是无法得到被攻击站点的cookie。这个和XSS不同，XSS是直接通过拿到Cookie等信息进行攻击的\n在CSRF攻击中，就Cookie相关的特性：\nhttp请求，会自动携带Cookie。携带的cookie，还是http请求所在域名的cookie。\n3 密码安全\n加盐\n对于密码存储来说，必然是不能明文存储在数据库中的，否则一旦数据库泄露，会对用户造成很大的损失。并且不建议只对密码单纯通过加密算法加密，因为存在彩虹表的关系\n通常需要对密码加盐，然后进行几次不同加密算法的加密\n// 加盐也就是给原密码添加字符串，增加原密码长度sha256(sha1(md5(salt+password+salt)))\n但是加盐并不能阻止别人盗取账号，只能确保即使数据库泄露，也不会暴露用户的真实密码。一旦攻击者得到了用户的账号，可以通过暴力破解的方式破解密码。对于这种情况，通常使用验证码增加延时或者限制尝试次数的方式。并且一旦用户输入了错误的密码，也不能直接提示用户输错密码，而应该提示账号或密码错误\n前端加密\n虽然前端加密对于安全防护来说意义不大，但是在遇到中间人攻击的情况下，可以避免明文密码被第三方获取\n4. 总结\nXSS：跨站脚本攻击，是一种网站应用程序的安全漏洞攻击，是代码注入的一种。常见方式是将恶意代码注入合法代码里隐藏起来，再诱发恶意代码，从而进行各种各样的非法活动\n防范：记住一点 “所有用户输入都是不可信的”，所以得做输入过滤和转义\nCSRF：跨站请求伪造，也称XSRF，是一种挟制用户在当前已登录的Web应用程序上执行非本意的操作的攻击方法。与XSS相比，XSS利用的是用户对指定网站的信任，CSRF利用的是网站对用户网页浏览器的信任。\n防范：用户操作验证（验证码），额外验证机制（token使用）等", "f": [{"q": "评论页已经把<、>全部转义了，安全同学却用一个带恶意参数的分享链接触发了脚本；团队因此认定“转义防不了XSS”，你怎么判断？", "a": "不能据此否定转义，先确认参数最终进入的是文本、属性、脚本还是其他上下文，以及是否存在绕过转义的直接插入。普通文本应按输出上下文转义；富文本则需白名单净化，单靠一套字符替换无法覆盖所有注入位置。"}, {"q": "内容平台允许作者提交含标题、链接和图片的HTML，前端负责人主张提交前用正则删掉<script>，后端认为原样入库即可；你会怎么落地？", "a": "不能把前端正则作为安全边界，调用接口即可绕过，而且危险标签和属性很多，黑名单容易遗漏。服务端应使用成熟净化方案按允许的标签、属性和资源规则处理，前端可同步净化改善预览，并用严格CSP限制可加载、可执行的资源。"}, {"q": "业务为了接入外部图片和统计脚本，要求把CSP放宽为允许任意来源；富文本白名单仍然保留，这时你接受吗？", "a": "不应直接允许任意来源，否则CSP对遗漏注入点的兜底能力会明显下降。应按资源类型分别配置可信来源，例如限制脚本、图片和子页面，并优先通过响应头下发；若业务必须放宽某类资源，要明确它不会连带放宽脚本执行。"}, {"q": "用户反馈打开一条历史评论后账号出现异常操作，数据库里发现了可执行片段，但安全人员抓不到攻击者读取Cookie的请求；你如何区分是持久型 `XSS还是CSRF``？", "a": "评论中的代码被入库并在访问者页面执行，首先按持久型XSS排查，不以是否成功读取Cookie作为必要条件。检查渲染入口、净化结果和CSP报告；若只是外站借浏览器自动携带目标域Cookie发请求、外站拿不到其内容，才更符合CSRF。"}, {"q": "支付团队已给会话Cookie设置SameSite=Strict，因此想删除所有CSRF token；接口里还保留一个通过GET修改订阅状态的入口，你同意吗？", "a": "不同意把SameSite当成唯一措施，更不能保留有副作用的GET，因为图片等元素就能自动发起这类请求。应先把修改操作改为合适的方法，再附带服务端可验证的token或必要的用户验证；SameSite、接口访问限制和验证信息属于纵深防御。"}]}, {"s": "浏览器", "t": "Service Worker", "m": "Service Worker本质上是运行在网页与浏览器、网络之间的代理，可拦截请求并提供离线缓存能力。它通常在install事件中预先缓存文件，再通过fetch事件决定返回缓存还是访问网络，因此常用于缓存静态文件和改善首屏加载。它不能直接操作DOM，需要通过postMessage与受控页面通信。使用时还要注意只能运行在本地环境或HTTPS下，并且作用范围受脚本所在路径限制。\nService workers本质上充当Web应用程序与浏览器之间的代理服务器，也可以在网络可用时作为浏览器和网络间的代理。它们旨在（除其他之外）使得能够创建有效的离线体验，拦截网络请求并基于网络是否可用以及更新的资源是否驻留在服务器上来采取适当的动作。他们还允许访问推送通知和后台同步API\n浏览器对 ServiceWorker 做了很多限制\n在ServiceWorker中无法直接访问DOM，但可以通过postMessage接口发送的消息来与其控制的页面进行通信ServiceWorker只能在本地环境下或HTTPS网站中使用ServiceWorker有作用域的限制，一个ServiceWorker脚本只能作用于当前路径及其子路径；\n目前该技术通常用来做缓存文件，提高首屏速度\n// index.jsif(navigator.serviceWorker){navigator.serviceWorker.register(\"sw.js\").then(function(registration){console.log(\"service worker 注册成功\");}).catch(function(err){console.log(\"servcie worker 注册失败\");});}// sw.js// 监听 `install` 事件，回调中缓存所需文件self.addEventListener(\"install\",e=>{e.waitUntil(caches.open(\"my-cache\").then(function(cache){returncache.addAll([\"./index.html\",\"./index.js\"]);}));});// 拦截所有请求事件// 如果缓存中已经有请求的数据就直接用缓存，否则去请求数据self.addEventListener(\"fetch\",e=>{e.respondWith(caches.match(e.request).then(function(response){if(response){returnresponse;}console.log(\"fetch source\");}));});\n打开页面，可以在开发者工具中的Application看到Service Worker已经启动了\n在 Cache 中也可以发现我们所需的文件已被缓存\n当我们重新刷新页面可以发现我们缓存的数据是从ServiceWorker中读取的", "f": [{"q": "新闻站把资源写入Cache API后就宣称“已经支持离线”，但断网刷新时HTML仍然白屏；这说明团队混淆了什么？", "a": "写入缓存不等于请求会自动命中缓存，必须由已激活且处于作用域内的Service Worker在fetch事件中返回匹配响应。还要确认入口文档确实被缓存并有离线回退；首次访问、注册失败或尚未被控制的页面仍可能无法离线。"}, {"q": "商城准备缓存index.html和index.js来提升再次访问速度，代码只在install中执行cache.addAll()；你会怎样补全最小可用链路？", "a": "注册脚本后，在install的event.waitUntil()中等待预缓存完成，并在fetch中用event.respondWith()返回caches.match(request)的结果。缓存未命中时必须继续请求网络或返回明确回退，否则处理器可能得不到有效响应；同时要保证站点使用HTTPS或本地环境。"}, {"q": "主站从根路径注册sw.js后缓存正常，迁到/shop/sw.js后首页请求不再被拦截，但/shop/cart正常；运维认为是浏览器缓存损坏，你怎么看？", "a": "更可能是作用域变化，而不是缓存损坏，因为Service Worker默认只能控制脚本所在路径及其子路径。应检查注册记录、当前页面的控制器与注册作用域；若确需覆盖首页，应调整脚本位置或在受控配置下声明合适作用域。"}, {"q": "线上显示Service Worker注册成功，Application面板也能看到它，但刷新后静态文件仍全部走网络；你按什么顺序排查？", "a": "先确认当前页面已被该注册实例控制，并核对页面路径是否落在其作用域内，再检查fetch事件是否触发。随后验证缓存名称、请求键和caches.match()是否一致，以及respondWith()是否真正返回了响应；只完成注册或安装并不代表页面已经从缓存读取。"}, {"q": "运营后台要求直接在Service Worker中读取表单DOM，以便断网时保存编辑内容；架构师则建议页面把数据发给它，你支持哪一方？", "a": "应由页面读取表单，再通过postMessage与Service Worker通信，因为后者不能直接访问DOM。离线缓存、请求拦截、推送和后台同步适合放在该代理层；页面状态采集仍属于受控页面职责，消息协议也要处理版本与校验。"}]}, {"s": "浏览器", "t": "DOM 节点操作", "m": "DOM节点操作主要包括创建、增删替换、查找和属性修改，关键是按场景选对接口。创建元素或文本可用createElement、createTextNode，批量组织节点时可先放进createDocumentFragment。插入、删除和替换分别可以用appendChild、removeChild、replaceChild或insertBefore。查找时精确id可用getElementById，复杂选择条件更适合querySelector或querySelectorAll，属性则通过getAttribute、setAttribute等接口处理。\n（1）创建新节点\ncreateDocumentFragment()//创建一个DOM片段createElement()//创建一个具体的元素createTextNode()//创建一个文本节点\n（2）添加、移除、替换、插入\nappendChild(node)removeChild(node)replaceChild(new,old)insertBefore(new,old)\n（3）查找\ngetElementById();getElementsByName();getElementsByTagName();getElementsByClassName();querySelector();querySelectorAll();\n（4）属性操作\ngetAttribute(key);setAttribute(key,value);hasAttribute(key);removeAttribute(key);", "f": [{"q": "表格里有一个<tr id=\"row-7\">，同事说querySelector('#row-7')返回的是“动态集合”，删掉该行后变量会自动变空；你怎么纠正？", "a": "querySelector()返回首个匹配元素或空值，不是会随文档变化的集合，已保存的变量也不会因节点移除而自动清空。若要获取多个匹配项应使用querySelectorAll()；代码还持有节点引用时，该引用依然存在，不能把查询结果与文档结构变化混为一谈。"}, {"q": "评论列表一次插入 300 个新条目，代码在循环里逐个appendChild()到可见容器；只允许使用题目列出的原生节点能力，你会怎么改？", "a": "先用createDocumentFragment()作为临时容器，把createElement()和createTextNode()创建的条目依次追加进去，最后一次性挂到列表。这样组织也便于在入树前完成内容和属性设置；片段只减少零散插入，仍需控制总节点规模。"}, {"q": "设计稿要求把现有占位节点换成真实卡片，并保证它仍位于原来的兄弟节点之间；有人提议先removeChild()再appendChild()，你选什么？", "a": "应使用父节点的replaceChild(newNode, oldNode)，它直接在原位置完成替换，不会把新卡片移动到容器末尾。若只是把节点插到某个兄弟节点前，则用insertBefore(newNode, referenceNode)；操作前仍要确保旧节点确实属于该父节点。"}, {"q": "线上筛选器偶发报错，日志显示removeChild的目标节点并不在当前列表中；页面同时会重新查询并替换整块列表，你怎么定位？", "a": "先核对删除时使用的父节点和目标节点是否来自同一轮查询，整块替换后旧引用可能已不属于新容器。删除前检查父子关系或重新按稳定标识查找，再调用removeChild()；若目标可能已被其他流程删除，应把并发更新设计成幂等。"}, {"q": "组件要判断按钮是否配置disabled，评审中一方读取getAttribute('disabled')，另一方用hasAttribute('disabled')；这里只关心属性是否存在，该怎么选？", "a": "应使用hasAttribute('disabled')，因为需求判断的是属性存在性，而不是取得其字符串值。需要读取具体属性内容时才用getAttribute()，写入和清理分别用setAttribute()、removeAttribute()；不要把缺失属性和值为空混作同一业务状态。"}]}, {"s": "浏览器", "t": "掌握页面的加载过程", "m": "页面加载就是浏览器获取并从上到下解析HTML，遇到外部资源按需下载，解析到<body>后逐步渲染页面。普通<script>会暂停文档解析，等脚本下载、解析和执行完再继续，所以放在<head>里可能拖慢内容展示。工程中可以把脚本放到文档底部，或用defer、async控制加载和执行时机；前者通常等解析完成后执行，后者下载完成就执行且顺序不可预测。判断完成状态时，DOMContentLoaded表示初始HTML已解析，load则还要等待DOM、CSS、JS和图片全部加载。\n网页加载流程\n当我们打开网址的时候，浏览器会从服务器中获取到 HTML 内容浏览器获取到 HTML 内容后，就开始从上到下解析 HTML 的元素<head>元素内容会先被解析，此时浏览器还没开始渲染页面我们看到<head>元素里有用于描述页面元数据的<meta>元素，还有一些<link>元素涉及外部资源（如图片、CSS 样式等），此时浏览器会去获取这些外部资源。除此之外，我们还能看到<head>元素中还包含着不少的<script>元素，这些<script>元素通过src属性指向外部资源当浏览器解析到这里时（步骤 3），会暂停解析并下载 JavaScript 脚本当 JavaScript 脚本下载完成后，浏览器的控制权转交给 JavaScript 引擎。当脚本执行完成后，控制权会交回给渲染引擎，渲染引擎继续往下解析HTML页面此时<body>元素内容开始被解析，浏览器开始渲染页面\n我们看到<head>元素里有用于描述页面元数据的<meta>元素，还有一些<link>元素涉及外部资源（如图片、CSS 样式等），此时浏览器会去获取这些外部资源。除此之外，我们还能看到<head>元素中还包含着不少的<script>元素，这些<script>元素通过src属性指向外部资源\n在这个过程中，我们看到<head>中放置的<script>元素会阻塞页面的渲染过程：把 JavaScript 放在<head>里，意味着必须把所有 JavaScript 代码都下载、解析和解释完成后，才能开始渲染页面。如果外部脚本加载时间很长（比如一直无法完成下载），就会造成网页长时间失去响应，浏览器就会呈现“假死”状态，用户体验会变得很糟糕因此，对于对性能要求较高、需要快速将内容呈现给用户的网页，常常会将 JavaScript 脚本放在<body>的最后面。这样可以避免资源阻塞，页面得以迅速展示。我们还可以使用defer/async/preload等属性来标记<script>标签，来控制 JavaScript 的加载顺序\n延迟加载的方式有哪些\njs 的加载、解析和执行会阻塞页面的渲染过程，因此我们希望 js 脚本能够尽可能的延迟加载，提高页面的渲染速度。\n几种方式是：\n将 js 脚本放在文档的底部，来使 js 脚本尽可能的在最后来加载执行给 js 脚本添加defer属性，这个属性会让脚本的加载与文档的解析同步解析，然后在文档解析完成后再执行这个脚本文件，这样的话就能使页面的渲染不被阻塞。多个设置了defer属性的脚本按规范来说最后是顺序执行的，但是在一些浏览器中可能不是这样给 js 脚本添加async属性，这个属性会使脚本异步加载，不会阻塞页面的解析过程，但是当脚本加载完成后立即执行 js脚本，这个时候如果文档没有解析完成的话同样会阻塞。多个async属性的脚本的执行顺序是不可预测的，一般不会按照代码的顺序依次执行动态创建DOM标签的方式，我们可以对文档的加载事件进行监听，当文档加载完成后再动态的创建script标签来引入 js 脚本\n怎么判断页面是否加载完成\nLoad事件触发代表页面中的DOM，CSS，JS，图片已经全部加载完毕。DOMContentLoaded事件触发代表初始的HTML被完全加载和解析，不需要等待CSS，JS，图片加载", "f": [{"q": "活动页把同步<script src=\"vendor.js\">放在<head>，网络慢时<body>长时间不出现；后端说HTML已经返回，所以不该白屏，你怎么解释？", "a": "浏览器自上而下解析到该脚本时会暂停HTML解析，等待脚本下载并交给JavaScript引擎执行，之后才继续处理<body>。因此响应到达不等于页面可渲染；可把非关键脚本移到文档末尾，或按依赖关系选择defer、async。"}, {"q": "页面有三个存在依赖顺序的脚本，又不希望它们阻塞文档解析；一位开发选async，另一位选defer，你支持谁？", "a": "支持defer，它让脚本下载与文档解析并行，并在文档解析完成后执行，多个脚本按规范保持顺序。async下载完成就立即执行，执行时仍可能打断尚未完成的解析，而且多个脚本的先后不可预测，不适合存在依赖的链路。"}, {"q": "第三方统计脚本完全独立，但产品要求数据尽早上报，前端又不能让它持续阻塞首屏；你会在async、defer和动态插入之间怎么选？", "a": "独立且希望尽早执行时可选async，它不会等待文档解析完成，但下载完成后的执行仍可能短暂阻塞解析。若必须等文档完成再加载，可监听加载事件后动态创建script；选择取决于上报时效与渲染影响，不能假定async保证执行顺序。"}, {"q": "监控显示DOMContentLoaded已触发，但load又过了很久才发生，页面包含大图、样式和多个外部脚本；你如何判断卡在哪类资源？", "a": "这表示初始HTML已完成加载和解析，但页面依赖的其他资源尚未全部结束。结合网络请求检查仍在下载或失败重试的CSS、JS、图片等资源；load要等页面资源完成，不能只凭两事件间隔断定某一种资源。"}, {"q": "旧项目把所有脚本都移到<body>末尾，新负责人要求统一改成async，理由是“两者都不会阻塞首屏”；你会接受统一替换吗？", "a": "不接受统一替换，末尾脚本按文档顺序遇到并执行，而async的执行顺序不可预测，下载完成时还可能打断解析。独立脚本适合async，有依赖顺序且需延后执行的脚本更适合defer；动态插入则适用于明确等待某个加载事件的场景。"}]}, {"s": "浏览器", "t": "从输入URL到页面展示过程", "m": "输入URL后，浏览器会完成域名解析、建立连接、收发HTTP响应，再把资源渲染成可见页面。域名先经过多级缓存或DNS查询得到IP，随后通过三次握手建立TCP连接；如果是HTTPS，还要进行TLS握手来加密传输。服务器处理请求时会校验if-none-match、if-modified-since等缓存信息，缓存有效返回304，否则以200返回资源。浏览器拿到内容后构建DOM、计算样式和布局，再分层、栅格化并合成显示；大页面通常优先处理视口附近的图块，避免无意义的绘制开销。\n1. DNS域名解析\n根 DNS 服务器 ：返回顶级域 DNS 服务器的 IP 地址顶级域 DNS 服务器：返回权威 DNS 服务器的 IP 地址权威 DNS 服务器 ：返回相应主机的 IP 地址\nDNS的域名查找，在客户端和浏览器，本地DNS之间的查询方式是递归查询；在本地DNS服务器与根域及其子域之间的查询方式是迭代查询；\n在客户端输入 URL 后，会有一个递归查找的过程，从浏览器缓存中查找->本地的hosts文件查找->找本地DNS解析器缓存查找->本地DNS服务器查找，这个过程中任何一步找到了都会结束查找流程。\n如果本地DNS服务器无法查询到，则根据本地DNS服务器设置的转发器进行查询。若未用转发模式，则迭代查找过程如下图：\n结合起来的过程，可以用一个图表示：\n在查找过程中，有以下优化点：\nDNS存在着多级缓存，从离浏览器的距离排序的话，有以下几种:浏览器缓存，系统缓存，路由器缓存，IPS服务器缓存，根域名服务器缓存，顶级域名服务器缓存，主域名服务器缓存。在域名和 IP 的映射过程中，给了应用基于域名做负载均衡的机会，可以是简单的负载均衡，也可以根据地址和运营商做全局的负载均衡。\n2. 建立TCP连接\n首先，判断是不是https的，如果是，则HTTPS其实是HTTP + SSL / TLS 两部分组成，也就是在HTTP上又加了一层处理加密信息的模块。服务端和客户端的信息传输都会通过TLS进行加密，所以传输的数据都是加密后的数据\n进行三次握手，建立TCP连接。\n第一次握手：建立连接。客户端发送连接请求报文段第二次握手：服务器收到SYN报文段。服务器收到客户端的SYN报文段，需要对这个SYN报文段进行确认第三次握手：客户端收到服务器的SYN+ACK报文段，向服务器发送ACK报文段\nSSL握手过程\n第一阶段 建立安全能力 包括协议版本 会话Id 密码构件 压缩方法和初始随机数第二阶段 服务器发送证书 密钥交换数据和证书请求，最后发送请求-相应阶段的结束信号第三阶段 如果有证书请求客户端发送此证书 之后客户端发送密钥交换数据 也可以发送证书验证消息第四阶段 变更密码构件和结束握手协议\n完成了之后，客户端和服务器端就可以开始传送数据\n发送HTTP请求，服务器处理请求，返回响应结果\nTCP连接建立后，浏览器就可以利用HTTP／HTTPS协议向服务器发送请求了。服务器接受到请求，就解析请求头，如果头部有缓存相关信息如if-none-match与if-modified-since，则验证缓存是否有效，若有效则返回状态码为304，若无效则重新返回资源，状态码为200\n这里有发生的一个过程是HTTP缓存，是一个常考的考点，大致过程如图：\n3. 关闭TCP连接\n4. 浏览器渲染\n按照渲染的时间顺序，流水线可分为如下几个子阶段：构建 DOM 树、样式计算、布局阶段、分层、栅格化和显示。如图：\n渲染进程将 HTML 内容转换为能够读懂DOM 树结构。渲染引擎将 CSS 样式表转化为浏览器可以理解的styleSheets，计算出DOM节点的样式。创建布局树，并计算元素的布局信息。对布局树进行分层，并生成分层树。为每个图层生成绘制列表，并将其提交到合成线程。合成线程将图层分图块，并栅格化将图块转换成位图。合成线程发送绘制图块命令给浏览器进程。浏览器进程根据指令生成页面，并显示到显示器上。\n构建 DOM 树\n转码（Bytes -> Characters）—— 读取接收到的 HTML 二进制数据，按指定编码格式将字节转换为 HTML 字符串Tokens 化（Characters -> Tokens）—— 解析 HTML，将 HTML 字符串转换为结构清晰的 Tokens，每个 Token 都有特殊的含义同时有自己的一套规则构建 Nodes（Tokens -> Nodes）—— 每个 Node 都添加特定的属性（或属性访问器），通过指针能够确定 Node 的父、子、兄弟关系和所属 treeScope（例如：iframe 的 treeScope 与外层页面的 treeScope 不同）构建 DOM 树（Nodes -> DOM Tree）—— 最重要的工作是建立起每个结点的父子兄弟关系\n样式计算\n渲染引擎将 CSS 样式表转化为浏览器可以理解的 styleSheets，计算出 DOM 节点的样式。\nCSS 样式来源主要有 3 种，分别是通过 link 引用的外部 CSS 文件、style标签内的 CSS、元素的 style 属性内嵌的 CSS。\n页面布局\n布局过程，即排除 script、meta 等功能化、非视觉节点，排除display: none的节点，计算元素的位置信息，确定元素的位置，构建一棵只包含可见元素布局树。如图：\n其中，这个过程需要注意的是回流和重绘\n生成分层树\n页面中有很多复杂的效果，如一些复杂的 3D 变换、页面滚动，或者使用 z-indexing 做 z 轴排序等，为了更加方便地实现这些效果，渲染引擎还需要为特定的节点生成专用的图层，并生成一棵对应的图层树（LayerTree）\n栅格化\n合成线程会按照视口附近的图块来优先生成位图，实际生成位图的操作是由栅格化来执行的。所谓栅格化，是指将图块转换为位图\n通常一个页面可能很大，但是用户只能看到其中的一部分，我们把用户可以看到的这个部分叫做视口（viewport）。在有些情况下，有的图层可以很大，比如有的页面你使用滚动条要滚动好久才能滚动到底部，但是通过视口，用户只能看到页面的很小一部分，所以在这种情况下，要绘制出所有图层内容的话，就会产生太大的开销，而且也没有必要。\n显示\n最后，合成线程发送绘制图块命令给浏览器进程。浏览器进程根据指令生成页面，并显示到显示器上，渲染过程完成。", "f": [{"q": "用户输入域名后很快拿到IP，同事据此说“接下来浏览器只要发HTTP请求，不需要再建立连接”；如果地址是HTTPS，你怎么纠正？", "a": "拿到IP只完成了域名解析，通常还需通过三次握手建立TCP连接，并在其上完成TLS安全协商，之后才能传输加密的HTTP数据。缓存或连接复用可能缩短部分阶段，但不能把DNS命中等同于请求已经可发送。"}, {"q": "跨地区用户首次打开页面时域名解析明显偏慢，架构组准备调整权威解析和负载均衡；前端排查报告应把完整查询链路写到什么程度？", "a": "应先记录浏览器缓存、hosts、本地解析器缓存到本地DNS的递归查找，再说明本地DNS未命中时会向根域、顶级域和权威DNS迭代查询。域名到IP的映射还能承载按地区或运营商的负载均衡，但实际慢点需用观测数据确认。"}, {"q": "接口请求带上If-None-Match和If-Modified-Since后返回304，开发却仍等待一个完整响应体再渲染；这里的缓存流程应怎样理解？", "a": "304表示服务器验证缓存仍有效，浏览器应复用本地资源，而不是等待服务器重新发送完整实体。缓存无效或资源已变化时才重新返回资源并通常使用200；条件请求仍发生了网络往返，因此它与完全不发请求的本地命中不同。"}, {"q": "线上偶发“接口响应很快但页面仍白屏”，性能记录显示时间集中在样式计算和布局之后；你会沿渲染流水线怎么查？", "a": "响应完成后仍要经历构建DOM、样式计算、生成布局树、分层、生成绘制列表、栅格化和显示。应定位是复杂样式与布局计算、过大的图层，还是视口附近图块迟迟未完成栅格化；只看接口耗时无法解释渲染进程和合成线程的开销。"}, {"q": "长列表页面高达数十屏，产品要求首次打开就栅格化全部内容以保证滚动绝对流畅，浏览器工程师反对；你站哪边？", "a": "支持按视口附近图块优先栅格化，因为用户首次只能看到视口内的一小部分，提前处理全部大图层会产生不必要开销。合成线程负责分块并安排栅格化，再把绘制命令交给浏览器进程显示；过度预处理还可能拖慢首屏。"}, {"q": "面试中候选人把“构建DOM”直接说成“把字符串变成节点数组”，但线上乱码只发生在某种响应编码下；你会怎样继续追问并判断？", "a": "构建DOM前先要按指定编码把接收的字节转成字符，编码判断错误就可能在分词前产生乱码。随后才是字符转Token、Token转带属性和关系的节点，最终建立父子兄弟关系形成DOM树；节点数组的说法遗漏了转码和树结构。"}]}, {"s": "浏览器", "t": "渲染引擎什么情况下才会为特定的节点创建新的图层", "m": "当节点形成独立层叠上下文，或者内容需要被裁剪时，渲染引擎会为它创建新的图层。常见触发条件可以归为定位与层级、视觉效果和渲染隔离，例如非auto的z-index、非none的transform、小于1的opacity，以及will-change。本质上，这些属性要求节点独立参与合成或控制绘制范围。比如固定尺寸容器中的内容发生溢出时，文字裁剪区域会单独成层；如果还有滚动条，滚动条也可能成为独立图层。\n层叠上下文是HTML元素的三维概念，这些HTML元素在一条假想的相对于面向（电脑屏幕的）视窗或者网页的用户的z轴上延伸，HTML元素依据其自身属性按照优先级顺序占用层叠上下文的空间。\n拥有层叠上下文属性的元素会被提升为单独的一层。\n拥有层叠上下文属性：\n根元素 (HTML),z-index 值不为 \"auto\"的 绝对/相对定位元素，position,固定（fixed） / 沾滞（sticky）定位（沾滞定位适配所有移动设备上的浏览器，但老的桌面浏览器不支持）z-index值不为 \"auto\"的 flex 子项 (flex item)，即：父元素 display: flex|inline-flex，z-index值不为\"auto\"的grid子项，即：父元素display：gridopacity 属性值小于 1 的元素（参考 the specification for opacity），transform 属性值不为 \"none\"的元素，mix-blend-mode 属性值不为 \"normal\"的元素，filter值不为\"none\"的元素，perspective值不为\"none\"的元素，clip-path值不为\"none\"的元素mask / mask-image / mask-border不为\"none\"的元素isolation 属性被设置为 \"isolate\"的元素在 will-change 中指定了任意CSS属性（参考 这篇文章）-webkit-overflow-scrolling 属性被设置 \"touch\"的元素contain属性值为\"layout\"，\"paint\"，或者综合值比如\"strict\"，\"content\"\n需要剪裁（clip）的地方也会被创建为图层。\n这里的剪裁指的是，假如我们把 div 的大小限定为 200 * 200 像素，而 div 里面的文字内容比较多，文字所显示的区域肯定会超出 200 * 200 的面积，这时候就产生了剪裁，渲染引擎会把裁剪文字内容的一部分用于显示在 div 区域。出现这种裁剪情况的时候，渲染引擎会为文字部分单独创建一个层，如果出现滚动条，滚动条也会被提升为单独的层。", "f": [{"q": "商品卡片只设置了position: relative，没有z-index，同事却断言它一定会生成独立图层；你会怎么纠正这个判断？", "a": "不能仅凭position: relative得出结论，源码列出的条件是绝对或相对定位元素同时满足z-index不为auto。还要区分层叠上下文与渲染引擎实际创建的图层，不能把所有定位元素一概视为已经提层。"}, {"q": "长列表里有 500 个卡片，为了准备悬浮动画统一加了will-change: transform，上线后资源占用反而增加，你会怎么改？", "a": "不应给全部卡片长期声明will-change，它会向渲染引擎提示相关属性即将变化，可能促使元素提前拥有独立层。只在动画临近开始时作用于实际运动的卡片，并在结束后移除；大量常驻图层会增加资源和合成管理成本。"}, {"q": "设计师把半透明浮层的opacity从0.99改成1，同时去掉了transform，你会预期它的层级行为发生什么变化？", "a": "opacity小于1和transform不为none都属于源码列出的层叠上下文条件，两者移除后，这个浮层不再因它们获得独立层。它仍可能被filter、isolation、定位与z-index等其他属性影响，因此必须结合完整样式判断。"}, {"q": "一个200px × 200px的滚动容器出现文字残影，现场只检查了z-index；你会继续查哪些与分层有关的线索？", "a": "应继续检查容器的裁剪范围、溢出内容和滚动条，因为源码指出需要剪裁时，文字内容与滚动条可能被分别创建为图层。再核对transform、filter、clip-path、mask和contain等属性，避免把裁剪问题误判成单纯的层叠顺序错误。"}, {"q": "弹窗既能用定位配合非auto的z-index，也能在父容器上设置isolation: isolate；评审时你会如何取舍？", "a": "两者都会建立新的层叠上下文，但表达的约束不同：前者直接参与定位元素的层叠排序，后者用于隔离内部元素与外部混合。应按弹窗的层级关系选择，不能只为“提层”叠加属性，否则会制造新的层叠边界并增加排查难度。"}]}, {"s": "浏览器", "t": "定时器与requestAnimationFrame、requestIdleCallback", "m": "定时任务用setTimeout，动画更新用requestAnimationFrame，低优先级工作用requestIdleCallback。setTimeout到点只是把回调加入任务队列，主线程忙时仍要等待，并不保证准时执行。requestAnimationFrame会跟随屏幕刷新，在重绘前回调，后台标签页还会暂停，但主线程阻塞时同样可能掉帧。requestIdleCallback适合切割长任务或发送数据，不过只有当前帧有空闲时间才会执行，必要时可设置timeout。\n1. setTimeout\nsetTimeout的运行机制：执行该语句时，是立即把当前定时器代码推入事件队列，当定时器在事件列表中满足设置的时间值时将传入的函数加入任务队列，之后的执行就交给任务队列负责。但是如果此时任务队列不为空，则需等待，所以执行定时器内代码的时间可能会大于设置的时间\nsetTimeout(()=>{console.log(1);},0)console.log(2);\n输出 2， 1；\nsetTimeout的第二个参数表示在执行代码前等待的毫秒数。上面代码中，设置为0，表面意思为 执行代码前等待的毫秒数为0，即立即执行。但实际上的运行结果我们也看到了，并不是表面上看起来的样子，千万不要被欺骗了。\n实际上，上面的代码并不是立即执行的，这是因为setTimeout有一个最小执行时间，HTML5标准规定了setTimeout()的第二个参数的最小值（最短间隔）不得低于4毫秒。 当指定的时间低于该时间时，浏览器会用最小允许的时间作为setTimeout的时间间隔，也就是说即使我们把setTimeout的延迟时间设置为0，实际上可能为4毫秒后才事件推入任务队列。\n定时器代码在被推送到任务队列前，会先被推入到事件列表中，当定时器在事件列表中满足设置的时间值时会被推到任务队列，但是如果此时任务队列不为空，则需等待，所以执行定时器内代码的时间可能会大于设置的时间\nsetTimeout(()=>{console.log(111);},100);\n上面代码表示100ms后执行console.log(111)，但实际上实行的时间肯定是大于100ms后的， 100ms 只是表示 100ms 后将任务加入到\"任务队列\"中，必须等到当前代码（执行栈）执行完，主线程才会去执行它指定的回调函数。要是当前代码耗时很长，有可能要等很久，所以并没有办法保证，回调函数一定会在setTimeout()指定的时间执行。\n2. setTimeout 和 setInterval区别\nsetTimeout: 指定延期后调用函数，每次setTimeout计时到后就会去执行，然后执行一段时间后才继续setTimeout,中间就多了误差，（误差多少与代码的执行时间有关）。setInterval：以指定周期调用函数，而setInterval则是每次都精确的隔一段时间推入一个事件（但是，事件的执行时间不一定就不准确，还有可能是这个事件还没执行完毕，下一个事件就来了）.\nbtn.onclick=function(){setTimeout(function(){console.log(1);},250);}\n击该按钮后，首先将onclick事件处理程序加入队列。该程序执行后才设置定时器，再有250ms后，指定的代码才被添加到队列中等待执行。 如果上面代码中的onclick事件处理程序执行了300ms，那么定时器的代码至少要在定时器设置之后的300ms后才会被执行。队列中所有的代码都要等到javascript进程空闲之后才能执行，而不管它们是如何添加到队列中的。\n如图所示，尽管在255ms处添加了定时器代码，但这时候还不能执行，因为onclick事件处理程序仍在运行。定时器代码最早能执行的时机是在300ms处，即onclick事件处理程序结束之后。\n3. setInterval存在的一些问题：\nJavaScript中使用setInterval开启轮询。定时器代码可能在代码再次被添加到队列之前还没有完成执行，结果导致定时器代码连续运行好几次，而之间没有任何停顿。而javascript引擎对这个问题的解决是：当使用setInterval()时，仅当没有该定时器的任何其他代码实例时，才将定时器代码添加到队列中。这确保了定时器代码加入到队列中的最小时间间隔为指定间隔。\n但是，这样会导致两个问题：\n某些间隔被跳过；多个定时器的代码执行之间的间隔可能比预期的小\n假设，某个onclick事件处理程序使用setInterval()设置了200ms间隔的定时器。如果事件处理程序花了300ms多一点时间完成，同时定时器代码也花了差不多的时间，就会同时出现跳过某间隔的情况\n例子中的第一个定时器是在205ms处添加到队列中的，但是直到过了300ms处才能执行。当执行这个定时器代码时，在405ms处又给队列添加了另一个副本。在下一个间隔，即605ms处，第一个定时器代码仍在运行，同时在队列中已经有了一个定时器代码的实例。结果是，在这个时间点上的定时器代码不会被添加到队列中\n使用setTimeout构造轮询能保证每次轮询的间隔。\nsetTimeout(function(){console.log('我被调用了');setTimeout(arguments.callee,100);},100);\ncallee是arguments对象的一个属性。它可以用于引用该函数的函数体内当前正在执行的函数。在严格模式下，第5版 ECMAScript (ES5) 禁止使用arguments.callee()。当一个函数必须调用自身的时候, 避免使用arguments.callee(), 通过要么给函数表达式一个名字,要么使用一个函数声明.\nsetTimeout(functionfn(){console.log('我被调用了');setTimeout(fn,100);},100);\n这个模式链式调用了setTimeout()，每次函数执行的时候都会创建一个新的定时器。第二个setTimeout()调用当前执行的函数，并为其设置另外一个定时器。这样做的好处是，在前一个定时器代码执行完之前，不会向队列插入新的定时器代码，确保不会有任何缺失的间隔。而且，它可以保证在下一次定时器代码执行之前，至少要等待指定的间隔，避免了连续的运行。\n4. requestAnimationFrame\n4.160fps与设备刷新率\n目前大多数设备的屏幕刷新率为60次/秒，如果在页面中有一个动画或者渐变效果，或者用户正在滚动页面，那么浏览器渲染动画或页面的每一帧的速率也需要跟设备屏幕的刷新率保持一致。\n卡顿：其中每个帧的预算时间仅比16毫秒多一点（1秒/ 60 = 16.6毫秒）。但实际上，浏览器有整理工作要做，因此您的所有工作是需要在10毫秒内完成。如果无法符合此预算，帧率将下降，并且内容会在屏幕上抖动。此现象通常称为卡顿，会对用户体验产生负面影响。\n跳帧: 假如动画切换在 16ms, 32ms, 48ms时分别切换，跳帧就是假如到了32ms，其他任务还未执行完成，没有去执行动画切帧，等到开始进行动画的切帧，已经到了该执行48ms的切帧。就好比你玩游戏的时候卡了，过了一会，你再看画面，它不会停留你卡的地方，或者这时你的角色已经挂掉了。必须在下一帧开始之前就已经绘制完毕;\nChrome devtool 查看实时 FPS, 打开 More tools => Rendering, 勾选 FPS meter\n4.2requestAnimationFrame实现动画\nrequestAnimationFrame是浏览器用于定时循环操作的一个接口，类似于setTimeout，主要用途是按帧对网页进行重绘。\n在requestAnimationFrame之前，主要借助setTimeout/ setInterval来编写 JS 动画，而动画的关键在于动画帧之间的时间间隔设置，这个时间间隔的设置有讲究，一方面要足够小，这样动画帧之间才有连贯性，动画效果才显得平滑流畅；另一方面要足够大，确保浏览器有足够的时间及时完成渲染。\n显示器有固定的刷新频率（60Hz或75Hz），也就是说，每秒最多只能重绘60次或75次，requestAnimationFrame的基本思想就是与这个刷新频率保持同步，利用这个刷新频率进行页面重绘。此外，使用这个API，一旦页面不处于浏览器的当前标签，就会自动停止刷新。这就节省了CPU、GPU和电力。\nrequestAnimationFrame是在主线程上完成。这意味着，如果主线程非常繁忙，requestAnimationFrame的动画效果会大打折扣。\nrequestAnimationFrame使用一个回调函数作为参数。这个回调函数会在浏览器重绘之前调用。\nrequestID=window.requestAnimationFrame(callback);window.requestAnimFrame=(function(){returnwindow.requestAnimationFrame||window.webkitRequestAnimationFrame||window.mozRequestAnimationFrame||window.oRequestAnimationFrame||window.msRequestAnimationFrame||function(callback){window.setTimeout(callback,1000/60);};})();\n上面的代码按照1秒钟60次（大约每16.7毫秒一次），来模拟requestAnimationFrame。\n5. requestIdleCallback()\nMDN上的解释：requestIdleCallback()方法将在浏览器的空闲时段内调用的函数排队。这使开发者能够在主事件循环上执行后台和低优先级工作，而不会影响延迟关键事件，如动画和输入响应。函数一般会按先进先调用的顺序执行，然而，如果回调函数指定了执行超时时间timeout，则有可能为了在超时前执行函数而打乱执行顺序。\nrequestAnimationFrame会在每次屏幕刷新的时候被调用，而requestIdleCallback则会在每次屏幕刷新时，判断当前帧是否还有多余的时间，如果有，则会调用requestIdleCallback的回调函数，\n图片中是两个连续的执行帧，大致可以理解为两个帧的持续时间大概为16.67，图中黄色部分就是空闲时间。所以，requestIdleCallback中的回调函数仅会在每次屏幕刷新并且有空闲时间时才会被调用.\n利用这个特性，我们可以在动画执行的期间，利用每帧的空闲时间来进行数据发送的操作，或者一些优先级比较低的操作，此时不会使影响到动画的性能，或者和requestAnimationFrame搭配，可以实现一些页面性能方面的的优化，\nreact 的fiber架构也是基于requestIdleCallback实现的, 并且在不支持的浏览器中提供了polyfill\n总结\n从单线程模型和任务队列出发理解setTimeout(fn, 0)，并不是立即执行。JS 动画, 用requestAnimationFrame会比setInterval效果更好requestIdleCallback()常用来切割长任务，利用空闲时间执行，避免主线程长时间阻塞\n---", "f": []}, {"s": "Vue", "t": "Vue 响应式原理", "m": "Vue响应式本质上是拦截数据读写：读取时收集依赖，修改时通知Watcher更新视图。Vue 2用Object.defineProperty()递归劫持属性，数组则重写push、splice等变异方法，再通过虚拟DOM的对比局部更新真实DOM。它无法直接感知对象属性的新增和删除，需要使用Vue.set()、Vue.delete()。Vue 3改用Proxy代理对象和数组，嵌套对象可在读取时继续交给reactive处理。\nVue 的响应式原理是核心是通过 ES5 的保护对象的Object.defindeProperty中的访问器属性中的 get 和 set 方法，data 中声明的属性都被添加了访问器属性，当读取 data 中的数据时自动调用 get 方法，当修改 data 中的数据时，自动调用 set 方法，检测到数据的变化，会通知观察者 Wacher，观察者 Wacher自动触发重新render 当前组件（子组件不会重新渲染）,生成新的虚拟 DOM 树，Vue 框架会遍历并对比新虚拟 DOM 树和旧虚拟 DOM 树中每个节点的差别，并记录下来，最后，加载操作，将所有记录的不同点，局部修改到真实 DOM树上。\n虚拟DOM (Virtaul DOM): 用 js 对象模拟的，保存当前视图内所有 DOM 节点对象基本描述属性和节点间关系的树结构。用 js 对象，描述每个节点，及其父子关系，形成虚拟 DOM 对象树结构。因为只要在data中声明的基本数据类型的数据，基本不存在数据不响应问题，所以重点介绍数组和对象在vue中的数据响应问题，vue可以检测对象属性的修改，但无法监听数组的所有变动及对象的新增和删除，只能使用数组变异方法及$set方法。\n可以看到，arrayMethods首先继承了Array，然后对数组中所有能改变数组自身的方法，如push、pop等这些方法进行重写。重写后的方法会先执行它们本身原有的逻辑，并对能增加数组长度的 3 个方法push、unshift、splice方法做了判断，获取到插入的值，然后把新添加的值变成一个响应式对象，并且再调用ob.dep.notify()手动触发依赖通知，这就很好地解释了用vm.items.splice(newLength) 方法可以检测到变化\n总结：Vue 采用数据劫持结合发布—订阅模式的方法，通过Object.defineProperty()来劫持各个属性的 setter，getter，在数据变动时发布消息给订阅者，触发相应的监听回调。\nObserver遍历数据对象，给所有属性加上setter和getter，监听数据的变化compile解析模板指令，将模板中的变量替换成数据，然后初始化渲染页面视图，并将每个指令对应的节点绑定更新函数，添加监听数据的订阅者，一旦数据有变动，收到通知，更新视图\nWatcher订阅者是Observer和Compile之间通信的桥梁，主要做的事情\n在自身实例化时往属性订阅器 (dep) 里面添加自己待属性变动dep.notice()通知时，调用自身的update()方法，并触发Compile中绑定的回调\nObject.defineProperty()，那么它的用法是什么，以及优缺点是什么呢？\n可以检测对象中数据发生的修改对于复杂的对象，层级很深的话，是不友好的，需要经行深度监听，这样子就需要递归到底，这也是它的缺点。对于一个对象中，如果你新增加属性，删除属性，**Object.defineProperty()**是不能观测到的，那么应该如何解决呢？可以通过Vue.set()和Vue.delete()来实现。\n// 模拟 Vue 中的 data 选项letdata={msg:'hello'}// 模拟 Vue 的实例letvm={}// 数据劫持:当访问或者设置 vm 中的成员的时候，做一些干预操作Object.defineProperty(vm,'msg',{// 可枚举(可遍历)enumerable:true,// 可配置(可以使用 delete 删除，可以通过 defineProperty 重新定义)configurable:true,// 当获取值的时候执行get(){console.log('get: ',data.msg)returndata.msg},// 当设置值的时候执行set(newValue){console.log('set: ',newValue)if(newValue===data.msg){return}data.msg=newValue// 数据更改，更新 DOM 的值document.querySelector('#app').textContent=data.msg}})// 测试vm.msg='Hello World'console.log(vm.msg)\nVue3.x响应式数据原理\nVue3.x改用Proxy替代Object.defineProperty。因为Proxy可以直接监听对象和数组的变化，并且有多达13种拦截方法。并且作为新标准将受到浏览器厂商重点持续的性能优化。\nProxy只会代理对象的第一层，那么Vue3又是怎样处理这个问题的呢？\n判断当前Reflect.get的返回值是否为Object，如果是则再通过reactive方法做代理， 这样就实现了深度观测。\n监测数组的时候可能触发多次get/set，那么如何防止触发多次呢？\n我们可以判断key是否为当前被代理对象target自身属性，也可以判断旧值与新值是否相等，只有满足以上两个条件之一时，才有可能执行trigger\n// 模拟 Vue 中的 data 选项letdata={msg:'hello',count:0}// 模拟 Vue 实例letvm=newProxy(data,{// 当访问 vm 的成员会执行get(target,key){console.log('get, key: ',key,target[key])returntarget[key]},// 当设置 vm 的成员会执行set(target,key,newValue){console.log('set, key: ',key,newValue)if(target[key]===newValue){return}target[key]=newValue document.querySelector('#app').textContent=target[key]}})// 测试vm.msg='Hello World'console.log(vm.msg)\nProxy 相比于 defineProperty 的优势\n数组变化也能监听到不需要深度遍历监听\nProxy是ES6中新增的功能，可以用来自定义对象中的操作\nletp=newProxy(target,handler);// `target` 代表需要添加代理的对象// `handler` 用来自定义对象中的操作// 可以很方便的使用 Proxy 来实现一个数据绑定和监听letonWatch=(obj,setBind,getLogger)=>{lethandler={get(target,property,receiver){getLogger(target,property)returnReflect.get(target,property,receiver);},set(target,property,value,receiver){setBind(value);returnReflect.set(target,property,value);}};returnnewProxy(obj,handler);};letobj={a:1}letvalueletp=onWatch(obj,(v)=>{value=v},(target,property)=>{console.log(`Get '${property}' =${target[property]}`);})p.a=2// bind `value` to `2`p.a// -> Get 'a' = 2\n总结\nVue记录传入的选项，设置$data/$el把data的成员注入到Vue实例负责调用Observer实现数据响应式处理(数据劫持)负责调用Compiler编译指令/插值表达式等Observer数据劫持负责把data中的成员转换成getter/setter负责把多层属性转换成getter/setter如果给属性赋值为新对象，把新对象的成员设置为getter/setter添加Dep和Watcher的依赖关系数据变化发送通知Compiler负责编译模板，解析指令/插值表达式负责页面的首次渲染过程当数据变化后重新渲染Dep收集依赖，添加订阅者(watcher)通知所有订阅者Watcher自身实例化的时候往dep对象中添加自己当数据变化dep通知所有的Watcher实例更新视图\n记录传入的选项，设置$data/$el把data的成员注入到Vue实例负责调用Observer实现数据响应式处理(数据劫持)负责调用Compiler编译指令/插值表达式等\n数据劫持负责把data中的成员转换成getter/setter负责把多层属性转换成getter/setter如果给属性赋值为新对象，把新对象的成员设置为getter/setter添加Dep和Watcher的依赖关系数据变化发送通知\n负责把data中的成员转换成getter/setter负责把多层属性转换成getter/setter如果给属性赋值为新对象，把新对象的成员设置为getter/setter\n负责编译模板，解析指令/插值表达式负责页面的首次渲染过程当数据变化后重新渲染\n收集依赖，添加订阅者(watcher)通知所有订阅者\n自身实例化的时候往dep对象中添加自己当数据变化dep通知所有的Watcher实例更新视图", "f": [{"q": "Vue 2表单里执行form.newKey = 1后控制台数据已变化，但输入区没有更新；有人说是虚拟DOM的diff漏了，你同意吗？", "a": "不同意，根因更可能是新增属性没有被Object.defineProperty()转换成getter和setter，变化因而没有进入依赖通知链路。应使用Vue.set()或$set添加属性；若是删除则使用Vue.delete()，而不是先怀疑diff。"}, {"q": "一个多层配置对象初始化后会频繁替换子对象，你要用Observer、Dep和Watcher解释一次更新链路，会怎么说？", "a": "Observer会把已声明的多层属性转换为访问器，并在新对象被赋值时继续处理其成员；读取阶段由Dep收集对应的Watcher。写入触发通知后，Watcher更新视图并生成新的虚拟DOM，再比较差异并局部修改真实DOM。"}, {"q": "列表页直接执行items[3] = row和items.length = 2后视图不同步，但改成splice就恢复了；你如何解释并修复？", "a": "在Vue 2中，并非所有数组变动都能被访问器机制观察，而splice等变异方法经过重写，会处理新增值并调用依赖通知。应使用框架支持的数组变异方法或$set；直接索引赋值和改长度不能假定会触发视图更新。"}, {"q": "把同一份深层对象从Vue 2迁到Vue 3后，评审者认为Proxy只代理第一层，所以嵌套字段不会响应；你怎么回应？", "a": "外层Proxy的确先拦截第一层访问，但读取结果若仍是对象，可以在Reflect.get后继续通过reactive建立代理，从而实现深度观测。它通常是按访问过程继续代理，并不等于所有嵌套对象失去响应性；具体行为仍要检查实际实现。"}, {"q": "Vue 3数组一次写入触发了多次get、set日志，页面也重复更新；你会在哪些条件上收紧触发逻辑？", "a": "应检查写入的key是否是目标对象自身属性，并比较旧值与新值，避免没有实际变化时仍调用trigger。数组操作可能经过多个代理陷阱，不能把每次拦截都等同于有效变更；条件过宽会造成多余通知，过窄则可能漏更新。"}, {"q": "有人主张在大型对象初始化时递归给所有层级做defineProperty比Proxy更合适，因为两者都能深度监听；你如何权衡？", "a": "defineProperty需要遍历并改造已有属性，新增和删除还要借助Vue.set()、Vue.delete()；深层对象会放大递归处理成本。Proxy能直接监听对象和数组，并可在读取到嵌套对象时继续代理，但它属于不同的运行机制，迁移不能只替换一个API。"}]}, {"s": "Vue", "t": "发布订阅模式和观察者模式", "m": "两种模式的核心区别是有没有事件中心：观察者模式直接关联双方，发布订阅模式通过中间层转发。在观察者模式中，目标对象保存观察者，状态变化后直接调用其update()，所以双方存在依赖。发布订阅中，发布者只负责$emit，订阅者只负责$on，彼此不需要知道对方。像兄弟组件通过eventBus通信，就是典型的发布订阅场景。\n1. 发布/订阅模式\n发布/订阅模式订阅者发布者信号中心\n订阅者发布者信号中心\n我们假定，存在一个\"信号中心\"，某个任务执行完成，就向信号中心\"发布\"(publish)一个信 号，其他任务可以向信号中心\"订阅\"(subscribe)这个信号，从而知道什么时候自己可以开始执 行。这就叫做\"发布/订阅模式\"(publish-subscribe pattern)\nVue 的自定义事件\nletvm=newVue()vm.$on('dataChange',()=>{console.log('dataChange')})vm.$on('dataChange',()=>{console.log('dataChange1')})vm.$emit('dataChange')\n兄弟组件通信过程\n// eventBus.js// 事件中心leteventHub=newVue()// ComponentA.vue// 发布者addTodo:function(){// 发布消息(事件)eventHub.$emit('add-todo',{text:this.newTodoText})this.newTodoText=''}// ComponentB.vue// 订阅者created:function(){// 订阅消息(事件)eventHub.$on('add-todo',this.addTodo)}\n模拟 Vue 自定义事件的实现\nclassEventEmitter{constructor(){// { eventType: [ handler1, handler2 ] }this.subs={}}// 订阅通知$on(eventType,fn){this.subs[eventType]=this.subs[eventType]||[]this.subs[eventType].push(fn)}// 发布通知$emit(eventType){if(this.subs[eventType]){this.subs[eventType].forEach(v=>v())}}}// 测试varbus=newEventEmitter()// 注册事件bus.$on('click',function(){console.log('click')})bus.$on('click',function(){console.log('click1')})// 触发事件bus.$emit('click')\n2. 观察者模式\n观察者(订阅者) --Watcherupdate():当事件发生时，具体要做的事情目标(发布者) --Depsubs数组:存储所有的观察者addSub():添加观察者notify():当事件发生，调用所有观察者的update()方法没有事件中心\nupdate():当事件发生时，具体要做的事情\nsubs数组:存储所有的观察者addSub():添加观察者notify():当事件发生，调用所有观察者的update()方法\n// 目标(发布者)// DependencyclassDep{constructor(){// 存储所有的观察者this.subs=[]}// 添加观察者addSub(sub){if(sub&&sub.update){this.subs.push(sub)}}// 通知所有观察者notify(){this.subs.forEach(sub=>sub.update())}}// 观察者(订阅者)classWatcher{update(){console.log('update')}}// 测试letdep=newDep()letwatcher=newWatcher()dep.addSub(watcher)dep.notify()\n3. 总结\n观察者模式是由具体目标调度，比如当事件触发，Dep就会去调用观察者的方法，所以观察者模 式的订阅者与发布者之间是存在依赖的发布/订阅模式由统一调度中心调用，因此发布者和订阅者不需要知道对方的存在", "f": [{"q": "订单页里OrderStore直接保存三个视图观察者并逐个调用update()，同事却把它叫作发布订阅；你会怎么追问他？", "a": "这更符合观察者模式，因为具体目标直接持有观察者，并在状态变化时调用它们的update()，中间没有独立信号中心。应让他指出发布者与订阅者之间的调度媒介；若只能找到目标内部的订阅列表，就不能按发布订阅描述。"}, {"q": "兄弟组件通过全局eventHub.$emit('add-todo')通信，新增页面也要监听同名事件；你会要求怎样组织注册与清理？", "a": "应把事件名称、载荷约定和订阅位置集中管理，并在组件不再需要监听时解除对应处理函数。发布订阅让双方通过事件中心解耦，但也隐藏了调用关系；监听增长后若缺少生命周期管理，会留下重复执行和难以追踪的订阅。"}, {"q": "需求改成一个发布者要通知多个完全不认识的业务模块，而且模块可以独立上下线；你会选直接观察还是信号中心？", "a": "更适合使用带信号中心的发布订阅，让发布者只发布事件，各模块自行订阅，不需要互相持有引用。代价是执行链路不再直观，必须约束事件命名、载荷和取消订阅；若参与者固定且关系紧密，直接观察反而更简单。"}, {"q": "线上一次点击打印了三遍click，代码里只有一次$emit；你会怎样定位这个EventEmitter故障？", "a": "先检查同一处理函数是否被多次$on，以及组件重复创建时旧订阅是否仍保留，因为$emit会遍历事件类型对应的全部处理函数。再记录注册与销毁路径确认订阅数量；只在触发处防抖会掩盖重复注册，不能消除泄漏。"}, {"q": "状态库里的Dep已经能通知Watcher，架构师仍要求再套一层全局事件中心；你认为值得吗？", "a": "若Dep已直接维护并通知明确的Watcher，再加信号中心会把观察者链路改成更间接的调度，未必带来收益。只有确实需要让互不认识的发布者和订阅者解耦时才值得引入；否则会增加事件契约和故障定位成本。"}, {"q": "评审中有人说Watcher既然叫订阅者，Vue响应式就必然是发布订阅模式；你会如何串联两个概念？", "a": "“订阅者”这个角色名不足以决定模式，关键是通知由谁调度。Dep保存Watcher并直接调用其update()时属于观察者结构；只有发布者和订阅者通过独立信号中心交互，才符合这里的发布订阅模式。"}]}, {"s": "Vue", "t": "为什么使用 Virtual DOM", "m": "使用虚拟DOM，是为了用普通对象描述视图，并在状态变化后统一计算真实DOM应该怎样更新。它会保留上一次视图状态，通过diff找出前后差异，只把必要变化同步到页面，避免开发者手动维护复杂的DOM操作。它在复杂视图下有助于控制渲染成本，但不能简单理解为任何场景都更快。更重要的是它解耦了具体渲染平台，同一套视图描述还能用于SSR、原生应用或小程序。\n手动操作DOM比较麻烦，还需要考虑浏览器兼容性问题，虽然有jQuery等库简化DOM操作，但是随着项目的复杂 DOM 操作复杂提升为了简化DOM的复杂操作于是出现了各种MVVM框架，MVVM框架解决了视图和状态的同步问题为了简化视图的操作我们可以使用模板引擎，但是模板引擎没有解决跟踪状态变化的问题，于是Virtual DOM出现了Virtual DOM的好处是当状态改变时不需要立即更新 DOM，只需要创建一个虚拟树来描述DOM，Virtual DOM内部将弄清楚如何有效(diff)的更新DOM虚拟DOM可以维护程序的状态，跟踪上一次的状态通过比较前后两次状态的差异更新真实DOM\n虚拟 DOM 的作用\n维护视图和状态的关系复杂视图情况下提升渲染性能除了渲染DOM以外，还可以实现SSR(Nuxt.js/Next.js)、原生应用(Weex/React Native)、小程序(mpvue/uni-app)等", "f": [{"q": "活动页只有一个计数器，候选人说引入Virtual DOM后每次更新一定比精准设置textContent更快；你认同吗？", "a": "不能作出“一定更快”的结论，因为Virtual DOM还要创建虚拟树并比较前后状态，精准的直接更新可能路径更短。它的核心价值是维护视图与状态关系，并由框架确定差异更新；简单节点不应仅凭性能口号选型。"}, {"q": "复杂表单有几十处状态联动，团队现在到处手写DOM查询和赋值；引入Virtual DOM能解决哪部分工程问题？", "a": "它能用虚拟树描述当前视图，在状态变化后比较新旧状态，只把记录到的差异更新到真实DOM。这样状态与视图的同步由统一机制维护，减少散落的手动操作；但业务状态设计不合理时，虚拟DOM本身不会消除逻辑复杂度。"}, {"q": "同一套组件既要在浏览器渲染，又计划支持SSR和原生容器；你会怎样评价虚拟树这层抽象？", "a": "虚拟树不只用于浏览器DOM，还可作为视图描述支持SSR、原生应用或小程序等不同渲染目标。它让状态与具体宿主操作之间多一层抽象，但各目标仍需要对应渲染实现；不能把“能描述”误解为无需适配即可运行。"}, {"q": "线上列表更新后出现旧行残留，研发只盯着真实DOM截图；按虚拟DOM链路你会依次核对什么？", "a": "先确认状态是否真的变化，再检查新虚拟树是否正确描述目标视图，以及新旧树比较是否记录了应有差异，最后核对差异是否应用到真实DOM。若状态源本身错误，继续调DOM更新没有意义；若有人绕过框架直接改DOM，也会破坏前后状态的一致性。"}, {"q": "可视化大屏每帧都要更新大量节点，一方坚持全部交给虚拟DOM，另一方要求全部手写；你会如何决策？", "a": "不能只凭技术立场决定，应先区分高频热点区域与普通状态视图，并验证比较和真实更新分别占据多少成本。复杂视图可借虚拟DOM维护状态关系，高频且更新路径固定的局部则可能需要更直接的策略；混用时必须划清所有权，避免双方同时修改同一节点。"}, {"q": "模板引擎已经能根据数据生成HTML，产品经理质疑为何还要虚拟DOM；你会怎么解释两者边界？", "a": "模板能简化视图生成，但仅生成内容并不等于持续跟踪状态变化。虚拟DOM会保留上一次视图状态，在新状态到来时比较两棵虚拟树并更新差异；代价是增加运行时表示与比较过程，因此静态页面未必需要这层机制。"}]}, {"s": "Vue", "t": "VDOM：三个 part", "m": "VDOM可以拆成三个部分：用VNode描述节点、通过diff记录差异、再根据patch更新真实DOM。VNode只是保留必要信息的普通JS对象，创建成本通常低于直接创建属性繁多的真实节点。数据变化先集中反映到虚拟树，再统一修改DOM tree，可以减少零散操作引发的重绘和回流。它的价值也不只在性能，还解耦了对HTML的依赖，方便做AOT、SSR和其他平台渲染。\n虚拟节点类，将真实DOM节点用js对象的形式进行展示，并提供render方法，将虚拟节点渲染成真实DOM节点diff比较：对虚拟节点进行js层面的计算，并将不同的操作都记录到patch对象re-render：解析patch对象，进行re-render\n补充1��VDOM 的必要性？\n创建真实DOM的代价高：真实的DOM节点node实现的属性很多，而vnode仅仅实现一些必要的属性，相比起来，创建一个vnode的成本比较低。触发多次浏览器重绘及回流：使用vnode，相当于加了一个缓冲，让一次数据变动所带来的所有node变化，先在vnode中进行修改，然后diff之后对所有产生差异的节点集中一次对DOM tree进行修改，以减少浏览器的重绘及回流。\n补充2：vue 为什么采用 vdom？\n引入Virtual DOM在性能方面的考量仅仅是一方面。\n性能受场景的影响是非常大的，不同的场景可能造成不同实现方案之间成倍的性能差距，所以依赖细粒度绑定及Virtual DOM哪个的性能更好还真不是一个容易下定论的问题。Vue之所以引入了Virtual DOM，更重要的原因是为了解耦HTML依赖，这带来两个非常重要的好处是：\n不再依赖HTML解析器进行模版解析，可以进行更多的AOT工作提高运行时效率：通过模版AOT编译，Vue的运行时体积可以进一步压缩，运行时效率可以进一步提升；可以渲染到DOM以外的平台，实现SSR、同构渲染这些高级特性，Weex等框架应用的就是这一特性。\n综上，Virtual DOM在性能上的收益并不是最主要的，更重要的是它使得Vue具备了现代框架应有的高级特性。", "f": [{"q": "商品列表更新后页面已经变了，同事却说VDOM就是缓存的DOM字符串；结合一次更新链路，你会怎么纠正他？", "a": "VDOM不是HTML字符串，而是用精简的JavaScript对象描述节点。数据变化后先生成并比较虚拟节点，把差异记录为patch，再解析patch更新真实DOM；如果直接跳过比较操作真实节点，就不属于这条更新链路。"}, {"q": "一个表格组件每次筛选都直接遍历并修改多处真实DOM，评审时你为什么可能要求改成先计算vnode差异再统一更新？", "a": "多处真实DOM修改可能连续触发重绘和回流，而虚拟节点可充当更新缓冲。应先在JavaScript层完成diff、汇总为patch，再集中修改DOM tree；但更新范围很小且路径固定时，不能仅凭使用VDOM就断言一定更快。"}, {"q": "一个画布编辑器每帧只移动单个元素，负责人仍坚持“用了VDOM性能必然最好”，你会怎样判断？", "a": "不能把VDOM视为所有场景下的性能保证，它还要承担创建虚拟节点和执行diff的成本。应根据更新频率、节点规模和真实DOM操作范围验证收益；对高度可预测的细粒度更新，直接绑定或定向修改可能更合适。"}, {"q": "线上出现“状态已变化但局部视图没更新”，你沿着vnode → diff → patch → re-render会怎么定位？", "a": "先确认新状态是否生成了预期vnode，再检查diff是否识别出对应差异并写入patch。若patch正确，则继续查看解析和真实DOM更新阶段；逐段核对能区分响应数据、差异计算与提交渲染三类故障。"}, {"q": "团队要让同一套组件既支持浏览器页面又支持SSR和非DOM平台，为什么VDOM的价值不只在减少回流？", "a": "虚拟节点把组件描述与浏览器HTML、真实DOM解耦，使渲染结果能交给不同平台的渲染器处理。模板还可提前编译为render function，承载更多AOT优化；代价是增加虚拟节点、diff和平台适配层的复杂度。"}]}, {"s": "Vue", "t": "vue 和 react技术选型", "m": "Vue和React都能做数据驱动、组件化和服务端渲染，选型关键是项目复杂度与团队匹配度。Vue提供v-model，写法直观，但底层仍是单向数据流；React更强调不可变数据和单向更新。大规模数据渲染及多人协作的复杂项目，可以考虑React配合Redux；追求快速开发的小型项目，Vue通常更顺手。开发风格上，React常用JSX，而Vue多用单文件组件组织模板、逻辑和样式。\n相同点：\n数据驱动页面，提供响应式的视图组件都有virtual DOM,组件化的开发，通过props参数进行父子之间组件传递数据，都实现了webComponents规范数据流动单向，都支持服务器的渲染SSR都有支持native的方法，react有React native， vue有wexx\n不同点：\n数据绑定：Vue实现了双向的数据绑定v-model，底层本质上还是单向数据流（v-model 隐藏了背后 :value 和 @input 的细节）数据渲染：大规模的数据渲染，react更快使用场景：React配合Redux架构适合大规模多人协作复杂项目，Vue适合小快的项目开发风格：react推荐做法jsx + inline style把html和css都写在js了\nvue是采用webpack +vue-loader单文件组件格式，html, js, css同一个文件", "f": [{"q": "表单页用了v-model，评审同事因此认定Vue是双向数据流、React才是单向数据流，你会怎么解释？", "a": "v-model提供双向绑定的使用体验，但底层仍可理解为值向下传递、事件向上通知。它隐藏了类似:value与@input的配对细节，并没有推翻单向数据流；复杂表单仍要明确数据所有者，避免状态来源不清。"}, {"q": "五十人协作的大型业务平台要在Vue与React间选型，架构负责人只拿模板语法简洁度做决定，你会补充哪些判断？", "a": "大型多人协作项目应重点评估状态管理、代码约束和团队协作方式，来源材料更倾向React配合Redux。Vue的单文件组件适合集中组织模板、逻辑和样式，但规模结论不是绝对规则，最终仍要结合团队能力与既有生态。"}, {"q": "数据看板要渲染大规模数据，团队因为材料写着“React更快”就准备整体迁移，你会批准吗？", "a": "不会仅凭框架级结论批准迁移，材料只给出了大规模数据渲染中React更快的概括，未限定组件结构和更新模式。应先定位瓶颈并用当前页面验证；迁移会引入重写与协作成本，而且无法替代列表裁剪等针对性优化。"}, {"q": "新项目既要做浏览器端SSR，半年后又可能扩展原生端，产品要求现在就锁定技术栈，你怎样比较两者？", "a": "两者都支持组件化、单向数据流和服务器端渲染，因此SSR本身不足以拉开差距。原生方向可比较React Native与Weex相关方案及团队能力；跨端只是选型约束之一，不能忽略生态成熟度和维护成本。"}, {"q": "线上页面出现父组件数据已变、子组件显示仍旧值，开发者把原因归结为“Vue和React数据流不同”，你会先查什么？", "a": "两者都通过props进行父子数据传递，并以单向数据流组织更新，不能先用框架差异解释故障。应检查父组件传值、子组件是否复制并滞留旧状态，以及更新是否实际触发；若使用v-model，还要核对值与事件链路。"}]}, {"s": "Vue", "t": "nextTick", "m": "nextTick用来把回调推迟到下一轮DOM更新结束后执行，从而拿到更新后的页面节点。因为Vue不会在每次数据变化时立刻更新DOM，而是把任务放进队列并统一清空，多次调用也会先收集起来。实现时会根据环境依次尝试Promise、MutationObserver、setImmediate，最后才使用setTimeout。所以修改数据后马上读取尺寸或内容时，我一般会放到nextTick回调里。\nnextTick可以让我们在下次DOM更新循环结束之后执行延迟回调，用于获得更新后的DOM\nnextTick主要使用了宏任务和微任务。根据执行环境分别尝试采用\nPromiseMutationObserversetImmediate如果以上都不行则采用setTimeout\n定义了一个异步方法，多次调用nextTick会将方法存入队列中，通过这个异步方法清空当前队列", "f": [{"q": "详情页执行this.count++后立刻读取节点文本仍是旧值，同事认为响应式失效了；你会怎样解释并修正？", "a": "数据变化不代表真实DOM已在当前同步代码中完成更新，因此立即读取可能仍是旧内容。把读取逻辑放进nextTick，让它在下一次DOM更新循环结束后执行；它只保证等待本轮更新，不负责修复错误的数据依赖。"}, {"q": "批量导入页在一个同步循环里调用了二十次nextTick，这些回调会各自启动一次定时任务吗？", "a": "多次调用会先把回调保存到同一队列，再由选定的异步方法清空当前队列，而不是简单理解为每次都独立刷新。回调仍应保持轻量；如果其中执行大量计算，即使合并调度也会阻塞后续渲染。"}, {"q": "老旧运行环境没有原生Promise，业务依赖nextTick更新后聚焦输入框，它是否必然失效？", "a": "不必然失效，nextTick会按环境尝试Promise、MutationObserver、setImmediate，最后可退回setTimeout。不同回退方式属于微任务或宏任务，执行时机可能有差异；业务不应依赖比“更新循环结束后”更细的顺序假设。"}, {"q": "弹窗打开后偶发拿不到更新后的输入框，日志显示数据已改且nextTick已调用，你会怎样排查？", "a": "先确认修改数据和注册nextTick的先后关系，并检查输入框是否受条件渲染控制。随后在回调中验证节点是否已生成，排除组件尚未进入对应更新循环；若节点由后续异步数据决定，一次nextTick并不能等待那次未来更新。"}, {"q": "开发者想把所有nextTick都替换成setTimeout(fn, 0)，理由是最终都能异步执行，你会接受吗？", "a": "不应直接等价替换，nextTick面向的是本轮DOM更新结束后的回调队列，并会选择当前环境可用的异步机制。setTimeout只是最终回退方案，通常属于更晚的宏任务；替换会削弱与框架更新周期之间的语义关联。"}]}, {"s": "Vue", "t": "生命周期", "m": "Vue生命周期描述了组件从初始化、挂载、更新到销毁的完整过程，不同阶段适合处理不同事情。beforeCreate时状态还没初始化，到了created已完成数据响应性，但真实DOM尚未生成；执行渲染和挂载后才进入mounted。数据变化时会经过依赖通知、差异比较，并依次触发beforeUpdate和updated；销毁时则在对应钩子间移除节点、依赖和监听。keep-alive组件不会直接销毁，而是通过激活与失活钩子切换状态。\ninit\ninitLifecycle/Event，往vm上挂载各种属性callHook: beforeCreated: 实例刚创建initInjection/initState: 初始化注入和data响应性created: 创建完成，属性已经绑定， 但还未生成真实dom`进行元素的挂载：$el / vm.$mount()是否有template: 解析成render function*.vue文件:vue-loader会将<template>编译成render functionbeforeMount: 模板编译/挂载之前执行render function，生成真实的dom，并替换到dom tree中mounted: 组件已挂载\n*.vue文件:vue-loader会将<template>编译成render function\nupdate\n执行diff算法，比对改变是否需要触发UI更新flushScheduleQueuewatcher.before: 触发beforeUpdate钩子 -watcher.run(): 执行watcher中的notify，通知所有依赖项更新UI触发updated钩子: 组件已更新actived / deactivated(keep-alive): 不销毁，缓存，组件激活与失活destroybeforeDestroy: 销毁开始销毁自身且递归销毁子组件以及事件监听remove(): 删除节点watcher.teardown(): 清空依赖vm.$off(): 解绑监听destroyed: 完成后触发钩子\nbeforeDestroy: 销毁开始销毁自身且递归销毁子组件以及事件监听remove(): 删除节点watcher.teardown(): 清空依赖vm.$off(): 解绑监听destroyed: 完成后触发钩子\nremove(): 删除节点watcher.teardown(): 清空依赖vm.$off(): 解绑监听\n上面是vue的声明周期的简单梳理，接下来我们直接以代码的形式来完成vue的初始化\nnewVue({})// 初始化Vue实例function_init(){// 挂载属性initLifeCycle(vm)// 初始化事件系统，钩子函数等initEvent(vm)// 编译slot、vnodeinitRender(vm)// 触发钩子callHook(vm,'beforeCreate')// 添加inject功能initInjection(vm)// 完成数据响应性 props/data/watch/computed/methodsinitState(vm)// 添加 provide 功能initProvide(vm)// 触发钩子callHook(vm,'created')// 挂载节点if(vm.$options.el){vm.$mount(vm.$options.el)}}// 挂载节点实现functionmountComponent(vm){// 获取 render functionif(!this.options.render){// template to render// Vue.compile = compileToFunctionslet{render}=compileToFunctions()this.options.render=render}// 触发钩子callHook('beforeMounte')// 初始化观察者// render 渲染 vdom，vdom=vm.render()// update: 根据 diff 出的 patchs 挂载成真实的 domvm._update(vdom)// 触发钩子callHook(vm,'mounted')}// 更新节点实现funtionqueueWatcher(watcher){nextTick(flushScheduleQueue)}// 清空队列functionflushScheduleQueue(){// 遍历队列中所有修改for(){// beforeUpdatewatcher.before()// 依赖局部更新节点watcher.update()callHook('updated')}}// 销毁实例实现Vue.prototype.$destory=function(){// 触发钩子callHook(vm,'beforeDestory')// 自身及子节点remove()// 删除依赖watcher.teardown()// 删除监听vm.$off()// 触发钩子callHook(vm,'destoryed')}", "f": [{"q": "图表页在created中读取this.$refs.chart得到undefined，但接口请求能正常发出；这两个现象为什么不矛盾？", "a": "created时数据、方法和注入等状态已经初始化，因此可以启动不依赖真实节点的逻辑。此时组件尚未挂载并生成真实DOM，所以$refs不可用；图表初始化应放到mounted，同时考虑卸载时释放实例。"}, {"q": "后台页一次修改多项响应式数据，开发者期望每次赋值都立即触发一轮beforeUpdate和updated，你会如何纠正？", "a": "更新会进入调度队列，由flushScheduleQueue统一处理相关watcher。更新前触发beforeUpdate，依赖完成更新后再触发updated；不要把钩子次数与赋值次数机械对应，也不应在updated中无条件继续改状态。"}, {"q": "带keep-alive的搜索页切走后定时器仍在运行，但组件没有触发销毁钩子，你会把清理放在哪里？", "a": "keep-alive切换时组件只是失活并被缓存，不会走完整销毁流程，因此应在deactivated暂停定时器或其他活跃任务。再次进入时在activated恢复；真正销毁仍需在beforeDestroy或onBeforeUnmount等阶段完成最终清理。"}, {"q": "单页应用运行数小时后内存持续上涨，路由离开组件时节点消失了，你会沿销毁阶段检查哪些链路？", "a": "先确认是否进入beforeDestroy、执行自身及子组件移除，再核对watcher.teardown()和vm.$off()是否完成。还要检查业务自行注册的全局监听、定时器或第三方实例；框架解绑自身事件，并不必然覆盖所有外部资源。"}, {"q": "团队把Vue 2组件迁到Vue 3，准备逐个把created改成onCreated，你会怎么调整方案？", "a": "Vue 3没有对应的onCreated，beforeCreate和created的初始化职责通常由setup承接。其余阶段使用onBeforeMount、onMounted、onBeforeUpdate、onUpdated、onBeforeUnmount与onUnmounted；迁移时还要重新确认逻辑对真实DOM的依赖。"}]}, {"s": "Vue", "t": "vue-router", "m": "vue-router的本质是监听URL变化，匹配路由配置，再通过router-view渲染对应组件。hash模式监听hashchange，history模式监听popstate，区别主要在于感知地址变化的方式。代码跳转可以用this.$router.push()，模板里则常用router-link，最终都驱动当前路由状态变化。它作为插件通过install接入Vue，注册全局组件，并用响应式的current让匹配组件自动重新渲染。\nmode\nhashhistory\n跳转\nthis.$router.push()<router-link to=\"\"></router-link>\n占位\n<router-view></router-view>\nvue-router源码实现\n作为一个插件存在:实现VueRouter类和install方法实现两个全局组件:router-view用于显示匹配组件内容，router-link用于跳转监控url变化:监听hashchange或popstate事件响应最新url:创建一个响应式的属性current，当它改变时获取对应组件并显示\n// 我们的插件：// 1.实现一个Router类并挂载期实例// 2.实现两个全局组件router-link和router-viewletVue;classVueRouter{// 核心任务：// 1.监听url变化constructor(options){this.$options=options;// 缓存path和route映射关系// 这样找组件更快this.routeMap={}this.$options.routes.forEach(route=>{this.routeMap[route.path]=route})// 数据响应式// 定义一个响应式的current，则如果他变了，那么使用它的组件会rerenderVue.util.defineReactive(this,'current','')// 请确保onHashChange中this指向当前实例window.addEventListener('hashchange',this.onHashChange.bind(this))window.addEventListener('load',this.onHashChange.bind(this))}onHashChange(){// console.log(window.location.hash);this.current=window.location.hash.slice(1)||'/'}}// 插件需要实现install方法// 接收一个参数，Vue构造函数，主要用于数据响应式VueRouter.install=function(_Vue){// 保存Vue构造函数在VueRouter中使用Vue=_Vue// 任务1：使用混入来做router挂载这件事情Vue.mixin({beforeCreate(){// 只有根实例才有router选项if(this.$options.router){Vue.prototype.$router=this.$options.router}}})// 任务2：实现两个全局组件// router-link: 生成一个a标签，在url后面添加#// <a href=\"#/about\">aaaa</a>// <router-link to=\"/about\">aaa</router-link>Vue.component('router-link',{props:{to:{type:String,required:true},},render(h){// h(tag, props, children)returnh('a',{attrs:{href:'#'+this.to}},this.$slots.default)// 使用jsx// return <a href={'#'+this.to}>{this.$slots.default}</a>}})Vue.component('router-view',{render(h){// 根据current获取组件并render// current怎么获取?// console.log('render',this.$router.current);// 获取要渲染的组件letcomponent=nullconst{routeMap,current}=this.$routerif(routeMap[current]){component=routeMap[current].component}returnh(component)}})}exportdefaultVueRouter", "f": [{"q": "订单页点击<router-link to=\"/orders\">后没有整页刷新，同事却说它只是普通链接；路由插件实际还做了什么？", "a": "router-link负责生成可跳转的链接，router-view根据当前地址渲染匹配组件。插件还要监听hashchange或popstate，并把最新地址写入响应式的current；仅生成<a>标签不足以完成视图联动。"}, {"q": "你要实现一个最小版hash路由，配置里有二十条静态路径，为什么先构建routeMap，地址变化后又怎样更新页面？", "a": "初始化时把path与路由配置缓存到routeMap，可在地址变化时直接查找对应组件。监听hashchange和首次load，更新响应式current，依赖它的router-view就会重新渲染；该简化实现未覆盖动态参数等复杂匹配。"}, {"q": "产品要求把地址从#/detail改成/detail，开发只删除了#，结果浏览器前进后退不再更新视图，你会检查哪里？", "a": "无#的历史模式应监听popstate，不能继续只依赖hashchange。还要确认跳转逻辑确实更新浏览器历史，并同步响应式当前地址；仅改变链接外观不会自动补齐历史模式的监听和状态维护。"}, {"q": "页面地址已经变成#/about，但<router-view>仍显示首页，你按源码链路会怎么定位？", "a": "先检查hashchange是否触发，以及处理函数中的this是否仍指向路由实例。再核对current是否截取为/about、routeMap是否存在该路径，最后检查router-view是否读取同一个路由实例；任一环节断开都会导致视图不更新。"}, {"q": "微前端团队争论用<router-link>还是业务代码统一调用this.$router.push()，你会如何划分？", "a": "声明式导航适合模板中明确可点击的页面入口，能让跳转意图直接体现在视图结构里。命令式this.$router.push()更适合校验、保存或权限判断后的跳转；两者最终都要驱动地址变化，选择代价主要在控制流程和可读性。"}]}, {"s": "Vue", "t": "vuex", "m": "Vuex是集中管理组件共享状态的方案，核心是用固定的数据流让状态变化可预测。state保存状态，getters负责派生数据，mutations同步修改状态，actions承载异步或业务逻辑并提交mutation，复杂应用再用modules拆分。其内部会让state响应式，并通过commit、dispatch找到对应处理函数。实际使用时，我只会把跨组件共享、需要统一管理的状态放进去，局部状态仍留在组件内。\nVuex 集中式存储管理应用的所有组件的状态，并以相应的规则保证状态以可预测的方式发生变化\n核心概念\nstate: 状态中心mutations: 更改状态actions: 异步更改状态getters: 获取状态modules: 将state分成多个modules，便于管理\n状态- state\nstate保存应用状态\nexportdefaultnewVuex.Store({state:{counter:0},})\n状态变更- mutations\nmutations用于修改状态，store.js\nexportdefaultnewVuex.Store({mutations:{add(state){state.counter++}}})\n派生状态- getters\n从state派生出新状态，类似计算属性\nexportdefaultnewVuex.Store({getters:{doubleCounter(state){// 计算剩余数量 return state.counter * 2;}}})\n动作- actions\n加业务逻辑，类似于controller\nexportdefaultnewVuex.Store({actions:{add({commit}){setTimeout(()=>{}}})\n测试代码:\n<p@click=\"$store.commit('add')\">counter: {{$store.state.counter}}</p><p@click=\"$store.dispatch('add')\">async counter: {{$store.state.counter}}</p><p>double:{{$store.getters.doubleCounter}}</p>\nvuex原理解析\n实现一个插件:声明Store类，挂载$storeStore具体实现:创建响应式的state，保存mutations、actions和getters实现commit根据用户传入type执行对应mutation实现dispatch根据用户传入type执行对应action，同时传递上下文实现getters，按照getters定义对state做派生\n创建响应式的state，保存mutations、actions和getters实现commit根据用户传入type执行对应mutation实现dispatch根据用户传入type执行对应action，同时传递上下文实现getters，按照getters定义对state做派生\n// 目标1：实现Store类，管理state（响应式的），commit方法和dispatch方法// 目标2：封装一个插件，使用更容易使用letVue;classStore{constructor(options){// 定义响应式的state// this.$store.state.xx// 借鸡生蛋this._vm=newVue({data:{$$state:options.state}})this._mutations=options.mutationsthis._actions=options.actions// 绑定this指向this.commit=this.commit.bind(this)this.dispatch=this.dispatch.bind(this)}// 只读getstate(){returnthis._vm._data.$$state}setstate(val){console.error('不能直接赋值呀，请换别的方式！！天王盖地虎！！');}// 实现commit方法，可以修改statecommit(type,payload){// 拿出mutations中的处理函数执行它constentry=this._mutations[type]if(!entry){console.error('未知mutaion类型');return}entry(this.state,payload)}dispatch(type,payload){constentry=this._actions[type]if(!entry){console.error('未知action类型');return}// 上下文可以传递当前store实例进去即可entry(this,payload)}}functioninstall(_Vue){Vue=_Vue// 混入store实例Vue.mixin({beforeCreate(){if(this.$options.store){Vue.prototype.$store=this.$options.store}}})}// { Store, install }相当于Vuex// 它必须实现install方法exportdefault{Store,install}", "f": [{"q": "商品详情页把接口请求直接写进mutation，点击购买时组件用commit触发；评审同事认为“反正最后都要改state”，你会接受吗？", "a": "不会，mutation应承担同步、可预测的状态变更，异步流程和业务编排应放进action，完成后再commit。把请求塞进mutation会让变更发生时间难以判断，也破坏按规则追踪状态的链路；纯同步修改则没必要额外套一层action。"}, {"q": "后台有用户、权限、订单三个域，所有state、mutation和action都堆在一个store.js，新增成员经常提交错同名操作，你会怎样拆？", "a": "我会按业务域拆成modules，让每个模块管理自己的state、mutations、actions和getters，组件只依赖对应域的公开能力。拆分边界应跟业务职责一致，而不是按文件数量平均切割；模块过细会增加跳转和调用成本。"}, {"q": "购物车页要求“一键清空”立即完成，而结算页要求先调用接口、成功后再清空；两处都想复用名为clearCart的逻辑，你会怎样安排commit和dispatch？", "a": "本地立即清空应由mutation完成，结算流程则由action负责异步调用，成功后再commit同一个清空操作。这样状态写入入口保持明确，业务流程也能复用；若接口失败仍提前提交，就需要额外回滚并可能造成界面与服务端不一致。"}, {"q": "线上点击“异步加一”后计数始终不变，控制台提示未知action类型，但同步按钮正常；你会沿着Vuex的哪条执行链排查？", "a": "先核对dispatch的type是否存在于已注册的actions，再确认该action是否通过上下文调用了正确的commit。Vuex的dispatch只负责找到并执行动作，真正修改state仍要落到mutation；模块注册或命名不一致也会在入口处中断。"}, {"q": "团队准备写一个轻量状态库，要求支持响应式state、同步提交和异步动作，但暂时不要模块系统；最小实现需要保留哪些机制？", "a": "至少要用Vue的响应式能力承载state，保存操作表，并实现按type查找的commit与dispatch，同时把实例注入组件成为$store。还要绑定方法的this，并拒绝整体替换只读state；省略getters后就不具备统一派生状态能力。"}]}, {"s": "Vue", "t": "vue3带来的新特性/亮点", "m": "Vue 3的亮点可以归为响应式升级、代码组织改善和编译运行性能优化。响应式从Object.defineProperty换成Proxy，代理对象本身；Composition API让同一业务逻辑聚合在一起，也更利于复用和类型推导。编译阶段会标记动态节点，使虚拟DOM的更新成本更多取决于动态内容，并配合Tree-shaking减少未使用代码。组件层面还加入了Fragment、Teleport和Suspense，分别覆盖多根节点、跨容器渲染和异步等待场景。\n1. 压缩包体积更小\n当前最小化并被压缩的 Vue 运行时大小约为 20kB（2.6.10 版为 22.8kB）。Vue 3.0捆绑包的大小大约会减少一半，即只有10kB！\n2. Object.defineProperty -> Proxy\nObject.defineProperty是一个相对比较昂贵的操作，因为它直接操作对象的属性，颗粒度比较小。将它替换为es6的Proxy，在目标对象之上架了一层拦截，代理的是对象而不是对象的属性。这样可以将原本对对象属性的操作变为对整个对象的操作，颗粒度变大。javascript引擎在解析的时候希望对象的结构越稳定越好，如果对象一直在变，可优化性降低，proxy不需要对原始对象做太多操作。\n3. Virtual DOM 重构\nvdom的本质是一个抽象层，用javascript描述界面渲染成什么样子。react用jsx，没办法检测出可以优化的动态代码，所以做时间分片，vue中足够快的话可以不用时间分片\n传统vdom的性能瓶颈：虽然 Vue 能够保证触发更新的组件最小化，但在单个组件内部依然需要遍历该组件的整个 vdom 树。传统 vdom 的性能跟模版大小正相关，跟动态节点的数量无关。在一些组件整个模版内只有少量动态节点的情况下，这些遍历都是性能的浪费。JSX 和手写的 render function 是完全动态的，过度的灵活性导致运行时可以用于优化的信息不足那为什么不直接抛弃vdom呢？高级场景下手写render function获得更强的表达力生成的代码更简洁兼容2.x\n传统vdom的性能瓶颈：\n虽然 Vue 能够保证触发更新的组件最小化，但在单个组件内部依然需要遍历该组件的整个 vdom 树。传统 vdom 的性能跟模版大小正相关，跟动态节点的数量无关。在一些组件整个模版内只有少量动态节点的情况下，这些遍历都是性能的浪费。JSX 和手写的 render function 是完全动态的，过度的灵活性导致运行时可以用于优化的信息不足\n那为什么不直接抛弃vdom呢？\n高级场景下手写render function获得更强的表达力生成的代码更简洁兼容2.x\nvue的特点是底层为Virtual DOM，上层包含有大量静态信息的模版。为了兼容手写 render function，最大化利用模版静态信息，vue3.0采用了动静结合的解决方案，将vdom的操作颗粒度变小，每次触发更新不再以组件为单位进行遍历，主要更改如下\n将模版基于动态节点指令切割为嵌套的区块每个区块内部的节点结构是固定的每个区块只需要以一个Array追踪自身包含的动态节点\nvue3.0将 vdom 更新性能由与模版整体大小相关提升为与动态内容的数量相关\nVue 3.0 动静结合的 Dom diff\nVue3.0 提出动静结合的 DOM diff 思想，动静结合的 DOM diff其实是在预编译阶段进行了优化。之所以能够做到预编译优化，是因为 Vue core 可以静态分析 template，在解析模版时，整个 parse 的过程是利用正则表达式顺序解析模板，当解析到开始标签、闭合标签和文本的时候都会分别执行对应的回调函数，来达到构造 AST 树的目的。借助预编译过程，Vue 可以做到的预编译优化就很强大了。比如在预编译时标记出模版中可能变化的组件节点，再次进行渲染前 diff 时就可以跳过“永远不会变化的节点”，而只需要对比“可能会变化的动态节点”。这也就是动静结合的 DOM diff 将 diff 成本与模版大小正相关优化到与动态节点正相关的理论依据。\n4. Performance\nvue3在性能方面比vue2快了2倍。\n重写了虚拟DOM的实现运行时编译update性能提高SSR速度提高\n5. Tree-shaking support\nvue3中的核心api都支持了tree-shaking，这些api都是通过包引入的方式而不是直接在实例化时就注入，只会对使用到的功能或特性进行打包（按需打包），这意味着更多的功能和更小的体积。\n6. Composition API\nvue2中，我们一般会采用mixin来复用逻辑代码，用倒是挺好用的，不过也存在一些问题：例如代码来源不清晰、方法属性等冲突。基于此在vue3中引入了Composition API（组合API），使用纯函数分隔复用代码。和React中的hooks的概念很相似\n更好的逻辑复用和代码组织更好的类型推导\n<template><div>X: {{ x }}</div><div>Y: {{ y }}</div></template><script>import{defineComponent,onMounted,onUnmounted,ref}from\"vue\";constuseMouseMove=()=>{constx=ref(0);consty=ref(0);functionmove(e){x.value=e.clientX;y.value=e.clientY;}onMounted(()=>{window.addEventListener(\"mousemove\",move);});onUnmounted(()=>{window.removeEventListener(\"mousemove\",move);});return{x,y};};exportdefaultdefineComponent({setup(){const{x,y}=useMouseMove();return{x,y};}});</script>\n7. 新增的三个组件Fragment、Teleport、Suspense\nFragment\n在书写vue2时，由于组件必须只有一个根节点，很多时候会添加一些没有意义的节点用于包裹。Fragment组件就是用于解决这个问题的（这和React中的Fragment组件是一样的）。\n这意味着现在可以这样写组件了。\n/* App.vue */<template><header>...</header><mainv-bind=\"$attrs\">...</main><footer>...</footer></template><script>exportdefault{};</script>\n或者这样\n// app.jsimport{defineComponent,h,Fragment}from'vue';exportdefaultdefineComponent({render(){returnh(Fragment,{},[h('header',{},['...']),h('main',{},['...']),h('footer',{},['...']),]);}});\nTeleport\nTeleport其实就是React中的Portal。Portal 提供了一种将子节点渲染到存在于父组件以外的 DOM 节点的优秀的方案。\n一个 portal 的典型用例是当父组件有 overflow: hidden 或 z-index 样式时，但你需要子组件能够在视觉上“跳出”其容器。例如，对话框、悬浮卡以及提示框。\n/* App.vue */<template><div>123</div><Teleportto=\"#container\">Teleport</Teleport></template><script>import{defineComponent}from\"vue\";exportdefaultdefineComponent({setup(){}});</script>/* index.html */<divid=\"app\"></div><divid=\"container\"></div>\nSuspense\n同样的，这和React中的Supense是一样的。\nSuspense让你的组件在渲染之前进行“等待”，并在等待时显示 fallback 的内容\n// App.vue<template><Suspense><template#default><AsyncComponent/></template><template#fallback>Loading...</template></Suspense></template><scriptlang=\"ts\">import{defineComponent}from\"vue\";importAsyncComponentfrom'./AsyncComponent.vue';exportdefaultdefineComponent({name:\"App\",components:{AsyncComponent}});</script>// AsyncComponent.vue<template><div>Async Component</div></template><scriptlang=\"ts\">import{defineComponent}from\"vue\";constsleep=()=>{returnnewPromise(resolve=>setTimeout(resolve,1000));};exportdefaultdefineComponent({asyncsetup(){awaitsleep();}});</script>\n8. Better TypeScript support\n在vue2中使用过TypesScript的童鞋应该有过体会，写起来实在是有点难受。vue3则是使用ts进行了重写，开发者使用vue3时拥有更好的类型支持和更好的编写体验。", "f": [{"q": "一个展示页只有标题会变化，模板里还有上百个静态节点；同事说Vue3 更新时仍必须完整比较整棵Virtual DOM，你怎么判断？", "a": "这个判断忽略了Vue3 的编译期优化：模板会被分析为结构稳定的区块，并追踪其中可能变化的动态节点。更新成本因此更接近动态内容数量，而非模板总体大小；手写render function或高度动态的结构无法同等利用静态信息。"}, {"q": "组件库要同时提供表单、动画和状态能力，但业务项目只用了其中一部分，构建负责人担心Vue3 功能越多包越大；你会关注什么？", "a": "应关注这些核心API是否以可导入、可摇树的形式使用，让构建工具只保留实际引用的功能。Vue3 的tree-shaking支持能减少未使用能力进入产物，但最终体积仍受业务代码、第三方依赖和构建配置影响，不能只凭框架特性下结论。"}, {"q": "管理后台频繁给响应式对象新增字段，技术负责人认为Vue3 仍要像基于Object.defineProperty的方案那样逐属性处理，你会如何解释选型变化？", "a": "Vue3 以Proxy在对象外层建立拦截，代理对象操作，而不是预先逐个改造已有属性。它减少了对原对象属性结构的直接操作，也更适合对象结构变化的场景；代价是运行环境必须具备相应的Proxy能力，不能把它当作旧机制的无条件替换。"}, {"q": "弹窗放在有overflow: hidden和复杂z-index的卡片组件里，线上始终被裁切；产品又要求弹窗逻辑仍归卡片维护，你会用Vue3 的什么能力？", "a": "可用Teleport把弹窗实际渲染到卡片外的目标DOM容器，同时保留它在原组件中的声明和逻辑归属。这样能绕开祖先容器的裁切或层叠限制；必须确保目标节点存在，并继续处理焦点、遮罩和关闭等交互职责。"}, {"q": "订单页的异步子组件加载前会短暂空白，团队在“父组件手写加载状态”和Vue3 内建组件之间争论，你会怎么取舍？", "a": "若等待发生在异步组件渲染阶段，可用Suspense提供default与fallback内容，把加载占位集中在边界上。简单请求或需要精细控制错误、重试与局部状态时，显式状态可能更清楚；Suspense解决等待展示，不会自动替代完整的异常处理。"}, {"q": "一个大型组件把搜索、分页和鼠标坐标逻辑分散在data、computed、methods与生命周期中，维护者找不到同一业务链；Vue3 哪项变化最直接？", "a": "Composition API允许按业务逻辑聚合相关状态、计算、监听和生命周期，并把鼠标等逻辑提取为可复用组合函数。它还能改善来源识别与类型推导；简单组件继续使用Options API也成立，强行组合反而可能增加抽象层次。"}]}, {"s": "Vue", "t": "Compositon api", "m": "Composition API是按业务功能组织和复用组件逻辑的一套组合式接口。Options API会把同一功能拆到data、computed、methods等区域，组件变大后阅读和维护都更费劲；组合式写法则能把相关状态与操作放在一起。相比mixin，它通过显式调用组合函数返回数据，来源更清楚，也降低了属性命名冲突和类型推断困难。它基于Vue响应式系统自动收集依赖，调用也不依赖固定顺序，但中小型、逻辑简单的组件并不一定必须改写。\nComposition API也叫组合式API，是Vue3.x的新特性。\n通过创建 Vue 组件，我们可以将接口的可重复部分及其功能提取到可重用的代码段中。仅此一项就可以使我们的应用程序在可维护性和灵活性方面走得更远。然而，我们的经验已经证明，光靠这一点可能是不够的，尤其是当你的应用程序变得非常大的时候——想想几百个组件。在处理如此大的应用程序时，共享和重用代码变得尤为重要\nVue2.0中，随着功能的增加，组件变得越来越复杂，越来越难维护，而难以维护的根本原因是Vue的API设计迫使开发者使用watch，computed，methods选项组织代码，而不是实际的业务逻辑。另外Vue2.0缺少一种较为简洁的低成本的机制来完成逻辑复用，虽然可以minxis完成逻辑复用，但是当mixin变多的时候，会使得难以找到对应的data、computed或者method来源于哪个mixin，使得类型推断难以进行。所以Composition API的出现，主要是也是为了解决Option API带来的问题，第一个是代码组织问题，Compostion API可以让开发者根据业务逻辑组织自己的代码，让代码具备更好的可读性和可扩展性，也就是说当下一个开发者接触这一段不是他自己写的代码时，他可以更好的利用代码的组织反推出实际的业务逻辑，或者根据业务逻辑更好的理解代码。第二个是实现代码的逻辑提取与复用，当然mixin也可以实现逻辑提取与复用，但是像前面所说的，多个mixin作用在同一个组件时，很难看出property是来源于哪个mixin，来源不清楚，另外，多个mixin的property存在变量命名冲突的风险。而Composition API刚好解决了这两个问题。\n通俗的讲：\n没有Composition API之前vue相关业务的代码需要配置到option的特定的区域，中小型项目是没有问题的，但是在大型项目中会导致后期的维护性比较复杂，同时代码可复用性不高。Vue3.x中的composition-api就是为了解决这个问题而生的\ncompositon api提供了以下几个函数：\nsetuprefreactivewatchEffectwatchcomputedtoRefs生命周期的hooks\n都说Composition API与React Hook很像，说说区别\n从React Hook的实现角度看，React Hook是根据useState调用的顺序来确定下一次重渲染时的state是来源于哪个useState，所以出现了以下限制\n不能在循环、条件、嵌套函数中调用Hook必须确保总是在你的React函数的顶层调用HookuseEffect、useMemo等函数必须手动确定依赖关系\n而Composition API是基于Vue的响应式系统实现的，与React Hook的相比\n声明在setup函数内，一次组件实例化只调用一次setup，而React Hook每次重渲染都需要调用Hook，使得React的GC比Vue更有压力，性能也相对于Vue来说也较慢Compositon API的调用不需要顾虑调用顺序，也可以在循环、条件、嵌套函数中使用响应式系统自动实现了依赖收集，进而组件的部分的性能优化由Vue内部自己完成，而React Hook需要手动传入依赖，而且必须必须保证依赖的顺序，让useEffect、useMemo等函数正确的捕获依赖变量，否则会由于依赖不正确使得组件性能下降。\n虽然Compositon API看起来比React Hook好用，但是其设计思想也是借鉴React Hook的。", "f": [{"q": "一个表格组件用了五个mixin，模板里的load和rows找不到来源，另一个mixin又声明了同名属性；你会怎样重构？", "a": "应把各业务能力提取为显式调用的组合函数，例如由useTable返回rows和load，组件能直接看出来源。需要重名时可在解构或返回结构上主动处理；组合函数过度拆碎也会让调用关系分散，因此边界应围绕完整业务逻辑。"}, {"q": "搜索页的筛选、分页、请求取消逻辑分别散在watch、computed、methods和卸载钩子里，新成员改一次要跳四处；setup中你会怎么组织？", "a": "我会让同一搜索流程的ref、computed、watch和生命周期钩子相邻放置，再把稳定可复用的部分提取成useSearch。这种组织方式按业务关系而非选项类型聚合代码；若组合函数同时承担多个领域，仍会退化成难维护的大函数。"}, {"q": "权限组合函数只在管理员分支中需要，评审者套用React规则，禁止在条件分支调用它；在Vue的Composition API下这个限制成立吗？", "a": "不应机械套用ReactHook的调用顺序规则，Vue的setup对每个组件实例执行一次，组合式调用不靠固定序号匹配状态。它可以出现在条件、循环或嵌套函数中；但分支调用产生的生命周期与资源清理仍要清晰，否则工程上依然容易遗漏。"}, {"q": "线上筛选条件变化后副作用没有执行，同事刚把相关读取移出watchEffect，却认为响应式系统会自动追踪文件里所有变量；你会怎样排查？", "a": "先确认响应式值是否在该副作用执行期间被实际读取，因为依赖收集来自运行中的访问关系，而不是静态扫描整个文件。再检查值是否仍由ref或reactive持有；脱离响应式容器或不在执行链中读取，都不会建立预期依赖。"}, {"q": "团队要在watch和watchEffect间选型：风控页必须明确监听账户和金额，并记录变更前后值；哪一个更合适？", "a": "这里更适合watch，因为依赖目标需要显式表达，并且业务要使用新旧值进行审计或判断。watchEffect更适合让系统自动收集副作用中读取的依赖；自动收集虽简洁，但依赖边界不够显眼，复杂风控逻辑会更难审查。"}, {"q": "React开发者说组合式函数就是换名后的Hook，并准备给每个副作用手写依赖数组；你会指出哪些实现差异？", "a": "Vue的组合式能力建立在响应式依赖收集上，watchEffect等机制可根据实际读取自动建立依赖，并不依靠Hook调用顺序。setup通常按组件实例执行一次，而ReactHook随重渲染再次调用；两者思想相近，但不能照搬依赖数组和顶层调用约束。"}]}, {"s": "Vue", "t": "computed 的实现原理", "m": "computed本质上是带缓存的惰性观察者，通过dirty标记决定是否重新求值。首次读取时它会执行计算并缓存结果；依赖变化后，若当前没有订阅者，通常只把dirty设为true，等下次读取再计算。存在订阅者时，它会重新计算并比较新旧结果，最终值确实变化才通知渲染观察者，从而减少无效更新。因此，同一派生结果会被多次读取时适合用computed，单纯执行动作则不必依赖这层缓存。\ncomputed本质是一个惰性求值的观察者computed watcher。其内部通过this.dirty属性标记计算属性是否需要重新求值。\n当 computed 的依赖状态发生改变时,就会通知这个惰性的 watcher,computed watcher通过this.dep.subs.length判断有没有订阅者,有的话,会重新计算,然后对比新旧值,如果变化了,会重新渲染。 (Vue 想确保不仅仅是计算属性依赖的值发生变化，而是当计算属性最终计算的值发生变化时才会触发渲染 watcher重新渲染，本质上是一种优化。)没有的话,仅仅把this.dirty = true(当计算属性依赖于其他数据时，属性并不会立即重新计算，只有之后其他地方需要读取属性的时候，它才会真正计算，即具备 lazy（懒计算）特性。)", "f": [{"q": "报表页把totalPrice写成普通方法，模板在十几个位置调用，任意按钮触发重渲染都会重新遍历订单；改成computed为什么有意义？", "a": "computed由惰性的computed watcher管理，依赖未变化时可复用已有结果，不必在每次渲染中重复执行遍历。依赖变化后它才进入需要重新求值的状态；若计算本身极轻且只调用一次，缓存带来的收益可能并不明显。"}, {"q": "商品列表有一个昂贵的派生筛选值，但折叠面板关闭时没有任何模板或其他逻辑读取它；源数据更新后会马上重新计算吗？", "a": "在没有订阅者时，依赖变化通常只会把计算观察者的dirty标记设为true，不会立刻执行筛选。等面板展开并再次读取该值时才真正求值，这就是惰性计算；若其他观察者仍订阅它，就不能假设始终无人触发。"}, {"q": "一个computed同时依赖价格和折扣，价格变化后被连续读取三次；面试现场让你描述dirty从变化到复用的过程，你会怎么说？", "a": "依赖变化会通知对应的惰性观察者，使其进入需要求值的状态；首次读取时重新执行计算并产出结果，随后读取可继续使用缓存。直到相关依赖再次变化前，不应重复计算；前提是这些依赖确实在求值过程中被响应式系统收集。"}, {"q": "线上日志显示源字段频繁变化，但依赖某个computed的视图并非每次都重渲染；你会先判断这是故障还是优化？", "a": "先比较计算属性最终的新旧值，而不是只看底层依赖是否变化；最终值未变时，不触发渲染观察者属于预期优化。若最终值已变化仍不更新，再检查依赖是否具有响应性、计算属性是否被订阅，以及读取链路是否真正建立。"}, {"q": "库存页需要在数量变化后写日志并请求补货，同事想把这些副作用塞进computed以利用缓存；你会如何取舍？", "a": "不应把日志和请求作为计算属性的核心职责，computed更适合根据响应式状态产生可缓存的派生值。副作用应放到侦听器或明确的业务动作中，以便控制触发和失败处理；否则惰性读取会让请求时机取决于“是否有人访问”。"}]}, {"s": "Vue", "t": "watch 的理解", "m": "watch本质上是观察数据变化，并在变化后执行回调，它本身没有缓存能力。当监听对象内部的属性时，可以开启deep: true，让对象中的每一项都被监听。深度监听覆盖得更全面，但也会带来额外的性能开销。明确知道要观察哪个属性时，我一般会使用字符串形式监听，缩小监听范围。\nwatch没有缓存性，更多的是观察的作用，可以监听某些数据执行回调。当我们需要深度监听对象中的属性时，可以打开deep：true选项，这样便会对对象中的每一项进行监听。这样会带来性能问题，优化的话可以使用字符串形式监听\n注意：Watcher : 观察者对象 , 实例分为渲染 watcher(render watcher),计算属性 watcher(computed watcher),侦听器 watcher（user watcher）三种", "f": [{"q": "个人资料页把完整用户对象做成computed，只为了姓名变化时调用埋点；同事认为computed和watch都能依赖收集，你会接受吗？", "a": "不接受，埋点属于数据变化后的副作用，应使用watch执行回调，而computed应保留给派生值和缓存。混用会让读取动作夹带外部行为，触发时机也更难判断；若只是展示拼接姓名，才适合计算属性。"}, {"q": "配置中心监听一个包含大量嵌套字段的表单，任意字段改动都触发自动保存；直接设置deep: true有什么工程风险？", "a": "深度监听会遍历对象中的各项以建立监听，结构越大，初始化与变更处理的负担越值得警惕。更稳妥的是只监听确实影响保存的字段，或拆成多个边界明确的对象；若需求就是捕获任意深层修改，精确监听会增加维护清单。"}, {"q": "订单页原来只监听status，产品临时要求地址任一层级变化也重新校验；你会继续写单字段监听，还是改成深度监听？", "a": "若地址结构稳定且只有少数字段参与校验，优先精确监听这些路径，避免无关字段也触发回调。只有规则明确要求捕获整个地址对象的任意深层变化时才考虑deep: true；范围扩大后必须评估回调频率和对象规模。"}, {"q": "线上修改profile.contact.phone后校验没有执行，但替换整个profile对象时会执行；当前只监听了对象本身，你会怎么定位？", "a": "先确认侦听器是否只观察了对象引用，因为深层属性变化不等于引用被替换。随后核对目标字段路径，并选择精确的字符串路径监听或按需求开启deep；开启深度监听虽能覆盖该变化，也可能把无关字段纳入回调。"}, {"q": "审计模块既要计算账户风险等级，又要在等级变化时发送告警；架构师想全部交给一个侦听器，你会怎样拆分？", "a": "风险等级应由computed watcher负责派生和缓存，告警则由user watcher观察等级变化后执行回调。组件渲染仍由render watcher响应最终展示变化；拆分能区分计算、用户副作用和渲染职责，但要避免告警回调反向修改依赖造成循环。"}]}, {"s": "Vue", "t": "vue 渲染过程", "m": "Vue的渲染过程是先把模板编译成render函数，再生成VNode，最后通过patch更新真实DOM。编译时，parse把模板转成AST，optimize标记静态节点，generate再生成render函数字符串。数据变化会触发渲染Watcher，重新执行render得到新的VNode。随后通过DOM diff对比新旧节点，只对真实DOM做必要的添加、修改或删除。\n调用compile函数,生成 render 函数字符串 ,编译过程如下:parse 使用大量的正则表达式对template字符串进行解析，将标签、指令、属性等转化为抽象语法树AST。模板 -> AST （最消耗性能）optimize 遍历AST，找到其中的一些静态节点并进行标记，方便在页面重渲染的时候进行diff比较时，直接跳过这一些静态节点，优化runtime的性能generate 将最终的AST转化为render函数字符串调用new Watcher函数,监听数据的变化,当数据发生变化时，Render 函数执行生成 vnode 对象调用patch方法,对比新旧 vnode 对象,通过 DOM diff 算法,添加、修改、删除真正的 DOM 元素\nparse 使用大量的正则表达式对template字符串进行解析，将标签、指令、属性等转化为抽象语法树AST。模板 -> AST （最消耗性能）optimize 遍历AST，找到其中的一些静态节点并进行标记，方便在页面重渲染的时候进行diff比较时，直接跳过这一些静态节点，优化runtime的性能generate 将最终的AST转化为render函数字符串", "f": [{"q": "商品详情页只修改购物车数量，为什么不能理解成Vue会重新创建整棵真实DOM？", "a": "数据变化会触发对应的Watcher，重新执行render生成新的VNode，但不会直接重建整棵真实DOM。随后patch对比新旧VNode，只对需要变化的真实节点执行添加、修改或删除；若节点结构或标识发生变化，复用范围才会缩小。"}, {"q": "后台表格有 3000 行，筛选条件每输入一个字符就明显卡顿，你会把耗时拆到渲染链路的哪些阶段？", "a": "先区分数据变化后执行render的脚本耗时，以及patch后真实DOM更新带来的布局和绘制耗时。若生成大量VNode已经很慢，应减少参与渲染的数据；若主要慢在真实节点更新，则应分页或缩小一次更新的节点规模。"}, {"q": "运营平台允许用户在线修改template，而普通业务组件在构建时已有render，两类组件的渲染链路有什么关键差异？", "a": "在线模板必须先经过parse、optimize和generate，得到render后才能进入创建VNode与patch的流程。构建阶段已经生成render的组件可跳过运行时编译；动态模板若频繁变化，编译成本会重新出现，不能只优化后续diff。"}, {"q": "线上页面数据已更新，控制台也能看到新值，但视图保持旧内容，你会沿着哪条链路定位断点？", "a": "先确认变化是否触发负责渲染的Watcher，再检查render是否生成了符合预期的新VNode，最后观察patch是否更新了真实DOM。若新旧VNode本身没有差异，应回查渲染依赖；若差异存在但节点未变，再检查节点匹配与补丁过程。"}, {"q": "内容团队要求首屏模板高度动态，性能团队要求彻底移除运行时编译，你会如何划分方案边界？", "a": "固定结构的组件应在构建阶段完成模板编译，运行时直接执行render，避免承担最耗性能的模板到AST转换。真正由用户配置结构的区域才保留动态编译，并限制其范围和触发次数；完全动态与完全预编译无法同时满足同一块模板。"}]}, {"s": "Vue", "t": "说一说keep-alive实现原理", "m": "keep-alive的核心是缓存组件对应的VNode，再次命中时直接从缓存中取出。它通过include和exclude按组件的name控制哪些需要缓存，参数可以是字符串、正则或数组。max用来限制缓存数量，超过上限后会删除最先进入的缓存项。这个淘汰过程采用LRU思路，让最近访问过的内容更有机会继续保留。\nkeep-alive组件接受三个属性参数：include、exclude、max\ninclude指定需要缓存的组件name集合，参数格式支持String, RegExp, Array。当为字符串的时候，多个组件名称以逗号隔开。exclude指定不需要缓存的组件name集合，参数格式和include一样。max指定最多可缓存组件的数量,超过数量删除第一个。参数格式支持String、Number。\n原理\nkeep-alive实例会缓存对应组件的VNode,如果命中缓存，直接从缓存对象返回对应VNode\nLRU（Least recently used）算法根据数据的历史访问记录来进行淘汰数据，其核心思想是“如果数据最近被访问过，那么将来被访问的几率也更高”。(墨菲定律：越担心的事情越会发生)", "f": [{"q": "订单列表被keep-alive包裹后，从详情页返回仍保留筛选条件，这是否说明它把页面真实DOM截图后直接恢复了？", "a": "不是，keep-alive的核心是缓存对应组件的VNode，再次命中时从缓存对象返回该VNode。保留页面状态来自组件实例及其虚拟节点被复用，而不是保存一份页面截图；未命中缓存时仍需按正常组件流程创建。"}, {"q": "移动端有商品、订单、消息三类列表，只希望订单页返回时保留状态，你会怎样配置include和组件名称？", "a": "给订单页设置稳定的组件name，并让include只匹配该名称，商品页和消息页便不会进入缓存。include支持String、RegExp或Array，字符串包含多个名称时用逗号分隔；名称不匹配会导致看似配置成功却始终无法命中。"}, {"q": "路由扩展到 20 个详情页，产品要求全部缓存，客户端负责人又要求限制缓存数量，你会怎样处理max？", "a": "设置max限制缓存组件数量，超出后按最近使用情况淘汰长期未访问的缓存项。max支持String或Number，但它只能控制数量，不能判断业务重要性；关键页面仍应结合include、exclude明确缓存范围。"}, {"q": "线上反馈用户返回列表时状态偶尔丢失，但同一路由有时又能恢复，你会优先核对哪些缓存条件？", "a": "先核对组件name是否稳定且确实命中include，再检查是否被exclude排除或因超过max而被淘汰。随后确认访问顺序，因为LRU会改变淘汰对象；若组件从未命中缓存，问题不在恢复阶段，而在缓存筛选条件。"}, {"q": "中后台有 30 个路由，团队争论是全部套keep-alive，还是只缓存高频表单页，你会支持哪种方案？", "a": "应只缓存确实需要保留编辑状态或页面上下文的组件，并用include或exclude明确名单，同时设置合理的max。全量缓存会让缓存集合持续扩大并触发淘汰，命中结果更依赖访问历史；选择性缓存更可控，但未缓存页面需要自行恢复状态。"}]}, {"s": "Vue", "t": "为什么访问data属性不需要带data", "m": "访问data属性时不需要再写data，是因为Vue把这些属性代理到了实例上。简单来说，读取this.xxx时，代理的get会转发到内部对象对应的属性；给this.xxx赋值时，set也会把新值写回去。这个代理通过Object.defineProperty实现，并将属性设置为可枚举、可配置，因此模板和实例代码都能用更简洁的方式访问数据。\nvue中访问属性代理this.data.xxx转换this.xxx的实现\n/** 将 某一个对象的属性 访问 映射到 对象的某一个属性成员上 */functionproxy(target,prop,key){Object.defineProperty(target,key,{enumerable:true,configurable:true,get(){returntarget[prop][key];},set(newVal){target[prop][key]=newVal;}});}", "f": [{"q": "组件里写了data: { count: 1 }，模板和方法都能访问this.count，这是否意味着count被复制到实例上了？", "a": "不是复制，而是在实例的count属性上通过Object.defineProperty建立访问代理。读取this.count时，get返回this.data.count；写入时，set再把新值交给对应的数据属性，因此两处不会维护两份独立状态。"}, {"q": "表单组件有 80 个字段，团队想手写this.data.xxx来减少一层代理，你会如何评价这个改动？", "a": "现有代理让模板和实例方法统一使用this.xxx，手写完整路径会改变组件的访问约定，却不会改变底层数据仍位于data的事实。是否值得改动应以实际测量为依据；仅凭多一次get转发，无法断言它就是表单卡顿来源。"}, {"q": "基础组件实例上已经存在status，业务又在data中声明同名字段并希望通过this.status访问，代理实现需要满足什么约束？", "a": "代理要求在目标实例上为该key定义可配置的访问属性，因此同名成员会产生覆盖或定义冲突风险。初始化阶段应检测并避免名称碰撞，必要时重命名业务字段；否则this.status指向哪一层会变得不可靠。"}, {"q": "线上出现this.total = 20后this.data.total仍是旧值，你会怎样验证代理是否被破坏？", "a": "先用属性描述符检查实例total是否仍具有代理的get和set，再确认它们是否指向预期的data.total。若该属性后来被重新定义，赋值就可能不再转发；若代理完整，则继续核对实际代理容器和字段名是否一致。"}, {"q": "代码评审中有人建议用一次性赋值this.count = this.data.count代替访问器代理，两种方案的行为差异是什么？", "a": "一次性赋值只同步初始化时的值，之后修改实例属性或data.count都不会自动反映到另一处。访问器代理则让每次读写都落到同一个底层字段；代价是实例属性必须保留这组get、set，不能随意改成普通值属性。"}]}, {"s": "Vue", "t": "template预编译是什么", "m": "模板预编译，就是在项目构建阶段把template转换成render function。如果留到运行时处理，组件实例化时需要先完成一次模板编译，之后才能执行渲染，这部分工作会带来运行时损耗。提前编译后，组件运行时可以直接使用生成的渲染函数，从而跳过模板编译。它优化的是组件启动和渲染前的准备过程，并不是让模板在运行时反复编译。\n对于 Vue 组件来说，模板编译只会在组件实例化的时候编译一次，生成渲染函数之后在也不会进行编译。因此，编译对组件的 runtime 是一种性能损耗。\n而模板编译的目的仅仅是将template转化为render function，这个过程，正好可以在项目构建的过程中完成，这样可以让实际组件在 runtime 时直接跳过模板渲染，进而提升性能，这个在项目构建的编译template的过程，就是预编译。", "f": [{"q": "一个组件只实例化一次，负责人因此认为运行时编译template没有成本，你会怎样反驳？", "a": "模板即使只在组件实例化时编译一次，也必须完成从template到render function的转换，这仍是一次运行时性能损耗。预编译把这段工作移到项目构建过程，使实际组件运行时直接执行渲染函数；实例少只能降低影响，不能让成本消失。"}, {"q": "营销页有 40 个固定组件模板，构建负责人问是否值得做预编译，你会给出什么落地判断？", "a": "这些模板在发布前已经确定，适合在构建阶段统一转换为render function，运行时无需再次处理模板。落地时应确认产物包含可直接执行的渲染函数；若仍把原始template留到浏览器处理，就没有获得预编译的核心收益。"}, {"q": "低代码平台的模板由运营发布后即时生效，架构组仍要求所有模板构建期预编译，这个约束为何无法直接满足？", "a": "构建期预编译只能处理构建时已经存在的模板，而运营发布的模板在运行时才确定，无法被先前构建产物完整覆盖。可把固定外壳预编译，将动态区域限制在必要范围；若动态结构也必须即时变化，就仍需承担相应的模板转换成本。"}, {"q": "线上首开页面卡顿，火焰图显示组件实例化阶段出现明显模板编译工作，你会检查构建链路的什么结果？", "a": "先检查发布产物中的组件是否已经携带render function，以及运行时是否仍接收到原始template。若模板没有在构建阶段转换，实例化时就会再次承担编译；若已有渲染函数，则应继续排查render和后续真实节点更新，而非归因于预编译。"}, {"q": "插件系统在“构建期预编译”和“运行时接收模板”之间选型，产品强调灵活性，性能团队强调启动速度，你会怎样定边界？", "a": "核心页面和稳定插件应优先预编译，让运行时直接进入渲染流程；必须动态下发结构的插件才保留运行时模板转换。前者牺牲发布后的即时结构变化，后者增加实例化阶段的编译工作，边界应由模板是否能在构建时确定来划分。"}]}, {"s": "Vue", "t": "介绍一下Vue中的Diff算法", "m": "Vue的Diff算法用于比较新旧虚拟DOM，找出同层节点中真正需要更新的部分。它先判断节点是否相同，不同就删除旧节点并创建新节点，相同则通过patchVnode继续处理子节点。双方都有多个子节点时，才进入updateChildren做核心比较，匹配到相同子节点后再递归处理。它不做跨层级比较，因此把原本可能达到O(n³)的复杂度降低到了O(n)。\n在新老虚拟DOM对比时\n首先，对比节点本身，判断是否为同一节点，如果不为相同节点，则删除该节点重新创建节点进行替换如果为相同节点，进行patchVnode，判断如何对该节点的子节点进行处理，先判断一方有子节点一方没有子节点的情况(如果新的children没有子节点，将旧的子节点移除)比较如果都有子节点，则进行updateChildren，判断如何对这些新老节点的子节点进行操作（diff核心）。 匹配时，找到相同的子节点，递归比较子节点\n在diff中，只对同层的子节点进行比较，放弃跨级的节点比较，使得时间复杂从O(n^3)降低值O(n)，也就是说，只有当新旧children都为多个子节点时才需要用核心的Diff算法进行同层级比较。", "f": [{"q": "资讯列表把一个卡片从第三层容器移到相邻栏目，内容完全相同，为什么不能期待diff跨层找到并直接复用？", "a": "该算法只比较同层子节点，主动放弃跨级匹配，因此相同内容移动到不同层级时不会按跨层节点处理。旧位置可能被删除，新位置再创建对应节点；这项约束换来了线性的同层比较范围，代价是跨层重排无法直接复用。"}, {"q": "商品列表更新 1000 条数据时，只有价格字段变化，你会如何解释从根节点到真实DOM的处理路径？", "a": "先比较新旧虚拟节点本身，若判定为同一节点，就进入patchVnode处理其子节点。双方都有多个子节点时进入updateChildren，找到相同子节点后递归比较，最终只把确定的差异落实到真实DOM；若节点不相同，则会删除并重新创建。"}, {"q": "空状态页从“无订单”切换为包含 20 条订单的列表，和两个列表各有 20 条数据时，是否都会进入核心updateChildren？", "a": "不会，只有新旧双方都具有多个子节点时，才需要进入核心的同层子节点比较。由无子节点变为有子节点时可直接添加，新的一方没有子节点时则移除旧子节点；因此应先判断子节点存在形态，再决定是否执行列表diff。"}, {"q": "线上更新一条记录后整行输入状态丢失，你怀疑节点被替换，会沿着diff的哪个判断排查？", "a": "先检查新旧行节点是否被判定为同一节点，因为一旦不相同，旧节点会被删除并创建新节点，内部状态也无法沿用。若节点相同，再进入patchVnode检查子节点处理；排查重点是生成的虚拟节点结构与身份条件，而不只是数据值。"}, {"q": "设计器团队希望支持任意节点跨层拖拽，同时要求每次更新都保持现有diff的低复杂度，你会如何说明取舍？", "a": "现有算法通过只比较同层子节点，把原本可能达到O(n^3)的一般树比较收敛到O(n)范围。任意跨层匹配会扩大搜索空间，不能直接沿用这一复杂度承诺；可把拖拽结果视为旧层删除、新层创建，或由业务层额外维护映射。"}, {"q": "代码评审时有人把patchVnode和updateChildren当成同一层逻辑，你会用“新节点没有子节点”的场景怎样区分？", "a": "patchVnode负责同一节点内部该如何更新，其中先判断新旧双方是否存在子节点。只有双方都有多个子节点时才调用updateChildren做核心同层比较；若新节点没有子节点，直接移除旧子节点即可，无需进入列表匹配。"}]}, {"s": "Vue", "t": "说说Vue2.0和Vue3.0有什么区别", "m": "Vue 3相比Vue 2，核心变化集中在响应式、代码组织和编译运行效率上。响应式从Object.defineProperty改为Proxy，可以直接监听数组变化和属性增删，也不必预先遍历对象的每个属性。它新增Composition API，让相关逻辑更容易组织和复用；同时重构虚拟DOM，对静态节点、slot和内联事件做编译优化。代码结构也更利于Tree shaking，并使用TypeScript替代Flow。\n重构响应式系统，使用Proxy替换Object.defineProperty，使用Proxy优势：可直接监听数组类型的数据变化监听的目标为对象本身，不需要像Object.defineProperty一样遍历每个属性，有一定的性能提升可拦截apply、ownKeys、has等13种方法，而Object.defineProperty不行直接实现对象属性的新增/删除新增Composition API，更好的逻辑复用和代码组织重构Virtual DOM模板编译时的优化，将一些静态节点编译成常量slot优化，将slot编译为lazy函数，将slot的渲染的决定权交给子组件模板中内联事件的提取并重用（原本每次渲染都重新生成内联函数）代码结构调整，更便于Tree shaking，使得体积更小使用Typescript替换Flow\n可直接监听数组类型的数据变化监听的目标为对象本身，不需要像Object.defineProperty一样遍历每个属性，有一定的性能提升可拦截apply、ownKeys、has等13种方法，而Object.defineProperty不行直接实现对象属性的新增/删除\n模板编译时的优化，将一些静态节点编译成常量slot优化，将slot编译为lazy函数，将slot的渲染的决定权交给子组件模板中内联事件的提取并重用（原本每次渲染都重新生成内联函数）", "f": [{"q": "商品编辑页给一个已有响应式对象动态增加discount字段，Vue 2页面不更新，迁到Vue 3后却正常了；你怎么解释这种差异，又会提醒团队别得出什么错误结论？", "a": "差异来自响应式实现：Vue 2的Object.defineProperty主要在初始化时逐项劫持属性，后续新增或删除属性不能被直接感知；Vue 3的Proxy代理对象本身，可以拦截这类操作。不能因此认定所有更新都会更快，组件划分、依赖数量和渲染成本仍会决定实际表现。"}, {"q": "一个Vue 2后台有几十种动态表单，字段和数组项频繁增删；如果准备迁移到Vue 3，你会优先验证哪些响应式代码，而不是只确认页面能启动？", "a": "应优先检查依赖Vue.set、Vue.delete、数组变更补丁以及深层对象初始化的代码，因为迁移后这些路径会由Proxy直接跟踪。再用新增字段、删除字段和数组增删等真实操作做回归，确认计算属性与视图同步；响应式改善并不能替代业务状态和表单边界测试。"}, {"q": "一个多人维护的复杂业务组件在Vue 2中把请求、筛选、分页都塞进mixins，升级时有人主张只换响应式实现、不引入Composition API；你会如何取舍？", "a": "只完成兼容迁移是更低风险的路径，Composition API并不是升级后必须立即采用。若组件长期存在逻辑分散、同名覆盖或复用困难，可按业务能力抽成组合函数并逐段验证；一次性同时改框架、状态结构和组织方式，会扩大回归范围，也更难定位问题。"}, {"q": "迁移后列表数据没变，但性能面板显示重复创建了大量模板内联事件函数；你会从Vue 3的编译与虚拟DOM优化角度检查什么？", "a": "先确认生产构建确实使用了对应的Vue 3模板编译器，并检查模板是否让静态节点、内联事件和slot获得编译期优化。Vue 3可提升静态内容、复用内联事件并将slot编译为惰性函数；若大量内容本身是动态的，这些优化空间会自然缩小。"}, {"q": "团队争论升级收益时，一方只谈Proxy，另一方只谈包体积；你会怎样给出更完整的技术判断？", "a": "应把收益拆成运行时、开发组织和构建产物三类：Proxy改善对象与数组变化追踪，Composition API改善逻辑复用，编译器和虚拟DOM重构减少部分重复工作。代码结构也更利于Tree Shaking，且实现语言由Flow转向TypeScript；最终收益仍受依赖兼容和业务代码形态约束。"}]}, {"s": "React", "t": "对虚拟DOM的理解", "m": "虚拟DOM的核心价值不是比手写DOM操作更快，而是让声明式更新和视图抽象成为可能。它作为局部更新链路的一环，用更轻量的数据结构承载视图，使新旧视图能够执行Diff，避免每次都做全量更新。直接、精准地操作真实DOM通常仍可能更快，因为人为已经省掉了比较过程。工程上的取舍是接受一定抽象成本，换取state = UI的开发方式以及更好的跨平台能力。\n虚拟dom从来不是用来和直接操作dom对比的，它们俩最终殊途同归。虚拟dom只不过是局部更新的一个环节而已，整个环节的对比对象是全量更新。虚拟dom对于state＝UI的意义是，虚拟dom使diff成为可能（理论上也可以直接用dom对象diff，但是太臃肿），促进了新的开发思想，又不至于性能太差。但是性能再好也不可能好过直接操作dom，人脑连diff都省了。还有一个很重要的意义是，对视图抽象，为跨平台助力\n其实我最终希望你明白的事情只有一件：虚拟 DOM 的价值不在性能，而在别处。因此想要从性能角度来把握虚拟 DOM 的优势，无异于南辕北辙。偏偏在面试场景下，10 个人里面有 9 个都走这条歧路，最后9个人里面自然没有一个能自圆其说，实在让人惋惜。\n真正理解虚拟DOM(opens new window)", "f": [{"q": "活动页只有一个计数器，工程师说直接写textContent肯定比虚拟DOM快，所以虚拟DOM没价值；你会怎样回应这个反例？", "a": "单点且更新路径已知时，直接操作真实DOM通常少了一层创建和比较，虚拟DOM并不保证更快。它的价值是让开发者用state = UI描述界面，再通过差异计算完成通用的局部更新；若把比较对象限定成一次精准手工操作，结论本身就失去了公平性。"}, {"q": "一个表格页面每次筛选都重新生成整棵虚拟节点树，但最终只改了几个单元格；这层虚拟DOM在工程上解决了什么，又没有解决什么？", "a": "它提供了轻量的视图描述，使新旧结果能够执行diff，再把必要变更提交到真实DOM，而不必由业务代码维护每个更新路径。它没有消除生成对象和比较树的成本，也不会自动修复不合理的全量状态更新；组件边界和状态粒度仍需工程控制。"}, {"q": "同一套业务组件要同时运行在浏览器和原生容器中，产品要求交互逻辑复用但宿主控件完全不同；虚拟DOM在这里承担哪一层职责？", "a": "虚拟DOM抽象的是视图结构与变化，而不是某一种宿主节点，因此同一套组件描述可以交给不同渲染器落地。浏览器渲染器操作DOM，其他平台可映射到自身视图对象；跨端仍需处理平台能力、布局和交互差异，不能理解为业务代码百分之百无需适配。"}, {"q": "线上长列表滚动卡顿，监控只显示主线程繁忙，团队立即把责任归到diff；你会怎样验证，而不是直接移除虚拟DOM？", "a": "先区分耗时发生在状态计算、虚拟树创建与比较，还是最终布局、绘制和真实DOM操作，并用性能轨迹定位长任务。虚拟DOM只是局部更新链路中的一环，卡顿也可能来自全量重渲染或节点规模过大；没有分段证据就改渲染模型，往往只会转移成本。"}, {"q": "后台搭建器需要支持大量动态条件和组件插槽，一方主张手写DOM获得极致性能，另一方坚持虚拟DOM；你会如何做选型？", "a": "动态结构复杂且更新路径难以人工维护时，虚拟DOM能用统一声明式模型换取可组合性和可维护性，通常更适合作为默认方案。只有热点路径稳定、性能瓶颈已被证明确实位于通用比较时，才值得局部采用更直接的更新方式；代价是手工同步状态与视图的复杂度会上升。"}]}, {"s": "React", "t": "谈谈你对React的理解", "m": "React本质上是一个组件化的视图层框架，用声明式方式把状态映射成界面。声明式让界面逻辑更直观，组件化便于拆分和复用，也更容易做到高内聚、低耦合。借助虚拟DOM，同一套组件思想还能用于Web、Native和VR等场景。它只负责视图层，大型应用仍要结合社区方案处理路由和状态管理，因此会增加选型与学习成本。\nReact 是一个网页 UI 框架，通过组件化的方式解决视图层开发复用的问题，本质是一个组件化框架。\n它的核心设计思路有三点，分别是声明式、组件化与 通用性。声明式的优势在于直观与组合。组件化的优势在于视图的拆分与模块复用，可以更容易做到高内聚低耦合。通用性在于一次学习，随处编写。比如 React Native，React 360 等， 这里主要靠虚拟 DOM 来保证实现。这使得 React 的适用范围变得足够广，无论是 Web、Native、VR，甚至 Shell 应用都可以进行开发。这也是 React 的优势。但作为一个视图层的框架，React 的劣势也十分明显。它并没有提供完整的一揽子解决方 案，在开发大型前端应用时，需要向社区寻找并整合解决方案。虽然一定程度上促进了社区的繁荣，但也为开发者在技术选型和学习适用上造成了一定的成本。承接在优势后，可以再谈一下自己对于 React 优化的看法、对虚拟 DOM 的看法", "f": [{"q": "一个静态营销页只有几处按钮交互，候选人说用了React就一定比原生实现更先进；作为技术负责人你会怎么判断？", "a": "React的优势不在于天然更先进，而在于用声明式和组件化组织会持续变化、需要复用的视图。若页面结构简单且长期稳定，引入组件运行时与配套工具未必带来足够收益；选型应看状态复杂度、复用需求和团队维护成本，而不是框架标签。"}, {"q": "订单后台把筛选栏、表格和详情弹窗拆成二十多个组件，但改一个字段仍要跨层修改；为什么“拆成组件”没有自动实现高内聚低耦合？", "a": "组件化只提供拆分与组合机制，边界若按标签数量而非业务职责划分，依赖仍会散落在多层。应把相关状态、行为和视图收拢到稳定能力中，并明确输入输出；拆得过细会增加数据传递和理解成本，拆得过粗又会削弱复用与隔离。"}, {"q": "同一支团队要把Web端的内容流迁到原生端，管理层认为React Native意味着整套页面直接复制；你会如何校正预期？", "a": "可复用的核心是声明式组件思维和部分业务逻辑，虚拟DOM所代表的视图抽象让不同渲染器成为可能。浏览器DOM与原生视图的能力、样式和交互并不相同，因此宿主相关代码仍要适配；“一次学习，随处编写”不等于“一份界面代码，所有平台无差别运行”。"}, {"q": "大型React项目上线前，路由、数据请求和状态管理分别来自不同社区库，升级时出现版本冲突；这是偶发管理失误，还是框架定位带来的成本？", "a": "这与React主要聚焦视图层的定位直接相关，它没有为大型应用提供完整的一揽子方案。社区生态带来选择空间，也把选型、集成和升级责任交给团队；应通过统一技术基线、封装边界和升级策略控制成本，但无法把这部分复杂度完全消除。"}, {"q": "架构评审中，一方看重声明式与跨端能力，另一方担心生态拼装成本；什么项目条件下你仍会选择React？", "a": "当界面状态复杂、组件复用价值高，或团队需要在多种渲染目标间共享开发模型时，React的声明式、组件化和通用性更有吸引力。若项目更需要开箱即用的全套约束，团队又缺少整合生态的能力，则其视图层定位可能成为负担；选择必须连同路由、数据和工程规范一起评估。"}]}, {"s": "React", "t": "如何避免React生命周期中的坑", "m": "避免生命周期的坑，关键是让渲染阶段保持纯净，把副作用和清理放到合适的提交阶段。Fiber让渲染可以暂停或重来，因此不要在已废弃的componentWillMount、componentWillReceiveProps和componentWillUpdate中请求数据或绑定事件。异步请求一般放在componentDidMount，更新前读取DOM可结合getSnapshotBeforeUpdate与componentDidUpdate。卸载时还要取消定时器和事件监听，并用错误边界避免渲染异常直接白屏。\n16.3版本\n>=16.4版本\n在线查看：https://projects.wojtekmaj.pl/react-lifecycle-methods-diagram(opens new window)\n避免生命周期中的坑需要做好两件事：不在恰当的时候调用了不该调用的代码；在需要调用时，不要忘了调用。那么主要有这么 7 种情况容易造成生命周期的坑getDerivedStateFromProps容易编写反模式代码，使受控组件与非受控组件区分模糊componentWillMount在 React 16.3 起被标记弃用、React 19 已移除（仅存UNSAFE_componentWillMount），不要在新代码中使用，主要原因是新的异步渲染架构会导致它被多次调用。所以网络请求及事件绑定代码应移至componentDidMount中。componentWillReceiveProps同样被标记弃用、React 19 已移除，被getDerivedStateFromProps所取代，主要原因是性能问题shouldComponentUpdate通过返回true或者false来确定是否需要触发新的渲染。主要用于性能优化componentWillUpdate同样是由于新的异步渲染机制而被标记废弃、React 19 已移除，不要在新代码中使用，原先的逻辑可结合getSnapshotBeforeUpdate与componentDidUpdate改造使用。如果在componentWillUnmount函数中忘记解除事件绑定，取消定时器等清理操作，容易引发 bug如果没有添加错误边界处理，当渲染发生异常时，用户将会看到一个无法操作的白屏，所以一定要添加\ngetDerivedStateFromProps容易编写反模式代码，使受控组件与非受控组件区分模糊componentWillMount在 React 16.3 起被标记弃用、React 19 已移除（仅存UNSAFE_componentWillMount），不要在新代码中使用，主要原因是新的异步渲染架构会导致它被多次调用。所以网络请求及事件绑定代码应移至componentDidMount中。componentWillReceiveProps同样被标记弃用、React 19 已移除，被getDerivedStateFromProps所取代，主要原因是性能问题shouldComponentUpdate通过返回true或者false来确定是否需要触发新的渲染。主要用于性能优化componentWillUpdate同样是由于新的异步渲染机制而被标记废弃、React 19 已移除，不要在新代码中使用，原先的逻辑可结合getSnapshotBeforeUpdate与componentDidUpdate改造使用。如果在componentWillUnmount函数中忘记解除事件绑定，取消定时器等清理操作，容易引发 bug如果没有添加错误边界处理，当渲染发生异常时，用户将会看到一个无法操作的白屏，所以一定要添加\n“React 的请求应该放在哪里，为什么?” 这也是经常会被追问的问题。你可以这样回答。\n对于异步请求，应该放在componentDidMount中去操作。从时间顺序来看，除了componentDidMount还可以有以下选择：\nconstructor：可以放，但从设计上而言不推荐。constructor 主要用于初始化 state 与函数绑定，并不承载业务逻辑。而且随着类属性的流行，constructor 已经很少使用了componentWillMount：已被标记废弃、React 19 已移除，在新的异步渲染架构下会触发多次渲染，容易引发 Bug，不利于未来 React 升级后的代码维护。所以React 的请求放在componentDidMount 里是最好的选择。\n透过现象看本质：React 16 缘何两次求变？\nFiber 架构简析\nFiber 是 React 16 对 React 核心算法的一次重写。你只需要 get 到这一个点：Fiber 会使原本同步的渲染过程变成异步的。\n在 React 16 之前，每当我们触发一次组件的更新，React 都会构建一棵新的虚拟 DOM 树，通过与上一次的虚拟 DOM 树进行 diff，实现对 DOM 的定向更新。这个过程，是一个递归的过程。下面这张图形象地展示了这个过程的特征：\n如图所示，同步渲染的递归调用栈是非常深的，只有最底层的调用返回了，整个渲染过程才会开始逐层返回。这个漫长且不可打断的更新过程，将会带来用户体验层面的巨大风险：同步渲染一旦开始，便会牢牢抓住主线程不放，直到递归彻底完成。在这个过程中，浏览器没有办法处理任何渲染之外的事情，会进入一种无法处理用户交互的状态。因此若渲染时间稍微长一点，页面就会面临卡顿甚至卡死的风险。\n而 React 16 引入的 Fiber 架构，恰好能够解决掉这个风险：Fiber 会将一个大的更新任务拆解为许多个小任务。每当执行完一个小任务时，渲染线程都会把主线程交回去，看看有没有优先级更高的工作要处理，确保不会出现其他任务被“饿死”的情况，进而避免同步渲染带来的卡顿。在这个过程中，渲染线程不再“一去不回头”，而是可以被打断的，这就是所谓的“异步渲染”，它的执行过程如下图所示：\n换个角度看生命周期工作流\nFiber 架构的重要特征就是可以被打断的异步渲染模式。但这个“打断”是有原则的，根据“能否被打断”这一标准，React 16 的生命周期被划分为了 render 和 commit 两个阶段，而 commit 阶段又被细分为了pre-commit 和 commit。每个阶段所涵盖的生命周期如下图所示：\n我们先来看下三个阶段各自有哪些特征\nrender 阶段：纯净且没有副作用，可能会被 React 暂停、终止或重新启动。pre-commit 阶段：可以读取 DOM。commit 阶段：可以使用 DOM，运行副作用，安排更新。\n总的来说，render 阶段在执行过程中允许被打断，而 commit 阶段则总是同步执行的。\n为什么这样设计呢？简单来说，由于 render 阶段的操作对用户来说其实是“不可见”的，所以就算打断再重启，对用户来说也是零感知。而commit 阶段的操作则涉及真实 DOM 的渲染，所以这个过程必须用同步渲染来求稳。\n为什么 React 16 要更改组件的生命周期详解\nReact16为什么要更改生命周期(opens new window)React16为什么要更改生命周期(opens new window)", "f": [{"q": "一个类组件在componentWillMount里发起订单请求，开发环境偶尔出现两笔相同请求；为什么把去重逻辑塞进去仍不是可靠修复？", "a": "componentWillMount属于可能因异步渲染而重复执行的旧生命周期，且在React 19已移除，仅遗留UNSAFE_形式用于迁移。请求应移到提交后的componentDidMount，并按业务需要处理取消或幂等；仅在旧钩子内部去重会继续绑定不稳定的执行时机。"}, {"q": "资料编辑页收到新userId后，用getDerivedStateFromProps把全部表单状态覆盖一遍，结果用户刚输入的内容频繁丢失；你会怎样重构？", "a": "这里混淆了受控数据和组件内部草稿，任何父组件更新都可能触发错误覆盖。应先明确表单由父级完全控制，还是仅在身份变化时重新初始化，再缩小需要派生的状态；getDerivedStateFromProps必须保持纯净，但纯净并不能消除状态双源带来的反模式。"}, {"q": "聊天页卸载后仍持续收到消息并触发更新，线上表现为内存上涨和重复提示；你会从哪个生命周期切入排查？", "a": "应检查componentWillUnmount是否对事件订阅、定时器及其他长期资源执行了对称清理，并确认绑定与解绑使用的是同一引用。仅阻止卸载后的状态更新不能终止资源本身；若组件反复挂载，遗漏清理还会累积监听器，使故障越来越明显。"}, {"q": "发布后某个商品卡片渲染异常导致整个内容区白屏，但网络和接口都正常；生命周期层面应补什么保护，它又保护不了什么？", "a": "应在合适的组件边界增加错误边界，把渲染异常限制在局部并提供降级界面，避免整棵相关视图不可操作。边界位置要兼顾隔离范围与恢复体验；它不是所有错误的通用捕获器，网络失败和事件回调等场景仍需各自处理。"}, {"q": "滚动列表更新前必须记录容器高度，DOM更新后再恢复滚动位置；有人准备把读取和写入都放进componentDidUpdate，你会怎么拆？", "a": "更新前需要读取旧DOM状态时，应使用getSnapshotBeforeUpdate获取快照，再把结果交给componentDidUpdate完成更新后的DOM操作。这样对应pre-commit可读DOM、commit执行副作用的阶段划分；若读写时机混在提交后，旧布局信息可能已经丢失。"}]}, {"s": "React", "t": "React Fiber架构", "m": "Fiber是对React核心协调算法的重构，本质上把不可中断的递归遍历改成可暂停、可恢复的任务单元遍历。每个Fiber节点通过child、sibling和return连接，使协调工作能够拆分，并按优先级让高优任务先执行。可中断的reconciliation阶段负责比较并标记变化，commit阶段则同步更新真实DOM。它不一定缩短总计算时间，主要价值是避免长任务持续占用主线程，让交互保持响应。\n最主要的思想就是将任务拆分。\nDOM需要渲染时暂停，空闲时恢复。window.requestIdleCallbackReact内部实现的机制\nReact 追求的是 “快速响应”，那么，“快速响应“的制约因素都有什么呢\nCPU的瓶颈：当项目变得庞大、组件数量繁多、遇到大计算量的操作或者设备性能不足使得页面掉帧，导致卡顿。IO的瓶颈：发送网络请求后，由于需要等待数据返回才能进一步操作导致不能快速响应。\nfiber架构主要就是用来解决CPU和网络的问题，这两个问题一直也是最影响前端开发体验的地方，一个会造成卡顿，一个会造成白屏。为此 react 为前端引入了两个新概念：Time Slicing时间分片和Suspense。\n1. React 都做过哪些优化\nReact渲染页面的两个阶段调度阶段（reconciliation）：在这个阶段 React 会更新数据生成新的Virtual DOM，然后通过Diff算法，快速找出需要更新的元素，放到更新队列中去，得到新的更新队列。渲染阶段（commit）：这个阶段 React 会遍历更新队列，将其所有的变更一次性更新到DOM上React 15 架构React15架构可以分为两层Reconciler（协调器）—— 负责找出变化的组件；Renderer（渲染器）—— 负责将变化的组件渲染到页面上；\n调度阶段（reconciliation）：在这个阶段 React 会更新数据生成新的Virtual DOM，然后通过Diff算法，快速找出需要更新的元素，放到更新队列中去，得到新的更新队列。渲染阶段（commit）：这个阶段 React 会遍历更新队列，将其所有的变更一次性更新到DOM上\nReact15架构可以分为两层Reconciler（协调器）—— 负责找出变化的组件；Renderer（渲染器）—— 负责将变化的组件渲染到页面上；\nReconciler（协调器）—— 负责找出变化的组件；Renderer（渲染器）—— 负责将变化的组件渲染到页面上；\n在React15及以前，Reconciler采用递归的方式创建虚拟DOM，递归过程是不能中断的。如果组件树的层级很深，递归会占用线程很多时间，递归更新时间超过了16ms，用户交互就会卡顿。为了解决这个问题，React16将递归的无法中断的更新重构为异步的可中断更新，由于曾经用于递归的虚拟DOM数据结构已经无法满足需要。于是，全新的Fiber架构应运而生。\nReact 16 架构为了解决同步更新长时间占用线程导致页面卡顿的问题，也为了探索运行时优化的更多可能，React开始重构并一直持续至今。重构的目标是实现Concurrent Mode（并发模式）。从v15到v16，React团队花了两年时间将源码架构中的Stack Reconciler重构为Fiber ReconcilerReact16架构可以分为三层：Scheduler（调度器）—— 调度任务的优先级，高优任务优先进入Reconciler；Reconciler（协调器）—— 负责找出变化的组件：更新工作从递归变成了可以中断的循环过程。Reconciler内部采用了Fiber的架构；Renderer（渲染器）—— 负责将变化的组件渲染到页面上。React 17 优化使用Lane来管理任务的优先级。Lane用二进制位表示任务的优先级，方便优先级的计算（位运算），不同优先级占用不同位置的“赛道”，而且存在批的概念，优先级越低，“赛道”越多。高优先级打断低优先级，新建的任务需要赋予什么优先级等问题都是Lane所要解决的问题。Concurrent Mode的目的是实现一套可中断/恢复的更新机制。其由两部分组成：一套协程架构：Fiber Reconciler基于协程架构的启发式更新算法：控制协程架构工作方式的算法\nReact 16 架构\n为了解决同步更新长时间占用线程导致页面卡顿的问题，也为了探索运行时优化的更多可能，React开始重构并一直持续至今。重构的目标是实现Concurrent Mode（并发模式）。从v15到v16，React团队花了两年时间将源码架构中的Stack Reconciler重构为Fiber ReconcilerReact16架构可以分为三层：Scheduler（调度器）—— 调度任务的优先级，高优任务优先进入Reconciler；Reconciler（协调器）—— 负责找出变化的组件：更新工作从递归变成了可以中断的循环过程。Reconciler内部采用了Fiber的架构；Renderer（渲染器）—— 负责将变化的组件渲染到页面上。\nScheduler（调度器）—— 调度任务的优先级，高优任务优先进入Reconciler；Reconciler（协调器）—— 负责找出变化的组件：更新工作从递归变成了可以中断的循环过程。Reconciler内部采用了Fiber的架构；Renderer（渲染器）—— 负责将变化的组件渲染到页面上。\nReact 17 优化\n使用Lane来管理任务的优先级。Lane用二进制位表示任务的优先级，方便优先级的计算（位运算），不同优先级占用不同位置的“赛道”，而且存在批的概念，优先级越低，“赛道”越多。高优先级打断低优先级，新建的任务需要赋予什么优先级等问题都是Lane所要解决的问题。Concurrent Mode的目的是实现一套可中断/恢复的更新机制。其由两部分组成：一套协程架构：Fiber Reconciler基于协程架构的启发式更新算法：控制协程架构工作方式的算法\n一套协程架构：Fiber Reconciler基于协程架构的启发式更新算法：控制协程架构工作方式的算法\n2. 浏览器一帧都会干些什么以及requestIdleCallback的启示\n我们都知道，页面的内容都是一帧一帧绘制出来的，浏览器刷新率代表浏览器一秒绘制多少帧。原则上说 1s 内绘制的帧数也多，画面表现就也细腻。目前浏览器大多是 60Hz（60帧/s），每一帧耗时也就是在 16.6ms 左右。那么在这一帧的（16.6ms） 过程中浏览器又干了些什么呢\n通过上面这张图可以清楚的知道，浏览器一帧会经过下面这几个过程：\n接受输入事件执行事件回调开始一帧执行 RAF (RequestAnimationFrame)页面布局，样式计算绘制渲染执行 RIC (RequestIdelCallback)\n第七步的 RIC 事件不是每一帧结束都会执行，只有在一帧的 16.6ms 中做完了前面 6 件事儿且还有剩余时间，才会执行。如果一帧执行结束后还有时间执行 RIC 事件，那么下一帧需要在事件执行结束才能继续渲染，所以 RIC 执行不要超过 30ms，如果长时间不将控制权交还给浏览器，会影响下一帧的渲染，导致页面出现卡顿和事件响应不及时。\nrequestIdleCallback 的启示：我们以浏览器是否有剩余时间作微任务中断的标准，那么我们需要一种机制，当浏览器有剩余时间时通知我们。\nrequestIdleCallback((deadline)=>{// deadline 有两个参数// timeRemaining(): 当前帧还剩下多少时间// didTimeout: 是否超时// 另外 requestIdleCallback 后如果跟上第二个参数 {timeout: ...} 则会强制浏览器在当前帧执行完后执行。if(deadline.timeRemaining()>0){// TODO}else{requestIdleCallback(otherTasks);}});\n// 用法示例vartasksNum=10000requestIdleCallback(unImportWork)functionunImportWork(deadline){while(deadline.timeRemaining()&&tasksNum>0){console.log(`执行了${10000-tasksNum+1}个任务`)tasksNum--}if(tasksNum>0){// 在未来的帧中继续执行requestIdleCallback(unImportWork)}}\n其实部分浏览器已经实现了这个API，这就是requestIdleCallback。但是由于以下因素，Facebook 抛弃了requestIdleCallback的原生 API：\n浏览器兼容性；触发频率不稳定，受很多因素影响。比如当我们的浏览器切换tab后，之前tab注册的requestIdleCallback触发的频率会变得很低。\n基于以上原因，在React中实现了功能更完备的requestIdleCallbackpolyfill，这就是Scheduler。除了在空闲时触发回调的功能外，Scheduler还提供了多种调度优先级供任务设置\n3. React Fiber是什么\nReact Fiber是对核心算法的一次重新实现。React Fiber把更新过程碎片化，把一个耗时长的任务分成很多小片，每一个小片的运行时间很短，虽然总时间依然很长，但是在每个小片执行完之后，都给其他任务一个执行的机会，这样唯一的线程就不会被独占，其他任务依然有运行的机会\n在React Fiber中，一次更新过程会分成多个分片完成，所以完全有可能一个更新任务还没有完成，就被另一个更高优先级的更新过程打断，这时候，优先级高的更新任务会优先处理完，而低优先级更新任务所做的工作则会完全作废，然后等待机会重头再来因为一个更新过程可能被打断，所以React Fiber一个更新过程被分为两个阶段(Phase)：第一个阶段Reconciliation Phase和第二阶段Commit Phase在第一阶段Reconciliation Phase，React Fiber会找出需要更新哪些DOM，这个阶段是可以被打断的；但是到了第二阶段Commit Phase，那就一鼓作气把DOM更新完，绝不会被打断这两个阶段大部分工作都是React Fiber做，和我们相关的也就是生命周期函数\nReact Fiber改变了之前react的组件渲染机制，新的架构使原来同步渲染的组件现在可以异步化，可中途中断渲染，执行更高优先级的任务。释放浏览器主线程\n关键特性\n增量渲染（把渲染任务拆分成块，匀到多帧）更新时能够暂停，终止，复用渲染任务给不同类型的更新赋予优先级并发方面新的基础能力\n增量渲染用来解决掉帧的问题，渲染任务拆分之后，每次只做一小段，做完一段就把时间控制权交还给主线程，而不像之前长时间占用\n4. 组件的渲染顺序\n假如有A,B,C,D组件，层级结构为：\n我们知道组件的生命周期为：\n下面列的是React 16 之前的生命周期（历史对照）。componentWillMount、componentWillReceiveProps、componentWillUpdate在 React 16.3 起被标记废弃，React 19 已移除，仅保留UNSAFE_前缀版本供存量迁移；对应替代分别是constructor/componentDidMount、getDerivedStateFromProps、getSnapshotBeforeUpdate。\n挂载阶段：\nconstructor()componentWillMount()（React 19 已移除）render()componentDidMount()\n更新阶段为：\ncomponentWillReceiveProps()（React 19 已移除）shouldComponentUpdate()componentWillUpdate()（React 19 已移除）render()componentDidUpdate\n那么在挂载阶段，A,B,C,D的生命周期渲染顺序是如何的呢？\n那么在挂载阶段，A,B,C,D的生命周期渲染顺序是如何的呢？\n以render()函数为分界线。从顶层组件开始，一直往下，直至最底层子组件。然后再往上\n组件update阶段同理\n前面是react16以前的组建渲染方式。这就存在一个问题\n如果这是一个很大，层级很深的组件，react渲染它需要几十甚至几百毫秒，在这期间，react会一直占用浏览器主线程，任何其他的操作（包括用户的点击，鼠标移动等操作）都无法执行\nFiber架构就是为了解决这个问题\n看一下fiber架构 组建的渲染顺序\n加入fiber的react将组件更新分为两个时期\n这两个时期以render为分界\nrender前的生命周期为phase1,render后的生命周期为phase2\nphase1的生命周期是可以被打断的，每隔一段时间它会跳出当前渲染进程，去确定是否有其他更重要的任务。此过程，React在workingProgressTree（并不是真实的virtualDomTree）上复用current上的Fiber数据结构来一步地（通过requestIdleCallback）来构建新的 tree，标记处需要更新的节点，放入队列中phase2的生命周期是不可被打断的，React将其所有的变更一次性更新到DOM上\n这里最重要的是phase1这是时期所做的事。因此我们需要具体了解phase1的机制\n如果不被打断，那么phase1执行完会直接进入render函数，构建真实的virtualDomTree如果组件再phase1过程中被打断，即当前组件只渲染到一半（也许是在willMount,也许是willUpdate~反正是在render之前的生命周期），那么react会怎么干呢？react会放弃当前组件所有干到一半的事情，去做更高优先级更重要的任务（当然，也可能是用户鼠标移动，或者其他react监听之外的任务），当所有高优先级任务执行完之后，react通过callback回到之前渲染到一半的组件，从头开始渲染。（看起来放弃已经渲染完的生命周期，会有点不合理，反而会增加渲染时长，但是react确实是这么干的）\n所有phase1的生命周期函数都可能被执行多次，因为可能会被打断重来\n这样的话，就和react16版本之前有很大区别了，因为可能会被执行多次，那么我们最好就得保证phase1的生命周期每一次执行的结果都是一样的，否则就会有问题，因此，最好都是纯函数\n如果高优先级的任务一直存在，那么低优先级的任务则永远无法进行，组件永远无法继续渲染。这个问题facebook目前好像还没解决所以，facebook在react16增加fiber结构，其实并不是为了减少组件的渲染时间，事实上也并不会减少，最重要的是现在可以使得一些更高优先级的任务，如用户的操作能够优先执行，提高用户的体验，至少用户不会感觉到卡顿\n5 React Fiber架构总结\nReact Fiber如何性能优化\n更新的两个阶段调度算法阶段-执行diff算法，纯js计算Commit阶段-将diff结果渲染dom可能会有性能问题JS是单线程的，且和DOM渲染公用一个线程当组件足够复杂，组件更新时计算和渲染压力都大同时再有DOM操作需求（动画、鼠标拖拽等），将卡顿解决方案fiber将调度算法阶段阶段任务拆分（Commit无法拆分）DOM需要渲染时暂停，空闲时恢复分散执行:任务分割后，就可以把小任务单元分散到浏览器的空闲期间去排队执行，而实现的关键是两个新API:requestIdleCallback与requestAnimationFrame低优先级的任务交给requestIdleCallback处理，这是个浏览器提供的事件循环空闲期的回调函数，需要pollyfill，而且拥有deadline参数，限制执行事件，以继续切分任务；高优先级的任务交给requestAnimationFrame处理；\n调度算法阶段-执行diff算法，纯js计算Commit阶段-将diff结果渲染dom\nJS是单线程的，且和DOM渲染公用一个线程当组件足够复杂，组件更新时计算和渲染压力都大同时再有DOM操作需求（动画、鼠标拖拽等），将卡顿\n将调度算法阶段阶段任务拆分（Commit无法拆分）DOM需要渲染时暂停，空闲时恢复分散执行:任务分割后，就可以把小任务单元分散到浏览器的空闲期间去排队执行，而实现的关键是两个新API:requestIdleCallback与requestAnimationFrame低优先级的任务交给requestIdleCallback处理，这是个浏览器提供的事件循环空闲期的回调函数，需要pollyfill，而且拥有deadline参数，限制执行事件，以继续切分任务；高优先级的任务交给requestAnimationFrame处理；\n低优先级的任务交给requestIdleCallback处理，这是个浏览器提供的事件循环空闲期的回调函数，需要pollyfill，而且拥有deadline参数，限制执行事件，以继续切分任务；高优先级的任务交给requestAnimationFrame处理；\nReact 的核心流程可以分为两个部分:\nreconciliation(调度算法，也可称为render)更新state与props；调用生命周期钩子；生成virtual dom这里应该称为Fiber Tree更为符合；通过新旧 vdom 进行 diff 算法，获取 vdom change确定是否需要重新渲染commit如需要，则操作dom节点更新\n更新state与props；调用生命周期钩子；生成virtual dom这里应该称为Fiber Tree更为符合；通过新旧 vdom 进行 diff 算法，获取 vdom change确定是否需要重新渲染\n这里应该称为Fiber Tree更为符合；\n如需要，则操作dom节点更新\n要了解 Fiber，我们首先来看为什么需要它\n问题: 随着应用变得越来越庞大，整个更新渲染的过程开始变得吃力，大量的组件渲染会导致主进程长时间被占用，导致一些动画或高频操作出现卡顿和掉帧的情况。而关键点，便是 同步阻塞。在之前的调度算法中，React 需要实例化每个类组件，生成一颗组件树，使用 同步递归 的方式进行遍历渲染，而这个过程最大的问题就是无法 暂停和恢复。解决方案: 解决同步阻塞的方法，通常有两种: 异步 与 任务分割。而 React Fiber 便是为了实现任务分割而诞生的简述在React V16将调度算法进行了重构， 将之前的stack reconciler重构成新版的 fiberreconciler，变成了具有链表和指针的 单链表树遍历算法。通过指针映射，每个单元都记录着遍历当下的上一步与下一步，从而使遍历变得可以被暂停和重启这里我理解为是一种 任务分割调度算法，主要是 将原先同步更新渲染的任务分割成一个个独立的 小任务单位，根据不同的优先级，将小任务分散到浏览器的空闲时间执行，充分利用主进程的事件循环机制核心Fiber这里可以具象为一个 数据结构\n在React V16将调度算法进行了重构， 将之前的stack reconciler重构成新版的 fiberreconciler，变成了具有链表和指针的 单链表树遍历算法。通过指针映射，每个单元都记录着遍历当下的上一步与下一步，从而使遍历变得可以被暂停和重启这里我理解为是一种 任务分割调度算法，主要是 将原先同步更新渲染的任务分割成一个个独立的 小任务单位，根据不同的优先级，将小任务分散到浏览器的空闲时间执行，充分利用主进程的事件循环机制\nFiber这里可以具象为一个 数据结构\nclassFiber{constructor(instance){this.instance=instance// 指向第一个 child 节点this.child=child// 指向父节点this.return=parent// 指向第一个兄弟节点this.sibling=previous}}\n链表树遍历算法: 通过 节点保存与映射，便能够随时地进行 停止和重启，这样便能达到实现任务分割的基本前提首先通过不断遍历子节点，到树末尾；开始通过sibling遍历兄弟节点；return 返回父节点，继续执行2；直到 root 节点后，跳出遍历；任务分割，React 中的渲染更新可以分成两个阶段reconciliation 阶段: vdom 的数据对比，是个适合拆分的阶段，比如对比一部分树后，先暂停执行个动画调用，待完成后再回来继续比对Commit 阶段: 将 change list 更新到 dom 上，并不适合拆分，才能保持数据与 UI 的同步。否则可能由于阻塞 UI 更新，而导致数据更新和 UI 不一致的情况分散执行:任务分割后，就可以把小任务单元分散到浏览器的空闲期间去排队执行，而实现的关键是两个新API:requestIdleCallback与requestAnimationFrame低优先级的任务交给requestIdleCallback处理，这是个浏览器提供的事件循环空闲期的回调函数，需要pollyfill，而且拥有deadline参数，限制执行事件，以继续切分任务；高优先级的任务交给requestAnimationFrame处理；\n首先通过不断遍历子节点，到树末尾；开始通过sibling遍历兄弟节点；return 返回父节点，继续执行2；直到 root 节点后，跳出遍历；\nreconciliation 阶段: vdom 的数据对比，是个适合拆分的阶段，比如对比一部分树后，先暂停执行个动画调用，待完成后再回来继续比对Commit 阶段: 将 change list 更新到 dom 上，并不适合拆分，才能保持数据与 UI 的同步。否则可能由于阻塞 UI 更新，而导致数据更新和 UI 不一致的情况\n低优先级的任务交给requestIdleCallback处理，这是个浏览器提供的事件循环空闲期的回调函数，需要pollyfill，而且拥有deadline参数，限制执行事件，以继续切分任务；高优先级的任务交给requestAnimationFrame处理；\n// 类似于这样的方式requestIdleCallback((deadline)=>{// 当有空闲时间时，我们执行一个组件渲染；// 把任务塞到一个个碎片时间中去；while((deadline.timeRemaining()>0||deadline.didTimeout)&&nextComponent){nextComponent=performWork(nextComponent);}});\n优先级策略:文本框输入 > 本次调度结束需完成的任务 > 动画过渡 > 交互反馈 > 数据更新 > 不会显示但以防将来会显示的任务\nFiber 其实可以算是一种编程思想，在其它语言中也有许多应用(Ruby Fiber)。核心思想是 任务拆分和协同，主动把执行权交给主线程，使主线程有时间空挡处理其他高优先级任务。当遇到进程阻塞的问题时，任务分割、异步调用 和 缓存策略 是三个显著的解决思路。", "f": [{"q": "搜索页一次更新要遍历上千个组件，接入Fiber后总计算时间没有明显下降，团队据此认为架构改造失败；这个判断哪里有问题？", "a": "Fiber的主要目标不是减少总工作量，而是把协调阶段拆成可暂停、恢复或放弃的小任务，避免长时间独占主线程。总计算甚至可能没有下降，但输入等高优先级工作能更早响应；如果瓶颈是单个不可分割的同步任务，任务拆分也难以发挥作用。"}, {"q": "输入框联动一个大型结果列表，用户连续键入时旧列表还没算完；Scheduler、Reconciler和Renderer分别应承担什么职责？", "a": "Scheduler根据优先级安排工作，使更紧急的输入更新先进入处理；采用Fiber的Reconciler可中断地找出视图变化，Renderer再把确定的变更提交给宿主环境。若所有更新都被赋予相同优先级，架构具备中断能力也不等于产品自然获得理想调度。"}, {"q": "低优先级报表渲染被新的交互更新打断，日志显示前面一部分协调工作后来没有提交；这是数据丢失还是预期行为？", "a": "这是可中断协调阶段允许出现的预期行为：更高优先级任务到来后，未完成的低优先级工作可能被放弃并在之后重新计算。用户尚未看到这些中间结果，因此不会产生半棵界面；这也要求协调阶段保持纯净，不能把不可重复的副作用放进去。"}, {"q": "线上拖拽仍会掉帧，性能记录显示一次commit中有大量真实DOM更新；为什么Fiber没把这段工作切到多帧完成？", "a": "可拆分的是生成和比较更新结果的reconciliation阶段，commit要同步应用变更，以避免数据与可见UI长时间处于不一致状态。应继续排查本次提交的节点规模与DOM成本，而不是期待提交阶段被任意暂停；过大的commit仍可能造成明显卡顿。"}, {"q": "有人建议直接用浏览器的requestIdleCallback实现全部React调度，理由是它能返回剩余时间；你会指出哪些工程约束？", "a": "requestIdleCallback提供了空闲时间提示，但兼容性和触发频率并不稳定，切换标签页等情况还可能显著降低调用频率。React因此实现自己的Scheduler，并额外支持多种任务优先级；单纯等待空闲时间也可能让持续被高优任务挤压的工作长期得不到执行。"}, {"q": "代码评审发现类组件在协调阶段的旧will生命周期里注册事件，而团队正启用可中断更新；这和Fiber的哪条约束直接冲突？", "a": "协调阶段可能暂停、终止或重新开始，因此其中的生命周期存在重复执行的可能，注册事件会产生不可安全重放的副作用。副作用应移到同步提交阶段，并在卸载时清理；这正是旧componentWillMount、componentWillUpdate等生命周期被弃用并最终移除的重要背景。"}]}, {"s": "React", "t": "createElement过程", "m": "React.createElement会根据类型、属性和子节点创建一个描述界面的React元素。JSX编译后本质上就是这类调用，第一个参数是标签或组件类型，第二个参数是props，后续参数表示子节点。返回结果只是普通的界面描述，并不会在这一步直接操作真实DOM，实际更新要经过协调和提交。React18 起挂载通常使用createRoot(container).render(element)，但不影响createElement的参数和作用。\nReact.createElement()： 根据指定的第一个参数创建一个React元素\nReact.createElement(type,[props],[...children])\n第一个参数是必填，传入的是似HTML标签名称，eg: ul, li第二个参数是选填，表示的是属性，eg: className第三个参数是选填, 子节点，eg: 要显示的文本内容\n下面示例里的ReactDOM.render是历史写法：React 18 起改用ReactDOM.createRoot(container).render(element)，React 19 已移除ReactDOM.render。示例本身讲的是createElement的参数形式，挂载 API 换成createRoot后结论不变。\n//写法一：varchild1=React.createElement('li',null,'one');varchild2=React.createElement('li',null,'two');varcontent=React.createElement('ul',{className:'teststyle'},child1,child2);// 第三个参数可以分开也可以写成一个数组ReactDOM.render(content,document.getElementById('example'));//写法二：varchild1=React.createElement('li',null,'one');varchild2=React.createElement('li',null,'two');varcontent=React.createElement('ul',{className:'teststyle'},[child1,child2]);ReactDOM.render(content,document.getElementById('example'));", "f": [{"q": "商品列表页把React.createElement('li', { className: 'item' }, sku)当成已经插入页面的节点，随后立刻查询.item却找不到，误判在哪里？", "a": "误判是把创建React元素与挂载到页面混为一谈；这次调用只按type、属性和子节点构造待渲染内容。还要把返回结果交给根节点渲染，React18 应使用ReactDOM.createRoot(container).render(element)，否则页面中不会出现对应节点。"}, {"q": "后台菜单有 200 项，开发者把子项数组作为第三个参数传给React.createElement('ul', null, children)；评审认为必须展开成...children，你怎么判断？", "a": "两种传法在该参数形式下都成立：子节点既能作为后续参数逐个传入，也能以数组形式放在第三个参数。工程上应统一团队写法并保证数组内容确实是合法子节点；不要仅因没有展开数组，就把渲染异常归因于createElement。"}, {"q": "老管理台仍用ReactDOM.render(content, container)展示createElement示例，升级到React18 后负责人与维护者对是否重写有分歧，你会怎么处理？", "a": "示例所讲的三个参数形式不需要改变，但挂载代码应迁移为ReactDOM.createRoot(container).render(content)。React18 起旧写法已经是历史用法，React19 更移除了ReactDOM.render；继续保留会把元素创建和版本相关的挂载接口混在一起。"}, {"q": "营销页生成的列表没有样式，代码是React.createElement('ul', { classname: 'teststyle' }, items)，DOM结构和文本都正常，你先查哪里？", "a": "先核对第二个参数中的属性名，React示例使用的是className，而不是classname。第三个参数已经能正常产生文本和列表结构，说明排查重点应落在属性传递及样式选择器；改挂载API不能修复属性拼写错误。"}, {"q": "低代码渲染器需要同时生成两个<li>，有人主张先各自调用createElement，另一人主张直接传配置对象数组；哪种边界必须守住？", "a": "传给父元素的子项应当是已经创建好的React子节点，例如两个React.createElement('li', ...)的结果，再逐个或以数组传入。普通配置对象不能仅凭形状替代子节点；若选择配置驱动，仍需先把配置映射为元素，代价是多一层转换与校验。"}]}, {"s": "React", "t": "调和阶段 setState内部干了什么", "m": "调用setState后，React会合并或排队保存状态更新，并启动调和流程。调和时会根据新状态构建新的React元素树，再与上一次的元素树做diff，找出真正发生变化的部分。这样最终只更新必要的UI，而不是每次都重建整个页面；状态计算和界面更新也不一定在调用处立即完成。\n当调用 setState 时，React会做的第一件事情是将传递给 setState 的对象合并到组件的当前状态这将启动一个称为和解（reconciliation）的过程。和解（reconciliation）的最终目标是以最有效的方式，根据这个新的状态来更新UI。 为此，React将构建一个新的React元素树（您可以将其视为UI的对象表示）一旦有了这个树，为了弄清 UI 如何响应新的状态而改变，React 会将这个新树与上一个元素树相比较（ diff ）\n通过这样做， React 将会知道发生的确切变化，并且通过了解发生什么变化，只需在绝对必要的情况下进行更新即可最小化 UI 的占用空间", "f": [{"q": "订单页调用setState({ status: 'paid' })后，开发者认为React会直接把按钮文本改成“已支付”，这个判断漏掉了什么过程？", "a": "setState触发的不是一次面向具体DOM文本的直接修改，而是基于新状态重新构建React元素树。React还要把新树与上一棵树比较，确定真正发生变化的位置后再更新UI；因此不能把状态对象与某个DOM操作简单画等号。"}, {"q": "看板一次收到 300 条状态变化，产品要求只更新发生变化的卡片；你如何用调和过程解释实现判断？", "a": "React会依据新状态生成新的元素树，再与上一次的元素树进行比较，从而找出需要更新的部分。组件仍应让状态准确表达每张卡片的结果，不能手工假定只有某个节点变化；错误的状态建模会让生成的新树本身就不符合预期。"}, {"q": "同一份组件升级后改用并发能力，负责人仍要求“每次调用setState后立刻完成整棵树比较”，这个约束合理吗？", "a": "不应把setState理解为调用返回前必须完成全部UI更新；可靠结论是它会发起状态更新与后续调和。具体调度时机不由本节的树比较模型保证，业务若依赖立即可见结果，就把实现绑到了未承诺的执行时序上。"}, {"q": "库存页状态已经变成0，页面却仍显示“有货”；值班同学只盯着接口响应，你会沿哪条链路排查？", "a": "先确认传入setState的新状态是否正确，再检查该状态生成的新元素树是否真的改变了展示分支。随后核对树比较后对应UI是否提交；若新旧树表达相同内容，调和没有可更新之处，问题通常在状态到元素的映射而非接口本身。"}, {"q": "团队想绕过React，监听状态后直接改DOM，以避免元素树比较；在频繁更新的仪表盘里你如何取舍？", "a": "直接改DOM会绕开“新状态生成元素树、与旧树比较、只更新必要部分”的一致更新链路。除非该区域明确由外部系统独占管理，否则React后续调和可能覆盖手工结果；省下一次声明式计算的同时，会增加状态与页面失配的风险。"}]}, {"s": "React", "t": "setState", "m": "setState的核心是排队和批量更新，不能简单理解成一个异步函数。对象写法连续读取旧state时，同名字段可能互相覆盖；依赖前一次结果时，应使用函数式写法，让更新按顺序计算。React18 的createRoot和React19 会在定时器、原生事件等场景自动批处理，React17 及以前则可能同步刷新。需要在提交后读取结果，可以使用回调；确需强制同步时可用flushSync。\n先看版本结论（React 17 / 18 / 19 三档）\n本节余下的Transaction（事务）与isBatchingUpdates分析描述的是React 16 / 17 的内部实现，属于历史对照：现在 Fiber 走的是 lane 优先级调度，不再依赖单个全局布尔标志。这些内容仍然值得读——它解释了「为什么当年定时器里会变成同步」——但不要当作 React 18 及以后的现状。\n下面凡是出现「在原生事件和 setTimeout 中 setState 是同步的」这类表述，都请补上前提：React 17 及以前，或 React 18 的ReactDOM.render兼容模式。\n在了解setState之前，我们先来简单了解下 React 一个包装结构: Transaction:\n事务 (Transaction)\n是 React 中的一个调用结构，用于包装一个方法，结构为: initialize - perform(method) - close。通过事务，可以统一管理一个方法的开始与结束；处于事务流中，表示进程正在执行一些操作\nsetState: React 中用于修改状态，更新视图。它具有以下特点:\n异步与同步:setState并不是单纯的异步或同步，这其实与调用时的环境相关:\n在合成事件和生命周期钩子(除 componentDidUpdate) 中，setState是\"异步\"的；原因: 因为在setState的实现中，有一个判断: 当更新策略正在事务流的执行中时，该组件更新会被推入dirtyComponents队列中等待执行；否则，开始执行batchedUpdates队列更新；在生命周期钩子调用中，更新策略都处于更新之前，组件仍处于事务流中，而componentDidUpdate是在更新之后，此时组件已经不在事务流中了，因此则会同步执行；在合成事件中，React 是基于 事务流完成的事件委托机制 实现，也是处于事务流中；问题: 无法在setState后马上从this.state上获取更新后的值。解决: 如果需要马上同步去获取新值，setState其实是可以传入第二个参数的。setState(updater, callback)，在回调中即可获取最新值；在原生事件和 setTimeout 中，setState是同步的，可以马上获取更新后的值（仅限 React 17 及以前 / React 18 兼容模式；React 18createRoot起这些场景同样批量合并）；原因: 原生事件是浏览器本身的实现，与事务流无关，自然是同步；而setTimeout是放置于定时器线程中延后执行，此时事务流已结束，因此也是同步；批量更新: 在 合成事件 和 生命周期钩子 中，setState更新队列时，存储的是 合并状态(Object.assign)。因此前面设置的 key 值会被后面所覆盖，最终只会执行一次更新；函数式: 由于 Fiber 及 合并 的问题，官方推荐可以传入 函数 的形式。setState(fn)，在fn中返回新的state对象即可，例如this.setState((state, props) => newState)；使用函数式，可以用于避免setState的批量更新的逻辑，传入的函数将会被 顺序调用；\n原因: 因为在setState的实现中，有一个判断: 当更新策略正在事务流的执行中时，该组件更新会被推入dirtyComponents队列中等待执行；否则，开始执行batchedUpdates队列更新；在生命周期钩子调用中，更新策略都处于更新之前，组件仍处于事务流中，而componentDidUpdate是在更新之后，此时组件已经不在事务流中了，因此则会同步执行；在合成事件中，React 是基于 事务流完成的事件委托机制 实现，也是处于事务流中；问题: 无法在setState后马上从this.state上获取更新后的值。解决: 如果需要马上同步去获取新值，setState其实是可以传入第二个参数的。setState(updater, callback)，在回调中即可获取最新值；\n在生命周期钩子调用中，更新策略都处于更新之前，组件仍处于事务流中，而componentDidUpdate是在更新之后，此时组件已经不在事务流中了，因此则会同步执行；在合成事件中，React 是基于 事务流完成的事件委托机制 实现，也是处于事务流中；\n原因: 原生事件是浏览器本身的实现，与事务流无关，自然是同步；而setTimeout是放置于定时器线程中延后执行，此时事务流已结束，因此也是同步；\n使用函数式，可以用于避免setState的批量更新的逻辑，传入的函数将会被 顺序调用；\n注意事项:\nsetState 合并，在 合成事件 和 生命周期钩子 中多次连续调用会被优化为一次；当组件已被销毁，如果再次调用setState，React 会报错警告，通常有两种解决办法将数据挂载到外部，通过 props 传入，如放到 Redux 或 父级中；在组件内部维护一个状态量 (isUnmounted)，componentWillUnmount中标记为 true，在setState前进行判断；\n将数据挂载到外部，通过 props 传入，如放到 Redux 或 父级中；在组件内部维护一个状态量 (isUnmounted)，componentWillUnmount中标记为 true，在setState前进行判断；\n总结\nsetState 并非真异步，只是看上去像异步。在源码中，通过isBatchingUpdates来判断\nsetState是先存进state队列还是直接更新，如果值为 true 则执行异步操作，为 false 则直接更新。那么什么情况下isBatchingUpdates会为true呢？在 React 可以控制的地方，就为 true，比如在 React 生命周期事件和合成事件中，都会走合并操作，延迟更新的策略。但在 React 无法控制的地方，比如原生事件，具体就是在addEventListener、setTimeout、setInterval等事件中，就只能同步更新。这一条到 React 18createRoot为止：automatic batching 之后 React 不再依赖「是否在自己的调用栈里」来决定批处理，上述场景也会合并。\n一般认为，做异步设计是为了性能优化、减少渲染次数，React 团队还补充了两点。\n保持内部一致性。如果将 state 改为同步更新，那尽管 state 的更新是同步的，但是 props不是。启用并发更新，完成异步渲染。\nsetState只有在 React 自身的合成事件和钩子函数中是异步的，在原生事件和 setTimeout 中都是同步的——这是 React 17 及以前的结论；React 18createRoot与 React 19 里全部场景都是批处理的异步更新setState的异步并不是说内部由异步代码实现，其实本身执行的过程和代码都是同步的，只是合成事件和钩子函数中没法立马拿到更新后的值，形成了所谓的异步。当然可以通过 setState 的第二个参数中的 callback 拿到更新后的结果setState的批量更新优化也是建立在异步（合成事件、钩子函数）之上的，在原生事件和 setTimeout 中不会批量更新，在异步中如果对同一个值进行多次 setState，setState 的批量更新策略会对其进行覆盖，去最后一次的执行，如果是同时 setState 多个不同的值，在更新时会对其进行合并批量更新\nReact 17 及以前：\n合成事件中是异步钩子函数中的是异步原生事件中是同步setTimeout中是同步\nReact 18createRoot与 React 19：上面四条统一为「都是批处理的异步更新」，只有flushSync能强制同步。\n这是一道经常会出现的 React setState 笔试题：下面的代码输出什么呢？\nclassTestextendsReact.Component{state={count:0};componentDidMount(){this.setState({count:this.state.count+1});console.log(this.state.count);this.setState({count:this.state.count+1});console.log(this.state.count);setTimeout(()=>{this.setState({count:this.state.count+1});console.log(this.state.count);this.setState({count:this.state.count+1});console.log(this.state.count);},0);}render(){returnnull;}};\n我们可以进行如下的分析：\n首先第一次和第二次的console.log，都在 React 的生命周期事件中，所以是异步的处理方式，则输出都为0；而在setTimeout中的console.log处于原生事件中，所以会同步的处理再输出结果，但需要注意，虽然count在前面经过了两次的this.state.count + 1，但是每次获取的this.state.count都是初始化时的值，也就是0；所以此时count是1，那么后续在setTimeout中的输出则是2和3。\n所以完整答案是0,0,2,3。\n版本提醒：这个0,0,2,3只在 React 17 及以前（或 React 18 兼容模式）成立。在 React 18createRoot与 React 19 下，setTimeout里两次setState也会被批处理并互相覆盖，输出变成0,0,1,1，最终count为2。\n同步场景\n异步场景中的案例使我们建立了这样一个认知：setState 是异步的，但下面这个案例又会颠覆你的认知。如果我们将 setState 放在 setTimeout 事件中，那情况就完全不同了。\nclassTestextendsComponent{state={count:0}componentDidMount(){this.setState({count:this.state.count+1});console.log(this.state.count);setTimeout(()=>{this.setState({count:this.state.count+1});console.log(\"setTimeout: \"+this.state.count);},0);}render(){...}}\n那这时输出的应该是什么呢？如果你认为是 0,0，那么又错了。\n在React 17 及以前，正确的结果是0,2：setState并不是真正的异步函数，它通过队列延迟执行，源码里用isBatchingUpdates判断是先入队还是直接更新——值为 true 走延迟合并，false 直接同步更新。\n在React 18createRoot与 React 19下，结果是0,1：定时器里的更新也被批处理，console.log读到的是提交前的1（第一次setState已在componentDidMount的批次里提交），而不是2。\n接下来这个案例的答案是什么呢\nclassTestextendsComponent{state={count:0}componentDidMount(){this.setState({count:this.state.count+1},()=>{console.log(this.state.count)})this.setState({count:this.state.count+1},()=>{console.log(this.state.count)})}render(){...}}\n如果你觉得答案是1,2，那肯定就错了。这种迷惑性极强的考题在面试中非常常见，因为它反直觉。\n如果重新仔细思考，你会发现当前拿到的this.state.count的值并没有变化，都是0，所以输出结果应该是1,1。\n当然，也可以在setState函数中获取修改后的state值进行修改。\nclassTestextendsComponent{state={count:0}componentDidMount(){this.setState(preState=>({count:preState.count+1}),()=>{console.log(this.state.count)})this.setState(preState=>({count:preState.count+1}),()=>{console.log(this.state.count)})}render(){...}}\n这些通通是异步的回调，如果你以为输出结果是 1,2，那就又错了，实际上是 2,2。\n为什么会这样呢？当调用setState函数时，就会把当前的操作放入队列中。React 根据队列内容，合并 state 数据，完成后再逐一执行回调，根据结果更新虚拟 DOM，触发渲染。所以回调时，state 已经合并计算完成了，输出的结果就是2,2了。", "f": [{"q": "类组件计数器在一次点击里连续执行两次this.setState({ count: this.state.count + 1 })，两个回调都打印1；候选人说队列丢了一次更新，你同意吗？", "a": "不同意，两次对象式更新读取的都是当时相同的this.state.count，同一批次里同名字段会被后一次结果覆盖。改成this.setState(state => ({ count: state.count + 1 }))才会顺序消费前一次结果；两个提交后回调看到的是合并完成的最终值。"}, {"q": "React19 的搜索页在Promise.then里连续更新筛选条件和页码，产品担心产生两次渲染；你如何落地并验证？", "a": "React19 会对该异步回调中的更新进行批处理，不应沿用旧版“异步回调逐次同步刷新”的判断。把相互依赖的状态更新写成函数式形式，并用渲染日志或Profiler验证实际提交；批处理只减少提交次数，不会替你修正错误的状态依赖。"}, {"q": "遗留系统一部分用React18 的ReactDOM.render，新页面使用createRoot，同样在setTimeout中两次setState却表现不同，怎么解释？", "a": "React18 的兼容根沿用React17 行为，定时器中的更新会逐次同步刷新；createRoot则启用automaticbatching，将这些更新一起处理。排查时必须先确认根节点创建方式，不能只看React主版本；迁移根节点还可能改变依赖同步读值的旧代码。"}, {"q": "线上弹窗执行setState后马上读取this.state.open，在React18createRoot下仍是旧值，开发者准备套一层setTimeout，你会怎么改？", "a": "不要依赖定时器碰运气，因为automaticbatching已覆盖setTimeout，回调中的读取也未必对应预想的提交边界。类组件可使用setState(updater, callback)在更新完成后读取，或在componentDidUpdate中响应变化；若新值依赖旧值，应同时改用函数式更新。"}, {"q": "展开面板后必须立刻测量新DOM高度，架构师禁止所有同步刷新，动画负责人坚持使用flushSync；你支持哪边？", "a": "该场景确实需要在状态更新后立即读取已提交的DOM，可把最小范围的更新包在flushSync中。它是React18createRoot与React19 打破批处理的出口，但会牺牲批处理收益；应限制在必要测量点，不能作为普通状态更新的默认写法。"}, {"q": "组件卸载后请求回调仍调用setState，监控出现警告；有人提议每个组件都维护isUnmounted，另一人要把数据提升到父层，你怎么选？", "a": "先让异步任务在卸载时停止回写；若数据生命周期确实长于组件，再考虑放到父级或外部状态中通过props传入。isUnmounted判断是局部防线，会在大量组件中形成重复状态；无论选哪种方案，都不能让已销毁实例继续进入更新队列。"}]}, {"s": "React", "t": "setState原理分析", "m": "setState本质上会把状态变更放进更新队列，再由React在合适的时机统一计算和渲染。React16、17 的旧实现通过isBatchingUpdates和dirtyComponents控制批处理，React18 起则改用Fiber的lane调度和自动批处理。直接修改this.state不会进入这套更新流程，因此不能可靠触发重新渲染。若新值依赖旧值，应使用函数式更新；提交后的值可在回调或componentDidUpdate中读取。\n本节是 React 16 / 17 内部实现的历史对照：下面的pre/post钩子、isBatchingUpdates、dirtyComponents属于旧的 Transaction 批处理实现。React 18 起改为 Fiber 的 lane 优先级调度 + automatic batching，「是否在 React 调用栈里」不再决定批处理与否。版本结论见上一节开头的三档对照表。\n1. setState异步更新\n我们都知道，React通过this.state来访问state，通过this.setState()方法来更新state。当this.setState()方法被调用的时候，React会重新调用render方法来重新渲染UI首先如果直接在setState后面获取state的值是获取不到的。在React内部机制能检测到的地方，setState就是异步的；在React检测不到的地方，例如setInterval,setTimeout，setState就是同步更新的（React 17 及以前；React 18createRoot起这些场景同样批量合并，React 19 无法退回旧行为）\n因为setState是可以接受两个参数的，一个state，一个回调函数。因此我们可以在回调函数里面获取值\nsetState方法通过一个队列机制实现state更新，当执行setState的时候，会将需要更新的state合并之后放入状态队列，而不会立即更新this.state如果我们不使用setState而是使用this.state.key来修改，将不会触发组件的re-render。如果将this.state赋值给一个新的对象引用，那么其他不在对象上的state将不会被放入状态队列中，当下次调用setState并对状态队列进行合并时，直接造成了state丢失\n1.1 setState批量更新的过程\n在react生命周期和合成事件执行前后都有相应的钩子，分别是pre钩子和post钩子，pre钩子会调用batchedUpdate方法将isBatchingUpdates变量置为true，开启批量更新，而post钩子会将isBatchingUpdates置为false\nisBatchingUpdates变量置为true，则会走批量更新分支，setState的更新会被存入队列中，待同步代码执行完后，再执行队列中的state更新。isBatchingUpdates为true，则把当前组件（即调用了setState的组件）放入dirtyComponents数组中；否则batchUpdate所有队列中的更新而在原生事件和异步操作中，不会执行pre钩子，或者生命周期的中的异步操作之前执行了pre钩子，但是pos钩子也在异步操作之前执行完了，isBatchingUpdates必定为false，也就不会进行批量更新\nenqueueUpdate包含了React避免重复render的逻辑。mountComponent和updateComponent方法在执行的最开始，会调用到batchedUpdates进行批处理更新，此时会将isBatchingUpdates设置为true，也就是将状态标记为现在正处于更新阶段了。isBatchingUpdates为true，则把当前组件（即调用了setState的组件）放入dirtyComponents数组中；否则batchUpdate所有队列中的更新\n1.2 为什么直接修改this.state无效\n要知道setState本质是通过一个队列机制实现state更新的。 执行setState时，会将需要更新的state合并后放入状态队列，而不会立刻更新state，队列机制可以批量更新state。如果不通过setState而直接修改this.state，那么这个state不会放入状态队列中，下次调用setState时对状态队列进行合并时，会忽略之前直接被修改的state，这样我们就无法合并了，而且实际也没有把你想要的state更新上去\n1.3 什么是批量更新 Batch Update\n在一些mv*框架中，，就是将一段时间内对model的修改批量更新到view的机制。比如那前端比较火的React、vue（nextTick机制,视图的更新以及实现）\n1.4 setState之后发生的事情\nsetState操作并不保证是同步的，也可以认为是异步的React在setState之后，会经对state进行diff，判断是否有改变，然后去diff dom决定是否要更新UI。如果这一系列过程立刻发生在每一个setState之后，就可能会有性能问题在短时间内频繁setState。React会将state的改变压入栈中，在合适的时机，批量更新state和视图，达到提高性能的效果\n1.5 如何知道state已经被更新\n传入回调函数\nsetState({ index: 1 }}, function(){ console.log(this.state.index); })\n在钩子函数中体现\ncomponentDidUpdate(){ console.log(this.state.index); }\n2. setState循环调用风险\n当调用setState时，实际上会执行enqueueSetState方法，并对partialState以及_pending-StateQueue更新队列进行合并操作，最终通过enqueueUpdate执行state更新而performUpdateIfNecessary方法会获取_pendingElement,_pendingStateQueue，_pending-ForceUpdate，并调用receiveComponent和updateComponent方法进行组件更新如果在shouldComponentUpdate或者componentWillUpdate方法中调用setState，此时this._pending-StateQueue != null，就会造成循环调用，使得浏览器内存占满后崩溃\n3 事务\n事务就是将需要执行的方法使用wrapper封装起来，再通过事务提供的perform方法执行，先执行wrapper中的initialize方法，执行完perform之后，在执行所有的close方法，一组initialize及close方法称为一个wrapper。那么事务和setState方法的不同表现有什么关系，首先我们把4次setState简单归类，前两次属于一类，因为它们在同一调用栈中执行，setTimeout中的两次setState属于另一类在setState调用之前，已经处在batchedUpdates执行的事务中了。那么这次batchedUpdates方法是谁调用的呢，原来是ReactMount.js中的_renderNewRootComponent方法。也就是说，整个将React组件渲染到DOM中的过程就是处于一个大的事务中。而在componentDidMount中调用setState时，batchingStrategy的isBatchingUpdates已经被设为了true，所以两次setState的结果没有立即生效再反观setTimeout中的两次setState，因为没有前置的batchedUpdates调用，所以导致了新的state马上生效\n4. 总结\n通过setState去更新this.state，不要直接操作this.state，请把它当成不可变的调用setState更新this.state不是马上生效的，它是异步的，所以不要天真以为执行完setState后this.state就是最新的值了多个顺序执行的setState不是同步地一个一个执行滴，会一个一个加入队列，然后最后一起执行，即批处理", "f": [{"q": "资料编辑页直接执行this.state.profile.name = value，调试器里值已变化但页面没重渲染；开发者认为React的diff失效了，你怎么纠正？", "a": "直接修改this.state没有通过setState进入状态更新队列，因此不能据此期待React重新调用render。应把状态视为不可直接修改的数据，通过setState提交新值；否则后续队列合并还可能忽略这次私改，造成页面与内存观察结果不一致。"}, {"q": "表格一次编辑三个字段，代码连续调用对象式setState，评审担心每次都重绘整页；你会怎样实现？", "a": "这些更新会先进入状态队列，并在合适时机批量合并后更新视图，而不是把每次调用都等同于一次立即DOM修改。若字段互不覆盖，可提交各自的部分状态；若新值依赖旧状态，则使用函数式更新，避免对象式更新在队列中覆盖结果。"}, {"q": "老代码用isBatchingUpdates和dirtyComponents解释React19 的所有更新时序，面试中你会指出什么版本边界？", "a": "这套pre、post、isBatchingUpdates与dirtyComponents描述的是React16、17 的Transaction实现，只适合作为历史对照。React18 起采用Fiber的lane优先级调度与automaticbatching，触发点是否位于React调用栈内已不再决定是否批处理。"}, {"q": "支付页调用setState后日志仍是旧金额，值班同学认定更新失败；你要求他按什么顺序定位？", "a": "先确认日志是否紧跟在setState后，因为状态更新入队并不保证当场改写this.state。再把观察点移到setState回调或componentDidUpdate，确认提交后的值；若那里仍异常，继续检查对象式更新是否被后续同字段更新覆盖。"}, {"q": "类组件需要在价格更新完成后上报一次埋点，有人放在render，有人放进setState第二个回调；你怎么取舍？", "a": "不要在render中上报，因为重新渲染不等于这次价格更新的专属完成通知，容易产生重复副作用。针对单次类组件更新可使用setState的第二个回调，跨多次价格变化的统一响应则放在componentDidUpdate并比较前后值；两者都要防止回调再次无条件更新状态。"}, {"q": "遗留组件在shouldComponentUpdate中调用setState修正数据，线上出现持续更新和内存上涨；根因与改法是什么？", "a": "该生命周期本就在判断一次待处理更新，内部再次调用setState会继续向待处理队列追加更新，可能形成循环调用。应把数据修正移到事件入口、状态计算阶段或受条件约束的更新位置；仅加日志或扩大内存不能消除循环更新链路。"}]}, {"s": "React", "t": "React事务机制", "m": "React事务机制是Stack架构时期用来包裹更新流程、实现批处理的一套机制。它会在目标方法执行前运行initialize，执行完成后再运行close，从而统一开启和结束批量更新。处于事务中的多次setState会先被收集，所以调用后看起来没有立即生效。需要注意，这属于旧版实现；Fiber架构下已经由lane优先级调度等机制接替。", "f": [{"q": "遗留后台在React合成事件里连续调用三次setState，开发者用“每次调用都立刻刷新一次”解释页面行为；结合事务图你会怎么反驳？", "a": "旧实现会在受React管理的调用过程外包一层事务，在开始阶段进入批量更新状态，结束阶段再统一处理队列。因而三次调用不能简单等同于三次即时提交；但该解释属于旧版内部机制，不应直接套到React18、19 的lane调度上。"}, {"q": "维护者准备把旧Transaction图直接写进React19 的故障手册，用它判断setTimeout一定逃离批处理；你批准吗？", "a": "不批准，这组图更适合解释旧实现中事务边界与批处理的关系，不能证明现代版本的定时器更新仍逐次刷新。React18createRoot起已有automaticbatching，React19 也保持批处理；手册必须先标注根节点和版本边界。"}, {"q": "React17 页面在原生事件回调里两次更新状态，迁移createRoot后日志时序改变；值班人员只检查业务代码，漏了什么？", "a": "他漏查了根节点迁移带来的批处理边界变化：旧事务机制无法覆盖的原生回调，在React18createRoot下也会进入automaticbatching。应对照迁移前后的根节点、触发来源和提交日志；不能继续用旧图中的事务包裹范围推断新版本时序。"}, {"q": "数据大屏要求更新状态后立即读取新DOM，团队想修改事务开关强制提交；你会给出什么工程方案？", "a": "不要操作内部事务状态，这些标志属于旧实现细节，也不是稳定公共接口。现代React应在确有同步DOM读取需求时，以最小范围使用flushSync包住更新；它会打破批处理并增加同步工作，因此只适合作为受控逃生口。"}, {"q": "培训中有人把Transaction、automaticbatching和reconciliation说成同一件事，导致新人认为合并更新就是跳过树比较；你如何串起来？", "a": "Transaction描述旧版如何包裹一段执行并在边界处理更新，automaticbatching描述现代版本如何合并一批状态更新；它们都不等于reconciliation。批次形成后，React仍需根据新状态构建元素树并与旧树比较，再决定必要的UI更新。"}]}, {"s": "React", "t": "React组件和渲染更新过程", "m": "React组件的渲染分为计算差异的render阶段和提交变更的commit阶段，前者可中断，后者不可中断。JSX本质上会调用createElement生成虚拟节点，组件再根据props和state执行render()，最终把结果更新到页面。调用setState后，相关组件会被标记为待更新，重新生成新节点并与旧节点比较，因此副作用应留到提交阶段处理。\n渲染和更新过程\njsx如何渲染为页面setState之后如何更新页面面试考察全流程\nJSX本质和vdom\nJSX即createElement函数执行生成vnodepatch(elem,vnode)和patch(vnode,newNode)\n组件渲染过程\nprops staterender()生成vnodepatch(elem, vnode)\n组件更新过程\nsetState-->dirtyComponents(可能有子组件)render生成newVnodepatch(vnode, newVnode)", "f": [{"q": "商品详情页只修改了购物车数量，父组件和多个商品推荐子组件的render都执行了，但DOM几乎没变；你会把这次更新描述成整页重绘吗？", "a": "不会，组件重新执行render只是在依据新的props、state生成newVnode，不等于重新创建整页真实DOM。随后patch(vnode, newVnode)会找出变化并更新对应节点；若只看到函数执行次数，不能直接判定真实DOM操作过多。"}, {"q": "后台表格点击“刷新状态”后调用了setState，面试官要求你从这一行代码一直讲到页面文字变化，中间链路怎么说？", "a": "setState会使相关组件进入待更新集合，可理解为被标记到dirtyComponents，其中也可能涉及子组件。React随后重新执行组件渲染得到newVnode，再与旧vnode做patch，最终把差异落实到真实DOM；它不是直接修改页面节点。"}, {"q": "一个组件首次挂载和后续调用setState时都执行了render，前端同学认为两条链路完全相同，你会指出哪处关键差异？", "a": "首次挂载是由props、state生成vnode，再执行类似patch(elem, vnode)的过程，把描述落实到容器。更新阶段已有旧vnode，所以重点变成生成newVnode并执行patch(vnode, newVnode)；两者都渲染，但比较基准不同。"}, {"q": "订单页调用setState后数据已经变了，界面却仍显示旧金额；你会按哪几层定位，而不是直接怀疑浏览器缓存？", "a": "先确认更新是否让目标组件进入待更新流程，再检查render生成的newVnode是否真的包含新金额。若虚拟节点正确，再沿patch(vnode, newVnode)检查差异是否落到对应真实节点；若渲染输入仍旧，问题通常在状态或属性传递链路，而非DOM补丁阶段。"}, {"q": "团队想绕过虚拟节点，在库存变化时直接找到节点改textContent，理由是只有一个数字变化；你如何评估这种取舍？", "a": "直接改DOM可能立即显示结果，但会绕开由props、state生成vnode再执行patch的一致更新链路。后续组件再次渲染时，声明结果可能覆盖手工修改，也会增加状态与页面不一致的排查成本；除非明确隔离该节点，否则不宜作为常规方案。"}]}, {"s": "React", "t": "如何解释 React 的渲染流程", "m": "React的渲染流程可以概括为触发更新、调度任务、计算差异和提交结果。React 16以前的Stack Reconciler以递归和事务为核心，执行期间容易持续占用主线程；之后的Fiber Reconciler把节点组织成可遍历的Fiber结构，并支持按优先级让出执行权。render阶段构造workInProgress树，可中断且不应产生副作用；commit阶段同步更新DOM、执行生命周期和处理ref，不可中断。普通管理后台里两者差距未必明显，但动画、画布或手势场景更容易体现Fiber调度的价值。\nReact 的渲染过程大致一致，但协调并不相同，以React 16为分界线，分为Stack Reconciler和Fiber Reconciler。这里的协调从狭义上来讲，特指 React 的 diff 算法，广义上来讲，有时候也指 React 的reconciler模块，它通常包含了diff算法和一些公共逻辑。回到Stack Reconciler中，Stack Reconciler的核心调度方式是递归。调度的基本处理单位是事务，它的事务基类是Transaction，这里的事务是 React 团队从后端开发中加入的概念。在 React 16 以前，挂载主要通过 ReactMount 模块完成，更新通过ReactUpdate模块完成，模块之间相互分离，落脚执行点也是事务。在React 16及以后，协调改为了Fiber Reconciler。它的调度方式主要有两个特点，第一个是协作式多任务模式，在这个模式下，线程会定时放弃自己的运行权利，交还给主线程，通过requestIdleCallback实现。第二个特点是策略优先级，调度任务通过标记tag的方式分优先级执行，比如动画，或者标记为high的任务可以优先执行。Fiber Reconciler的基本单位是Fiber，Fiber基于过去的React Element提供了二次封装，提供了指向父、子、兄弟节点的引用，为diff工作的双链表实现提供了基础。在新的架构下，整个生命周期被划分为Render 和 Commit 两个阶段。Render 阶段的执行特点是可中断、可停止、无副作用，主要是通过构造workInProgress树计算出diff。以current树为基础，将每个Fiber作为一个基本单位，自下而上逐个节点检查并构造 workInProgress 树。这个过程不再是递归，而是基于循环来完成在执行上通过requestIdleCallback来调度执行每组任务，每组中的每个计算任务被称为work，每个work完成后确认是否有优先级更高的work需要插入，如果有就让位，没有就继续。优先级通常是标记为动画或者high的会先处理。每完成一组后，将调度权交回主线程，直到下一次requestIdleCallback调用，再继续构建workInProgress树在commit阶段需要处理effect列表，这里的effect列表包含了根据diff 更新 DOM 树、回调生命周期、响应 ref等。但一定要注意，这个阶段是同步执行的，不可中断暂停，所以不要在componentDidMount、componentDidUpdate、componentWiilUnmount中去执行重度消耗算力的任务如果只是一般的应用场景，比如管理后台、H5 展示页等，两者性能差距并不大，但在动画、画布及手势等场景下，Stack Reconciler的设计会占用占主线程，造成卡顿，而fiber reconciler的设计则能带来高性能的表现", "f": [{"q": "动画面板更新时主线程连续被占用，同事说React只是在递归整棵组件树；放到React 16之后的架构里，这个说法哪里不准确？", "a": "React 16之后使用Fiber Reconciler，基本处理单位是带父、子、兄弟引用的Fiber，工作过程不再等同于旧架构的连续递归。协调工作可被拆分、暂停和恢复，并结合任务优先级让位；但后续commit仍是同步且不可中断的。"}, {"q": "画布编辑器同时收到拖拽更新和低优先级列表刷新，产品要求拖拽优先响应；渲染流程中的哪一层负责体现这种优先级？", "a": "优先级体现在Fiber Reconciler的调度与render工作阶段，任务会被标记并按策略安排，高优先级工作可插入并获得执行机会。它改善的是协调过程对主线程的占用方式，不代表所有工作都会异步；真正应用变更的commit仍要同步完成。"}, {"q": "管理后台只有少量表单和列表，架构评审却把从Stack Reconciler到Fiber Reconciler当成必然的大幅性能收益；你会接受吗？", "a": "不会直接接受，一般管理后台或H5展示页中，两种协调方式的体验差距未必明显。Fiber的优势更容易在动画、画布、手势或大批量更新等主线程敏感场景体现；是否有收益仍要结合更新规模和交互特征判断。"}, {"q": "线上弹窗打开时页面长时间不响应，日志显示节点差异已经算完，随后生命周期回调里执行了重计算；你会把故障定位到哪个阶段？", "a": "应重点定位commit阶段，因为它会同步处理effect列表，包括更新DOM、生命周期回调和ref。该阶段不可中断，在componentDidMount、componentDidUpdate或卸载回调中执行重计算会直接延长阻塞；把渲染阶段做成可中断并不能抵消这类开销。"}, {"q": "面试官让你用current树、workInProgress树和真实DOM串起一次更新，不能只背“渲染与提交”，你会怎么讲？", "a": "render阶段以current树为基础，逐个处理Fiber并构造workInProgress树，在此期间计算差异且应避免副作用。完成后进入同步commit，按照effect信息更新真实DOM、触发生命周期并处理ref；前一阶段可调度，后一阶段不能暂停。"}]}, {"s": "React", "t": "diff算法是怎么运作", "m": "React的diff通过只比较同层节点、按组件类型判断复用，并用key识别同层元素，把树比较从O(n³)简化到O(n)。节点类型不同会直接删除旧子树并创建新子树，类型相同才继续比较属性和子节点，所以跨层移动通常也会被当成删除与新增。列表更新时会优先处理可复用节点，剩余节点再执行插入、删除或移动。key应保持唯一和稳定，否则在头部插入或顺序变化时，可能让后续节点失去正确的复用关系；用索引作key也有同样风险。\n每一种节点类型有自己的属性，也就是prop，每次进行diff的时候，react会先比较该节点类型，假如节点类型不一样，那么react会直接删除该节点，然后直接创建新的节点插入到其中，假如节点类型一样，那么会比较prop是否有更新，假如有prop不一样，那么react会判定该节点有更新，那么重渲染该节点，然后在对其子节点进行比较，一层一层往下，直到没有子节点\n把树形结构按照层级分解，只比较同级元素。给列表结构的每个单元添加唯一的key属性，方便比较。React只会匹配相同class的component（这里面的class指的是组件的名字）合并操作，调用component的setState方法的时候,React将其标记为 -dirty.到每一个事件循环结束,React检查所有标记dirty的component重新绘制.选择性子树渲染。开发人员可以重写shouldComponentUpdate提高diff的性能\n优化⬇️\n为了降低算法复杂度，React的diff会预设三个限制：\n只对同级元素进行Diff。如果一个DOM节点在前后两次更新中跨越了层级，那么React不会尝试复用他。两个不同类型的元素会产生出不同的树。如果元素由div变为p，React会销毁div及其子孙节点，并新建p及其子孙节点。开发者可以通过key prop来暗示哪些子元素在不同的渲染下能保持稳定。考虑如下例子：\nDiff的思路\n该如何设计算法呢？如果让我设计一个Diff算法，我首先想到的方案是：\n判断当前节点的更新属于哪种情况如果是新增，执行新增逻辑如果是删除，执行删除逻辑如果是更新，执行更新逻辑\n按这个方案，其实有个隐含的前提——不同操作的优先级是相同的但是React团队发现，在日常开发中，相较于新增和删除，更新组件发生的频率更高。所以Diff会优先判断当前节点是否属于更新。\n基于以上原因，Diff算法的整体逻辑会经历两轮遍历：\n第一轮遍历：处理更新的节点。第二轮遍历：处理剩下的不属于更新的节点。\ndiff算法的作用\n计算出Virtual DOM中真正变化的部分，并只针对该部分进行原生DOM操作，而非重新渲染整个页面。\n传统diff算法\n通过循环递归对节点进行依次对比，算法复杂度达到O(n^3)，n是树的节点数，这个有多可怕呢？——如果要展示1000个节点，得执行上亿次比较。。即便是CPU快能执行30亿条命令，也很难在一秒内计算出差异。\nReact的diff算法\n什么是调和？\n将Virtual DOM树转换成actual DOM树的最少操作的过程 称为 调和 。\n什么是React diff算法？\ndiff算法是调和的具体实现。\ndiff策略\nReact用 三大策略 将O(n^3)复杂度 转化为 O(n)复杂度\n策略一（tree diff）：\nWeb UI中DOM节点跨层级的移动操作特别少，可以忽略不计。\n策略二（component diff）：\n拥有相同类的两个组件 生成相似的树形结构，拥有不同类的两个组件 生成不同的树形结构。\n策略三（element diff）：\n对于同一层级的一组子节点，通过唯一id区分。\ntree diff\nReact通过updateDepth对Virtual DOM树进行层级控制。对树分层比较，两棵树 只对同一层次节点 进行比较。如果该节点不存在时，则该节点及其子节点会被完全删除，不会再进一步比较。只需遍历一次，就能完成整棵DOM树的比较。\n那么问题来了，如果DOM节点出现了跨层级操作,diff会咋办呢？\n答：diff只简单考虑同层级的节点位置变换，如果是跨层级的话，只有创建节点和删除节点的操作。\n如上图所示，以A为根节点的整棵树会被重新创建，而不是移动，因此 官方建议不要进行DOM节点跨层级操作，可以通过CSS隐藏、显示节点，而不是真正地移除、添加DOM节点\ncomponent diff\nReact对不同的组件间的比较，有三种策略\n同一类型的两个组件，按原策略（层级比较）继续比较Virtual DOM树即可。同一类型的两个组件，组件A变化为组件B时，可能Virtual DOM没有任何变化，如果知道这点（变换的过程中，Virtual DOM没有改变），可节省大量计算时间，所以 用户 可以通过shouldComponentUpdate()来判断是否需要 判断计算。不同类型的组件，将一个（将被改变的）组件判断为dirty component（脏组件），从而替换 整个组件的所有节点。\n注意：如果组件D和组件G的结构相似，但是 React判断是 不同类型的组件，则不会比较其结构，而是删除 组件D及其子节点，创建组件G及其子节点。\nelement diff\n当节点处于同一层级时，diff提供三种节点操作：删除、插入、移动。\n插入：组件 C 不在集合（A,B）中，需要插入删除：组件 D 在集合（A,B,D）中，但 D的节点已经更改，不能复用和更新，所以需要删除 旧的 D ，再创建新的。组件 D 之前在 集合（A,B,D）中，但集合变成新的集合（A,B）了，D 就需要被删除。移动：组件D已经在集合（A,B,C,D）里了，且集合更新时，D没有发生更新，只是位置改变，如新集合（A,D,B,C），D在第二个，无须像传统diff，让旧集合的第二个B和新集合的第二个D 比较，并且删除第二个位置的B，再在第二个位置插入D，而是 （对同一层级的同组子节点） 添加唯一key进行区分，移动即��。\n组件 D 在集合（A,B,D）中，但 D的节点已经更改，不能复用和更新，所以需要删除 旧的 D ，再创建新的。组件 D 之前在 集合（A,B,D）中，但集合变成新的集合（A,B）了，D 就需要被删除。\n总结\ntree diff：只对比同一层的 dom 节点，忽略 dom 节点的跨层级移动\n如下图，react 只会对相同颜色方框内的 DOM 节点进行比较，即同一个父节点下的所有子节点。当发现节点不存在时，则该节点及其子节点会被完全删除掉，不会用于进一步的比较。\n这样只需要对树进行一次遍历，便能完成整个 DOM 树的比较。\n这就意味着，如果 dom 节点发生了跨层级移动，react 会删除旧的节点，生成新的节点，而不会复用。\ncomponent diff：如果不是同一类型的组件，会删除旧的组件，创建新的组件\nelement diff：对于同一层级的一组子节点，需要通过唯一 id 进行来区分\n如果没有 id 来进行区分，一旦有插入动作，会导致插入位置之后的列表全部重新渲染这也是为什么渲染列表时为什么要使用唯一的 key。\ndiff的不足与待优化的地方\n尽量减少类似将最后一个节点移动到列表首部的操作，当节点数量过大或更新操作过于频繁时，会影响React的渲染性能\n与其他框架相比，React 的 diff 算法有何不同？\ndiff 算法探讨的就是虚拟 DOM 树发生变化后，生成 DOM 树更新补丁的方式。它通过对比新旧两株虚拟 DOM 树的变更差异，将更新补丁作用于真实 DOM，以最小成本完成视图更新\n具体的流程是这样的：\n真实 DOM 与虚拟 DOM 之间存在一个映射关系。这个映射关系依靠初始化时的 JSX 建立完成；当虚拟 DOM 发生变化后，就会根据差距计算生成 patch，这个 patch 是一个结构化的数据，内容包含了增加、更新、移除等；最后再根据 patch 去更新真实的 DOM，反馈到用户的界面上。\n在回答有何不同之前，首先需要说明下什么是 diff 算法。\ndiff 算法是指生成更新补丁的方式，主要应用于虚拟 DOM 树变化后，更新真实 DOM。所以 diff 算法一定存在这样一个过程：触发更新 → 生成补丁 → 应用补丁React 的 diff 算法，触发更新的时机主要在 state 变化与 hooks 调用之后。此时触发虚拟 DOM 树变更遍历，采用了深度优先遍历算法。但传统的遍历方式，效率较低。为了优化效率，使用了分治的方式。将单一节点比对转化为了 3 种类型节点的比对，分别是树、组件及元素，以此提升效率。树比对：由于网页视图中较少有跨层级节点移动，两株虚拟 DOM 树只对同一层次的节点进行比较。组件比对：如果组件是同一类型，则进行树比对，如果不是，则直接放入到补丁中。元素比对：主要发生在同层级中，通过标记节点操作生成补丁，节点操作对应真实的 DOM 剪裁操作。同一层级的子节点，可以通过标记 key 的方式进行列表对比。以上是经典的 React diff 算法内容。自 React 16 起，引入了 Fiber 架构。为了使整个更新过程可随时暂停恢复，节点与树分别采用了FiberNode 与 FiberTree 进行重构。fiberNode 使用了双链表的结构，可以直接找到兄弟节点与子节点然后拿 Vue 和 Preact 与 React 的 diff 算法进行对比Preact的Diff算法相较于React，整体设计思路相似，但最底层的元素采用了真实DOM对比操作，也没有采用Fiber设计。Vue 的Diff算法整体也与React相似，同样未实现Fiber设计然后进行横向比较，React 拥有完整的 Diff 算法策略，且拥有随时中断更新的时间切片能力，在大批量节点更新的极端情况下，拥有更友好的交互体验。Preact 可以在一些对性能要求不高，仅需要渲染框架的简单场景下应用。Vue 的整体diff 策略与 React 对齐，虽然缺乏时间切片能力，但这并不意味着 Vue 的性能更差，因为在 Vue 3 初期引入过，后期因为收益不高移除掉了。除了高帧率动画，在 Vue 中其他的场景几乎都可以使用防抖和节流去提高响应性能。\n树比对：由于网页视图中较少有跨层级节点移动，两株虚拟 DOM 树只对同一层次的节点进行比较。组件比对：如果组件是同一类型，则进行树比对，如果不是，则直接放入到补丁中。元素比对：主要发生在同层级中，通过标记节点操作生成补丁，节点操作对应真实的 DOM 剪裁操作。同一层级的子节点，可以通过标记 key 的方式进行列表对比。\nPreact的Diff算法相较于React，整体设计思路相似，但最底层的元素采用了真实DOM对比操作，也没有采用Fiber设计。Vue 的Diff算法整体也与React相似，同样未实现Fiber设计\n**学习原理的目的就是应用。那如何根据 React diff 算法原理优化代码呢？**这个问题其实按优化方式逆向回答即可。\n根据diff算法的设计原则，应尽量避免跨层级节点移动。通过设置唯一key进行优化，尽量减少组件层级深度。因为过深的层级会加深遍历深度，带来性能问题。设置shouldComponentUpdate或者React.pureComponet减少diff次数。", "f": [{"q": "消息列表头部插入一条新消息，开发者没有提供稳定key，结果插入点后的多行都被当成变化；为什么diff没识别出只是新增一项？", "a": "同层子节点缺少稳定标识时，React主要按位置判断对应关系，头部插入会改变后续节点的位置映射。为每条消息使用唯一且稳定的业务key，才能帮助element diff区分插入、删除和移动；key只在同一组兄弟节点中参与匹配。"}, {"q": "可拖拽看板把一个带输入状态的卡片从A列移到B列，业务希望组件实例和状态原样保留；仅让两边使用相同key能做到吗？", "a": "不能保证，因为React只在同一层级、同一组子节点中利用key建立对应关系，不会把跨父节点移动当作可复用的同一次移动。旧子树会被删除，新位置会创建节点，局部状态可能随之丢失；需要保持层级不变并调整展示，或把需保留的状态移到更稳定的位置。"}, {"q": "设计系统把列表项根节点从div改成p，内部子组件和属性看起来完全一致；为什么上线后仍出现整段节点重建？", "a": "根元素类型不同会触发不同树的假设，React会销毁旧div及其子孙节点，再创建新的p子树，而不是继续比较内部结构。只有类型相同时才会比较prop并向下处理子节点；结构相似并不足以跨类型复用。"}, {"q": "千行表格更新一行后出现大量节点操作，你拿到新旧虚拟树时会先查哪些破坏复用关系的线索？", "a": "先核对同层列表是否有唯一、稳定的key，以及更新前后元素或组件类型是否被意外替换。再检查节点是否跨父级移动，因为这会表现为旧子树删除和新子树创建；这些线索比单纯统计组件执行次数更能解释异常补丁规模。"}, {"q": "前端负责人建议实现完整的跨层树匹配，认为能减少任何移动场景的节点创建；你如何回应复杂度与收益冲突？", "a": "完整树比较的传统复杂度可达到O(n^3)，React通过只比较同层节点、按组件类型判断、用key区分同组元素，把常见比较约束到O(n)。支持任意跨层复用会破坏这些前提并显著增加成本；更稳妥的是调整页面结构，减少跨层移动。"}, {"q": "一个类组件渲染代价较高，但其输入在多数更新中不变；shouldComponentUpdate与diff的关系该怎样向评审解释？", "a": "shouldComponentUpdate可在已知组件输出无需变化时阻止继续计算其子树，从而减少不必要的diff。它依赖开发者正确判断props与state是否影响输出，判断错误会让页面漏更新；因此应针对明确稳定的子树使用，而不是无条件阻断。"}]}, {"s": "React", "t": "合成事件原理", "m": "合成事件本质上是React对原生事件的统一封装，事件通常委托到顶层容器，而不是直接绑定在每个子元素上。原生事件冒泡到委托节点后，React会创建SyntheticEvent，找到对应组件，并按组件树分发处理函数。这样既能屏蔽浏览器差异，也便于统一管理事件并减少大量监听器带来的内存开销。需要注意版本边界：React 17以前主要委托到document，之后改为各自的根容器；混用原生事件时也要留意两套传播路径的差别。\n为了解决跨浏览器兼容性问题，React会将浏览器原生事件（Browser Native Event）封装为合成事件（SyntheticEvent）传入设置的事件处理器中。这里的合成事件提供了与原生事件相同的接口，不过它们屏蔽了底层浏览器的细节差异，保证了行为的一致性。另外有意思的是，React并没有直接将事件附着到子元素上，而是以单一事件监听器的方式将所有的事件发送到顶层进行处理。这样React在更新DOM的时候就不需要考虑如何去处理附着在DOM上的事件监听器，最终达到优化性能的目的\n所有的事件挂在document上，DOM 事件触发后冒泡到 document；React 找到对应的组件，造出一个合成事件出来；并按组件树模拟一遍事件冒泡。event不是原生的，是SyntheticEvent合成事件对象和Vue事件不同,和DOM事件也不同\nReact 17 之前的事件冒泡流程图\n所以这就造成了，在一个页面中，只能有一个版本的 React。如果有多个版本，事件就乱套了。值得一提的是，这个问题在 React 17 中得到了解决，事件委托不再挂在 document 上，而是挂在 DOM 容器上，也就是ReactDom.Render所调用的节点上。\nReact 17 后的事件冒泡流程图\n那到底哪些事件会被捕获生成合成事件呢？可以从 React 的源码测试文件中一探究竟。下面的测试快照中罗列了大量的事件名，也只有在这份快照中的事件，才会被捕获生成合成事件。\n// react/packages/react-dom/src/__tests__/__snapshots__/ReactTestUtils-test.js.snapArray[\"abort\",\"animationEnd\",\"animationIteration\",\"animationStart\",\"auxClick\",\"beforeInput\",\"blur\",\"canPlay\",\"canPlayThrough\",\"cancel\",\"change\",\"click\",\"close\",\"compositionEnd\",\"compositionStart\",\"compositionUpdate\",\"contextMenu\",\"copy\",\"cut\",\"doubleClick\",\"drag\",\"dragEnd\",\"dragEnter\",\"dragExit\",\"dragLeave\",\"dragOver\",\"dragStart\",\"drop\",\"durationChange\",\"emptied\",\"encrypted\",\"ended\",\"error\",\"focus\",\"gotPointerCapture\",\"input\",\"invalid\",\"keyDown\",\"keyPress\",\"keyUp\",\"load\",\"loadStart\",\"loadedData\",\"loadedMetadata\",\"lostPointerCapture\",\"mouseDown\",\"mouseEnter\",\"mouseLeave\",\"mouseMove\",\"mouseOut\",\"mouseOver\",\"mouseUp\",\"paste\",\"pause\",\"play\",\"playing\",\"pointerCancel\",\"pointerDown\",\"pointerEnter\",\"pointerLeave\",\"pointerMove\",\"pointerOut\",\"pointerOver\",\"pointerUp\",\"progress\",\"rateChange\",\"reset\",\"scroll\",\"seeked\",\"seeking\",\"select\",\"stalled\",\"submit\",\"suspend\",\"timeUpdate\",\"toggle\",\"touchCancel\",\"touchEnd\",\"touchMove\",\"touchStart\",\"transitionEnd\",\"volumeChange\",\"waiting\",\"wheel\",]\n如果DOM上绑定了过多的事件处理函数,整个页面响应以及内存占用可能都会受到影响。React为了避免这类DOM事件滥用,同时屏蔽底层不同浏览器之间的事件系统的差异,实现了一个中间层 - SyntheticEvent\n当用户在为onClick添加函数时,React并没有将Click绑定到DOM上面而是在document处监听所有支持的事件,当事件发生并冒泡至document处时,React将事件内容封装交给中间层 SyntheticEvent (负责所有事件合成)所以当事件触发的时候, 对使用统一的分发函数 dispatchEvent 将指定函数执行\n为何要合成事件\n兼容性和跨平台挂在统一的document上，减少内存消耗，避免频繁解绑方便事件的统一管理（事务机制）dispatchEvent事件机制", "f": [{"q": "列表里有一千个按钮，实习生认为每个onClick都会对应一个直接挂在按钮上的原生监听器；结合React的事件链路，你会如何纠正？", "a": "React不会按这种理解把每个处理器直接附着到对应子节点，而是统一监听支持的事件，再在事件到达委托层后定位组件并分发。传给业务处理器的是SyntheticEvent，这种中间层减少了逐节点管理监听器的负担，也统一了浏览器差异。"}, {"q": "同一页面嵌入两个独立React应用，旧方案都把事件交给document，团队担心事件互相影响；React 17的委托位置变化解决了什么？", "a": "React 17将事件委托从document下移到各自的React根容器，也就是渲染所使用的容器节点。这样多个应用或版本能在各自边界内处理合成事件，降低顶层事件系统相互干扰的风险；跨容器的原生冒泡规则仍需单独考虑。"}, {"q": "埋点SDK用原生addEventListener，业务组件使用onClick，一次点击出现顺序与组件树直觉不一致；排查时为什么不能只看JSX嵌套？", "a": "业务onClick经过React的统一监听、合成和dispatchEvent分发，并非直接绑定在JSX对应节点上。应同时确认原生监听器所在节点、捕获或冒泡阶段，以及React版本对应的委托层；混用两套事件系统时，原生传播路径与组件树模拟分发不能混为一谈。"}, {"q": "线上只有部分事件能进入组件处理器，click正常但某个自定义事件名称毫无响应；你会怎样判断是不是合成事件系统的问题？", "a": "先确认该事件是否属于React支持并会捕获生成SyntheticEvent的事件集合，不能假设任意事件名都会被统一分发。若不在支持范围，应改用合适的原生监听或封装桥接；若属于支持事件，再沿原生冒泡、根容器监听和dispatchEvent定位中断点。"}, {"q": "基础设施团队主张全部改用原生事件，理由是SyntheticEvent只是多包了一层；你会保留它的哪些工程价值？", "a": "合成事件统一了不同浏览器的事件接口，并让React集中管理监听和分发，避免组件更新时反复处理大量节点监听器。全部改用原生事件会获得更直接的底层控制，但需要自行承担兼容性、解绑和与React委托边界协作的成本；两套体系混用还会增加传播顺序的复杂度。"}]}, {"s": "React", "t": "JSX语法糖本质", "m": "JSX本质上是JavaScript的语法扩展，也是创建ReactElement的语法糖。经典转换中，Babel会先把它解析成AST，再转换为React.createElement调用，最终得到描述节点类型、属性和子元素的普通对象。这个对象组成虚拟DOM树，后续经过对比和更新渲染到页面。实际开发选择JSX，是因为它比手写嵌套函数更直观，也不需要额外引入模板指令。\nJSX是语法糖，通过babel转成React.createElement函数，在babel官网上可以在线把JSX转成React的JS语法\n首先解析出来的话，就是一个createElement函数然后这个函数执行完后，会返回一个vnode通过vdom的patch或者是其他的一个方法，最后渲染一个页面\nscript标签中不添加text/babel解析jsx语法的情况下\n<script>constele=React.createElement(\"h2\",null,\"Hello React!\");// 历史写法：React 18 起用 ReactDOM.createRoot(...).render(ele)，React 19 已移除 ReactDOM.renderReactDOM.render(ele,document.getElementById(\"app\"));</script>\nJSX的本质是React.createElement()函数\ncreateElement函数返回的对象是ReactEelement对象。\ncreateElement的写法如下\nclassAppextendsReact.Component{constructor(){super()this.state={}}render(){returnReact.createElement(\"div\",null,/*第一个子元素，header*/React.createElement(\"div\",{className:\"header\"},React.createElement(\"h1\",{title:\"\\u6807\\u9898\"},\"\\u6211\\u662F\\u6807\\u9898\")),/*第二个子元素，content*/React.createElement(\"div\",{className:\"content\"},React.createElement(\"h2\",null,\"\\u6211\\u662F\\u9875\\u9762\\u7684\\u5185\\u5BB9\"),React.createElement(\"button\",null,\"\\u6309\\u94AE\"),React.createElement(\"button\",null,\"+1\"),React.createElement(\"a\",{href:\"http://www.baidu.com\"},\"\\u767E\\u5EA6\\u4E00\\u4E0B\")),/*第三个子元素，footer*/React.createElement(\"div\",{className:\"footer\"},React.createElement(\"p\",null,\"\\u6211\\u662F\\u5C3E\\u90E8\\u7684\\u5185\\u5BB9\")));}}// 历史写法：React 18 起用 ReactDOM.createRoot(document.getElementById(\"app\")).render(<App />)ReactDOM.render(<App/>,document.getElementById(\"app\"));\n实际开发中不会使用createElement来创建ReactElement的，一般都是使用JSX的形式开发。\nReactElement在程序中打印一下\nrender(){letele=(<div><div className=\"header\"><h1 title=\"标题\">我是标题</h1></div><div className=\"content\"><h2>我是页面的内容</h2><button>按钮</button><button>+1</button><a href=\"http://www.baidu.com\">百度一下</a></div><div className=\"footer\"><p>我是尾部的内容</p></div></div>)console.log(ele);returnele;}\nreact通过babel把JSX转成createElement函数，生成ReactElement对象，然后通过ReactDOM.render函数把ReactElement渲染成真实的DOM元素\n为什么 React 使用 JSX\n在回答问题之前，我首先解释下什么是 JSX 吧。JSX 是一个JavaScript的语法扩展，结构类似 XML。JSX 主要用于声明React元素，但 React 中并不强制使用JSX。即使使用了JSX，也会在构建过程中，通过 Babel 插件编译为React.createElement。所以 JSX 更像是React.createElement的一种语法糖接下来与 JSX 以外的三种技术方案进行对比首先是模板，React 团队认为模板不应该是开发过程中的关注点，因为引入了模板语法、模板指令等概念，是一种不佳的实现方案其次是模板字符串，模板字符串编写的结构会造成多次内部嵌套，使整个结构变得复杂，并且优化代码提示也会变得困难重重所以 React 最后选用了 JSX，因为 JSX 与其设计思想贴合，不需要引入过多新的概念，对编辑器的代码提示也极为友好。\n首先是模板，React 团队认为模板不应该是开发过程中的关注点，因为引入了模板语法、模板指令等概念，是一种不佳的实现方案其次是模板字符串，模板字符串编写的结构会造成多次内部嵌套，使整个结构变得复杂，并且优化代码提示也会变得困难重重所以 React 最后选用了 JSX，因为 JSX 与其设计思想贴合，不需要引入过多新的概念，对编辑器的代码提示也极为友好。\nBabel 插件如何实现 JSX 到 JS 的编译？ 在 React 面试中，这个问题很容易被追问，也经常被要求手写。\n它的实现原理是这样的。Babel 读取代码并解析，生成 AST，再将 AST 传入插件层进行转换，在转换时就可以将 JSX 的结构转换为React.createElement的函数。如下代码所示：\nmodule.exports=function(babel){vart=babel.types;return{name:\"custom-jsx-plugin\",visitor:{JSXElement(path){varopeningElement=path.node.openingElement;vartagName=openingElement.name.name;varargs=[];args.push(t.stringLiteral(tagName));varattribs=t.nullLiteral();args.push(attribs);varreactIdentifier=t.identifier(\"React\");//objectvarcreateElementIdentifier=t.identifier(\"createElement\");varcallee=t.memberExpression(reactIdentifier,createElementIdentifier)varcallExpression=t.callExpression(callee,args);callExpression.arguments=callExpression.arguments.concat(path.node.children);path.replaceWith(callExpression,path.node);},},};};\nReact.createElement源码分析\n/** 101. React的创建元素方法 */exportfunctioncreateElement(type,config,children){// propName 变量用于储存后面需要用到的元素属性letpropName;// props 变量用于储存元素属性的键值对集合constprops={};// key、ref、self、source 均为 React 元素的属性，此处不必深究letkey=null;letref=null;letself=null;letsource=null;// config 对象中存储的是元素的属性if(config!=null){// 进来之后做的第一件事，是依次对 ref、key、self 和 source 属性赋值if(hasValidRef(config)){ref=config.ref;}// 此处将 key 值字符串化if(hasValidKey(config)){key=''+config.key;}self=config.__self===undefined?null:config.__self;source=config.__source===undefined?null:config.__source;// 接着就是要把 config 里面的属性都一个一个挪到 props 这个之前声明好的对象里面for(propNameinconfig){if(// 筛选出可以提进 props 对象里的属性hasOwnProperty.call(config,propName)&&!RESERVED_PROPS.hasOwnProperty(propName)){props[propName]=config[propName];}}}// childrenLength 指的是当前元素的子元素的个数，减去的 2 是 type 和 config 两个参数占用的长度constchildrenLength=arguments.length-2;// 如果抛去type和config，就只剩下一个参数，一般意味着文本节点出现了if(childrenLength===1){// 直接把这个参数的值赋给props.childrenprops.children=children;// 处理嵌套多个子元素的情况}elseif(childrenLength>1){// 声明一个子元素数组constchildArray=Array(childrenLength);// 把子元素推进数组里for(leti=0;i<childrenLength;i++){childArray[i]=arguments[i+2];}// 最后把这个数组赋值给props.childrenprops.children=childArray;}// 处理 defaultProps（React 16/17 源码逻辑，属历史对照）// React 19 已移除函数组件的 defaultProps，改用函数默认参数；类组件的 defaultProps 仍可用if(type&&type.defaultProps){constdefaultProps=type.defaultProps;for(propNameindefaultProps){if(props[propName]===undefined){props[propName]=defaultProps[propName];}}}// 最后返回一个调用ReactElement执行方法，并传入刚才处理过的参数returnReactElement(type,key,ref,self,source,ReactCurrentOwner.current,props,);}\n入参解读：创造一个元素需要知道哪些信息\nexportfunctioncreateElement(type,config,children)\ncreateElement 有 3 个入参，这 3 个入参囊括了 React 创建一个元素所需要知道的全部信息。\ntype：用于标识节点的类型。它可以是类似“h1”“div”这样的标准 HTML 标签字符串，也可以是 React 组件类型或React fragment类型。config：以对象形式传入，组件所有的属性都会以键值对的形式存储在 config 对象中。children：以对象形式传入，它记录的是组件标签之间嵌套的内容，也就是所谓的“子节点”“子元素”\nReact.createElement(\"ul\",{// 传入属性键值对className:\"list\"// 从第三个入参开始往后，传入的参数都是 children},React.createElement(\"li\",{key:\"1\"},\"1\"),React.createElement(\"li\",{key:\"2\"},\"2\"));\n这个调用对应的 DOM 结构如下：\n<ulclassName=\"list\"><likey=\"1\">1</li><likey=\"2\">2</li></ul>\ncreateElement 函数体拆解\ncreateElement 中并没有十分复杂的涉及算法或真实 DOM 的逻辑，它的每一个步骤几乎都是在格式化数据。\n现在看来，createElement原来只是个“参数中介”。此时我们的注意力自然而然地就聚焦在了ReactElement上\n出参解读：初识虚拟 DOM\ncreateElement执行到最后会 return 一个针对 ReactElement 的调用。这里关于 ReactElement，我依然先给出源码 + 注释形式的解析\nconstReactElement=function(type,key,ref,self,source,owner,props){constelement={// REACT_ELEMENT_TYPE是一个常量，用来标识该对象是一个ReactElement$$typeof:REACT_ELEMENT_TYPE,// 内置属性赋值type:type,key:key,ref:ref,props:props,// 记录创造该元素的组件_owner:owner,};//if(__DEV__){// 这里是一些针对 __DEV__ 环境下的处理，对于大家理解主要逻辑意义不大，此处我直接省略掉，以免混淆视听}returnelement;};\nReactElement其实只做了一件事情，那就是“创建”，说得更精确一点，是“组装”：ReactElement把传入的参数按照一定的规范，“组装”进了element对象里，并把它返回给了eact.createElement，最终React.createElement又把它交回到了开发者手中\nconstAppJSX=(<div className=\"App\"><h1 className=\"title\">Iam the title</h1><p className=\"content\">Iam the content</p></div>)console.log(AppJSX)\n你会发现它确实是一个标准的ReactElement对象实例\n这个ReactElement对象实例，本质上是以 JavaScript 对象形式存在的对DOM的描述，也就是老生常谈的“虚拟 DOM”（准确地说，是虚拟DOM中的一个节点)", "f": [{"q": "代码评审里有人说<UserCard name=\"Ada\" />会在浏览器里直接创建一个名为UserCard的DOM标签；你会用编译产物怎样反驳？", "a": "JSX会先经Babel转换为类似React.createElement(UserCard, {name: \"Ada\"})的调用，其中type是组件本身而不是浏览器标签字符串。该调用组装并返回ReactElement描述，之后才由React的渲染流程把它落实到真实DOM。"}, {"q": "低代码平台不能使用JSX，只允许执行普通JavaScript；要生成一个带两个li的ul，最少需要向createElement提供哪些信息？", "a": "需要提供节点type、保存属性的config，以及从第三个参数开始传入的一个或多个children。例如父节点类型为\"ul\"，子项分别是嵌套的React.createElement(\"li\", {key: \"...\"}, \"...\")；返回值仍是元素描述，不会在调用时直接操作DOM。"}, {"q": "调试台打印JSX变量时只看到带type、key、ref、props的普通对象，开发者以为渲染失败了；这个现象正常吗？", "a": "正常，createElement的职责主要是整理参数并组装ReactElement，所以JSX表达式的结果本来就是JavaScript对象形式的节点描述。真实节点要等后续渲染与补丁流程处理；仅打印元素对象看不到浏览器DOM，不能据此判断失败。"}, {"q": "组件传入key、ref、className和多个子节点后，业务代码发现props的形状与原始配置对象不同；createElement做了哪些整理？", "a": "createElement会单独提取key、ref等保留字段，把普通配置项放入props，并将子节点整理到props.children。一个子节点通常直接保存，多个子节点会组成数组；因此不能把所有JSX属性都假设为组件可读取的普通props。"}, {"q": "团队准备自己写Babel插件，把内部XML风格语法转换成React元素；最核心的AST转换动作是什么？", "a": "插件需要在Babel解析得到AST后访问JSXElement，提取标签、属性和子节点，再把该节点替换为React.createElement调用表达式。难点不只是替换函数名，还要完整保留组件类型、属性表达式和嵌套子节点；只处理字符串标签会在组件或复杂属性场景失效。"}, {"q": "模板方案和JSX方案选型时，有人认为JSX是运行时模板解释器；基于其工作链路，你会怎样说明取舍？", "a": "JSX是JavaScript的语法扩展，通常在构建阶段转换为React.createElement，并不依赖浏览器把它当模板解释。它减少了额外模板指令概念，并有利于编辑器提示，但仍需要编译工具处理；若运行环境没有对应转换，就必须直接编写普通JavaScript调用。"}]}, {"s": "React", "t": "为什么 React 元素有一个 $$typeof 属性", "m": "React元素上的$$typeof是身份标记，用来区分可信的ReactElement与可能从外部混入的普通对象。它的值使用Symbol，而Symbol不能被JSON序列化，因此服务端或数据库返回的JSON很难直接伪造成合法元素。React检查不到这个标记时会拒绝按元素处理，从而降低旧式元素注入导致XSS的风险。它是一层安全护栏，但不能替代对dangerouslySetInnerHTML内容本身的谨慎处理。\n目的是为了防止 XSS 攻击。因为 Synbol 无法被序列化，所以 React 可以通过有没有 $$typeof 属性来断出当前的 element 对象是从数据库来的还是自己生成的。\n如果没有 $$typeof 这个属性，react 会拒绝处理该元素。在 React 的古老版本中，下面的写法会出现 XSS 攻击：\n// 服务端允许用户存储 JSON let expectedTextButGotJSON = { type: 'div', props: { dangerouslySetInnerHTML: { __html: '/* 把你想的搁着 */' }, }, // ... }; let message = { text: expectedTextButGotJSON }; // React 0.13 中有风险 <p> {message.text} </p>", "f": [{"q": "评论页原本只显示后端返回的文本，但接口把一段带type、props和dangerouslySetInnerHTML的JSON对象交给了{message.text}；没有$$typeof校验时会发生什么？", "a": "旧版React可能把这个普通对象误认成元素，并按其中的props创建节点，最终让攻击者控制的HTML进入页面。$$typeof相当于元素身份标记；缺少该标记的对象不会作为合法元素处理，但普通文本输出是否安全仍取决于具体渲染入口。"}, {"q": "消息中心既接收服务端JSON，又允许业务代码通过React.createElement动态生成提示组件，你会怎样划分两类数据的信任边界？", "a": "服务端JSON只能作为文本或经过校验的业务数据，不能直接充当待渲染的元素对象；动态组件则应在前端通过JSX或React.createElement创建。这样生成的元素带有React认可的$$typeof，但组件类型和危险属性仍不能由用户输入任意决定。"}, {"q": "如果运行环境支持把Symbol转成自定义字符串并写入数据库，攻击者能否仅靠伪造同名的\"$$typeof\"字段绕过检查？", "a": "仅写入同名字符串通常不能等价于React内部使用的Symbol值，因为标准JSON无法表达Symbol。不过若应用在反序列化后主动把外部标记转换为真实标记，就重新打开了信任通道；因此不要为用户数据补造元素身份。"}, {"q": "一次发布后，富文本详情页仍出现脚本注入，而监控显示所有可疑对象都没有通过元素身份校验，你会先排查哪些渲染入口？", "a": "应先搜索dangerouslySetInnerHTML以及把外部内容交给组件类型或属性的路径，确认注入是否绕开了伪造元素这一入口。$$typeof只阻止普通JSON冒充React元素，不能替代HTML净化、URL协议校验或服务端输入治理。"}, {"q": "安全评审有人主张删掉$$typeof，改为递归检查对象是否只有type、props、children，这种方案为什么不可靠？", "a": "字段白名单只能判断对象的形状，攻击者仍可构造完全符合形状约束的JSON，并把危险内容放进允许的属性中。使用JSON无法序列化的Symbol作为身份标记，能区分前端创建的元素和数据库对象；代价是它只提供来源识别，不负责内容安全。"}]}, {"s": "React", "t": "Virtual DOM 的工作原理是什么", "m": "虚拟DOM的工作流程可以概括为用对象描述界面、比较前后差异，再把patch应用到真实DOM。JSX经编译并执行后会生成包含节点类型、props和children的普通对象，这些对象共同组成树。状态变化时，框架对新旧树执行diff，只根据差异更新真实页面，从而减少人为操作DOM的风险。它还便于服务端渲染和跨平台复用，但会占用额外内存，也不意味着所有高性能场景都更合适。\n虚拟 DOM 的工作原理是通过 JS 对象模拟 DOM 的节点。在 Facebook 构建 React 初期时，考虑到要提升代码抽象能力、避免人为的 DOM 操作、降低代码整体风险等因素，所以引入了虚拟 DOM虚拟 DOM 在实现上通常是Plain Object，以 React 为例，在render函数中写的JSX会在Babel插件的作用下，编译为React.createElement执行JSX中的属性参数React.createElement执行后会返回一个Plain Object，它会描述自己的tag类型、props属性以及children情况等。这些Plain Object通过树形结构组成一棵虚拟DOM树。当状态发生变更时，将变更前后的虚拟DOM树进行差异比较，这个过程称为diff，生成的结果称为patch。计算之后，会渲染Patch完成对真实DOM的操作。虚拟 DOM 的优点主要有三点：改善大规模DOM操作的性能、规避 XSS 风险、能以较低的成本实现跨平台开发。虚拟 DOM 的缺点在社区中主要有两点内存占用较高，因为需要模拟整个网页的真实DOM高性能应用场景存在难以优化的情况，类似像 Google Earth 一类的高性能前端应用在技术选型上往往不会选择 React\n内存占用较高，因为需要模拟整个网页的真实DOM高性能应用场景存在难以优化的情况，类似像 Google Earth 一类的高性能前端应用在技术选型上往往不会选择 React\n除了渲染页面，虚拟 DOM 还有哪些应用场景？\n这个问题考验面试者的想象力。通常而言，我们只是将虚拟 DOM 与渲染绑定在一起，但实际上虚拟 DOM 的应用更为广阔。比如，只要你记录了真实 DOM 变更，它甚至可以应用于埋点统计与数据记录等。\nSSR原理\n借助虚拟dom,服务器中没有dom概念的，react巧妙的借助虚拟dom，然后可以在服务器中nodejs可以运行起来react代码。", "f": [{"q": "商品页只更新购物车角标，有人却说Virtual DOM会直接把整页真实DOM重建一遍；你会怎样纠正这个判断？", "a": "状态变化后会先得到新的虚拟DOM树，再与旧树执行diff，计算出描述差异的patch，最后把必要变更应用到真实DOM。虚拟树本身通常是描述标签、props和子节点的普通JS对象，但比较和保存这些对象也有计算与内存成本。"}, {"q": "你接手一个用JSX写成的活动页，评审要求画出从render到浏览器节点更新的链路，关键步骤应该是什么？", "a": "JSX会经编译转换为创建元素的调用，执行后产生描述节点的普通对象，并按子节点关系组成虚拟DOM树。状态更新时生成新树，diff得出patch，渲染阶段再据此操作真实DOM；这条链路把声明式描述与具体DOM操作隔开。"}, {"q": "同一套资讯卡片既要在浏览器渲染，也计划输出到非DOM宿主环境，虚拟DOM在这个约束变化下提供了什么能力？", "a": "虚拟DOM是与真实DOM分离的对象描述，因此上层组件逻辑不必直接依赖浏览器节点，底层可以针对不同平台实现对应渲染过程。跨平台并非自动免费获得，各宿主仍需处理属性、事件和生命周期差异，平台特有能力也需要适配。"}, {"q": "一个包含大量节点的地图式页面持续掉帧，Profiler显示JS比较和对象分配都很重，但真实DOM更新并不多，你会如何解释并推进排查？", "a": "瓶颈可能位于虚拟树创建和diff，而不是最终的真实DOM写入；虚拟DOM不保证所有高性能场景都更快。应缩小参与更新的树和更新频率，并评估局部直接绘制或更专用的渲染技术；类似高性能地图场景可能并不适合由React主导全部渲染。"}, {"q": "服务端没有浏览器DOM，却要为文章详情页生成首屏HTML，虚拟DOM为什么仍能参与这条链路？", "a": "组件先生成与浏览器DOM无关的虚拟节点描述，因此相关代码可以在Node.js环境运行，再由服务端渲染器把描述转换为HTML。它解决的是渲染描述与宿主解耦，浏览器接管后的事件绑定和状态衔接仍需另一阶段完成。"}]}, {"s": "React", "t": "React有哪些优化性能的手段", "m": "React性能优化的核心是避免无意义的重渲染，并把暂时用不到的代码延后加载。类组件可以用PureComponent或shouldComponentUpdate控制更新，函数组件则常用React.memo；局部计算和回调再按需要交给useMemo、useCallback。列表更新时要使用稳定且唯一的key，避免用数组下标造成错误复用。体积较大的页面还可以通过lazy配合Suspense懒加载，但这些手段应在确有重复渲染或加载成本时使用。\n类组件中的优化手段\n使用纯组件PureComponent作为基类。使用shouldComponentUpdate生命周期函数来自定义渲染逻辑。\n方法组件中的优化手段\n使用React.memo高阶函数包装组件，React.memo可以实现类似于shouldComponentUpdate或者PureComponent的效果使用useMemo使用React.useMemo精细化的管控，useMemo 控制的则是是否需要重复执行某一段逻辑，而React.memo 控制是否需要重渲染一个组件使用useCallBack。\n使用React.useMemo精细化的管控，useMemo 控制的则是是否需要重复执行某一段逻辑，而React.memo 控制是否需要重渲染一个组件\n其他方式\n在列表需要频繁变动时，使用唯一 id 作为 key，而不是数组下标。必要时通过改变 CSS 样式隐藏显示组件，而不是通过条件判断显示隐藏组件。使用Suspense和 lazy 进行懒加载，例如：\nimportReact,{lazy,Suspense}from\"react\";exportdefaultclassCallingLazyComponentsextendsReact.Component{render(){varComponentToLazyLoad=null;if(this.props.name==\"Mayank\"){ComponentToLazyLoad=lazy(()=>import(\"./mayankComponent\"));}elseif(this.props.name==\"Anshul\"){ComponentToLazyLoad=lazy(()=>import(\"./anshulComponent\"));}return(<div><h1>This is the Base User:{this.state.name}</h1><Suspense fallback={<div>Loading...</div>}><ComponentToLazyLoad/></Suspense></div>)}}", "f": [{"q": "订单列表父组件每次输入筛选词都会创建新数组，团队给所有子组件套了React.memo，但渲染次数没有下降；你会指出哪里判断错了？", "a": "React.memo控制的是组件在属性可比较为未变化时跳过重渲染，新数组会破坏引用稳定性，使包装失去预期效果。应先确认筛选计算和传入属性是否需要useMemo，但缓存本身也有维护成本，简单计算或持续变化的依赖未必受益。"}, {"q": "后台表格有数千条可编辑记录，某个单元格更新会带动整页刷新；在不改业务功能的前提下，你会怎样落地优化？", "a": "先用React.memo或类组件的PureComponent隔离无关行，并保证列表使用稳定且唯一的业务id作为key。对确实昂贵的派生计算使用useMemo，对需要稳定传递的回调用useCallback；若属性始终变化，这些优化仍会失效。"}, {"q": "仪表盘组件切换后必须保留内部输入和滚动位置，产品同时要求隐藏期间减少开销；你会选择条件卸载还是CSS隐藏？", "a": "需要保留组件实例和内部状态时，可以通过CSS切换显示状态，避免反复卸载与挂载。代价是隐藏组件仍占用DOM和内存，也可能继续响应状态更新；若组件长期不用或资源昂贵，条件卸载通常更合适。"}, {"q": "一次重构把列表key从业务id换成数组下标，排序后出现输入框内容跟着错误行移动；你会如何定位并修复？", "a": "排序改变了下标与业务记录的对应关系，React会按旧key复用节点，从而把局部状态保留在错误条目上。恢复稳定且唯一的业务id，并检查新增、删除和排序链路；只有列表顺序和成员永久不变时，下标风险才相对有限。"}, {"q": "首屏包体过大，设置页中的两个低频模块只有用户进入特定分支才需要，你会怎样使用lazy和Suspense，又要防什么问题？", "a": "可用lazy(() => import(...))延迟加载分支组件，并在外层用Suspense提供加载期间的占位内容。这样把非首屏代码推迟到需要时下载，但首次进入会产生等待；加载边界、失败处理以及组件引用是否有效都需要单独设计。"}]}, {"s": "React", "t": "Redux实现原理解析", "m": "Redux本质上用单向数据流和唯一的store，把状态变化收口到dispatch(action)与纯函数reducer。createStore内部维护state和监听器，派发后调用reducer生成新状态，再通知订阅者更新视图。异步请求、日志等副作用由中间件拦截action，通过逐层包装dispatch扩展流程。使用时不能直接修改state，否则状态变化会变得不可预测。\n在 Redux 的整个工作过程中，数据流是严格单向的。这一点一定一定要背下来，面试的时候也一定一定要记得说\n为什么要用redux\n在React中，数据在组件中是单向流动的，数据从一个方向父组件流向子组件（通过props）,所以，两个非父子组件之间通信就相对麻烦，redux的出现就是为了解决state里面的数据问题\nRedux设计理念\nRedux是将整个应用状态存储到一个地方上称为store,里面保存着一个状态树store tree,组件可以派发(dispatch)行为(action)给store,而不是直接通知其他组件，组件内部通过订阅store中的状态state来刷新自己的视图\n如果你想对数据进行修改，只有一种途径：派发 action。action 会被 reducer 读取，进而根据 action 内容的不同对数据进行修改、生成新的 state（状态），这个新的 state 会更新到 store 对象里，进而驱动视图层面做出对应的改变。\nRedux三大原则\n唯一数据源\n整个应用的state都被存储到一个状态树里面，并且这个状态树，只存在于唯一的store中\n保持只读状态\nstate是只读的，唯一改变state的方法就是触发action，action是一个用于描述以发生时间的普通对象\n数据改变只能通过纯函数来执行\n使用纯函数来执行修改，为了描述action如何改变state的，你需要编写reducers\n从编码的角度理解 Redux 工作流\n使用createStore 来完成 store 对象的创建\n// 引入 reduximport{createStore}from'redux'// 创建 storeconststore=createStore(reducer,initial_state,applyMiddleware(middleware1,middleware2,...));\ncreateStore 方法是一切的开始，它接收三个入参：\nreducer；初始状态内容；指定中间件\nreducer 的作用是将新的 state 返回给 store\n一个 reducer 一定是一个纯函数，它可以有各种各样的内在逻辑，但它最终一定要返回一个 state：\nconstreducer=(state,action)=>{// 此处是各种样的 state处理逻辑returnnew_state}\n当我们基于某个 reducer 去创建 store 的时候，其实就是给这个 store 指定了一套更新规则：\n// 更新规则全都写在 reducer 里conststore=createStore(reducer)\naction 的作用是通知 reducer “让改变发生”\n要想让 state 发生改变，就必须用正确的 action 来驱动这个改变。\nconstaction={type:\"ADD_ITEM\",payload:'<li>text</li>'}\naction 对象中允许传入的属性有多个，但只有 type 是必传的。type 是 action 的唯一标识，reducer 正是通过不同的 type 来识别出需要更新的不同的 state，由此才能够实现精准的“定向更新”。\n派发 action，靠的是 dispatch\naction 本身只是一个对象，要想让 reducer 感知到 action，还需要“派发 action”这个动作，这个动作是由 store.dispatch 完成的。这里我简单地示范一下：\nimport{createStore}from'redux'// 创建 reducerconstreducer=(state,action)=>{// 此处是各种样的 state处理逻辑returnnew_state}// 基于 reducer 创建 stateconststore=createStore(reducer)// 创建一个 action，这个 action 用 “ADD_ITEM” 来标识constaction={type:\"ADD_ITEM\",payload:'<li>text</li>'}// 使用 dispatch 派发 action，action 会进入到 reducer 里触发对应的更新store.dispatch(action)\n以上这段代码，是从编码角度对 Redux 主要工作流的概括，这里我同样为你总结了一张对应的流程图：\nRedux源码\nletcreateStore=(reducer)=>{letstate;//获取状态对象//存放所有的监听函数letlisteners=[];letgetState=()=>state;//提供一个方法供外部调用派发actionletdispath=(action)=>{//调用管理员reducer得到新的statestate=reducer(state,action);//执行所有的监听函数listeners.forEach((l)=>l())}//订阅状态变化事件，当状态改变发生之后执行监听函数letsubscribe=(listener)=>{listeners.push(listener);}dispath();return{getState,dispath,subscribe}}letcombineReducers=(renducers)=>{//传入一个renducers管理组，返回的是一个renducerreturnfunction(state={},action={}){letnewState={};for(varattrinrenducers){newState[attr]=renducers[attr](state[attr],action)}returnnewState;}}export{createStore,combineReducers};\n聊聊 Redux 和 Vuex 的设计思想\n共同点\n首先两者都是处理全局状态的工具库，大致实现思想都是：全局state保存状态---->dispatch(action)------>reducer(vuex里的mutation)----> 生成newState; 整个状态为同步操作；\n区别\n最大的区别在于处理异步的不同，vuex里面多了一步commit操作，在action之后commit(mutation)之前处理异步，而redux里面则是通过中间件处理\nredux 中间件\n中间件提供第三方插件的模式，自定义拦截 action -> reducer 的过程。变为 action -> middlewares -> reducer 。这种机制可以让我们改变数据流，实现如异步 action ，action 过 滤，日志输出，异常报告等功能\n常见的中间件:\nredux-logger:提供日志输出;redux-thunk:处理异步操作;redux-promise: 处理异步操作;actionCreator的返回值是promise\nredux中间件的原理是什么\napplyMiddleware\n为什么会出现中间件？\n它只是一个用来加工dispatch的工厂，而要加工什么样的dispatch出来，则需要我们传入对应的中间件函数让每一个中间件函数，接收一个dispatch，然后返回一个改造后的dispatch，来作为下一个中间件函数的next，以此类推。\nfunctionapplyMiddleware(middlewares){middlewares=middlewares.slice()middlewares.reverse()letdispatch=store.dispatch middlewares.forEach(middleware=>dispatch=middleware(store)(dispatch))returnObject.assign({},store,{dispatch})}\n上面的middleware(store)(dispatch)就相当于是const logger = store => next => {}，这就是构造后的dispatch，继续向下传递。这里middlewares.reverse()，进行数组反转的原因，是最后构造的dispatch，实际上是最先执行的。因为在applyMiddleware串联的时候，每个中间件只是返回一个新的dispatch函数给下一个中间件，实际上这个dispatch并不会执行。只有当我们在程序中通过store.dispatch(action)，真正派发的时候，才会执行。而此时的dispatch是最后一个中间件返回的包装函数。然后依次向前递推执行。\n浅析中间件(opens new window)\naction、store、reducer分析\nredux的核心概念就是store、action、reducer，从调用关系来看如下所示\nstore.dispatch(action)-->reducer(state,action)-->final state\n// reducer方法, 传入的参数有两个// state: 当前的state// action: 当前触发的行为, {type: 'xx'}// 返回值: 新的statevarreducer=function(state,action){switch(action.type){case'add_todo':returnstate.concat(action.text);default:returnstate;}};// 创建store, 传入两个参数// 参数1: reducer 用来修改state// 参数2(可选): [], 默认的state值,如果不传, 则为undefinedvarstore=redux.createStore(reducer,[]);// 通过 store.getState() 可以获取当前store的状态(state)// 默认的值是 createStore 传入的第二个参数console.log('state is: '+store.getState());// state is:// 通过 store.dispatch(action) 来达到修改 state 的目的// 注意: 在redux里,唯一能够修改state的方法,就是通过 store.dispatch(action)store.dispatch({type:'add_todo',text:'读书'});// 打印出修改后的stateconsole.log('state is: '+store.getState());// state is: 读书store.dispatch({type:'add_todo',text:'写作'});console.log('state is: '+store.getState());// state is: 读书,写作\nstore、reducer、action关联\nstore\nstore在这里代表的是数据模型，内部维护了一个state变量store有两个核心方法，分别是getState、dispatch。前者用来获取store的状态（state），后者用来修改store的状态\n// 创建store, 传入两个参数// 参数1: reducer 用来修改state// 参数2(可选): [], 默认的state值,如果不传, 则为undefinedvarstore=redux.createStore(reducer,[]);// 通过 store.getState() 可以获取当前store的状态(state)// 默认的值是 createStore 传入的第二个参数console.log('state is: '+store.getState());// state is:// 通过 store.dispatch(action) 来达到修改 state 的目的// 注意: 在redux里,唯一能够修改state的方法,就是通过 store.dispatch(action)store.dispatch({type:'add_todo',text:'读书'});\naction\n对行为（如用户行为）的抽象，在redux里是一个普通的js对象action必须有一个type字段来标识这个行为的类型\n{type:'add_todo',text:'读书'}{type:'add_todo',text:'写作'}{type:'add_todo',text:'睡觉',time:'晚上'}\nreducer\n一个普通的函数，用来修改store的状态。传入两个参数state、action其中，state为当前的状态（可通过store.getState()获得），而action为当前触发的行为（通过store.dispatch(action)调用触发）reducer(state, action)返回的值，就是store最新的state值\n// reducer方法, 传入的参数有两个// state: 当前的state// action: 当前触发的行为, {type: 'xx'}// 返回值: 新的statevarreducer=function(state,action){switch(action.type){case'add_todo':returnstate.concat(action.text);default:returnstate;}};\n关于actionCreator\nactionCreator(args)=>action\nvaraddTodo=function(text){return{type:'add_todo',text:text};};addTodo('睡觉');// 返回：{type: 'add_todo', text: '睡觉'}\n异步Action及操作\n创建同步Action\nAction是数据从应用传递到store/state的载体，也是开启一次完成数据流的开始\n普通的action对象\nconstaction={type:'ADD_TODO',name:'poetries'}dispatch(action)\n封装action creator\nfunctionactionCreator(data){return{type:'ADD_TODO',data:data}}dispatch(actionCreator('poetries'))\nbindActionCreators合并\nfunctiona(name,id){reurn{type:'a',name,id}}functionb(name,id){reurn{type:'b',name,id}}letactions=Redux.bindActionCreators({a,b},store.dispatch)//调用actions.a('poetries','id001')actions.b('jing','id002')\naction创建的标准\n在Flux的架构中，一个Action要符合 FSA(Flux Standard Action) 规范，需要满足如下条件\n是一个纯文本对象只具备type、payload、error和meta中的一个或者多个属性。type字段不可缺省，其它字段可缺省若Action报错，error字段不可缺省，切必须为true\npayload是一个对象，用作Action携带数据的载体\n标准action示例\nA basic Flux Standard Action:\n{type:'ADD_TODO',payload:{text:'Do something.'}}\nAn FSA that represents an error, analogous to a rejected Promise\n{type:'ADD_TODO',payload:newError(),error:true}\nhttps://github.com/acdlite/flux-standard-action\n可以采用如下一个简单的方式检验一个Action是否符合FSA标准\n// every有一个匹配不到返回falseletisFSA=Object.keys(action).every((item)=>{return['payload','type','error','meta'].indexOf(item)>-1})\n创建异步action的多种方式\n最简单的方式就是使用同步的方式来异步，将原来同步时一个action拆分成多个异步的action的，在异步开始前、异步请求中、异步正常返回（异常）操作分别使用同步的操作，从而模拟出一个异步操作了。这样的方式是比较麻烦的，现在已经有redux-saga等插件来解决这些问题了\n异步action的实现方式一：setTimeout\nredux-thunk中间处理解析\nfunctionthunkAction(data){reutrn(dispatch)=>{setTimeout(function(){dispatch({type:'ADD_TODO',data})},3000)}}\n异步action的实现方式二：promise实现异步action\nredux-promise中间处理这种action\nfunctionpromiseAction(name){returnnewPromise((resolve,reject)=>{setTimeout((param)=>{resolve({type:'ADD_TODO',name})},3000)}).then((param)=>{dispatch(action(\"action2\"))return;}).then((param)=>{dispatch(action(\"action3\"))})}\nredux异步流程\n首先发起一个action，然后通过中间件，这里为什么要用中间件呢，因为这样dispatch的返回值才能是一个函数。通过store.dispatch，将状态的的改变传给store的小弟reducer，reducer根据action的改变，传递新的状态state。最后将所有的改变告诉给它的大哥，store。store保存着所有的数据，并将数据注入到组件的顶部，这样组件就可以获得它需要的数据了\nRedux异步方案选型\nredux-thunk\nRedux本身只能处理同步的Action，但可以通过中间件来拦截处理其它类型的action，比如函数(Thunk)，再用回调触发普通Action，从而实现异步处理\n发送异步的action其实是被中间件捕获的，函数类型的action就被middleware捕获。至于怎么定义异步的action要看你用哪个中间件，根据他们的实例来定义，这样才会正确解析action\nRedux本身不处理异步行为，需要依赖中间件。结合redux-actions使用，Redux有两个推荐的异步中间件\nredux-thunkredux-promise\nredux-thunk的源码如下\nfunctioncreateThunkMiddleware(extraArgument){return({dispatch,getState})=>next=>action=>{if(typeofaction==='function'){returnaction(dispatch,getState,extraArgument);}returnnext(action);};}constthunk=createThunkMiddleware();thunk.withExtraArgument=createThunkMiddleware;exportdefaultthunk;\n源码可知，action creator需要返回一个函数给redux-thunk进行调用，示例如下\nexportletaddTodoWithThunk=(val)=>async(dispatch,getState)=>{//请求之前的一些处理letvalue=awaitPromise.resolve(val+' thunk');dispatch({type:CONSTANT.ADD_TO_DO_THUNK,payload:{value}});};\n而它使用起来最大的问题，就是重复的模板代码太多\n//action typesconstGET_DATA='GET_DATA',GET_DATA_SUCCESS='GET_DATA_SUCCESS',GET_DATA_FAILED='GET_DATA_FAILED';//action creatorconstgetDataAction=(id)=>(dispatch,getState)=>{dispatch({type:GET_DATA,payload:id})api.getData(id)//注：本文所有示例的api.getData都返回promise对象.then(response=>{dispatch({type:GET_DATA_SUCCESS,payload:response})}).catch(error=>{dispatch({type:GET_DATA_FAILED,payload:error})})}}//reducerconstreducer=(oldState,action)=>{switch(action.type){caseGET_DATA:returnoldState;caseGET_DATA_SUCCESS:returnsuccessState;caseGET_DATA_FAILED:returnerrorState;}}\n这已经是最简单的场景了，请注意：我们甚至还没写一行业务逻辑，如果每个异步处理都像这样，重复且无意义的工作会变成明显的阻碍\n另一方面，像GET_DATA_SUCCESS、GET_DATA_FAILED这样的字符串声明也非常无趣且易错 上例中，GET_DATA这个action并不是多数场景需要的\nredux-promise\n由于redux-thunk写起来实在是太麻烦了，社区当然会有其它轮子出现。redux-promise则是其中比较知名的\n它自定义了一个middleware，当检测到有action的payload属性是Promise对象时，就会若resolve，触发一个此action的拷贝，但payload为promise的value，并设status属性为\"success\"若reject，触发一个此action的拷贝，但payload为promise的reason，并设status属性为\"error\"\n若resolve，触发一个此action的拷贝，但payload为promise的value，并设status属性为\"success\"若reject，触发一个此action的拷贝，但payload为promise的reason，并设status属性为\"error\"\n//action typesconstGET_DATA='GET_DATA';//action creatorconstgetData=function(id){return{type:GET_DATA,payload:api.getData(id)//payload为promise对象}}//reducerfunctionreducer(oldState,action){switch(action.type){caseGET_DATA:if(action.status==='success'){returnsuccessState}else{returnerrorState}}}\nredux-promise为了精简而做出的妥协非常明显：无法处理乐观更新\n场景解析之：乐观更新\n多数异步场景都是悲观更新的，即等到请求成功才渲染数据。而与之相对的乐观更新，则是不等待请求成功，在发送请求的同时立即渲染数据\n由于乐观更新发生在用户操作时，要处理它，意味着必须有action表示用户的初始动作在上面redux-thunk的例子中，我们看到了GET_DATA,GET_DATA_SUCCESS、GET_DATA_FAILED三个action，分别表示初始动作、异步成功和异步失败，其中第一个action使得redux-thunk具备乐观更新的能力而在redux-promise中，最初触发的action被中间件拦截然后过滤掉了。原因很简单，redux认可的action对象是plain JavaScript objects，即简单对象，而在redux-promise中，初始action的payload是个Promise\nredux-promise-middleware\nredux-promise-middleware相比redux-promise，采取了更为温和和渐进式的思路，保留了和redux-thunk类似的三个action\n//action typesconstGET_DATA='GET_DATA',GET_DATA_PENDING='GET_DATA_PENDING',GET_DATA_FULFILLED='GET_DATA_FULFILLED',GET_DATA_REJECTED='GET_DATA_REJECTED';//action creatorconstgetData=function(id){return{type:GET_DATA,payload:{promise:api.getData(id),data:id}}}//reducerconstreducer=function(oldState,action){switch(action.type){caseGET_DATA_PENDING:returnoldState;// 可通过action.payload.data获取idcaseGET_DATA_FULFILLED:returnsuccessState;caseGET_DATA_REJECTED:returnerrorState;}}\nredux异步操作代码演示\n根据官网的async例子分析 https://github.com/lewis617/react-redux-tutorial/tree/master/redux-examples/async\naction/index.js\nimportfetchfrom'isomorphic-fetch'exportconstRECEIVE_POSTS='RECEIVE_POSTS'//获取新闻成功的actionfunctionreceivePosts(reddit,json){return{type:RECEIVE_POSTS,reddit:reddit,posts:json.data.children.map(child=>child.data)}}functionfetchPosts(subreddit){returnfunction(dispatch){returnfetch(`http://www.subreddit.com/r/${subreddit}.json`).then(response=>response.json()).then(json=>dispatch(receivePosts(subreddit,json)))}}//如果需要则开始获取文章exportfunctionfetchPostsIfNeeded(subreddit){return(dispatch,getState)=>{returndispatch(fetchPosts(subreddit))}}\nfetchPostsIfNeeded这里就是一个中间件。redux-thunk会拦截fetchPostsIfNeeded这个action，会先发起数据请求，如果成功，就将数据传给action从而到达reducer那里\nreducers/index.js\nimport{combineReducers}from'redux'import{RECEIVE_POSTS}from'../actions'functionposts(state={items:[]},action){switch(action.type){caseRECEIVE_POSTS:// Object.assign是ES6的一个语法。合并对象，将对象合并为一个，前后相同的话，后者覆盖强者。详情可以看这里// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/assignreturnObject.assign({},state,{items:action.posts//数据都存在了这里})default:returnstate}}// 将所有的reducer结合为一个,传给storeconstrootReducer=combineReducers({postsByReddit})exportdefaultrootReducer\n这个跟正常的reducer差不多。判断action的类型，从而根据action的不同类型，返回不同的数据。这里将数据存储在了items这里。这里的reducer只有一个。最后结合成rootReducer,传给store\nstore/configureStore.js\nimport{createStore,applyMiddleware}from'redux'importthunkMiddlewarefrom'redux-thunk'importcreateLoggerfrom'redux-logger'importrootReducerfrom'../reducers'constcreateStoreWithMiddleware=applyMiddleware(thunkMiddleware,createLogger())(createStore)exportdefaultfunctionconfigureStore(initialState){conststore=createStoreWithMiddleware(rootReducer,initialState)if(module.hot){// Enable Webpack hot module replacement for reducersmodule.hot.accept('../reducers',()=>{constnextRootReducer=require('../reducers')store.replaceReducer(nextRootReducer)})}returnstore}\n我们是如何在dispatch机制中引入Redux Thunk middleware的呢？ 我们使用了applyMiddleware()通过使用指定的middleware，action creator除了返回action对象外还可以返回函数这时，这个action creator就成为了thunk\n界面上的调用：在containers/App.js\n//初始化渲染后触发componentDidMount(){const{dispatch}=this.props// 这里可以传两个值，一个是 reactjs 一个是 frontenddispatch(fetchPostsIfNeeded('frontend'))}\n改变状态的时候也是需要通过dispatch来传递的\n数据的获取是通过provider,将store里面的数据注入给组件。让顶级组件提供给他们的子孙组件调用。代码如下：\nimport'babel-core/polyfill'importReactfrom'react'import{render}from'react-dom'import{Provider}from'react-redux'importAppfrom'./containers/App'importconfigureStorefrom'./store/configureStore'conststore=configureStore()render(<Provider store={store}><App/></Provider>,document.getElementById('root'))\n这样就完成了redux的异步操作。其实最主要的区别还是action里面还有中间件的调用，其他的地方基本跟同步的redux差不多的。搞懂了中间件，就基本搞懂了redux的异步操作", "f": [{"q": "购物车页面有人绕过dispatch，直接修改从store.getState()取得的数组，视图偶尔更新、调试记录却没有对应事件；为什么这违背了Redux的工作流？", "a": "Redux规定状态只读，变更必须由普通action经store.dispatch送入reducer，再生成新的state。直接修改既绕开了订阅通知和行为记录，也破坏了可预测性；即使页面偶然显示正确，时间回溯和问题定位也会失真。"}, {"q": "请你在代码评审中补全一个最小createStore：它要支持读取状态、派发行为和订阅更新，dispatch内部的执行顺序应是什么？", "a": "闭包保存state和监听函数集合，getState返回当前状态，subscribe登记监听者。dispatch(action)先执行reducer(state, action)得到新状态，再依次通知监听者；初始化时还需触发一次reducer，使默认状态得以建立。"}, {"q": "支付流程要求dispatch接受函数来发起异步请求，但现有reducer只能消费带type的普通对象；你会把扩展点放在哪里？", "a": "应通过中间件包装dispatch，让函数类型的action先被类似redux-thunk的逻辑拦截，并在异步完成后派发普通action。reducer继续保持纯函数，只负责根据当前状态和action生成新状态；请求等副作用进入reducer会破坏结果可复现性。"}, {"q": "线上接入两个日志与异步中间件后，执行顺序和配置顺序相反，排查applyMiddleware时你会关注哪段函数组合？", "a": "中间件本质是加工dispatch的工厂，形态可表示为store => next => action，每层把包装后的函数交给下一层。组合时常通过反向构造形成嵌套，最终得到的最外层会最先执行；若某层未调用next(action)，后续中间件和reducer都会被截断。"}, {"q": "大型表单拆成多个领域reducer后，团队打算让每个reducer都返回完整状态树；combineReducers应怎样避免这种耦合？", "a": "combineReducers可遍历reducer映射，把各自对应的state[attr]和同一个action传入，再将返回值组装成新的总状态。每个reducer只管理自己的状态切片，边界更清晰；切片命名或状态结构调整会影响选择器和持久化数据，需要同步迁移。"}, {"q": "同事认为action自己会修改状态，因此在actionCreator里直接操作store；你会怎样区分action、reducer和store的职责？", "a": "action只是描述已发生行为的普通对象，至少需要用于识别行为的type；actionCreator只是生成该对象。store.dispatch负责把它交给reducer，后者计算新状态并由store保存、通知订阅者；把修改逻辑藏进actionCreator会破坏单向数据流。"}]}, {"s": "React", "t": "谈谈你对状态管理的理解", "m": "状态管理的核心，是用明确的数据流统一保存和修改跨组件状态，让变化可预测、可追踪。Flux通过Action、Dispatcher、Store到视图的单向流动，解决了数据流向不清的问题；Redux则进一步用单一数据源、只读状态和纯函数reducer约束更新。异步请求等副作用通常交给中间件处理。若更看重少量样板代码和自动响应更新，也可以选择Mobx。\n首先介绍 Flux，Flux 是一种使用单向数据流的形式来组合 React 组件的应用架构。Flux 包含了 4 个部分，分别是Dispatcher、Store、View、Action。Store存储了视图层所有的数据，当Store变化后会引起 View 层的更新。如果在视图层触发一个Action，就会使当前的页面数据值发生变化。Action 会被 Dispatcher 进行统一的收发处理，传递给 Store 层，Store 层已经注册过相关 Action 的处理逻辑，处理对应的内部状态变化后，触发 View 层更新。Flux 的优点是单向数据流，解决了 MVC 中数据流向不清的问题，使开发者可以快速了解应用行为。从项目结构上简化了视图层设计，明确了分工，数据与业务逻辑也统一存放管理，使在大型架构的项目中更容易管理、维护代码。其次是 Redux，Redux 本身是一个 JavaScript 状态容器，提供可预测化状态的管理。社区通常认为 Redux 是 Flux 的一个简化设计版本，它提供的状态管理，简化了一些高级特性的实现成本，比如撤销、重做、实时编辑、时间旅行、服务端同构等。Redux 的核心设计包含了三大原则：单一数据源、纯函数 Reducer、State 是只读的。Redux 中整个数据流的方案与 Flux 大同小异Redux 中的另一大核心点是处理“副作用”，AJAX 请求等异步工作，或不是纯函数产生的第三方的交互都被认为是 “副作用”。这就造成在纯函数设计的 Redux 中，处理副作用变成了一件至关重要的事情。社区通常有两种解决方案：第一类是在Dispatch的时候会有一个middleware 中间件层，拦截分发的Action 并添加额外的复杂行为，还可以添加副作用。第一类方案的流行框架有Redux-thunk、Redux-Promise、Redux-Observable、Redux-Saga等。第二类是允许Reducer层中直接处理副作用，采取该方案的有React Loop，React Loop在实现中采用了 Elm 中分形的思想，使代码具备更强的组合能力。除此以外，社区还提供了更为工程化的方案，比如rematch 或 dva，提供了更详细的模块架构能力，提供了拓展插件以支持更多功能。Redux 的优点很多：结果可预测；代码结构严格易维护；模块分离清晰且小函数结构容易编写单元测试；Action触发的方式，可以在调试器中使用时间回溯，定位问题更简单快捷；单一数据源使服务端同构变得更为容易；社区方案多，生态也更为繁荣。最后是 Mobx，Mobx 通过监听数据的属性变化，可以直接在数据上更改触发UI 的渲染。在使用上更接近 Vue，比起Flux 与 Redux的手动挡的体验，更像开自动挡的汽车。Mobx 的响应式实现原理与 Vue 相同，以Mobx 5为分界点，5 以前采用Object.defineProperty的方案，5 及以后使用Proxy的方案。它的优点是样板代码少、简单粗暴、用户学习快、响应式自动更新数据让开发者的心智负担更低。Mobx 在开发项目时简单快速，但应用 Mobx 的场景 ，其实完全可以用 Vue 取代。如果纯用 Vue，体积还会更小巧\n第一类是在Dispatch的时候会有一个middleware 中间件层，拦截分发的Action 并添加额外的复杂行为，还可以添加副作用。第一类方案的流行框架有Redux-thunk、Redux-Promise、Redux-Observable、Redux-Saga等。第二类是允许Reducer层中直接处理副作用，采取该方案的有React Loop，React Loop在实现中采用了 Elm 中分形的思想，使代码具备更强的组合能力。除此以外，社区还提供了更为工程化的方案，比如rematch 或 dva，提供了更详细的模块架构能力，提供了拓展插件以支持更多功能。\n结果可预测；代码结构严格易维护；模块分离清晰且小函数结构容易编写单元测试；Action触发的方式，可以在调试器中使用时间回溯，定位问题更简单快捷；单一数据源使服务端同构变得更为容易；社区方案多，生态也更为繁荣。", "f": [{"q": "一个小型资料页只有父子组件传值，架构师却要求所有输入都先经过Dispatcher和全局Store；你会如何评价这种选型？", "a": "Flux的价值在于用Action、Dispatcher、Store和View建立清晰的单向数据流，尤其适合状态关系复杂的应用。简单父子传值强行全局化会增加结构和行为跳转成本；是否引入应由共享范围、变更复杂度和维护需求决定。"}, {"q": "协作编辑页面需要统一管理文档、选区和工具栏状态，并希望每次变化都能定位到触发行为；你会怎样组织Flux数据流？", "a": "视图触发Action，由Dispatcher统一分发给已注册处理逻辑的Store，Store更新内部状态后通知View刷新。这样数据沿单一方向流动，行为入口容易追踪；若把所有局部交互都塞入同一Store，模块边界仍可能变得模糊。"}, {"q": "产品新增撤销、重做和服务端同构要求，现有响应式状态允许任意位置直接赋值；为什么团队可能转向Redux？", "a": "Redux以单一数据源、只读状态和纯函数Reducer约束变化，使同一组action更容易重放，因此更适合撤销、重做、时间回溯和同构等需求。代价是更新路径更严格，副作用不能随意混入reducer，还需额外的中间件或工程封装。"}, {"q": "线上偶发状态无法复现，日志中同一个action重放后得到不同结果，而reducer内部读取了时间并发起AJAX；你会怎样修正？", "a": "应把时间读取和AJAX等副作用移出纯函数reducer，通过dispatch前的中间件处理，再把确定结果封装成普通action。Reducer只依据传入的state与action计算新状态；否则时间旅行、单元测试和故障重放都会失去可信度。"}, {"q": "团队在Redux与Mobx之间争论：大型权限后台强调变更审计，原型团队更看重少样板和直接赋值，你会怎样取舍？", "a": "强调可预测、严格结构和行为追踪时，Redux的action与纯reducer更契合，也便于测试和时间回溯。追求快速开发和自动响应更新时，Mobx的属性监听与直接修改更轻量；代价是隐式依赖增多后，变化来源通常不如显式action清晰。"}, {"q": "维护一个旧项目时发现Mobx的响应式层依赖属性拦截，新模块却使用Proxy；这与它和Vue的相邻原理有什么关系？", "a": "Mobx与Vue的响应式思路相近，都是监听数据属性变化后驱动UI更新；资料所述的分界是Mobx 5以前使用Object.defineProperty，之后使用Proxy。两种机制的能力边界不同，混合维护时应以项目实际版本和构建环境为准，不能只按写法推断。"}]}, {"s": "React", "t": "connect组件原理分析", "m": "connect是连接React组件与Redux store的高阶函数，负责订阅状态并把数据和派发能力映射成组件的props。Provider先通过context向下传递store，connect再用mapStateToProps和mapDispatchToProps计算组件真正需要的内容。状态或组件自身props变化时，它会重新计算并判断是否需要渲染。组件卸载时还要取消订阅，避免遗留监听。\n1. connect用法\n作用：连接React组件与Redux store\nconnect([mapStateToProps],[mapDispatchToProps],[mergeProps],[options])// 这个函数允许我们将 store 中的数据作为 props 绑定到组件上constmapStateToProps=(state)=>{return{count:state.count}}\n这个函数的第一个参数就是Redux的store，我们从中摘取了count属性。你不必将state中的数据原封不动地传入组件，可以根据state中的数据，动态地输出组件需要的（最小）属性函数的第二个参数ownProps，是组件自己的props\n当state变化，或者ownProps变化的时候，mapStateToProps都会被调用，计算出一个新的stateProps，（在与ownProps merge后）更新给组件\nmapDispatchToProps(dispatch,ownProps):dispatchProps\nconnect的第二个参数是mapDispatchToProps，它的功能是，将action作为props绑定到组件上，也会成为MyComp的 `props\n2. 原理解析\n首先connect之所以会成功，是因为Provider组件\n在原应用组件上包裹一层，使原来整个应用成为Provider的子组件接收Redux的store作为props，通过context对象传递给子孙组件上的connect\nconnect做了些什么\n它真正连接Redux和React，它包在我们的容器组件的外一层，它接收上面Provider提供的store里面的state和dispatch，传给一个构造函数，返回一个对象，以属性形式传给我们的容器组件\n3. 源码\nconnect是一个高阶函数，首先传入mapStateToProps、mapDispatchToProps，然后返回一个生产Component的函数(wrapWithConnect)，然后再将真正的Component作为参数传入wrapWithConnect，这样就生产出一个经过包裹的Connect组件，该组件具有如下特点\n通过props.store获取祖先Component的store props包括stateProps、dispatchProps、parentProps,合并在一起得到nextState，作为props传给真正的ComponentcomponentDidMount时，添加事件this.store.subscribe(this.handleChange)，实现页面交互shouldComponentUpdate时判断是否有避免进行渲染，提升页面性能，并得到nextStatecomponentWillUnmount时移除注册的事件this.handleChange\n// 主要逻辑exportdefaultfunctionconnect(mapStateToProps,mapDispatchToProps,mergeProps,options={}){returnfunctionwrapWithConnect(WrappedComponent){classConnectextendsComponent{constructor(props,context){// 从祖先Component处获得storethis.store=props.store||context.storethis.stateProps=computeStateProps(this.store,props)this.dispatchProps=computeDispatchProps(this.store,props)this.state={storeState:null}// 对stateProps、dispatchProps、parentProps进行合并this.updateState()}shouldComponentUpdate(nextProps,nextState){// 进行判断，当数据发生改变时，Component重新渲染if(propsChanged||mapStateProducedChange||dispatchPropsChanged){this.updateState(nextProps)returntrue}}componentDidMount(){// 改变Component的statethis.store.subscribe(()={this.setState({storeState:this.store.getState()})})}render(){// 生成包裹组件Connectreturn(<WrappedComponent{...this.nextState}/>)}}Connect.contextTypes={store:storeShape}returnConnect;}}", "f": [{"q": "商品列表页把store直接作为一个data属性传给展示组件，团队说这样比写mapStateToProps省事；当无关的登录状态变化时，列表也收到新属性，这种接法的问题在哪？", "a": "connect的职责不是原样转发整个state，而是通过mapStateToProps摘取组件真正需要的最小数据。传递范围过大会扩大组件与全局状态的耦合，也让无关状态变化更容易进入更新链路；代价是后续拆分、测试和定位渲染来源都会更困难。"}, {"q": "订单容器既要读取state.orders，又要根据父组件传入的userId过滤数据，还要暴露reload操作；你会怎样组织connect的几个参数？", "a": "在mapStateToProps(state, ownProps)中结合userId计算订单属性，在mapDispatchToProps(dispatch, ownProps)中封装需要派发的操作。最后由默认或自定义mergeProps合并stateProps、dispatchProps与父级属性；合并规则越复杂，越要防止同名属性被意外覆盖。"}, {"q": "同一个详情组件要挂在两个不同的Provider下，父组件还可能通过props.store显式传入另一个store；此时包装层应该从哪里取状态，更新订阅又跟着谁？", "a": "按给出的实现，包装层优先读取props.store，否则才从context获取祖先Provider提供的store。状态读取、dispatch和subscribe必须绑定同一个实例，否则页面可能展示一个仓库的数据却监听另一个仓库；显式覆盖因此需要非常谨慎。"}, {"q": "线上出现“ReduxDevTools里状态已经变化，但被connect包装的库存组件没有刷新”，你会沿着包装组件的哪条链路排查？", "a": "先确认Provider传入的store是否正确，再检查挂载时是否执行了store.subscribe，订阅回调有没有通过setState触发包装层更新。随后核对mapStateToProps是否产出变化，以及shouldComponentUpdate是否错误拦截；卸载过早或订阅错误实例也会造成同样现象。"}, {"q": "团队想把所有mapStateToProps、mapDispatchToProps和父级属性都手工塞进一个对象再传下去，认为这样更可控；什么时候值得写mergeProps，什么时候应保持默认合并？", "a": "只有属性需要重命名、组合或根据父级参数形成特定接口时，才值得提供mergeProps。普通场景保留默认合并更容易追踪数据来源，也减少每次更新时的额外计算；自定义合并若持续产生不稳定结果，还会削弱包装层避免无效渲染的能力。"}, {"q": "类组件页面不能直接读取context中的Redux数据，架构师建议每个组件都手写订阅；connect相比这种方案补齐了哪些生命周期责任？", "a": "connect把取store、计算状态与派发属性、订阅变化、判断更新和向真实组件传参集中在高阶组件中。组件挂载时注册订阅，卸载时还应移除订阅，避免遗留监听；手写方案若漏掉清理或更新判断，很容易产生泄漏或冗余渲染。"}]}, {"s": "React", "t": "React Hooks", "m": "React Hooks让函数组件能够使用状态和副作用，并把相关逻辑抽成可复用的自定义Hook。它解决了类组件中生命周期拆散业务逻辑、this使用复杂，以及HOC或render props容易造成嵌套的问题。由于内部按调用顺序保存Hook，只能在函数组件或自定义Hook顶层调用，不能放进条件、循环或嵌套函数。普通副作用优先用useEffect，需要同步读取或调整DOM、避免闪烁时再用useLayoutEffect。\n代码逻辑聚合，逻辑复用HOC嵌套地狱代替class\nReact 中通常使用 类定义 或者 函数定义 创建组件:\n在类定义中，我们可以使用到许多 React 特性，例如 state、 各种组件生命周期钩子等，但是在函数定义中，我们却无能为力，因此 React 16.8 版本推出了一个新功能 (React Hooks)，通过它，可以更好的在函数定义组件中使用 React 特性。\n函数组件与类组件的对比：无关“优劣”，只谈“不同”\n类组件需要继承 class，函数组件不需要；类组件可以访问生命周期方法，函数组件不能；类组件中可以获取到实例化后的 this，并基于这个 this 做各种各样的事情，而函数组件不可以；类组件中可以定义并维护 state（状态），而函数组件不可以；\n但是类组件它太重了，对于解决许多问题来说，编写一个类组件实在是一个过于复杂的姿势。复杂的姿势必然带来高昂的理解成本，这也是我们所不想看到的\nreact hooks的好处:\n跨组件复用: 其实 render props / HOC 也是为了复用，相比于它们，Hooks 作为官方的底层 API，最为轻量，而且改造成本小，不会影响原来的组件层次结构和传说中的嵌套地狱；类定义更为复杂\n不同的生命周期会使逻辑变得分散且混乱，不易维护和管理；时刻需要关注this的指向问题；代码复用代价高，高阶组件的使用经常会使整个组件树变得臃肿；\n状态与UI隔离: 正是由于 Hooks 的特性，状态逻辑会变成更小的粒度，并且极容易被抽象成一个自定义 Hooks，组件中的状态和 UI 变得更为清晰和隔离。\n注意:\n避免在 循环/条件判断/嵌套函数 中调用 hooks，保证调用顺序的稳定；只有 函数定义组件 和 hooks 可以调用 hooks，避免在 类组件 或者 普通函数 中调用；不能在useEffect中使用useState，React 会报错提示；类组件不会被替换或废弃，不需要强制改造类组件，两种方式能并存；\n重要钩子\n状态钩子 (useState): 用于定义组件的 State，其到类定义中this.state的功能；\n// useState 只接受一个参数: 初始状态// 返回的是组件名和更改该组件对应的函数const[flag,setFlag]=useState(true);// 修改状态setFlag(false)// 上面的代码映射到类定义中:this.state={flag:true}constflag=this.state.flagconstsetFlag=(bool)=>{this.setState({flag:bool,})}\n生命周期钩子 (useEffect):\n类定义中有许多生命周期函数，而在 React Hooks 中也提供了一个相应的函数 (useEffect)，这里可以看做componentDidMount、componentDidUpdate和componentWillUnmount的结合。\nuseEffect(callback, [source])接受两个参数\ncallback: 钩子回调函数；source: 设置触发条件，仅当 source 发生改变时才会触发；useEffect钩子在没有传入[source]参数时，默认在每次 render 时都会优先调用上次保存的回调中返回的函数，后再重新调用回调；\nuseEffect(()=>{// 组件挂载后执行事件绑定console.log('on')addEventListener()// 组件 update 时会执行事件解绑return()=>{console.log('off')removeEventListener()}},[source]);// 每次 source 发生改变时，执行结果(以类定义的生命周期，便于大家理解):// --- DidMount ---// 'on'// --- DidUpdate ---// 'off'// 'on'// --- DidUpdate ---// 'off'// 'on'// --- WillUnmount ---// 'off'\n通过第二个参数，我们便可模拟出几个常用的生命周期:\ncomponentDidMount: 传入[]时，就只会在初始化时调用一次\nconst useMount = (fn) => useEffect(fn, [])\ncomponentWillUnmount: 传入[]，回调中的返回的函数也只会被最终执行一次\nconstuseUnmount=(fn)=>useEffect(()=>fn,[])\nmounted: 可以使用 useState 封装成一个高度可复用的 mounted 状态；\nconstuseMounted=()=>{const[mounted,setMounted]=useState(false);useEffect(()=>{!mounted&&setMounted(true);return()=>setMounted(false);},[]);returnmounted;}\ncomponentDidUpdate: useEffect每次均会执行，其实就是排除了 DidMount 后即可；\nconstmounted=useMounted()useEffect(()=>{mounted&&fn()})\n其它内置钩子:\nuseContext: 获取 context 对象useReducer: 类似于 Redux 思想的实现，但其并不足以替代 Redux，可以理解成一个组件内部的 redux:并不是持久化存储，会随着组件被销毁而销毁；属于组件内部，各个组件是相互隔离的，单纯用它并无法共享数据；配合useContext`的全局性，可以完成一个轻量级的 Redux；(easy-peasy)useCallback: 缓存回调函数，避免传入的回调每次都是新的函数实例而导致依赖组件重新渲染，具有性能优化的效果；useMemo: 用于缓存传入的 props，避免依赖的组件每次都重新渲染；useRef: 获取组件的真实节点；useLayoutEffectDOM更新同步钩子。用法与useEffect类似，只是区别于执行时间点的不同useEffect属于异步执行，并不会等待 DOM 真正渲染后执行，而useLayoutEffect则会真正渲染后才触发；可以获取更新后的 state；\n并不是持久化存储，会随着组件被销毁而销毁；属于组件内部，各个组件是相互隔离的，单纯用它并无法共享数据；配合useContext`的全局性，可以完成一个轻量级的 Redux；(easy-peasy)\nDOM更新同步钩子。用法与useEffect类似，只是区别于执行时间点的不同useEffect属于异步执行，并不会等待 DOM 真正渲染后执行，而useLayoutEffect则会真正渲染后才触发；可以获取更新后的 state；\n自定义钩子(useXxxxx): 基于 Hooks 可以引用其它 Hooks 这个特性，我们可以编写自定义钩子，如上面的useMounted。又例如，我们需要每个页面自定义标题:\nfunctionuseTitle(title){useEffect(()=>{document.title=title;});}// 使用:functionHome(){consttitle='我是首页'useTitle(title)return(<div>{title}</div>)}\nReact Hooks 的限制\n不要在循环、条件或嵌套函数中调用 Hook；在 React 的函数组件中调用Hook\n那为什么会有这样的限制呢？就得从 Hooks 的设计说起。Hooks 的设计初衷是为了改进 React 组件的开发模式。在旧有的开发模式下遇到了三个问题。\n组件之间难以复用状态逻辑。过去常见的解决方案是高阶组件、render props及状态管理框架。复杂的组件变得难以理解。生命周期函数与业务逻辑耦合太深，导致关联部分难以拆分。常见的有 this 的问题，但在 React 团队中还有类难以优化的问题，他们希望在编译优化层面做出一些改进。\n这三个问题在一定程度上阻碍了 React 的后续发展，所以为了解决这三个问题，Hooks 基于函数组件开始设计。然而第三个问题决定了 Hooks 只支持函数组件。\n那为什么不要在循环、条件或嵌套函数中调用 Hook 呢？因为 Hooks 的设计是基于数组实现。在调用时按顺序加入数组中，如果使用循环、条件或嵌套函数很有可能导致数组取值错位，执行错误的 Hook。当然，实质上 React 的源码里不是数组，是链表。\n这些限制会在编码上造成一定程度的心智负担，新手可能会写错，为了避免这样的情况，可以引入 ESLint 的 Hooks 检查插件进行预防。\nuseEffect 与 useLayoutEffect 区别在哪里\n它们的共同点很简单，底层的函数签名是完全一致的，都是调用的mountEffectImpl，在使用上也没什么差异，基本可以直接替换，也都是用于处理副作用。那不同点就很大了，useEffect在 React 的渲染过程中是被异步调用的，用于绝大多数场景，而LayoutEffect会在所有的 DOM 变更之后同步调用，主要用于处理 DOM 操作、调整样式、避免页面闪烁等问题。也正因为是同步处理，所以需要避免在LayoutEffect做计算量较大的耗时任务从而造成阻塞。在未来的趋势上，两个 API 是会长期共存的，暂时没有删减合并的计划，需要开发者根据场景去自行选择。React 团队的建议非常实用，如果实在分不清，先用useEffect，一般问题不大；如果页面有异常，再直接替换为useLayoutEffect即可。", "f": [{"q": "筛选面板只有展开时才执行useEffect订阅快捷键，收起时跳过；测试发现后面的useState偶尔读到别的状态，为什么不能把Hook放进if？", "a": "Hook依赖每次渲染中稳定的调用顺序，条件分支会使内部链表的取值位置发生错位。应始终调用Hook，把条件放到副作用内部，或把展开区域拆成独立组件；循环、嵌套函数中的条件调用同样存在风险。"}, {"q": "一个报表页把订阅、标题更新和请求状态全塞进类组件的多个生命周期，产品又要求三个页面复用；你会怎样用自定义Hook重组？", "a": "把彼此关联的状态和副作用分别抽成useSubscription、useTitle等自定义Hook，让每段业务逻辑在一个位置维护并跨组件复用。自定义Hook内仍可调用useState、useEffect，但自身也必须遵守顶层调用规则；拆得过碎会增加依赖关系的理解成本。"}, {"q": "聊天室的useEffect原来依赖[roomId]，有人为减少请求改成[]；用户在同一页面切换房间后仍收到旧房间消息，你会如何修正执行与清理时机？", "a": "订阅确实随roomId变化，因此依赖项应保留[roomId]。每次切换时，React会先执行上一次回调返回的退订函数，再建立新订阅，卸载时也会清理；改成[]只适用于整个挂载期间参数不会变化的副作用。"}, {"q": "线上弹窗打开时偶发绑定两次滚动监听，关闭后仍触发回调；相关代码用了没有依赖数组的useEffect，你会先查什么？", "a": "先检查副作用是否每次渲染都重新绑定，以及回调是否返回了与本次绑定对应的移除函数。没有依赖数组时，每次渲染会先清理旧副作用再执行新副作用；若清理缺失、函数引用不匹配或绑定逻辑不对称，就会残留监听。"}, {"q": "可视化页面需要读取更新后的DOM尺寸并立即调整样式，useEffect下会短暂闪动；同事提议所有副作用统一换成useLayoutEffect，你怎么取舍？", "a": "这类必须在DOM变更后同步测量并调整样式的逻辑适合useLayoutEffect，可减少绘制后的视觉闪动。普通订阅、日志和数据副作用仍优先使用useEffect；useLayoutEffect会同步阻塞后续绘制，放入耗时计算会拖慢页面呈现。"}, {"q": "编辑器把回调传给多个依赖组件，父组件每次渲染都创建新函数；评审同时提出useCallback、useMemo和useRef，三者在这里分别解决什么？", "a": "需要稳定回调引用时使用useCallback，需要缓存计算结果或传入属性时使用useMemo，访问真实节点或保存可变引用则使用useRef。它们用途不同，不能互相替代；缓存本身也增加依赖维护成本，应围绕明确的重复渲染或计算问题使用。"}]}, {"s": "React", "t": "受控组件和非受控组件", "m": "受控组件的值由React管理，非受控组件的值则由DOM自己保存。同时传入value和onChange时，组件的输入与更新都由外部控制；只传defaultValue，通常就是给非受控组件设置初始值。受控方式更方便统一管理和同步数据，非受控方式脱离了组件自身的state或props控制，一般不建议作为常规方案。\n<FInput value = {x} onChange = {fn} /> // 上面的是受控组件 下面的是非受控组件 <FInput defaultValue = {x} />\n当你一个组件同时传递一个value以及onChange事件时，它就是一个受控组件，收入输出都是我来控制的。第二个只是传递了默认的初时值，并没有传onchange事件，非受控组件是一种反模式，它的值不受组件自身的state或props控制", "f": [{"q": "登录页给<FInput value={name} />却没有传onChange，测试说输入框怎么敲都不变；开发认为删掉value就算修好，你怎么看？", "a": "传入value意味着显示值由外部控制，没有对应的onChange更新来源时，用户输入无法形成新的受控值。应补上变更回调并更新状态；若改用defaultValue，则只是交给组件内部维护，父级也就失去持续控制能力。"}, {"q": "后台编辑页有姓名、部门和角色三个字段，保存按钮必须随任一字段变化更新可用状态；你会让输入组件接收哪些属性？", "a": "这类实时联动适合受控方式：父级持有字段值，向每个输入传入value和onChange，变更后统一计算保存条件。数据入口和出口都由父级掌握，校验与提交更直接；代价是父级需要维护完整的状态更新链路。"}, {"q": "搜索框原来只需要一个初始关键词，改版后路由参数变化时必须立即覆盖输入内容；继续使用defaultValue={keyword}能满足吗？", "a": "defaultValue只表达初始值，后续路由参数变化不应依赖它持续驱动输入框。需要外部参数随时覆盖显示内容时，应改为value={keyword}并提供onChange更新来源；切换模式时还要避免同时混用两套值来源。"}, {"q": "线上表单从接口数据未返回时的undefined，切换成返回后的字符串，控制台提示输入组件控制方式发生变化；你会怎样定位？", "a": "先检查首屏是否没有传value，数据返回后才开始传入，导致组件从内部维护切到外部控制。若决定受控，应从首次渲染就提供稳定值，例如空字符串，并始终配套onChange；若决定非受控，则只传defaultValue，不要中途切换。"}, {"q": "内容发布页只在点击提交时读取标题，产品没有实时校验、联动或外部重置要求；受控和非受控该怎么选？", "a": "仅需初始值且提交时再读取的场景，可以采用defaultValue，让输入值不受父级state或props持续控制。若随后加入实时校验、跨字段联动或外部覆盖，就更适合受控方式；选择取决于谁必须成为稳定的数据源。"}]}, {"s": "React", "t": "如何避免ajax数据请求重新获取", "m": "避免重复请求的核心是缓存数据并对相同请求去重，通常优先交给React Query或SWR处理。它们能按请求键复用缓存、合并并发请求，并在数据过期后重新验证，比把服务端数据塞进Redux或Zustand更省维护成本。组件里还要正确设置useEffect依赖，并用AbortController取消过期请求；不常变化的接口也可以配合Cache-Control或ETag。\n核心思路是缓存 + 请求去重——把已经拿到的数据缓存起来，相同的请求不重复发。\n用数据请求库管理缓存（现代主线）：React Query / SWR内置了缓存、请求去重（同一时刻多个组件请求同一 key 只发一次）、后台重新验证（stale-while-revalidate）、失效控制。这是现在处理「服务端状态」的标准做法——避免重复请求本就是它们的核心能力，不用自己造轮子。放全局状态（旧做法）：把数据存进 Redux/Zustand，组件先读缓存、没有再请求。这是过去常见的做法，但要自己处理缓存失效、去重、更新，样板多——现在更推荐用 React Query 管服务端状态、全局 store 只放真正的客户端状态。请求去重（in-flight dedup）：对「正在进行中」的相同请求做去重——用一个 Map 缓存进行中的 Promise，相同 key 直接返回同一个 Promise，避免并发的重复请求。HTTP 缓存：对可缓存的接口配Cache-Control/ETag，让浏览器层面命中缓存(适合不常变的数据)。组件层面：用useEffect的依赖数组控制只在必要时请求、用AbortController取消过期请求避免竞态。\n一句话：优先用 React Query/SWR（自带缓存 + 去重 + 重验证），它们专门解决「服务端状态」的重复获取；全局 store 只放客户端状态；再配合请求去重和 HTTP 缓存。", "f": [{"q": "商品详情页三个区域同时请求/product?id=42，抓包看到首屏并发三次；只把最后一次结果存进缓存，为什么仍挡不住重复请求？", "a": "结果缓存只有请求完成后才能命中，三个调用在完成前仍会各自发出请求。请求层应按包含参数的key把进行中的Promise放进Map，后续调用复用它；结束后还要按缓存策略清理或转存结果，避免把失败请求永久保留。"}, {"q": "列表页返回时产品要求先展示旧数据、再静默校准库存，团队准备自己维护过期时间、重试和并发合并；你会怎么落地？", "a": "优先使用React Query或SWR，利用缓存先呈现已有数据，再通过后台重新验证更新库存。它们也覆盖请求去重与失效控制，能减少自建状态机；团队仍需定义合理的key和数据失效边界，否则不同筛选条件可能误用同一缓存。"}, {"q": "新闻接口从“几乎不变”改成“运营每分钟修订”，后端却设置了长时间Cache-Control；前端继续依赖浏览器缓存会有什么风险，怎么调整？", "a": "浏览器缓存适合不常变化的数据，更新频率提高后，过长的缓存时间会让用户持续看到旧内容。应由服务端调整Cache-Control，或使用ETag让浏览器重新验证；前端请求库的失效策略也要同步，否则两层缓存会产生不同的新鲜度判断。"}, {"q": "用户快速切换status筛选，慢的旧请求最后返回并覆盖了新结果；页面还偶发重复请求，你会按什么顺序排查？", "a": "先核对useEffect依赖是否准确反映status，避免无关渲染重复触发或漏掉必要请求。再用AbortController取消已经过期的请求，防止旧响应覆盖新状态；请求去重只能合并相同key，不能替代不同筛选条件间的竞态处理。"}, {"q": "架构评审争论把所有接口结果放进Redux，还是引入React Query；订单数据需要缓存、失效、重试，主题和侧栏状态只在客户端存在，你怎么划分？", "a": "订单数据属于服务端状态，优先交给React Query或SWR管理缓存、去重、重验证与失效。主题和侧栏这类客户端状态再放入全局store；把接口数据统一塞进Redux也能工作，但团队必须自行补齐更新和缓存生命周期。"}, {"q": "接口已经支持ETag，前端又用了SWR，同事认为两者重复只能留一个；在一次返回列表页的流程里，它们各自拦截什么？", "a": "SWR在应用层按key复用数据和进行中的请求，并可先展示旧数据后重新验证；ETag则让重新访问服务器时验证资源是否变化，可能避免重复传输完整响应。两层可以协作，但缓存时效要协调，否则应用层不发请求时不会触发HTTP验证。"}]}, {"s": "React", "t": "组件之间通信", "m": "组件通信通常按关系远近选择：父子之间用props和回调，跨多层用Context，复杂公共状态再考虑状态管理库。父组件可以把状态传给子组件，子组件则通过回调把输入或事件交回父组件，兄弟组件一般把状态提升到共同父级。主题、语言这类跨层信息适合Context；组件树两侧共享的复杂数据可用Redux等方案，但会增加学习和结构成本。需要父组件直接调用子组件方法时，也可以通过ref获取实例，不过这是比较特殊的通信方式。\n父子组件通信自定义事件redux和context\ncontext如何运用\n父组件向其下所有子孙组件传递信息如一些简单的信息：主题、语言复杂的公共信息用redux\n在跨层级通信中，主要分为一层或多层的情况\n如果只有一层，那么按照 React 的树形结构进行分类的话，主要有以下三种情况：父组件向子组件通信，子组件向父组件通信以及平级的兄弟组件间互相通信。在父与子的情况下，因为 React 的设计实际上就是传递Props即可。那么场景体现在容器组件与展示组件之间，通过Props传递state，让展示组件受控。在子与父的情况下，有两种方式，分别是回调函数与实例函数。回调函数，比如输入框向父级组件返回输入内容，按钮向父级组件传递点击事件等。实例函数的情况有些特别，主要是在父组件中通过 React 的 ref API 获取子组件的实例，然后是通过实例调用子组件的实例函数。这种方式在过去常见于 Modal 框的显示与隐藏多层级间的数据通信，有两种情况。第一种是一个容器中包含了多层子组件，需要最底部的子组件与顶部组件进行通信。在这种情况下，如果不断透传 Props 或回调函数，不仅代码层级太深，后续也很不好维护。第二种是两个组件不相关，在整个 React 的组件树的两侧，完全不相交。那么基于多层级间的通信一般有三个方案。第一个是使用 React 的Context API，最常见的用途是做语言包国际化第二个是使用全局变量与事件。第三个是使用状态管理框架，比如 Flux、Redux 及 Mobx。优点是由于引入了状态管理，使得项目的开发模式与代码结构得以约束，缺点是学习成本相对较高\n第一个是使用 React 的Context API，最常见的用途是做语言包国际化第二个是使用全局变量与事件。第三个是使用状态管理框架，比如 Flux、Redux 及 Mobx。优点是由于引入了状态管理，使得项目的开发模式与代码结构得以约束，缺点是学习成本相对较高", "f": [{"q": "商品卡片的收藏按钮点击后要通知父列表更新计数，开发准备用ref调子组件实例再反向取值；这条只有一层的通信为什么不合适？", "a": "一层子到父通信应由父组件传入回调，子组件在点击时把结果作为参数回传，数据流更清晰。通过ref获取实例并调用方法属于更特殊的命令式场景，过去常见于弹窗显隐；用它传普通业务数据会增加父子实现耦合。"}, {"q": "结算页的优惠金额由父组件计算，价格展示组件只负责渲染，数量组件还要把修改结果传回去；你会怎样设计属性边界？", "a": "父组件通过props把优惠金额和数量传给展示组件，再把数量变更回调传给数量组件。子组件触发回调后由父级更新状态并重新下发，形成单向数据流；不要让两个子组件私下同步副本，否则容易出现价格与数量不一致。"}, {"q": "主题值要从应用根节点传到第六层按钮，中间五层都不用它；设计师又要求切换主题后整棵页面同步，你会选Context还是逐层props？", "a": "这种面向一批子孙组件的简单公共信息适合Context，根节点提供主题，深层按钮直接消费，避免无意义的逐层透传。若信息演变为复杂且频繁变化的业务状态，则应重新评估状态管理框架；Context不等于所有全局状态的默认容器。"}, {"q": "两个位于组件树两侧的面板通过全局事件同步筛选条件，线上偶尔出现页面已卸载但监听仍执行；你会查哪里并考虑怎样替换？", "a": "先检查事件监听是否在挂载时注册、卸载时解除，以及同一组件是否重复注册。对于跨层级且互不相交的复杂共享状态，可改用Redux、Mobx等状态管理框架，让更新路径受到统一约束；全局事件虽直接，但生命周期和数据来源更难追踪。"}, {"q": "小型后台只有语言和主题两个公共值，架构师要求一开始就上Redux，另一位坚持所有层级都传props；你如何裁决？", "a": "语言和主题属于Context的典型用途，可由上层统一提供并让后代消费，不必为了简单信息引入完整状态管理框架。逐层props在层级很浅时仍最直观；只有公共状态复杂度和协作约束明显上升时，Redux等方案的结构化收益才更突出。"}, {"q": "父组件需要控制第三方弹窗的打开和关闭，但该弹窗只暴露实例方法；团队规定所有子到父通信都必须用回调，这里是否需要例外？", "a": "若第三方弹窗确实只提供命令式实例方法，父组件可通过React的ref获取实例并调用显示或隐藏方法。它适用于这类特殊控制，不应扩展成常规数据通信机制；能通过props和回调表达的状态，仍应优先保持声明式传递。"}]}, {"s": "React", "t": "类组件与函数组件有什么区别呢？", "m": "类组件和函数组件在展示及现代浏览器中的性能没有明显差异，真正不同的是开发时的心智模型。类组件围绕面向对象、this和生命周期组织代码，函数组件则更强调不可变数据、减少副作用，并通过Hooks组织和复用逻辑。性能优化时，类组件常用shouldComponentUpdate，函数组件可用React.memo；随着Hooks出现，函数组件已经能覆盖原先依赖生命周期的场景。组件设计也更推崇组合而非继承，因此函数组件通常更符合后续的并发与细粒度逻辑复用方向。\n作为组件而言，类组件与函数组件在使用与呈现上没有任何不同，性能上在现代浏览器中也不会有明显差异它们在开发时的心智模型上却存在巨大的差异。类组件是基于面向对象编程的，它主打的是继承、生命周期等核心概念；而函数组件内核是函数式编程，主打的是 immutable、没有副作用、引用透明等特点。之前，在使用场景上，如果存在需要使用生命周期的组件，那么主推类组件；设计模式上，如果需要使用继承，那么主推类组件。但现在由于 React Hooks 的推出，生命周期概念的淡出，函数组件可以完全取代类组件。其次继承并不是组件最佳的设计模式，官方更推崇“组合优于继承”的设计概念，所以类组件在这方面的优势也在淡出。性能优化上，类组件主要依靠shouldComponentUpdate阻断渲染来提升性能，而函数组件依靠React.memo缓存渲染结果来提升性能。从上手程度而言，类组件更容易上手，从未来趋势上看，由于React Hooks 的推出，函数组件成了社区未来主推的方案。类组件在未来时间切片与并发模式中，由于生命周期带来的复杂度，并不易于优化。而函数组件本身轻量简单，且在 Hooks 的基础上提供了比原先更细粒度的逻辑组织与复用，更能适应 React 的未来发展。", "f": [{"q": "商品详情页把类组件改成函数组件后，产品认为页面表现和性能都会明显提升，你会怎样纠正这个判断？", "a": "单纯改写组件形态通常不会带来可感知的展示或性能提升，两者作为组件时的使用结果并无本质差异。迁移价值主要来自Hooks带来的细粒度逻辑组织与复用，以及更适合后续并发能力的模型；若现有代码稳定且没有复用诉求，不应把重写包装成性能优化。"}, {"q": "一个类组件同时处理请求、订阅和表单校验，多个生命周期里散落着同一业务逻辑，准备迭代时你会怎样落地改造？", "a": "适合在本次业务迭代中改成函数组件，并按业务关注点拆出自定义Hook，让请求、订阅和校验各自聚合相关状态与清理逻辑。不要机械地把每个生命周期翻译成一个useEffect，否则只是换了语法；依赖项和副作用边界不清时，迁移后仍会难以维护。"}, {"q": "后台管理系统大量使用基类继承权限校验，新模块坚持继续继承，另一位开发主张全面改成组合，你会支持哪一边？", "a": "新模块应优先使用组合，把权限能力做成包装组件、Context或自定义Hook，而不是继续扩大组件继承层级。React的组件复用更适合“组合优于继承”，函数组件也因此削弱了类组件的传统优势；旧基类可渐进兼容，但一次性替换会扩大回归范围。"}, {"q": "函数组件接入实时行情后偶发重复订阅，类组件版本却正常；日志显示每次渲染后订阅数量都增加，你先查哪里？", "a": "先检查副作用是否写在渲染过程，或useEffect是否缺少正确依赖与清理函数，确保每次重新订阅前都会解除旧订阅。函数组件强调渲染无副作用，副作用应集中在对应的Hook中；若依赖对象每次都重新创建，还需稳定引用，否则清理正确也可能频繁重订阅。"}, {"q": "数据表格频繁刷新，类组件方案要写shouldComponentUpdate，函数组件方案准备套React.memo，你会如何决定并验证？", "a": "两种方案都能阻断不必要的渲染：类组件使用shouldComponentUpdate，函数组件通常使用React.memo缓存渲染结果。应先确认瓶颈来自重复渲染，再保证传入的对象和回调引用足够稳定；盲目增加比较逻辑也有成本，且无法修复组件内部昂贵计算。"}]}, {"s": "React", "t": "如何设计React组件", "m": "设计React组件的核心是单一职责、接口精简，并根据业务边界合理分层。我一般把纯展示和通用能力放进基础组件，把状态与业务逻辑留给容器组件或页面，避免为了复用而过度抽象。组件之间优先通过props、children等方式组合，接口命名要有语义，也要考虑受控与非受控场景。基础组件还应补齐TypeScript类型、无障碍能力、单测和Storybook文档。\nReact 组件应从设计与工程实践两个方向进行探讨\n从设计上而言，社区主流分类的方案是展示组件与灵巧组件\n展示组件内部没有状态管理，仅仅用于最简单的展示表达。展示组件中最基础的一类组件称作代理组件。代理组件常用于封装常用属性、减少重复代码。很经典的场景就是引入 Antd 的 Button 时，你再自己封一层。如果未来需要替换掉 Antd 或者需要在所有的 Button 上添加一个属性，都会非常方便。基于代理组件的思想还可以继续分类，分为样式组件与布局组件两种，分别是将样式与布局内聚在自己组件内部。从工程实践而言，通过文件夹划分的方式切分代码。我初步常用的分割方式是将页面单独建立一个目录，将复用性略高的 components 建立一个目录，在下面分别建立 basic、container 和 hoc 三类。这样可以保证无法复用的业务逻辑代码尽量留在 Page 中，而可以抽象复用的部分放入 components 中。其中 basic 文件夹放展示组件，由于展示组件本身与业务关联性较低，所以可以使用 Storybook 进行组件的开发管理，提升项目的工程化管理能力", "f": [{"q": "订单页把请求、权限判断和几十行表格结构都写进一个所谓“展示组件”，评审时你会指出什么设计矛盾？", "a": "它已经不是纯展示组件，因为数据获取和权限判断属于业务协调或状态管理，应该由页面或容器层承担。展示组件只接收明确数据并表达界面，才能低成本复用和独立验证；如果强行追求绝对无状态，也可能把紧密相关的交互拆得过散。"}, {"q": "六个页面都直接使用Antd Button，现在设计要求统一补埋点并可能更换组件库，你会怎样调整目录和依赖？", "a": "应增加项目自己的按钮代理组件，集中封装常用属性、样式和埋点，再让业务页面依赖这层稳定接口。通用展示组件可放入components/basic并用Storybook管理；代理层若暴露过多底层专有属性，未来替换组件库仍会产生大面积修改。"}, {"q": "团队规定所有组件必须放进公共components，结果两百多个只服务单一页面的组件也被共享，你会如何重新划分？", "a": "无法复用且高度依赖业务流程的代码应留在对应Page目录，只有抽象稳定、复用性较高的内容才进入公共组件目录。可进一步按basic、container和hoc等职责分类，使依赖方向清楚；过早公共化会固化尚未稳定的接口，并增加跨页面修改风险。"}, {"q": "改动会员卡样式后十几个页面错位，排查发现每页都复制了相同布局和间距，你会怎样止损并定位抽象边界？", "a": "先把重复的样式与布局收敛为样式组件或布局组件，再逐页替换，避免继续复制并分别修补。组件内部应内聚稳定的视觉规则，页面只提供业务内容；若不同页面的结构差异已经超过共同点，硬合并会制造大量条件属性，应保留少量明确变体。"}, {"q": "搜索页组件已经拆成三十个文件，阅读一次交互要跨越多层props，但架构师仍要求继续细拆，你会用什么标准取舍？", "a": "拆分应围绕职责、复用价值和封装边界，而不是文件数量或代码行数。稳定的展示单元可进入basic，负责数据与状态协调的部分留在容器或页面层；粒度过细会让父子关系和数据传递更难理解，粒度过粗则会把业务逻辑与展示再次绑死。"}]}, {"s": "React", "t": "组件的协同及（不）可控组件", "m": "组件协同本质上是组织父子关系和复用公共逻辑，而可控与不可控的区别在于状态由谁管理。父组件可以通过嵌套和props统一管理子组件，也可以抽离多个组件共有的方法，减少重复代码，但抽象过多会让逻辑分散、维护难度上升。可控组件的值来自外部状态，符合React数据流，也更方便读取和处理。不可控组件通常用defaultValue初始化，后续需要借助ref获取实际值。\n为什么要进行组件的协同\n我们在实际的开发项目的时候，不会只用几个组件，有时候遇到大型的项目，可能会有成千上百的组件，难免会遇到有功能重复的组件。要进行修改，就会修改大部分的文件。所以我们需要进行组件的协同开发。\n什么是组件的协同使用？\n组件的协同本质上是对组件的一种组织、管理的方式。目的：逻辑清晰：这是组件与组件之间的逻辑代码模块化封装细节：像面向对象一样将常用的方法以及数据封装起来提高代码的复用性：因为是组件，相当于一个封装好的东西，用的时候直接调用\n逻辑清晰：这是组件与组件之间的逻辑代码模块化封装细节：像面向对象一样将常用的方法以及数据封装起来提高代码的复用性：因为是组件，相当于一个封装好的东西，用的时候直接调用\n如何实现组件的协同使用\n第一种：增加一个父组件，将其他的组件进行嵌套，更多的是实现代码的封装第二种：通过一些操作从后台获取数据，React中的Mixin，更多的是实现代码的复用\n组件嵌套的含义\n组件嵌套的本质是父子关系\n组件嵌套的优缺点\n优点：逻辑清晰：父子关系类似于人类中的父子关系模块化开发：每个模块对应一个功能，不同的模块可以同步开发封装细节：开发者必须要关注组件的功能，不需要了解细节缺点：编写难度高：父子组件的关系需要经过深思熟虑，贸然编写可能导致关系混乱，代码难以维护无法掌握所有细节：使用者只知道组件的用法，不知道实现细节，遇到问题难以修复\n逻辑清晰：父子关系类似于人类中的父子关系模块化开发：每个模块对应一个功能，不同的模块可以同步开发封装细节：开发者必须要关注组件的功能，不需要了解细节\n编写难度高：父子组件的关系需要经过深思熟虑，贸然编写可能导致关系混乱，代码难以维护无法掌握所有细节：使用者只知道组件的用法，不知道实现细节，遇到问题难以修复\nMixin\nMixin的含义\nMixin=一组方法。他的目的是横向抽离出组件的相似代码，把组件的共同作用以及效果的代码提出来\nMixin的优缺点\n优点代码复用：抽离出通用的代码，减少开发成本，提高开发效率即插即用：可以使用许多现有的Mixin来开发自己的代码适应性强：改动一次代码，影响多个组件缺点编写难度高：Mixin可能被用在各种环境中，想要兼容多种环境就需要更多的 - 码与逻辑，通用的代价是提高复杂度降低代码的可读性：组件的优势在于将逻辑与是界面直接结合在一起，Mixin本质上会分散逻辑，理解起来难度大\n代码复用：抽离出通用的代码，减少开发成本，提高开发效率即插即用：可以使用许多现有的Mixin来开发自己的代码适应性强：改动一次代码，影响多个组件\n编写难度高：Mixin可能被用在各种环境中，想要兼容多种环境就需要更多的 - 码与逻辑，通用的代价是提高复杂度降低代码的可读性：组件的优势在于将逻辑与是界面直接结合在一起，Mixin本质上会分散逻辑，理解起来难度大\n不可控组件\n上图：defaultValue的值是固定的，这就是一个不可控组件如果要获取input的value值，只有使用ref获取节点来获取值\n可控组件\ndefaultValue的值是根据状态确定了，只需要拿到this.state.value的值就可以了这里需要注意一下：使用value的值是不可修改的，defaultValue的值是可以修改的\n可控组件的优点\n符合React的数据流数据存储在state中，便于获取便于处理数据", "f": [{"q": "注册页的手机号输入框传了defaultValue，用户编辑后父组件更新默认值，界面却没有跟着变化，为什么？", "a": "defaultValue只负责非受控输入框的初始值，挂载后输入内容由DOM自己维护，父组件随后修改默认值不会持续驱动界面。若业务要求外部状态始终决定内容，应改用value配合onChange；这样也意味着父组件必须及时更新状态，否则输入框会表现为不可编辑。"}, {"q": "结算页要求地址输入变化后立即联动运费、校验和提交按钮状态，你会选受控还是非受控组件？", "a": "应优先选择受控组件，把输入值保存在React的state中，并通过value和变更回调形成单向数据流。这样联动逻辑能读取同一份确定数据，也便于统一校验；字段数量很大或变更非常频繁时，需要控制状态作用域，避免无关区域跟随渲染。"}, {"q": "一个文件上传控件需要读取原生节点状态，团队却要求所有表单项都必须用value控制，你会怎样处理这组选型冲突？", "a": "不必把所有输入都强行设计成受控模式，无法由普通value完整驱动或更适合读取DOM状态的控件可使用非受控方案，并通过ref获取节点值。组件应封装这些实现细节，对外暴露清晰事件；代价是数据不再始终位于React状态中，联动和即时校验会更困难。"}, {"q": "筛选面板上线后偶发出现“输入框显示值与提交参数不一致”，代码同时通过ref读DOM，又在state中保存一份值，你先怎么排查？", "a": "先确认界面的唯一数据源，检查渲染使用的是value、defaultValue还是DOM当前值，并对照提交路径读取了哪一份数据。受控和非受控机制混用容易产生两个不同步的状态源；应统一为state驱动，或明确只在提交时通过ref读取，避免双向同步。"}, {"q": "十几个组件都要复用请求和日志逻辑，旧方案准备继续追加Mixin，新方案主张父组件协调，你会如何取舍？", "a": "组件间的结构协作可由父组件嵌套和组织，使父子关系、数据来源与封装边界保持清晰。Mixin能横向复用一组方法，但会把逻辑分散到组件之外，兼容场景越多复杂度越高；遗留代码可维护，新增逻辑则应优先选择边界更显式的组合方式。"}]}, {"s": "React", "t": "React-Router 的实现原理及工作方式分别是什么", "m": "React Router的实现原理是监听地址变化并匹配对应组件，工作时则通过上下文连接路由容器、匹配组件和导航组件。Hash模式依赖哈希变化，History模式使用pushState、replaceState等浏览器API，后者还要求服务端配置historyApiFallback。浏览器端使用History API，原生端可以使用基于数组维护记录的内存历史。像Router提供上下文，Route负责匹配，Link则完成平台相关的导航。\nReact Router路由的基础实现原理分为两种，如果是切换 Hash的方式，那么依靠浏览器Hash变化即可；如果是切换网址中的Path，就要用到HTML5 History API中的pushState、replaceState等。在使用这个方式时，还需要在服务端完成historyApiFallback配置在React Router内部主要依靠history库完成，这是由React Router自己封装的库，为了实现跨平台运行的特性，内部提供两套基础history，一套是直接使用浏览器的History API，用于支持react-router-dom；另一套是基于内存实现的版本，这是自己做的一个数组，用于支持react-router-native。React Router的工作方式可以分为设计模式与关键模块两个部分。从设计模式的角度出发，在架构上通过Monorepo进行库的管理。Monorepo具有团队间透明、迭代便利的优点。其次在整体的数据通信上使用了 Context API 完成上下文传递。在关键模块上，主要分为三类组件：第一类是 Context 容器，比如 Router 与 MemoryRouter；第二类是消费者组件，用以匹配路由，主要有 Route、Redirect、Switch 等；第三类是与平台关联的功能组件，比如Link、NavLink、DeepLinking等。\nReact router原理分析(opens new window)", "f": [{"q": "运营把商品详情从/product/42改成#/product/42后，认为两种地址只是写法不同，你会怎样解释底层差异？", "a": "Hash路由依赖地址中散列部分的变化，而普通Path路由依赖HTML5HistoryAPI的pushState、replaceState等能力。两者都能驱动客户端匹配组件，但服务器参与程度不同；选择Path后若没有回退配置，直接访问深层地址可能由服务器返回404。"}, {"q": "内容站准备从HashRouter切到正常路径，上线前除了替换路由容器，你会要求服务端完成什么配置？", "a": "服务端需要配置historyApiFallback或等价回退规则，让未知的前端路径仍返回应用入口，再由ReactRouter完成匹配。还要排除静态资源和真实接口路径，不能把所有请求无条件重写；否则深层链接虽能打开，资源请求或后端错误却可能被错误地返回HTML。"}, {"q": "同一套路由配置既要跑浏览器端管理后台，又要跑不具备浏览器HistoryAPI的测试环境，历史记录层怎么选？", "a": "浏览器环境使用基于HistoryAPI的实现，非浏览器场景可使用内存历史记录，以数组维护位置变化。ReactRouter通过不同的history基础实现支持跨平台，上层匹配逻辑可以保持接近；内存实现不会天然改变真实地址栏，不能把测试结果直接等同于浏览器导航行为。"}, {"q": "用户直接打开/orders/1001得到服务器404，但先访问首页再点Link却正常，你会如何定位？", "a": "现象说明客户端路由匹配大概率正常，故障位于服务器对深层路径的处理，应先检查入口回退配置和部署平台重写规则。Link在应用内通过HistoryAPI切换，因此不会重新请求对应文档；修复时需保留API、资源和真实文件的优先匹配，避免过度回退。"}, {"q": "微前端页面需要共享当前位置，团队争论是层层传location，还是让路由组件直接读取，你会怎么设计？", "a": "路由容器应通过Context API传递位置和导航上下文，负责匹配的消费者组件按需读取，避免每层手工转发。整体可区分Router、MemoryRouter等容器，路由匹配组件，以及Link、NavLink等平台功能组件；多个应用根并存时仍要明确各自上下文边界。"}]}, {"s": "React", "t": "React 17 带来了哪些改变", "m": "React 17的关键变化是更新JSX转换、重构事件系统，并引入更细致的Lane优先级模型。新转换会从react/jsx-runtime自动引入解析能力，因此使用JSX时不再必须手动导入React，编译输出也有简化空间。事件委托从全局document下移到各自的根容器，让多个React应用之间的影响更可控，同时取消事件池，异步访问事件不再需要e.persist()。源码调度方面，Lane用二进制位表达优先级，比原来的expirationTime能覆盖更多边界情况。\n最重要的是以下三点：\n新的JSX转换逻辑事件系统重构Lane 模型的引入\n1. 重构 JSX 转换逻辑\n在过去，如果我们在 React 项目中写入下面这样的代码：\nfunction MyComponent() { return <p>这是我的组件</p> }\nReact 是会报错的，原因是 React 中对 JSX 代码的转换依赖的是React.createElement这个函数。因此但凡我们在代码中包含了 JSX，那么就必须在文件中引入 React，像下面这样：\nimportReactfrom'react';functionMyComponent(){return<p>这是我的组件</p>}\n而React 17 则允许我们在不引入 React 的情况下直接使用 JSX。这是因为在 React 17 中，编译器会自动帮我们引入 JSX 的解析器，也就是说像下面这样一段逻辑：\nfunctionMyComponent(){return<p>这是我的组件</p>}\n会被编译器转换成这个样子：\nimport{jsxas_jsx}from'react/jsx-runtime';functionMyComponent(){return_jsx('p',{children:'这是我的组件'});}\nreact/jsx-runtime中的 JSX 解析器将取代React.createElement完成JSX的编译工作，这个过程对开发者而言是自动化、无感知的。因此，新的 JSX 转换逻辑带来的最显著的改变就是降低了开发者的学习成本。\nreact/jsx-runtime中的 JSX 解析器看上去似乎在调用姿势上和React.createElement区别不大，那么它是否只是React.createElement换了个马甲呢？当然不是，它在内部实现了React.createElement无法做到的性能优化和简化。在一定情况下，它可能会略微改善编译输出内容的大小\n2. 事件系统重构\n事件系统在 React 17 中的重构要从以下两个方面来看：\n卸掉历史包袱拥抱新的潮流\n2.1 卸掉历史包袱：放弃利用 document 来做事件的中心化管控\nReact 16.13.x 版本中的事件系统会通过将所有事件冒泡到 document 来实现对事件的中心化管控\n这样的做法虽然看上去已经足够巧妙，但仍然有它不聪明的地方——document 是整个文档树的根节点，操作 document 带来的影响范围实在是太大了，这将会使事情变得更加不可控\n在 React 17 中，React 团队终于正面解决了这个问题：事件的中心化管控不会再全部依赖document，管控相关的逻辑被转移到了每个 React 组件自己的容器 DOM 节点中。比如说我们在 ID 为 root 的 DOM 节点下挂载了一个 React 组件，像下面代码这样：\nconstrootElement=document.getElementById(\"root\");// 这里沿用 React 17 的写法说明事件挂载位置的变化；React 18 起写作// ReactDOM.createRoot(rootElement).render(<App />)，事件同样挂在这个容器节点上ReactDOM.render(<App/>,rootElement);\n那么事件管控相关的逻辑就会被安装到root 节点上去。这样一来， React 组件就能够自己玩自己的，再也无法对全局的事件流构成威胁了\n2.2 拥抱新的潮流：放弃事件池\n在 React 17 之前，合成事件对象会被放进一个叫作“事件池”的地方统一管理。这样做的目的是能够实现事件对象的复用，进而提高性能：每当事件处理函数执行完毕后，其对应的合成事件对象内部的所有属性都会被置空，意在为下一次被复用做准备。这也就意味着事件逻辑一旦执行完毕，我们就拿不到事件对象了，React 官方给出的这个例子就很能说明问题，请看下面这个代码\nfunctionhandleChange(e){// This won't work because the event object gets reused.setTimeout(()=>{console.log(e.target.value);// Too late!},100);}\n异步执行的setTimeout回调会在handleChange这个事件处理函数执行完毕后执行，因此它拿不到想要的那个事件对象e。\n要想拿到目标事件对象，必须显式地告诉 React——我永远需要它，也就是调用e.persist()函数，像下面这样：\nfunctionhandleChange(e){// Prevents React from resetting its properties:e.persist();setTimeout(()=>{console.log(e.target.value);// Works},100);}\n在 React 17 中，我们不需要e.persist()，也可以随时随地访问我们想要的事件对象。\n3. Lane 模型的引入\n初学 React 源码的同学由此可能会很自然地认为：优先级就应该是用 Lane 来处理的。但事实上，React 16 中处理优先级采用的是 expirationTime 模型。\nexpirationTime模型使用expirationTime（一个时间长度） 来描述任务的优先级；而Lane 模型则使用二进制数来表示任务的优先级：\nlane 模型通过将不同优先级赋值给一个位，通过31 位的位运算来操作优先级。\nLane 模型提供了一个新的优先级排序的思路，相对于expirationTime来说，它对优先级的处理会更细腻，能够覆盖更多的边界条件。", "f": [{"q": "一个只写<Card />的文件删掉import React from 'react'后仍能编译，同事认为浏览器已经原生支持JSX，你会怎样纠正？", "a": "不是浏览器直接执行JSX，而是新的JSX转换由编译器自动引入react/jsx-runtime，再生成相应的函数调用。它因此不再要求每个JSX文件显式导入React，并可能简化编译输出；若构建工具尚未启用新转换，删除导入仍可能导致编译失败。"}, {"q": "大型页面里同时挂载两个独立React根节点，升级到React17 后事件隔离为什么比旧版本更容易控制？", "a": "React17 将事件的中心化管控从全局document移到各自的根容器节点，因此不同React根对全局事件流的影响更小。工程上仍要检查原生事件监听、冒泡边界和第三方脚本；容器级委托降低了相互干扰，并不代表所有跨根事件冲突都会自动消失。"}, {"q": "表单代码在事件回调结束后通过setTimeout读取e.target.value，React16 需要e.persist()，升级到React17 后要怎么处理？", "a": "React17 放弃了合成事件池，异步回调可以继续访问事件对象，因此不再需要为此调用e.persist()。迁移时可清理依赖事件复用机制的兼容代码，但仍应注意目标DOM是否已被移除；取消事件池只保证对象不被统一置空，不保证相关节点永远存在。"}, {"q": "升级React17 后点击事件完全不触发，控制台也没有业务异常；页面由脚本动态替换过root容器，你先查什么？", "a": "先确认React实际挂载的根容器是否仍在文档中，以及动态替换节点后是否重新完成挂载，因为事件委托现在安装在该容器上。再检查容器附近的原生监听器是否阻止传播；继续只排查document上的React事件监听会偏离方向，但全局第三方监听仍可能造成干扰。"}, {"q": "调度模块准备继续用单一过期时间表达所有任务优先级，源码维护者建议切到Lane模型，你如何说明取舍？", "a": "React17 引入的Lane模型使用二进制位表示不同优先级，并通过位运算组合和处理任务，比单一expirationTime能表达更细腻的优先级关系。它为更多调度边界提供了空间，但属于内部复杂度的增加；业务代码不应依赖具体位值，也不能把升级直接等同于页面必然变快。"}]}, {"s": "性能", "t": "DNS 预解析", "m": "DNS预解析就是提前把域名解析成对应的IP，减少真正请求资源时等待解析的时间。浏览器访问外部域名之前通常需要先完成解析，因此预先处理可以让后续请求更早发出。比如页面确定会访问blog.poetries.top，可以通过<link rel=\"dns-prefetch\" href=\"//blog.poetries.top\">提前解析。\nDNS解析也是需要时间的，可以通过预解析的方式来预先获得域名所对应的IP\n<linkrel=\"dns-prefetch\"href=\"//blog.poetries.top\">", "f": [{"q": "商品页接入地图、客服、埋点和两个CDN，产品要求给全部第三方域名都加dns-prefetch，你会直接照做吗？", "a": "不会直接给所有域名都加，而是只保留当前页面较可能请求的域名。dns-prefetch只是提前完成域名到IP的解析，若用户根本不会触发客服或地图，请求前的准备工作就可能白做；候选域名过多时，还应通过实际请求链路筛选。"}, {"q": "首屏图片来自img.example.com，你准备怎样在页面里落地DNS预解析，并确认写法指向正确域名？", "a": "可在文档头部加入<link rel=\"dns-prefetch\" href=\"//img.example.com\">，让浏览器提前解析该图片域名。配置必须写资源实际访问的主机名，不能只写主站域名；若图片后来改到其他域名，这条提示不会替新域名完成解析。"}, {"q": "同一套站点内网环境直接访问固定IP，公网环境则通过多个域名加载资源，平台团队想统一保留dns-prefetch，是否都有收益？", "a": "固定IP的请求不存在域名到IP的解析过程，因此dns-prefetch对它没有对应收益；公网域名请求才可能减少后续等待DNS的时间。统一模板可以保留条件配置，但不能把该提示当成所有网络环境都有效的加速手段。"}, {"q": "活动页已经加了dns-prefetch，线上瀑布图里接口请求仍长时间等待，开发因此认定预解析失效，你会先查什么？", "a": "先确认<link>中的主机名是否与接口最终请求域名一致，并区分耗时究竟发生在DNS、连接还是服务端响应阶段。dns-prefetch只预先获得域名对应的IP，无法消除连接建立、重定向或接口处理耗时；若DNS本身不慢，整体变化可能并不明显。"}, {"q": "支付页只有一个首屏接口域名，但架构师希望用dns-prefetch解决整条请求链路延迟，你如何界定这个方案？", "a": "它只能针对请求前的DNS解析环节，适合域名确定且稍后会访问的场景，不能替代接口、网络连接或服务端优化。应先按请求阶段定位瓶颈，再决定是否加入预解析；若主要耗时不在DNS，继续堆叠该标签只会掩盖真正原因。"}]}, {"s": "性能", "t": "缓存", "m": "浏览器缓存主要分为强缓存和协商缓存，区别在于资源复用前是否还要向服务器发请求。强缓存命中时直接使用本地资源，常用Cache-Control，其优先级高于Expires；协商缓存则携带ETag或修改时间验证，未变化时返回304。频繁变化的资源可用no-cache配合ETag，代码文件通常设置长期缓存并通过文件指纹更新，不应缓存的资源则用no-store。\n缓存对于前端性能优化来说是个很重要的点，良好的缓存策略可以降低资源的重复加载提高网页的整体加载速度通常浏览器缓存策略分为两种：强缓存和协商缓存\n强缓存\n实现强缓存可以通过两种响应头实现：Expires和Cache-Control。强缓存表示在缓存期间不需要请求，state code为200\nExpires: Wed, 22 Oct 2018 08:41:00 GMT\nExpires是HTTP / 1.0的产物，表示资源会在Wed, 22 Oct 2018 08:41:00 GMT后过期，需要再次请求。并且Expires受限于本地时间，如果修改了本地时间，可能会造成缓存失效\nCache-control: max-age=30\nCache-Control出现于HTTP / 1.1，优先级高于Expires。该属性表示资源会在30秒后过期，需要再次请求\n协商缓存\n如果缓存过期了，我们就可以使用协商缓存来解决问题。协商缓存需要请求，如果缓存有效会返回 304协商缓存需要客户端和服务端共同实现，和强缓存一样，也有两种实现方式\nLast-Modified和If-Modified-Since\nLast-Modified表示本地文件最后修改日期，If-Modified-Since会将Last-Modified的值发送给服务器，询问服务器在该日期后资源是否有更新，有更新的话就会将新的资源发送回来但是如果在本地打开缓存文件，就会造成Last-Modified被修改，所以在HTTP / 1.1出现了ETag\nETag和If-None-Match\nETag类似于文件指纹，If-None-Match会将当前ETag发送给服务器，询问该资源 ETag 是否变动，有变动的话就将新的资源发送回来。并且ETag优先级比Last-Modified高\n选择合适的缓存策略\n对于大部分的场景都可以使用强缓存配合协商缓存解决，但是在一些特殊的地方可能需要选择特殊的缓存策略\n对于某些不需要缓存的资源，可以使用Cache-control: no-store，表示该资源不需要缓存对于频繁变动的资源，可以使用Cache-Control: no-cache并配合ETag使用，表示该资源已被缓存，但是每次都会发送请求询问资源是否更新。对于代码文件来说，通常使用Cache-Control: max-age=31536000并配合策略缓存使用，然后对文件进行指纹处理，一旦文件名变动就会立刻下载新的文件", "f": [{"q": "运营后台的配置接口每天变更多次，后端却给它设置一年max-age，前端说再配ETag就能保证实时，你认可吗？", "a": "不认可，因为强缓存有效期内浏览器通常不会发起请求，也就没有机会用ETag校验。频繁变动且仍允许保存副本的资源更适合Cache-Control: no-cache配合ETag；若内容根本不应落入缓存，则应使用no-store，代价是每次都要完整请求。"}, {"q": "构建后的脚本文件名带内容指纹，首页文档仍可能更新，你会怎样分别配置两类资源的缓存？", "a": "带指纹的代码文件可设置Cache-Control: max-age=31536000，内容变化后通过新文件名触发重新下载。入口文档若频繁变化，可采用no-cache并配合ETag做协商校验；若入口也长期强缓存，用户可能持续拿到引用旧资源的文档。"}, {"q": "公司代理仍会经过只理解部分旧缓存规则的链路，服务端同时返回Expires和Cache-Control，浏览器应以谁为准？", "a": "在支持HTTP/1.1缓存语义的客户端中，Cache-Control的优先级高于Expires，应以其指令和max-age为主要依据。Expires依赖绝对时间且受本地时间影响，可作为兼容信息保留；两者冲突会增加排查成本，配置时应保持意图一致。"}, {"q": "用户反馈刚发布的app.js仍是旧内容，网络面板显示200且请求没有发到服务器，你会怎样定位？", "a": "这更像资源命中了未过期的强缓存，应先检查响应中的Cache-Control、Expires、实际URL和文件名是否变化。强缓存期间不会发起校验请求，状态表现仍可能是200；若固定URL配了长max-age，应改用内容指纹，而不是期待ETag立即纠正。"}, {"q": "图片接口返回内容很大但一周才变化一次，团队在Last-Modified与ETag之间争论，你会如何选择？", "a": "两者都能用于协商缓存，但ETag更像资源指纹，且校验优先级高于Last-Modified，更适合需要准确判断内容是否变化的资源。Last-Modified依赖修改时间，本地打开缓存文件还可能改变该时间；采用ETag会增加服务端生成和维护标识的工作。"}, {"q": "安全团队要求登录后的敏感响应不能保存，性能团队却想用no-cache减少传输，你如何裁决？", "a": "若要求是不允许存储响应，应使用Cache-Control: no-store，因为no-cache仍允许缓存，只是每次使用前都要向服务器验证。性能团队可把协商缓存留给允许保存且频繁变化的普通资源；敏感数据场景需要接受重复传输带来的成本。"}]}, {"s": "性能", "t": "使用 HTTP / 2.0", "m": "HTTP/2的核心改进是多路复用，让多个请求可以共用同一个TCP连接。在HTTP/1.1下，请求受浏览器并发限制，连接建立和断开会消耗多个RTT，大文件还会受到TCP慢启动影响。HTTP/2通过复用连接减少这类等待，同时使用头部压缩降低请求数据量，因此更适合资源请求较多的页面。\n因为浏览器会有并发请求限制，在HTTP / 1.1时代，每个请求都需要建立和断开，消耗了好几个RTT时间，并且由于TCP慢启动的原因，加载体积大的文件会需要更多的时间在HTTP / 2.0中引入了多路复用，能够让多个请求使用同一个TCP链接，极大的加快了网页的加载速度。并且还支持Header压缩，进一步的减少了请求的数据大小", "f": [{"q": "新闻首页把二十个接口迁到HTTP/2后，开发认为每个请求从此都不再有网络成本，你会怎样纠正？", "a": "HTTP/2的收益在于多个请求可复用同一条TCP连接，并通过头部压缩减少传输数据，并不意味着请求本身没有成本。接口响应体、服务端处理和网络往返仍然存在；若瓶颈不在连接并发或请求头，迁移后的体感提升可能有限。"}, {"q": "一个首屏要并发加载十个脚本和六张图片，你会用什么证据确认线上确实按HTTP/2复用了连接？", "a": "应在浏览器网络面板或抓包结果中核对协议，并观察同一主机的多个请求是否共享连接，而不是只看服务器配置文件。只有客户端与服务端实际使用HTTP/2，多路复用和头部压缩才会生效；若链路中间环节回落，页面仍会表现为旧协议路径。"}, {"q": "站点只有一个体积较大的下载文件，没有并发小请求，负责人仍把升级HTTP/2当作唯一优化项，你会同意吗？", "a": "不会把它视为唯一方案，因为多路复用主要改善多个请求共享连接的场景，单个大文件仍受传输速率和TCP慢启动等因素影响。升级协议可能保留其他收益，但还应检查文件体积与传输链路；否则协议变化不一定解决主要耗时。"}, {"q": "网关宣称已启用HTTP/2，但页面仍出现大量连接建立时间和并发等待，你会按什么顺序排查？", "a": "先核对浏览器实际协商出的协议，再检查资源是否分散在不同主机，因为连接复用依赖实际使用的连接范围。随后区分等待来自旧协议并发限制、连接建立还是服务端响应；仅网关支持不代表每个资源请求都已通过同一条HTTP/2连接。"}, {"q": "老项目为绕过HTTP/1.1并发限制，把静态资源拆到多个域名；迁移HTTP/2后是否应立即合并？", "a": "多域名会形成不同连接，无法让所有资源共享同一条TCP连接，因此应重新评估拆分是否仍有价值。是否合并还要结合部署、缓存和故障隔离约束验证，不能只改域名；若资源仍跨主机，多路复用的覆盖范围自然会受限。"}]}, {"s": "性能", "t": "预加载", "m": "预加载适合那些暂时不用、但希望浏览器尽早获取的重要资源。它本质上是一种声明式的fetch，通过<link rel=\"preload\">强制浏览器提前发起请求，而且不会阻塞onload事件。合理使用可以降低首屏加载时间，例如提前获取后续会用到的重要文件；不过需要留意浏览器兼容性，不能把它当成所有环境都可靠的默认方案。\n在开发中，可能会遇到这样的情况。有些资源不需要马上用到，但是希望尽早获取，这时候就可以使用预加载预加载其实是声明式的fetch，强制浏览器请求资源，并且不会阻塞onload事件，可以使用以下代码开启预加载\n<linkrel=\"preload\"href=\"http://example.com\">\n预加载可以一定程度上降低首屏的加载时间，因为可以将一些不影响首屏但重要的文件延后加载，唯一缺点就是兼容性不好", "f": [{"q": "详情页有一个用户可能点击后才播放的视频，产品要求首屏用preload强制下载，理由是它不会阻塞onload，你接受吗？", "a": "不能仅凭不阻塞onload就预加载，因为preload会强制浏览器请求资源，用户不播放时可能产生无效下载。它更适合当前页面确定需要且希望尽早取得的资源；对低概率交互内容，应结合使用概率和资源成本再决定。"}, {"q": "首屏确定要执行的脚本发现得很晚，你准备如何用HTML声明预加载，并避免链接指向错误资源？", "a": "可在文档中加入<link rel=\"preload\" href=\"http://example.com/app.js\">，让浏览器更早发起该资源请求。落地时必须保证href与页面最终使用的资源一致，并在目标浏览器中验证兼容性；地址不一致会形成额外请求，兼容性不足时也不能依赖它保证效果。"}, {"q": "内嵌WebView的浏览器版本分布复杂，业务负责人仍要求把首屏达标完全押在preload上，你如何评估？", "a": "不应把结果完全押在preload上，因为其兼容性存在限制，部分运行环境可能无法获得预期收益。应先验证目标WebView是否支持并正确执行，再保留正常资源发现与加载路径；预加载只能作为提前请求的优化，不能成为资源可用性的唯一前提。"}, {"q": "上线后网络面板出现两次相同脚本请求，页面还加了对应preload，你会先核对哪些地方？", "a": "先核对预加载的href与脚本实际URL是否完全一致，包括协议、主机、路径和参数，再确认页面最终确实使用该资源。preload是一次声明式请求，若声明目标与实际目标不同，就可能产生两份下载；还应检查是否有重复标签或重定向。"}, {"q": "团队把下一页可能使用的模块也全部标成preload，架构师建议改成prefetch，你站哪一边？", "a": "下一页未来可能使用的资源更符合prefetch，而preload面向当前页面所需的脚本、样式等资源。把大量不确定资源设为preload会强制提前请求，可能浪费传输资源；若下一页并非高概率访问，连prefetch也应谨慎使用。"}, {"q": "页面把非首屏但确定重要的文件延后加载，开发担心这会拖慢首次呈现，preload与async、defer分别解决什么？", "a": "preload解决资源尽早获取的问题，本身不会阻塞onload；async和defer描述脚本异步读取后的执行方式。defer保持脚本顺序依赖，async则加载完成后执行；不能用执行属性替代资源优先发现，也不能用预加载替代脚本执行控制。"}]}, {"s": "性能", "t": "预渲染", "m": "预渲染就是让浏览器提前在后台渲染用户接下来大概率会访问的页面，从而缩短真正打开时的等待时间。可以通过<link rel=\"prerender\" href=\"http://poetries.com\">指定目标页面，本质上是用当前的网络和计算资源换取后续访问速度。这个优化只适合用户几乎确定会打开的页面，否则提前下载和渲染的内容没有被使用，反而会白白消耗资源。\n可以通过预渲染将下载的文件预先在后台渲染，可以使用以下代码开启预渲染\n<linkrel=\"prerender\"href=\"http://poetries.com\">\n预渲染虽然可以提高页面的加载速度，但是要确保该页面百分百会被用户在之后打开，否则就白白浪费资源去渲染\n总结\ndefer和async在网络读取的过程中都是异步解析defer是有顺序依赖的，async只要脚本加载完后就会执行preload可以对当前页面所需的脚本、样式等资源进行预加载prefetch加载的资源一般不是用于当前页面的，是未来很可能用到的这样一些资源", "f": [{"q": "电商首页列出一百个商品链接，运营想给每个详情页都加<link rel=\"prerender\">，说这样点击一定更快，你会批准吗？", "a": "不会批准批量预渲染，因为它会在后台提前渲染目标页面，用户未访问的页面会白白消耗资源。预渲染只适合之后几乎确定会打开的页面；面对一百个候选目标，应缩小到确定性极高的路径，否则加速收益建立在大量浪费之上。"}, {"q": "支付成功页之后固定进入订单详情页，你会怎样写预渲染声明，并说明它何时值得保留？", "a": "可在当前文档中加入<link rel=\"prerender\" href=\"http://poetries.com/order\">，让目标页面预先在后台渲染。只有用户后续几乎必然进入该地址时才值得保留；若流程允许关闭页面、跳转其他入口或目标URL不确定，就应重新评估。"}, {"q": "同一流程从“付款后必进订单页”改成“付款后可能去抽奖、订单页或首页”，原有prerender还应保留吗？", "a": "原有预渲染的成立条件已经变弱，不应继续按必达路径处理。它的代价不仅是提前获取，还包括后台渲染目标页面；当三个去向都可能发生时，应根据真实导航确定性缩减或移除，避免为未打开页面消耗资源。"}, {"q": "上线预渲染后，低端设备在当前页出现额外资源消耗，用户却很少进入目标页，你会怎样确认根因？", "a": "先检查页面中的prerender声明及其目标地址，再对照用户实际后续导航，确认目标页是否真的高概率打开。若大多数用户没有访问，后台渲染就是直接浪费；应移除或只在路径高度确定时声明，不能用当前页加载完成来证明配置合理。"}, {"q": "结算页的下一步几乎确定，但团队在prefetch和prerender之间争论，你如何选择？", "a": "若只想提前取得未来可能使用的资源，prefetch更贴近需求；若目标页面之后确定会打开，并希望连后台渲染也提前完成，才考虑prerender。后者工作更重，对导航确定性要求更高；一旦用户改走其他路径，已完成的渲染就会浪费。"}, {"q": "首屏脚本、下一页资源和下一页完整页面都想提前处理，评审会上有人建议统一使用preload，你会怎么拆分？", "a": "当前页面所需的脚本或样式使用preload，未来很可能用到的资源对应prefetch，几乎确定会访问的完整页面才适合prerender。三者提前处理的对象和成本不同，统一使用preload会混淆当前页资源与未来导航，也无法等同于后台渲染。"}]}, {"s": "性能", "t": "懒执行与懒加载", "m": "懒执行是推迟计算逻辑，懒加载则是推迟非关键资源的下载，两者优化的对象并不一样。耗时逻辑如果首屏用不到，可以等定时器或用户事件触发后再执行；图片、视频等资源则可以等进入指定区域时再加载。图片场景里常先展示占位图，把真实地址放在自定义属性中，满足加载条件后再赋给src，但要确保触发逻辑可靠。\n懒执行\n懒执行就是将某些逻辑延迟到使用时再计算。该技术可以用于首屏优化，对于某些耗时逻辑并不需要在首屏就使用的，就可以使用懒执行。懒执行需要唤醒，一般可以通过定时器或者事件的调用来唤醒\n懒加载\n懒加载就是将不关键的资源延后加载\n懒加载的原理就是只加载自定义区域（通常是可视区域，但也可以是即将进入可视区域）内需要加载的东西。对于图片来说，先设置图片标签的src属性为一张占位图，将真实的图片资源放入一个自定义属性中，当进入自定义区域时，就将自定义属性替换为src属性，这样图片就会去下载资源，实现了图片懒加载\n懒加载不仅可以用于图片，也可以使用在别的资源上。比如进入可视区域才开始播放视频等", "f": [{"q": "商品详情页把价格计算、推荐列表和首屏主图都改成进入视口后执行，结果页面先出现空壳，用户还没滚动就无法下单；这里把“懒”用错在哪了？", "a": "首屏成交链路不应被统一延后，价格、主图等当前区域必需内容应立即执行或加载。懒执行适合暂时不用的耗时逻辑，懒加载适合非关键资源；判断标准是当前区域与当前操作是否依赖它，延后关键内容只会把成本变成可感知等待。"}, {"q": "一个长列表有500张商品图，接口一次返回了全部图片地址；你会怎样落地图片懒加载，同时避免未加载区域把页面高度撑乱？", "a": "先为图片保留明确尺寸并展示占位图，把真实地址放在自定义属性中，再用IntersectionObserver监听图片进入或接近可视区域时替换src。加载完成后应取消观察，失败时保留占位或展示重试入口；没有预留尺寸会在图片出现时引发布局跳动。"}, {"q": "资讯页的下一个视频刚进入可视区域才开始下载并播放，用户在弱网下总看到黑屏；产品又不接受进入页面就加载全部视频，你怎么调整触发边界？", "a": "把触发区域从“已经可见”扩大为“即将进入可视区域”，提前加载下一段视频，但仍只播放当前可见内容。预加载距离应结合滚动速度、资源体积和网络状况控制；范围过大会退化为批量加载，范围过小则无法掩盖下载等待。"}, {"q": "图片懒加载上线后，部分图片滚到屏幕内仍保持占位图，而手动刷新又恢复；你会按什么顺序定位？", "a": "先确认观察器是否绑定到真实图片节点，以及列表更新后新增节点是否被继续观察，再检查真实地址写入和src替换逻辑。随后查看图片请求是否发出、是否失败，并检查容器滚动场景下观察区域是否设置正确；只监听初始节点会让动态列表稳定复现漏加载。"}, {"q": "筛选面板包含一段耗时统计逻辑：用setTimeout延后执行，还是等用户点击“展开”再执行？如果结果还要用于首屏角标，你怎么选？", "a": "若统计只服务于展开后的面板，应由展开事件唤醒，避免定时器在用户从不使用时仍消耗主线程。若首屏角标依赖同一结果，就不能整体懒执行，应拆出角标所需的最小计算，其余逻辑继续延后；拆分会增加状态与缓存一致性的维护成本。"}]}, {"s": "性能", "t": "文件优化", "m": "文件优化的核心是减少资源体积、避免阻塞渲染，并把静态资源更高效地送到用户手里。图片应按设备尺寸请求裁剪版本，照片可用JPEG，小图可用PNG或base64，图标优先考虑SVG，支持时也可选压缩效果更好的WebP。CSS放在head，脚本按依赖选择defer或async，耗时计算可交给Webworker。服务端还应开启压缩并使用不同于主站的CDN域名，避免静态资源请求携带主站Cookie。\n图片优化\n对于如何优化图片，有 2 个思路\n减少像素点减少每个像素点能够显示的颜色\n图片加载优化\n不用图片。很多时候会使用到很多修饰类图片，其实这类修饰图片完全可以用CSS去代替。对于移动端来说，屏幕宽度就那么点，完全没有必要去加载原图浪费带宽。一般图片都用 CDN 加载，可以计算出适配屏幕的宽度，然后去请求相应裁剪好的图片小图使用base64格式将多个图标文件整合到一张图片中（雪碧图）选择正确的图片格式：对于能够显示WebP格式的浏览器尽量使用WebP格式。因为WebP格式具有更好的图像数据压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图像质量，缺点就是兼容性并不好小图使用PNG，其实对于大部分图标这类图片，完全可以使用SVG代替照片使用JPEG\n对于能够显示WebP格式的浏览器尽量使用WebP格式。因为WebP格式具有更好的图像数据压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图像质量，缺点就是兼容性并不好小图使用PNG，其实对于大部分图标这类图片，完全可以使用SVG代替照片使用JPEG\n其他文件优化\nCSS文件放在head中服务端开启文件压缩功能将script标签放在body底部，因为JS文件执行会阻塞渲染。当然也可以把script标签放在任意位置然后加上defer，表示该文件会并行下载，但是会放到HTML解析完成后顺序执行。对于没有任何依赖的JS文件可以加上async，表示加载和渲染后续文档元素的过程将和JS文件的加载与执行并行无序进行。 执行JS代码过长会卡住渲染，对于需要很多时间计算的代码可以考虑使用Webworker。Webworker可以让我们另开一个线程执行脚本而不影响渲染。\nCDN\n静态资源尽量使用CDN加载，由于浏览器对于单个域名有并发请求上限，可以考虑使用多个CDN域名。对于CDN加载静态资源需要注意CDN域名要与主站不同，否则每次请求都会带上主站的Cookie", "f": [{"q": "活动页为了减少请求，把一张2MB照片转成base64塞进主CSS，请求数下降了但首屏更慢；你会如何判断这次优化？", "a": "这是把小图内联策略错误地用于大图，图片体积会进入阻塞渲染的CSS，还失去独立资源缓存和按需加载空间。base64只适合足够小且确实值得减少请求的资源；大照片应选择合适尺寸与格式，并交给静态资源缓存或CDN。"}, {"q": "移动商品页拿到的是1920px原图，而内容区最多只有375px；设计要求清晰，后端又已有图片裁剪服务，你会怎样改造请求？", "a": "根据展示宽度和设备需求向图片服务请求相应裁剪尺寸，避免移动端下载远超实际像素需求的原图。照片可优先评估WebP，并为不适用的环境保留JPEG等格式；裁剪过小或压缩过度会损害清晰度，需要在体积与观感之间验收。"}, {"q": "后台管理页有80个小图标，团队在雪碧图、SVG和逐个PNG之间争论；哪些约束会改变你的选择？", "a": "可缩放、需要换色的图标更适合SVG，固定的小位图可考虑合并为雪碧图以减少请求，少量独立资源则未必值得增加维护复杂度。选择还要看兼容要求和更新频率；雪碧图中单个图标频繁变更会使整张图失效，独立文件则增加请求数量。"}, {"q": "线上首页的CSS已放在head，但主体仍长期空白，网络面板显示一个同步script位于文档前部且执行时间很长；你会怎么处理？", "a": "将非首屏脚本移到body底部或使用defer，让它并行下载并在HTML解析完成后按顺序执行。没有依赖关系的脚本才适合评估async，因为执行顺序不受保证；若真正瓶颈是长时间计算，还应拆分任务或交给Web Worker。"}, {"q": "架构师想给图片、脚本拆出多个CDN域名提升并发，安全同学担心请求携带登录Cookie；你会怎样落方案？", "a": "静态资源域名应与主站区分，并确保请求不携带主站Cookie，否则每次资源请求都会增加无效传输并扩大暴露面。多域名能够缓解单域并发限制，但也会增加域名解析和运维成本；资源数量不多时，不应只为理论并发继续拆域。"}]}, {"s": "性能", "t": "其他", "m": "除了资源本身，构建产物和线上错误监控也是性能优化不能漏掉的两块。使用Webpack4时可启用production、基于ES6模块做tree shaking，再按路由拆包并给文件名加哈希，兼顾按需加载与浏览器缓存。线上错误可用window.onerror、Promise.catch或try catch捕获，跨域脚本还要配置crossorigin。由于生产代码通常经过压缩，构建时应生成sourceMap方便定位，并把捕获结果上传到服务端。\n使用 Webpack 优化项目\n对于Webpack4，打包项目使用production模式，这样会自动开启代码压缩使用ES6模块来开启tree shaking，这个技术可以移除没有使用的代码优化图片，对于小图可以使用base64的方式写入文件中按照路由拆分代码，实现按需加载给打包出来的文件名添加哈希，实现浏览器缓存文件\n监控\n对于代码运行错误，通常的办法是使用window.onerror拦截报错。该方法能拦截到大部分的详细报错信息，但是也有例外\n对于跨域的代码运行错误会显示Script error. 对于这种情况我们需要给script标签添加crossorigin属性对于某些浏览器可能不会显示调用栈信息，这种情况可以通过arguments.callee.caller来做栈递归对于异步代码来说，可以使用catch的方式捕获错误。比如Promise可以直接使用 catch 函数，async await可以使用try catch但是要注意线上运行的代码都是压缩过的，需要在打包时生成sourceMap文件便于debug。对于捕获的错误需要上传给服务器，通常可以通过img标签的src发起一个请求", "f": [{"q": "项目切到Webpack4的production后，负责人认为包体一定已经最优，不再做任何配置检查；你会拿什么反例追问他？", "a": "production会开启代码压缩，但不代表未使用代码、路由资源和图片都已按目标优化。还要确认源码使用ES6模块以支持tree shaking、路由是否按需拆分，以及小图是否适合内联；动态引用或存在副作用时，未使用代码也未必能安全移除。"}, {"q": "一个管理系统有30条路由，首屏只进入工作台；你会如何组织构建产物，并让浏览器可靠复用未变化的文件？", "a": "按路由拆分代码，让工作台只加载当前必需资源，其余路由在访问时再获取。输出文件名加入内容相关哈希，使未变化文件继续命中浏览器缓存、变化文件获得新地址；拆得过细会增加请求和运行时管理成本，需要结合页面依赖调整粒度。"}, {"q": "公司要求错误监控尽量不依赖业务接口，前端负责人提出所有异常都交给window.onerror；遇到Promise和async/await异常时，你会怎样补齐？", "a": "window.onerror可覆盖大部分运行时错误，但异步链路仍应在Promise上使用catch，在async/await周围使用try...catch。捕获后统一整理并上报，必要时可通过图片src发起轻量请求；遗漏异步出口会形成未记录的失败。"}, {"q": "线上监控只收到Script error，故障来自另一个域名上的脚本，本地却有完整堆栈；你先检查哪两处配置？", "a": "先检查跨域script是否添加了crossorigin，再确认资源服务是否允许相应的跨域访问，否则浏览器不会暴露详细错误。即使拿到文件名和行列号，线上代码仍是压缩产物，还必须保留与该版本匹配的sourceMap才能映射回源码。"}, {"q": "安全团队不允许公开部署sourceMap，研发又必须定位压缩代码报错；构建和监控之间应该怎样取舍？", "a": "构建阶段仍应生成与发布版本对应的sourceMap，供受控的调试或错误解析流程使用，不必把映射文件作为普通静态资源公开。监控事件还要携带可定位构建版本的信息；若映射文件与线上哈希不匹配，即使保留了文件也无法可靠还原位置。"}]}, {"s": "性能", "t": "如何根据chrome的timing优化", "m": "我会先用Chrome DevTools和performance.timing拆开各阶段耗时，再针对最长的一段优化。DNS慢就减少域名并配置dns-prefetch，请求和下载慢则优先检查缓存、压缩、资源体积及并发阻塞。首屏资源可通过异步、预加载或懒加载后置，图片则压缩并选择WebP等格式。单页应用里直接依赖传统时间点不一定合适，还要结合图片onload等事件判断首屏完成。\n性能优化API\nPerformance。performance.now()与new Date()区别，它是高精度的，且是相对时间，相对于页面加载的那一刻。但是不一定适合单页面场景window.addEventListener(\"load\", \"\");window.addEventListener(\"domContentLoaded\", \"\");Img的onload事件，监听首屏内的图片是否加载完成，判断首屏事件RequestFrameAnmation和RequestIdleCallbackIntersectionObserver、MutationObserver，PostMessageWeb Worker，耗时任务放在里面执行\n检测工具\nChrome Dev ToolsPage SpeedJspref\n前端指标\nwindow.onload=function(){setTimeout(function(){lett=performance.timing console.log('DNS查询耗时 ：'+(t.domainLookupEnd-t.domainLookupStart).toFixed(0))console.log('TCP链接耗时 ：'+(t.connectEnd-t.connectStart).toFixed(0))console.log('request请求耗时 ：'+(t.responseEnd-t.responseStart).toFixed(0))console.log('解析dom树耗时 ：'+(t.domComplete-t.domInteractive).toFixed(0))console.log('白屏时间 ：'+(t.responseStart-t.navigationStart).toFixed(0))console.log('domready时间 ：'+(t.domContentLoadedEventEnd-t.navigationStart).toFixed(0))console.log('onload时间 ：'+(t.loadEventEnd-t.navigationStart).toFixed(0))if(t=performance.memory){console.log('js内存使用占比 ：'+(t.usedJSHeapSize/t.totalJSHeapSize*100).toFixed(2)+'%')}})}\nDNS预解析优化\ndns解析是很耗时的，因此如果解析域名过多，会让首屏加载变得过慢，可以考虑dns-prefetch优化\nDNS Prefetch应该尽量的放在网页的前面，推荐放在 后面。具体使用方法如下：\n<metahttp-equiv=\"x-dns-prefetch-control\"content=\"on\"><linkrel=\"dns-prefetch\"href=\"//www.zhix.net\"><linkrel=\"dns-prefetch\"href=\"//api.share.zhix.net\"><linkrel=\"dns-prefetch\"href=\"//bdimg.share.zhix.net\">\nrequest请求耗时\n不请求，用cache（最好的方式就是尽量引用公共资源，同时设置缓存，不去重新请求资源，也可以运用PWA的离线缓存技术，可以帮助wep实现离线使用）前端打包时压缩服务器上的zip压缩图片压缩（比如tiny），使用webp等高压缩比格式把过大的包，拆分成多个较少的包，防止单个资源耗时过大同一时间针对同一域名下的请求有一定数量限制，超过限制数目的请求会被阻塞。如果资源来自于多个域下，可以增大并行请求和下载速度延迟、异步、预加载、懒加载对于非首屏的资源，可以使用 defer 或 async 的方式引入也可以按需加载，在逻辑中，只有执行到时才做请求对于多屏页面，滚动时才动态载入图片", "f": [{"q": "单页应用用new Date()记录按钮操作耗时，连续测量出现精度不足和系统时间调整带来的跳变；你会换成什么，并提醒什么边界？", "a": "短时耗时测量应使用高精度、相对页面加载时刻的performance.now()，避免依赖系统绝对时间变化。它适合计算同一页面生命周期内的时间差，但单页应用长期运行时不能把页面初始时刻直接当成每次路由的起点，应为各次操作单独记录基准。"}, {"q": "首页同时引用主站、接口、图片和分享服务四个域名，Chrome DevTools显示首次访问的域名解析拖慢首屏；你会把优化写在哪里？", "a": "对确定会在首屏使用的外部域名，在文档前部加入dns-prefetch，并通过x-dns-prefetch-control开启相应控制，让解析尽早发生。只应声明高概率访问的域名；预解析过多不会减少资源体积，还可能引入无意义的解析开销。"}, {"q": "瀑布图显示一个首屏包下载时间很长，团队一方主张继续拆包，另一方主张合并以减少请求；你依据哪些现象裁决？", "a": "先判断耗时来自单文件过大还是同域请求超过并发限制：前者适合压缩并拆出非首屏代码，后者可能需要减少首屏请求、利用缓存或调整资源域。拆包后仍应把非关键部分延迟或按需加载；仅把大包切成大量同时请求的小包，可能把下载耗时变成排队耗时。"}, {"q": "监控代码在window.onload回调里立刻读取loadEventEnd，线上偶尔得到不完整结果；你会如何修正采集时机，并区分哪些阶段？", "a": "应在load回调后再延迟到当前事件处理结束读取，避免loadEventEnd尚未写入。采集时分别计算DNS、连接、响应、DOM解析、白屏、DOMContentLoaded和load时间，先定位阶段再优化；单个总耗时无法说明瓶颈属于网络还是渲染。"}, {"q": "一个多屏图文页首屏资源已压缩，但用户流量仍很高；产品要求滚动顺滑，研发准备把所有图片都预加载，你会怎么取舍？", "a": "非首屏图片应随滚动按需加载，必要时在即将进入视口时提前触发，而不是一次预加载全部资源。首屏资源可继续使用缓存、压缩和合适图片格式；触发太晚会出现占位等待，触发太早则重新增加请求竞争与流量。"}]}, {"s": "性能", "t": "移动端优化", "m": "移动端优化既要控制资源加载，也要降低脚本执行和页面渲染的开销。我一般把首屏目标控制在三秒左右，静态资源使用缓存和GZip，图片按尺寸与格式压缩，非首屏内容按需或异步加载。交互阶段要减少DOM操作、重绘和回流，高频的scroll、touchmove可配合requestAnimationFrame限制渲染频率。懒加载可能带来额外重绘，GPU加速也应合理使用，不能为了指标全部开启。\n1. 概述\nPC优化手段在Mobile侧同样适用在Mobile侧我们提出三秒种渲染完成首屏指标基于第二点，首屏加载3秒完成或使用Loading基于联通3G网络平均338KB/s(2.71Mb/s），所以首屏资源不应超过1014KBMobile侧因手机配置原因，除加载外渲染速度也是优化重点基于第五点，要合理处理代码减少渲染损耗基于第二、第五点，所有影响首屏加载和渲染的代码应在处理逻辑中后置加载完成后用户交互使用时也需注意性能\n2. 加载优化\n加载过程是最为耗时的过程，可能会占到总耗时的80%时间，因此是优化的重点\n2.1 缓存\n使用缓存可以减少向服务器的请求数，节省加载时间，所以所有静态资源都要在服务器端设置缓存，并且尽量使用长Cache（长Cache资源的更新可使用时间戳）\n2.2 压缩HTML、CSS、JavaScript\n减少资源大小可以加快网页显示速度，所以要对HTML、CSS、JavaScript等进行代码压缩，并在服务器端设置GZip\na） 压缩（例如，多余的空格、换行符和缩进）b） 启用GZip\n2.3 无阻塞\n写在HTML头部的JavaScript（无异步），和写在HTML标签中的Style会阻塞页面的渲染，因此CSS放在页面头部并使用Link方式引入，避免在HTML标签中写Style，JavaScript放在页面尾部或使用异步方式加载\n2.4 使用首屏加载\n首屏的快速显示，可以大大提升用户对页面速度的感知，因此应尽量针对首屏的快速显示做优化。\n2.5 按需加载\n将不影响首屏的资源和当前屏幕资源不用的资源放到用户需要时才加载，可以大大提升重要资源的显示速度和降低总体流量。\nPS：按需加载会导致大量重绘，影响渲染性能\na）LazyLoadb） 滚屏加载c） 通过Media Query加载\n2.6 预加载\n大型重资源页面（如游戏）可使用增加Loading的方法，资源加载完成后再显示页面。但Loading时间过长，会造成用户流失。\n对用户行为分析，可以在当前页加载下一页资源，提升速度。\na）可感知Loadingb）不可感知的Loading（如提前加载下一页）\n2.7 压缩图片\n图片是最占流量的资源，因此尽量避免使用他，使用时选择最合适的格式（实现需求的前提下，以大小判断），合适的大小，然后使用智图压缩，同时在代码中用Srcset来按需显示\nPS：过度压缩图片大小影响图片显示效果\na）使用智图（ http://zhitu.tencent.com/ ）b）使用其它方式代替图片(1. 使用CSS32. 使用SVG3. 使用IconFont）c）使用Srcsetd）选择合适的图片(1.webP优于JPG2.PNG8优于GIF）e）选择合适的大小（1. 首次加载不大于1014KB2. 不宽于640（基于手机屏幕一般宽度））\n2.8 减少Cookie\nCookie会影响加载速度，所以静态资源域名不使用Cookie。\n2.9 避免重定向\n重定向会影响加载速度，所以在服务器正确设置避免重定向。\n2.10 异步加载第三方资源\n第三方资源不可控会影响页面的加载和显示，因此要异步加载第三方资源\n2.11 减少HTTP请求\n因为手机浏览器同时响应请求为4个请求（Android支持4个，iOS5后可支持6个），所以要尽量减少页面的请求数，首次加载同时请求数不能超过4个\na）合并CSS、JavaScriptb）合并小图片，使用雪碧图\n3. 三、脚本执行优化\n脚本处理不当会阻塞页面加载、渲染，因此在使用时需当注意\nCSS写在头部，JavaScript写在尾部或异步避免图片和iFrame等的空Src，空Src会重新加载当前页面，影响速度和效率。尽量避免重设图片大小重设图片大小是指在页面、CSS、JavaScript等中多次重置图片大小，多次重设图片大小会引发图片的多次重绘，影响性能图片尽量避免使用DataURL，DataURL图片没有使用图片的压缩算法文件会变大，并且要解码后再渲染，加载慢耗时长\n4. CSS优化\n尽量避免写在HTML标签中写Style属性\n4.1 css3过渡动画开启硬件加速\n.translate3d{ -webkit-transform: translate3d(0, 0, 0); -moz-transform: translate3d(0, 0, 0); -ms-transform: translate3d(0, 0, 0); transform: translate3d(0, 0, 0); }\n4.2 避免CSS表达式\nCSS表达式的执行需跳出CSS树的渲染，因此请避免CSS表达式。\n4.3 不滥用Float\nFloat在渲染时计算量比较大，尽量减少使用\n4.4 值为0时不需要任何单位\n为了浏览器的兼容性和性能，值为0时不要带单位\n5. JavaScript执行优化\n5.1 减少重绘和回流\n避免不必要的Dom操作尽量改变Class而不是Style，使用classList代替className避免使用document.write减少drawImage\n5.2 TOUCH事件优化\n使用touchstart、touchend代替click，因快影响速度快。但应注意Touch响应过快，易引发误操作\n6. 渲染优化\n6.1 HTML使用Viewport\nViewport可以加速页面的渲染，请使用以下代码\n<meta name=”viewport” content=”width=device-width, initial-scale=1″>\n6.2 动画优化\n尽量使用CSS3动画合理使用requestAnimationFrame动画代替setTimeout适当使用Canvas动画5个元素以内使用css动画，5个以上使用Canvas动画（iOS8可使用webGL）\n6.3 高频事件优化\nTouchmove、Scroll事件可导致多次渲染\n使用requestAnimationFrame监听帧变化，使得在正确的时间进行渲染增加响应变化的时间间隔，减少重绘次数\n6.4 GPU加速\nCSS中以下属性（CSS3 transitions、CSS3 3D transforms、Opacity、Canvas、WebGL、Video）来触发GPU渲染，请合理使用", "f": [{"q": "移动活动页为了满足“三秒首屏”，在资源没完成时一直显示全屏Loading，弱网用户等很久仍看不到任何内容；你如何评价这个方案？", "a": "Loading只能让等待可感知，不能替代首屏资源与渲染优化，持续过久仍会造成用户流失。应先后置不影响首屏的代码和资源，尽快展示可用的首屏结构；大型重资源页面才适合等待资源完成后整体呈现，也要控制等待范围。"}, {"q": "移动首页预算约1014KB，现在一张横幅原图就占了大半，设计不允许去掉；你会怎样压缩加载成本而不直接牺牲视觉需求？", "a": "先按实际展示宽度生成合适尺寸，通过srcset按需选择，并在满足兼容与观感时采用WebP等压缩率更高的格式。还应使用图片压缩工具并避免超过手机所需尺寸；过度压缩会产生明显失真，最终必须以目标设备上的显示效果验收。"}, {"q": "页面首屏有10个脚本和样式请求，旧移动浏览器同时只能处理较少请求；团队又希望所有模块独立发布，你怎么处理这个冲突？", "a": "首屏应优先减少必要请求数，可合并关键CSS、JavaScript和小图片，并把非关键模块异步或按需加载。模块独立发布不等于必须在首屏分别请求全部产物；合并过度会扩大缓存失效范围，因此只聚合稳定且共同使用的关键资源。"}, {"q": "商品列表滚动时明显掉帧，埋点显示scroll回调频繁修改多个元素的内联style；你会先改哪一段执行链？", "a": "把高频scroll更新合并到requestAnimationFrame中，并增加响应间隔，避免同一帧内反复触发渲染。批量切换class，减少零散DOM操作和不必要的重绘回流；若每帧计算本身仍然过重，仅限频不能消除卡顿。"}, {"q": "动画负责人要求给页面所有卡片加translate3d强制开启GPU，测试发现低端机内存压力反而上升；你会怎么划定使用范围？", "a": "硬件加速应只用于确实需要平滑过渡的动画元素，并优先使用transform、opacity配合requestAnimationFrame。大量元素长期占用独立合成资源会增加设备负担，因此要在真实移动设备上验证；静态卡片或偶发变化不应统一强制提升。"}, {"q": "第三方统计脚本偶尔超时并拖住移动首页，业务方又要求不能删除；你如何隔离它与首屏渲染？", "a": "将第三方资源改为异步加载，并确保首屏内容不依赖其下载或执行结果，因为外部服务的时延不可控。脚本还应避免放在会阻塞解析的位置；异步化可能改变埋点初始化时序，业务事件需要在脚本就绪前暂存或允许安全丢弃。"}]}, {"s": "工程化", "t": "介绍一下 webpack 的构建流程", "m": "Webpack的构建流程本质上是读取配置，从入口递归生成依赖图，再组织为chunk并输出资源。启动时它会合并配置和命令行参数，创建全局的Compiler，加载插件并开始一次Compilation。模块经过对应loader转换后，再通过AST分析依赖，直到所有入口依赖都处理完成。最后按依赖关系组装产物并写入文件系统，插件则通过Tapable的生命周期事件介入并修改构建结果。\n核心概念\nentry：入口。webpack是基于模块的，使用webpack首先需要指定模块解析入口(entry)，webpack从入口开始根据模块间依赖关系递归解析和处理所有资源文件。output：输出。源代码经过webpack处理之后的最终产物。loader：模块转换器。本质就是一个函数，在该函数中对接收到的内容进行转换，返回转换后的结果。因为 Webpack 只认识 JavaScript，所以 Loader 就成了翻译官，对其他类型的资源进行转译的预处理工作。plugin：扩展插件。基于事件流框架Tapable，插件可以扩展 Webpack 的功能，在 Webpack 运行的生命周期中会广播出许多事件，Plugin 可以监听这些事件，在合适的时机通过 Webpack 提供的 API 改变输出结果。module：模块。除了js范畴内的es module、commonJs、AMD等，css @import、url(...)、图片、字体等在webpack中都被视为模块。\n解释几个 webpack 中的术语\nmodule：指在模块化编程中我们把应用程序分割成的独立功能的代码模块chunk：指模块间按照引用关系组合成的代码块，一个chunk中可以包含多个modulechunk group：指通过配置入口点（entry point）区分的块组，一个chunk group中可包含一到多个 chunkbundling：webpack 打包的过程asset/bundle：打包产物\nwebpack 的打包思想可以简化为 3 点：\n一切源代码文件均可通过各种Loader转换为 JS 模块 （module），模块之间可以互相引用。webpack 通过入口点（entry point）递归处理各模块引用关系，最后输出为一个或多个产物包js(bundle)文件。每一个入口点都是一个块组（chunk group），在不考虑分包的情况下，一个chunk group中只有一个chunk，该 chunk 包含递归分析后的所有模块。每一个chunk都有对应的一个打包后的输出文件（asset/bundle）\n打包流程\n初始化参数：从配置文件和 Shell 语句中读取并合并参数，得出最终的配置参数。开始编译：从上一步得到的参数初始化Compiler对象，加载所有配置的插件，执行对象的run方法开始执行编译。确定入口：根据配置中的entry找出所有的入口文件。编译模块：从入口文件出发，调用所有配置的loader对模块进行翻译，再找出该模块依赖的模块，这个步骤是递归执行的，直至所有入口依赖的模块文件都经过本步骤的处理。完成模块编译：经过第 4 步使用 loader 翻译完所有模块后，得到了每个模块被翻译后的最终内容以及它们之间的依赖关系。输出资源：根据入口和模块之间的依赖关系，组装成一个个包含多个模块的chunk，再把每个chunk转换成一个单独的文件加入到输出列表，这一步是可以修改输出内容的最后机会。输出完成：在确定好输出内容后，根据配置确定输出的路径和文件名，把文件内容写入到文件系统。\n简版\nWebpack CLI 启动打包流程；载入 Webpack 核心模块，创建Compiler对象；使用Compiler对象开始编译整个项目；从入口文件开始，解析模块依赖，形成依赖关系树；递归依赖树，将每个模块交给对应的 Loader 处理；合并 Loader 处理完的结果，将打包结果输出到 dist 目录。\n在以上过程中，Webpack 会在特定的时间点广播出特定的事件，插件在监听到相关事件后会执行特定的逻辑，并且插件可以调用 Webpack 提供的 API 改变 Webpack 的运行结果\n构建流程核心概念：\nTapable：一个基于发布订阅的事件流工具类，Compiler和Compilation对象都继承于TapableCompiler：compiler对象是一个全局单例，他负责把控整个webpack打包的构建流程。在编译初始化阶段被创建的全局单例，包含完整配置信息、loaders、plugins以及各种工具方法Compilation：代表一次 webpack 构建和生成编译资源的的过程，在watch模式下每一次文件变更触发的重新编译都会生成新的Compilation对象，包含了当前编译的模块module, 编译生成的资源，变化的文件, 依赖的状态等而每个模块间的依赖关系，则依赖于AST语法树。每个模块文件在通过Loader解析完成之后，会通过acorn库生成模块代码的AST语法树，通过语法树就可以分析这个模块是否还有依赖的模块，进而继续循环执行下一个模块的编译解析。\n最终Webpack打包出来的bundle文件是一个IIFE的执行函数。\n// webpack 5 打包的bundle文件内容(()=>{// webpackBootstrapvar__webpack_modules__=({'file-A-path':((modules)=>{// ... })'index-file-path':((__unused_webpack_module,__unused_webpack_exports,__webpack_require__)=>{// ... })})// The module cachevar__webpack_module_cache__={};// The require functionfunction__webpack_require__(moduleId){// Check if module is in cachevarcachedModule=__webpack_module_cache__[moduleId];if(cachedModule!==undefined){returncachedModule.exports;}// Create a new module (and put it into the cache)varmodule=__webpack_module_cache__[moduleId]={// no module.id needed// no module.loaded neededexports:{}};// Execute the module function__webpack_modules__[moduleId](module,module.exports,__webpack_require__);// Return the exports of the modulereturnmodule.exports;}// startup// Load entry module and return exports// This entry module can't be inlined because the eval devtool is used.var__webpack_exports__=__webpack_require__(\"./src/index.js\");})\nwebpack详细工作流程", "f": [{"q": "一个后台项目只有一个entry，最终却生成了入口包、公共包和异步包；同事坚持“一个入口只会对应一个文件”，你怎么纠正？", "a": "entry对应的是入口点和chunk group，并不保证最终只有一个输出文件。Webpack会从入口递归建立模块依赖图，再根据代码分割等规则组装多个chunk，每个chunk才通常对应一个输出asset；不考虑分包时，才可近似理解为一个入口产生一个包。"}, {"q": "团队要让Webpack支持TypeScript、Less、字体和图片，你会把这些处理分别放到构建流程的什么位置？", "a": "这些资源都应先作为module进入依赖图，再由匹配的loader完成转换，例如ts-loader、less-loader和文件资源加载器。Webpack从entry递归解析引用，转换完成后才汇总模块关系并生成chunk；若处理需要观察整轮构建或修改最终产物，则应交给plugin。"}, {"q": "同一套配置由开发服务器以watch模式运行，连续改动三个文件时，Compiler和Compilation会怎样变化？", "a": "Compiler在初始化阶段创建并贯穿整个构建生命周期，配置、插件和工具方法都由它持有。每次文件变化触发重新编译时会新建一个Compilation，记录该轮模块、资源、变更文件和依赖状态；把轮次状态长期挂在Compiler上，容易造成数据串轮或缓存失真。"}, {"q": "一个新增的loader能输出合法代码，但它引用的子模块始终没有进入产物；你会沿构建链路查哪些环节？", "a": "先确认入口是否实际到达该文件，以及loader的匹配范围和输出是否保留了可被解析的模块引用。随后检查转换结果生成的AST能否识别依赖，再核对依赖解析、模块编译和chunk组装结果；若依赖是在运行时动态拼接的，静态分析可能无法发现。"}, {"q": "产品要求在写入dist前改写所有资源名，同时还要转换单个源码文件；你会怎样划分loader与plugin？", "a": "单文件内容转换放在loader，因为它处于模块编译阶段，只需接收并返回可继续处理的内容。资源名改写应由plugin在输出资源阶段监听合适的Tapable钩子，利用Compilation操作完整产物列表；若等文件写入后再处理，会增加额外文件操作并可能破坏引用关系。"}]}, {"s": "工程化", "t": "介绍 Loader", "m": "Loader是Webpack的模块转换器，负责把它原本不能直接处理的资源转换成可继续构建的内容。它本质上是接收内容并返回转换结果的函数，多个loader可以按配置形成链式处理。比如babel-loader转换新语法，css-loader解析@import和url()，style-loader再把样式插入页面。编写时应保持单一职责和相互独立，否则转换链过重，也会让配置与问题定位变复杂。\n常用 Loader:\nfile-loader: 加载文件资源，如 字体 / 图片 等，具有移动/复制/命名等功能；url-loader: 通常用于加载图片，可以将小图片直接转换为 Data Url，减少请求；babel-loader: 加载 js / jsx 文件， 将 ES6 / ES7 代码转换成 ES5，抹平兼容性问题；ts-loader: 加载 ts / tsx 文件，编译 TypeScript；style-loader: 将 css 代码以<style>标签的形式插入到 html 中；css-loader: 分析@import和url()，引用 css 文件与对应的资源；postcss-loader: 用于 css 的兼容性处理，具有众多功能，例如 添加前缀，单位转换 等；less-loader / sass-loader: css预处理器，在 css 中新增了许多语法，提高了开发效率；\n编写原则:\n单一原则: 每个 Loader 只做一件事；链式调用: Webpack 会按顺序链式调用每个 Loader；统一原则: 遵循 Webpack制定的设计规则和结构，输入与输出均为字符串，各个 Loader 完全独立，即插即用；", "f": [{"q": "一个Less文件先后经过less-loader、css-loader、style-loader，同事把三者都理解成“把样式塞进页面”，你怎么指出反例？", "a": "三者承担的是连续但不同的转换职责：less-loader处理预处理语法，css-loader分析@import和url()依赖，style-loader才把样式以<style>形式注入页面。把职责合进一个loader会削弱复用和可组合性，也违背单一职责原则。"}, {"q": "项目同时有src下的业务代码和大量已编译依赖，配置babel-loader时你会怎样控制匹配范围？", "a": "让babel-loader只处理确实需要转换的js或jsx，通常通过test配合include或exclude缩小到业务源码。这样既避免重复转换已编译的第三方代码，也减少无效搜索；若依赖包确实发布了目标环境不支持的语法，则要针对该包单独放开范围。"}, {"q": "设计一个图片规则时，小图希望内联，大图需要复制并重命名；你如何解释url-loader与file-loader的职责边界？", "a": "小图片可由url-loader转成DataURL，从而减少独立资源请求；超过限定条件的文件则交给文件资源处理逻辑完成复制、命名和输出。内联会增大承载它的代码或样式体积，因此不能只追求减少请求而无条件处理所有图片。"}, {"q": "新增自定义loader后，构建报“下一个loader无法处理输入”，但单独执行转换函数结果正常，你先查什么？", "a": "先检查整条loader链的执行顺序，以及当前输出是否符合下一个loader可识别的内容形式。每个loader都必须保持独立且可链式组合，不能只验证自身转换结果；若某一步输出了二进制或特殊结构，还需按Webpack的接口约定明确声明和适配。"}, {"q": "团队要实现“转换每个SVG文件”和“汇总全部图片生成资源清单”，负责人想统一写成一个loader，你怎么取舍？", "a": "SVG到可识别模块内容的转换适合loader，因为处理对象是单个文件，输入输出边界清晰。全量资源清单依赖整次编译的输出集合，应由plugin监听生命周期并读取产物；强行放进loader会让单文件转换产生跨模块状态和执行顺序风险。"}]}]}
